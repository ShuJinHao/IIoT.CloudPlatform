# 云端架构治理清单

本文档是 `IIoT.CloudPlatform` 安全与架构治理计划入口。最新状态以本文和滚动复盘 `改动复盘与规则沉淀.md` 为准。当前计划只覆盖 Cloud，不授权修改 `IIoT.EdgeClient`、`AICopilot` 或工作区顶层 `docs/总规则.md`。

## 0. 执行口径

- 当前生产部署必须保持内网受控 HTTP，不启用 HTTPS/TLS/443/HSTS/HTTPS redirection。
- TLS/HTTPS 不再作为当前批次安全红线；证书采购、证书签发、证书轮换和 HTTPS 改造不进入本计划。
- 未经用户明确批准证书预算、证书来源、变更窗口和允许修改目录，不得新增 nginx 443 listener、TLS 证书挂载、HSTS、`UseHttpsRedirection` 或强制 HTTPS 配置。
- HTTP 只允许用于受控内网、VPN 或等价专线环境；如果要公网暴露，必须先暂停并重新评审传输安全方案。
- 因 HTTP 不提供传输加密，本计划优先收紧补偿控制：强 secret、短期 JWT、bootstrap secret 恒必需、限流、最小权限、runner/SSH 权限收敛、日志脱敏、部署前 fail-fast 检查和网络边界控制。
- 没有进入本清单的审计项视为未处理；每项完成时必须更新状态、影响面、验证命令和规则沉淀位置。
- 代码改动完成前必须更新 `docs/改动复盘与规则沉淀.md`；形成长期规则时必须写入 `docs/云端规则.md` 或专题契约。

## 1. 当前已确认事实

- `deploy/nginx/nginx.conf` 当前监听容器内 `8080`，已有 CSP、`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、限流、上传体积限制和目录 listing 禁止。
- `deploy/docker-compose.prod.yml` 当前对外仍映射 `${GATEWAY_HTTP_PORT:-80}`，容器内部 nginx/gateway/HttpApi/web 均走 `8080`，保持 HTTP-only 口径但不再靠容器内低端口。
- `ConfigurationGuardTests` 已扩展 HTTP-only 守卫；后续不能把 HTTP-only 本身翻成失败项，只能补强 HTTP 基线下的安全补偿控制。
- `deploy/.env.example` 已移除 `10.98.90.154`、`change-me-*` 和 `ALLOW_INTRANET_HTTP_OIDC=true` 默认值，改用内网示例域名和不可上线占位 secret；`pre-deploy-check.sh` 已通过 `release-common.sh` 拦截模板值。
- Bootstrap 运行时已删除 `BootstrapAuthOptions.RequireSecret` 可配置开关；`ClientCode` 只能寻址，bootstrap 必须校验 `X-IIoT-Bootstrap-Secret`。
- JWT bearer 当前已启用 issuer、audience、lifetime、signing key 校验；本计划不全局禁止 `ReadJwtToken`，只禁止未验签 token 参与认证/授权判断。
- `CurrentUser` 已读取全部 role claims，Admin bypass 已统一使用 `Roles.Contains(SystemRoles.Admin)`。
- Human 登录和 Edge operator 登录已执行 Identity lockout：失败计数、锁定判断和成功清零。
- `DeleteCascadeAsync` 已在 EF 事务中显式删除并写审计，影响查询和批量删除已覆盖 `EdgeHostPlcRuntimeState`；旧 `EdgeHost` / `EdgeHostPlcBinding` 手工配置表不再作为运行时依赖。
- AI Read `/api/v1/ai/read/production-records` 已固化为唯一 AI 生产数据只读入口，生产记录查询已做公共列和 schema 字段过滤，并由 `docs/AI只读接口契约.md` 和 ConfigurationGuardTests 守卫。
- EdgeHost PLC runtime state 已固化为 Edge 设备身份写入、Human 只读和专用 store 方向，并由 `docs/Edge上传与PLC状态接口契约.md` 和 ConfigurationGuardTests 守卫。

## 2. 第一轮：HTTP 基线下的安全红线

### C-01 HTTP-only 生产基线

- 严重级：CRITICAL
- 状态：已实施；文档红线和 ConfigurationGuardTests 已守卫 HTTP-only 基线。
- 目标：防止后续 agent 再把 HTTPS/TLS 当成本轮修复目标。
- 代码/文档落点：
  - `docs/云端规则.md`
  - `docs/云端安全部署契约.md`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 保留 HTTP-only 部署口径。
  - 增加测试守卫：deploy/src 不得新增 `listen 443`、`Strict-Transport-Security`、`UseHttpsRedirection`、`GATEWAY_HTTPS_PORT`。
  - 增加测试守卫：HTTP-only 不等于安全通过，必须同时检查 secret、限流、bootstrap、runner/SSH 和日志脱敏。
- 验收命令：
  ```bash
  rg -n "云端部署传输红线|内网受控 HTTP|不启用 HTTPS/TLS" IIoT.CloudPlatform/docs
  ! rg -n "listen 443|Strict-Transport-Security|UseHttpsRedirection|GATEWAY_HTTPS_PORT" IIoT.CloudPlatform/deploy IIoT.CloudPlatform/src/hosts -g '*.conf' -g '*.yml' -g '*.cs'
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  ```

### C-02 `.env.example` 弱 secret 和真实 IP

- 严重级：CRITICAL
- 状态：已实施；`.env.example` 已改模板域名/占位 secret，preflight 已拦截模板 secret、过短 secret 和模板公开地址。
- 目标：示例文件不能被直接作为生产 `.env` 使用。
- 代码/文档落点：
  - `deploy/.env.example`
  - `deploy/scripts/release-common.sh`
  - `deploy/scripts/pre-deploy-check.sh`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 将 `PUBLIC_BASE_URL`、`OIDC_PROVIDER_ISSUER`、AICopilot redirect/challenge、Harbor registry 示例改成占位域名或变量说明，不提交真实现场 IP。
  - 将 `PG_PASSWORD`、`RABBITMQ_DEFAULT_PASS`、`JWTSETTINGS__SECRET`、`SEQ_ADMIN_PASSWORD`、`SEED_ADMIN_PASSWORD` 改为不可上线占位，并在 preflight 中拒绝空值、`change-me-*`、已知弱值、模板值和过短值；`JWTSETTINGS__SECRET` 至少 32 字符，其它生产密码至少 12 字符。
  - `ALLOW_INTRANET_HTTP_OIDC` 默认改为 `false`；只有真实内网部署明确设置时才允许。
  - `BOOTSTRAP_AUTH_REQUIRE_SECRET` 已在 C-03 删除。
- 验收命令：
  ```bash
  ! rg -n "10\\.98\\.90\\.154|change-me-|ALLOW_INTRANET_HTTP_OIDC=true" IIoT.CloudPlatform/deploy/.env.example
  rg -n "require_min_secret_length|JWTSETTINGS__SECRET 32|Required secret is too short" IIoT.CloudPlatform/deploy/scripts/release-common.sh IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  ```

### C-03 Bootstrap secret 恒必需

- 严重级：CRITICAL
- 状态：已实施；运行时代码、appsettings、compose 和 `.env.example` 已删除可关闭开关。
- 目标：删除 `RequireSecret=false` 绕过，Cloud bootstrap 必须同时具备 `ClientCode` 和 `X-IIoT-Bootstrap-Secret`。
- 代码/文档落点：
  - `src/services/IIoT.Services.Contracts/Contracts/Identity/BootstrapAuthOptions.cs`
  - `src/services/IIoT.ProductionService/Queries/Bootstrap/Devices/GetDeviceByInstance.cs`
  - `src/hosts/IIoT.HttpApi/Controllers/Edge/EdgeBootstrapController.cs`
  - `deploy/docker-compose.prod.yml`
  - `deploy/.env.example`
  - `src/tests/IIoT.ServiceLayer.Tests/ApplicationFlowGuardTests.cs`
  - `src/tests/IIoT.EndToEndTests/CloudProductionFlowTests.cs`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 删除 `BootstrapAuthOptions.RequireSecret` 或改成不可配置常量，不再允许关闭。
  - 删除无 secret 的 cache-only bootstrap 分支。
  - compose/env 删除 `BOOTSTRAP_AUTH_REQUIRE_SECRET`。
  - 补行为测试：无 header、空 header、错误 secret 必须拒绝；正确 secret 才返回 Edge device token。
  - 补架构测试：运行时代码和 deploy 配置不得再出现 `RequireSecret=false` 或可关闭变量。
- 验收命令：
  ```bash
  ! rg -n "BootstrapAuthOptions|BootstrapAuth__RequireSecret|BOOTSTRAP_AUTH_REQUIRE_SECRET" IIoT.CloudPlatform/src/hosts IIoT.CloudPlatform/src/services IIoT.CloudPlatform/deploy
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter ApplicationFlowGuardTests
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~EdgeBootstrap_ShouldRequireSecret_AndResolveByCode|FullyQualifiedName~EdgeBootstrap_ShouldAcceptBootstrapSecretHeader_WhenSecretIsRequired"
  ```

### C-04 self-hosted runner 生产密钥治理

- 严重级：CRITICAL
- 状态：已实施；灾备 workflow 已挂 production environment、校验非 root，写入生产 `.env` 后显式 `chmod 600`，临时 env 文件使用 0600/umask，日常路径仍是本机构建、推 Harbor、SSH 触发服务器部署。
- 目标：日常生产发布不依赖 runner 持有完整生产 secret；灾备路径受确认词、非 root 和环境保护约束。
- 代码/文档落点：
  - `.github/workflows/cloud-image.yml`
  - `.github/workflows/cloud-deploy.yml`
  - `.github/workflows/cloud-admin-repair.yml`
  - `deploy/RUNNER.md`
  - `deploy/README.md`
  - `deploy/OPERATIONS.md`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 保留 `cloud-image`、`cloud-deploy` 灾备确认词，确认词必须不可为空且不可默认通过。
  - workflow 必须检查 `id -u != 0`。
  - `cloud-image` 灾备构建的 `OCI_REGISTRY` 和 `VITE_AICOPILOT_CHALLENGE_URL` 必须拒绝 `.example` / `internal.example` 文档域；`cloud-deploy` 和 `cloud-admin-repair` 的 `OCI_REGISTRY` 也必须拒绝文档域；所有灾备 workflow 的 `OCI_REGISTRY` 都必须要求显式 Harbor/内部 registry host，不能接受 `harbor` 这类非显式 registry；灾备 `OCI_NAMESPACE` 必须是单段小写 Harbor project/namespace，不能包含斜杠、空白、反斜杠或模板域。
  - 灾备 `DEPLOY_ENV_FILE` 写入服务器 `.env` 后必须显式 `chmod 600`，不能只依赖 `umask`；已有 `.env` 被重定向覆盖时也必须收敛到 0600，不能在日志、summary 或 artifact 泄露。
  - 文档明确长期方向：OIDC/Vault 或等价短期凭据；短期至少用 GitHub environment protection 和最小文件权限。
  - 日常路径只允许本机构建、推 Harbor、SSH 触发服务器部署。
- 验收命令：
  ```bash
  rg -n "EMERGENCY_CLOUD_IMAGE_BUILD|EMERGENCY_CLOUD_DEPLOY|id -u|environment: production" IIoT.CloudPlatform/.github/workflows
  rg -n "OCI_REGISTRY still uses the documentation example domain|OCI_REGISTRY must include an explicit Harbor registry host" IIoT.CloudPlatform/.github/workflows/cloud-image.yml IIoT.CloudPlatform/.github/workflows/cloud-deploy.yml IIoT.CloudPlatform/.github/workflows/cloud-admin-repair.yml
  rg -n "VITE_AICOPILOT_CHALLENGE_URL still uses the documentation example domain|\\*\\.example\\*\\|\\*internal\\.example\\*" IIoT.CloudPlatform/.github/workflows/cloud-image.yml
  rg -n "chmod 600 \"\\$DEPLOY_TARGET_DIR/.env\"" IIoT.CloudPlatform/.github/workflows/cloud-deploy.yml IIoT.CloudPlatform/.github/workflows/cloud-admin-repair.yml
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  ```

### C-05 SSH root 部署口径

- 严重级：CRITICAL
- 状态：已实施；`local-release.sh` 默认拒绝 `root@`，文档和帮助示例改为专用部署用户，root 仅允许显式 `ALLOW_ROOT_SSH_DEPLOY=emergency` 应急。
- 目标：标准发布不使用 root SSH；root 只允许用户明确批准的应急路径。
- 代码/文档落点：
  - `deploy/scripts/local-release.sh`
  - `deploy/README.md`
  - `deploy/OPERATIONS.md`
  - `deploy/EDGE_INSTALLER_GO_LIVE.md`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 标准示例改为 `deploy@<cloud-host>`。
  - `local-release.sh` 默认拒绝 `root@`，只允许显式 `ALLOW_ROOT_SSH_DEPLOY=emergency` 类确认变量通过。
  - 文档说明专用部署用户 sudo 白名单：仅允许 docker compose、部署脚本、必要日志查看，不允许通用 root shell。
  - 顶层 `docs/上传部署总览.md` 含 root 示例，因本轮未授权顶层目录，只在 Cloud 文档内标注“后续需另行授权同步顶层入口”。
- 验收命令：
  ```bash
  ! rg -n "root@10\\.98\\.90\\.154|--ssh-target root@" IIoT.CloudPlatform/deploy/scripts IIoT.CloudPlatform/deploy/EDGE_INSTALLER_GO_LIVE.md IIoT.CloudPlatform/deploy/RUNNER.md
  rg -n "refuses root SSH|ALLOW_ROOT_SSH_DEPLOY=emergency" IIoT.CloudPlatform/deploy/scripts/local-release.sh
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  ```

### C-06 Cloud 容器非 root 运行

- 严重级：HIGH
- 状态：代码和发布前门禁已实施，仍需生产真实启动验收后关闭；`iiot-httpapi`、`iiot-gateway`、`iiot-dataworker`、`iiot-migration` 已创建固定非 root 用户并设置 `USER ${APP_UID}:${APP_GID}`；`iiot-web` 已监听容器内 `8080` 并切换 `USER 101:101`；`nginx-gateway` 已监听容器内 `8080` 并由 compose 指定 `NGINX_UID/GID`。`pre-deploy-check.sh` 已接入 `check-container-nonroot-readiness.sh`，生产 PFX、Edge 更新根目录和既有 `installers/plugins/velopack` 子目录权限不满足时会 fail-fast，并输出 `httpapi-logs` / `dataworker-logs` 命名卷非阻断诊断。
- 目标：应用容器默认不以 root 运行。
- 代码/文档落点：
  - `docs/云端容器非Root权限契约.md`
  - `deploy/scripts/ensure-oidc-signing-cert.sh`
  - `deploy/scripts/check-container-nonroot-readiness.sh`
  - `deploy/scripts/pre-deploy-check.sh`
  - `src/hosts/IIoT.HttpApi/Dockerfile`
  - `src/hosts/IIoT.Gateway/Dockerfile`
  - `src/hosts/IIoT.DataWorker/Dockerfile`
  - `src/hosts/IIoT.MigrationWorkApp/Dockerfile`
  - `src/hosts/IIoT.AppHost/iiot-web.Dockerfile`
  - `deploy/docker-compose.prod.yml`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 已为 `iiot-httpapi`、`iiot-gateway`、`iiot-dataworker`、`iiot-migration` 创建固定 UID/GID 的非 root 用户并切换 `USER`。
  - `ensure-oidc-signing-cert.sh` 已校验既有 PFX 是否可被目标容器 UID/GID 读取且不 world-readable；生成新 PFX 时按 `CLOUD_CONTAINER_UID/GID` 设置最小可读权限：部署用户 UID 等于容器 UID 时 `600`，否则尝试 `chgrp CLOUD_CONTAINER_GID` 并设置 `640`；无法授权 group read 时 fail-fast，并删除新生成的不可用 PFX。
  - `pre-deploy-check.sh` 已在 compose config 后运行非 root readiness 探针，发布前验证 PFX 可读、Edge 更新根目录和既有 `installers/plugins/velopack` 子目录可写、nginx 内部端口未回退。
  - 已把 `iiot-web` 和 `nginx-gateway` 内部 HTTP 端口从 `80` 收敛到 `8080`，外部 HTTP 端口仍由 compose 映射提供；readiness 必须正向确认 nginx `listen 8080`、`proxy_pass http://iiot-web:8080` 和 compose target `8080`，并对任何容器内 `80` 回退 fail-fast。
  - 确认 `/app/logs` 不可写时不阻断启动，readiness 输出日志卷诊断；OIDC certs 只读挂载、Edge updates rw 挂载权限不被破坏。
  - 对 one-shot migration 明确需要的文件权限，不放宽到 root。
  - web/nginx 容器不得恢复容器内 `80` 或 root 低端口绑定；如后续基础镜像变更，必须继续满足非特权端口和非 root 运行。
- 生产关闭条件：
  - 在生产服务器运行 `deploy/scripts/pre-deploy-check.sh <release-tag>` 并看到非 root readiness 通过。
  - 确认 `OIDC_PROVIDER_CERTS_DIR` PFX 文件可被容器 UID/GID 读取，且不是 world-readable。
  - 确认 `EDGE_UPDATES_DIR` 可被容器 UID/GID 写入，且不是 world-writable。
  - 确认既有 `EDGE_UPDATES_DIR/installers`、`EDGE_UPDATES_DIR/plugins`、`EDGE_UPDATES_DIR/velopack` 子目录可被容器 UID/GID 写入，且不是 world-writable；缺失子目录只允许作为空发布目录诊断，不得掩盖历史目录权限问题。
  - 先通过 Cloud 受控发布 API 上传真实 Edge installer bundle 和 plugin package；设置 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1`、`POST_DEPLOY_EDGE_EXPECTED_VERSION`、`POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID` 和 `POST_DEPLOY_EDGE_EXPECTED_PLUGIN_VERSION`，运行 `deploy/scripts/post-deploy-check.sh` 并看到 public catalog、installer artifact、installer stub、期望 plugin module/version、Velopack `RELEASES` 和 `.nupkg` 静态下载通过。
  - 设置 `POST_DEPLOY_VERIFY_OIDC_TOKEN=1`，通过 0600 类私有文件和 `POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE` / `POST_DEPLOY_OIDC_CODE_VERIFIER_FILE` 提供真实授权码流程产生的一次性 authorization code、匹配的 PKCE verifier 和 redirect URI，运行 `deploy/scripts/post-deploy-check.sh` 并看到 `/connect/token` 返回 bearer access token；脚本必须拒绝 group/other 有权限的 code/verifier 文件；不得用伪造凭据、password grant、client_credentials 或 discovery/JWKS 冒充 token 验收，也不得把 code/verifier 放进日志、文档、shell history、进程环境或 curl 进程参数。
  - 完成真实容器启动验证：`iiot-httpapi` OIDC discovery/JWKS、真实授权码下的 OIDC token 发行、Edge installer/plugin 上传后的期望版本 catalog 验证、public download catalog、Velopack 静态下载、`iiot-web` 静态页和 `nginx-gateway` API 代理。
- 验收命令：
  ```bash
  rg -n "云端容器非Root权限契约|PFX|EDGE_UPDATES_DIR|nginx|非特权端口|容器内 `8080`" IIoT.CloudPlatform/docs
  sh -n IIoT.CloudPlatform/deploy/scripts/ensure-oidc-signing-cert.sh
  sh -n IIoT.CloudPlatform/deploy/scripts/check-container-nonroot-readiness.sh
  CLOUD_CONTAINER_UID=10001 CLOUD_CONTAINER_GID=10001 sh IIoT.CloudPlatform/deploy/scripts/check-container-nonroot-readiness.sh
  rg -n "nonroot_readiness_log_volume_httpapi_logs|nonroot_readiness_log_volume_dataworker_logs|nonroot_readiness_edge_updates_subdir_|installers plugins velopack|nginx-gateway must listen on container port 8080|nginx-gateway compose port target must not be container port 80" IIoT.CloudPlatform/deploy/scripts/check-container-nonroot-readiness.sh IIoT.CloudPlatform/docs/云端容器非Root权限契约.md
  rg -n "^USER " IIoT.CloudPlatform/src/hosts -g Dockerfile
  docker compose --env-file IIoT.CloudPlatform/deploy/.env.example -f IIoT.CloudPlatform/deploy/docker-compose.prod.yml config -q
  ```

### C-07 编译期和架构门禁

- 严重级：HIGH
- 状态：已实施；已覆盖 HTTP-only、弱 secret、真实 IP 模板、bootstrap 可关闭变量、root SSH 默认拒绝、runner workflow、AI 只读、JWT bearer 必须验签、生产运行时代码禁止 `ReadJwtToken` 未验签解析、OIDC HTTP 边界、preflight 补偿控制、.NET 应用容器非 root、web/nginx 非特权内部端口和非 root readiness 探针等守卫。
- 目标：新增同类安全问题时 PR 阶段失败。
- 代码/文档落点：
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs`
  - `.github/workflows/cloud-ci.yml`
- 修复动作：
  - 扫描弱 secret、真实 IP、`RequireSecret=false`、Dockerfile root、root SSH、AI 写入口、未声明权限的人端命令。
  - 保留合法 SHA256 场景：文件 hash、幂等 hash、refresh token/API key hash；不得把 SHA256 全局禁用。
  - 保留合法 `ReadJwtToken` 场景：测试解析或非授权展示；当前 Cloud 生产运行时代码没有非授权展示场景，因此 ConfigurationGuardTests 暂按生产运行时代码零 `ReadJwtToken` 守卫；如后续新增诊断展示，必须先说明非授权用途并调整门禁，禁止未验签 token 参与认证/授权。
  - JWT bearer 配置必须保留 `ValidateIssuer`、`ValidateAudience`、`ValidateLifetime`、`ValidateIssuerSigningKey`、`IssuerSigningKey` 和 `ClockSkew = TimeSpan.Zero`；非 Development 环境缺失 `JwtSettings:Secret` 必须 fail-fast。
  - `JwtSettings.Validate()` 必须在运行时拒绝短于 32 字符的已配置 secret；该门禁补偿绕过 deploy preflight 的本机、灾备或应急启动路径。
  - 当前 HMAC 对称 JWT 签名风险已记录为后续 `RS256/JWKS` 专项；本轮不把它伪装成已完成，但不得从治理清单中消失。
- 验收命令：
  ```bash
  rg -n "ReadJwtToken|ValidateIssuer = true|ValidateAudience = true|ValidateLifetime = true|ValidateIssuerSigningKey = true|ClockSkew = TimeSpan.Zero" IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs IIoT.CloudPlatform/src/hosts/IIoT.HttpApi/DependencyInjection.cs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter InfrastructureBehaviorTests
  ```

### C-07B JWT secret 运行时下限与 RS256/JWKS 后续项

- 严重级：HIGH
- 状态：部分完成；`JwtSettings.Validate()` 已补 32 字符运行时下限，`RS256/JWKS` 仍是后续安全专项。
- 目标：绕过发布 preflight 的本机/灾备/应急启动也不能接受过短 JWT secret；同时明确 HMAC 对称签名风险归属，避免被 HTTP-only 或部署安全计划掩盖。
- 代码/文档落点：
  - `src/infrastructure/IIoT.Infrastructure/Authentication/JwtSettings.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs`
  - `docs/云端规则.md`
  - `docs/三项目生产清空重部署对齐手册.md`
- 修复动作：
  - `JwtSettings.MinimumSecretLength = 32`，已配置 secret 低于下限时 `Validate()` 抛出。
  - 保留开发环境缺失 secret 由 `JwtSecretResolver` 生成临时值的行为，但不得把缺失/短 secret 带入生产。
  - `RS256/JWKS` 改造作为后续安全专项挂账；目标是 Cloud 私钥签发、Edge/AI 只持公钥或 JWKS 验签，移除“持 Edge 配置者可伪造 Cloud token”的结构性风险。
- 验收命令：
  ```bash
  rg -n "MinimumSecretLength|JwtSettings_ShouldRejectConfiguredSecretShorterThanRuntimeMinimum|RS256|JWKS" IIoT.CloudPlatform/src/infrastructure/IIoT.Infrastructure/Authentication/JwtSettings.cs IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs IIoT.CloudPlatform/docs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~InfrastructureBehaviorTests.JwtSettings"
  ```

### C-19 生产清空重部署只保留客户端更新历史

- 严重级：HIGH
- 状态：已实施仓库内导入导出脚本和文档；真实生产执行仍需服务器窗口。
- 目标：生产服务器全清空重部署时，只保留 Cloud 下载中心/客户端发布管理需要展示的历史版本记录；其它业务数据、AI 数据和消息队列数据默认不保留。
- 代码/文档落点：
  - `deploy/scripts/export-client-release-history.sh`
  - `deploy/scripts/import-client-release-history.sh`
  - `docs/三项目生产清空重部署对齐手册.md`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 清空前只导出 `edge_client_release_components`、`edge_client_release_versions`、`edge_client_release_artifacts`、`edge_client_release_retention_policies`。
  - 清空后只导回上述 release history 表；导入默认要求目标 release 表为空，只有修复失败导入才允许 `ALLOW_CLIENT_RELEASE_HISTORY_IMPORT_OVERWRITE=true`。
  - 导入后必须验证 Human 客户端发布 catalog `includeArchived=true` 能看到历史版本；public/Edge catalog 仍只返回可分发状态，不能把 `Deleted` 包重新变成可安装版本。
- 验收命令：
  ```bash
  sh -n IIoT.CloudPlatform/deploy/scripts/export-client-release-history.sh
  sh -n IIoT.CloudPlatform/deploy/scripts/import-client-release-history.sh
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~ConfigurationGuardTests.ProductionRedeployGuide"
  ```

## 3. 第二轮：身份、权限和数据链路

### C-08 多角色授权和 Admin 判断

- 严重级：HIGH
- 状态：已实施；`ICurrentUser` 已删除单 `Role` 暴露，HttpApi 使用 `FindAll(ClaimTypes.Role)`，Admin bypass 统一改为 `Roles.Contains(SystemRoles.Admin)` 并补多角色测试。
- 目标：不再用第一个 role claim 决定用户角色和 Admin bypass。
- 代码/文档落点：
  - `src/services/IIoT.Services.Contracts/Contracts/Identity/ICurrentUser.cs`
  - `src/hosts/IIoT.HttpApi/Infrastructure/CurrentUser.cs`
  - `src/services/IIoT.Services.CrossCutting/Requests/Behaviors/AuthorizationBehavior.cs`
  - `src/services/IIoT.Services.CrossCutting/Authorization/CurrentUserDeviceAccessService.cs`
  - `src/services/IIoT.EmployeeService/Commands/Human/Employees/*`
  - `src/tests/IIoT.ServiceLayer.Tests/AuthorizationAndIdentityBehaviorTests.cs`
- 修复动作：
  - `ICurrentUser` 增加 `IReadOnlyCollection<string> Roles`。
  - `CurrentUser` 使用 `FindAll(ClaimTypes.Role)` 并去重。
  - Admin 判断统一改为 `Roles.Contains(SystemRoles.Admin)`。
  - 已删除 `ICurrentUser.Role` 单角色兼容属性；后续不得恢复单 role 授权判断。
  - 补测试：Admin claim 不在第一个时仍有 Admin 权限；普通多角色不能获得 Admin bypass。
- 验收命令：
  ```bash
  ! rg -n "currentUser\\.Role\\b|user\\.Role\\b|FindFirstValue\\(ClaimTypes\\.Role\\)|string\\?\\s+Role\\s*\\{" IIoT.CloudPlatform/src/services IIoT.CloudPlatform/src/hosts -g '*.cs'
  rg -n "IReadOnlyCollection<string> Roles|FindAll\\(ClaimTypes\\.Role\\)|Roles.Contains\\(SystemRoles.Admin|AuthorizationBehavior_ShouldAllowAdminWhenAdminRoleIsNotFirst|AuthorizationBehavior_ShouldNotTreatNonAdminMultiRoleAsAdmin" IIoT.CloudPlatform/src -g '*.cs'
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter AuthorizationAndIdentityBehaviorTests
  ```

### C-09 Identity lockout

- 严重级：HIGH
- 状态：已实施。
- 目标：连续失败登录触发锁定，成功登录清零失败计数。
- 代码/文档落点：
  - `src/infrastructure/IIoT.EntityFrameworkCore/DependencyInjection.cs`
  - `src/infrastructure/IIoT.EntityFrameworkCore/Identity/IdentityPasswordService.cs`
  - `src/services/IIoT.IdentityService/Commands/Human/LoginUser.cs`
  - `src/services/IIoT.IdentityService/Commands/Human/EdgeOperatorLoginCommand.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/AuthorizationAndIdentityBehaviorTests.cs`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `docs/云端规则.md`
- 修复动作：
  - 已配置 Identity lockout：新用户默认启用、失败 5 次锁定、锁定 15 分钟。
  - `CheckPasswordAsync` 已补 `GetLockoutEnabledAsync` / `SetLockoutEnabledAsync`，兼容历史 `LockoutEnabled=false` 的已部署账号。
  - `CheckPasswordAsync` 已在密码错误时调用 `AccessFailedAsync`，成功时调用 `ResetAccessFailedCountAsync`，锁定时返回统一失败。
  - Human 登录和 Edge operator 登录的凭据校验阶段已统一使用“账号不存在或密码错误”，不再区分账号不存在、停用、锁定或密码错误。
  - `ResetPasswordAsync` 未清除 lockout，并已补回归测试确认锁定账号重置密码后仍保持锁定；如需管理员显式解锁，必须单独建命令、写审计并补规则。
- 验收命令：
  ```bash
  rg -n "AccessFailedAsync|ResetAccessFailedCountAsync|IsLockedOutAsync|SetLockoutEnabledAsync|Lockout\\.AllowedForNewUsers|MaxFailedAccessAttempts" IIoT.CloudPlatform/src -g '*.cs'
  rg -n "Human 登录和 Edge operator 登录必须执行 Identity lockout|管理员重置密码不得隐式清除 lockout" IIoT.CloudPlatform/docs/云端规则.md
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AuthorizationAndIdentityBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

### C-10 设备级联删除批量化

- 严重级：HIGH
- 状态：已实施。
- 目标：保持高危删除审计和事务边界，同时减少多次 DB 往返和遗漏风险。
- 代码/文档落点：
  - `src/infrastructure/IIoT.EntityFrameworkCore/QueryServices/EfDeviceDeletionDependencyService.cs`
  - `src/services/IIoT.ProductionService/Commands/Human/Devices/DeleteDevice.cs`
  - `src/services/IIoT.ProductionService/Queries/Human/Devices/GetDeviceDeletionImpact.cs`
  - `src/services/IIoT.Services.Contracts/Contracts/Data/RecordQueries/IDeviceDeletionDependencyQueryService.cs`
  - `src/tests/IIoT.ProductionService.Tests/*`
  - `src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/ApplicationFlowGuardTests.cs`
  - `docs/云端规则.md`
- 修复动作：
  - `GetImpactAsync` 已从多次 `LongCountAsync`/单表 count 合并为一次 `SqlQuery<DeviceDeletionImpactRow>`，返回完整 `DeviceDeletionImpact`。
  - 已把实际会被删除的 `EdgeHostPlcRuntimeState` 纳入影响查询、DTO、审计摘要和总数；旧 `EdgeHost` / `EdgeHostPlcBinding` 手工配置表不再进入运行时设备删除链路。
  - 删除路径仍保持 EF transaction，关联归档/投影/配置表在同一事务内用参数化 SQL 批量删除，`Device` 本体仍走聚合 `MarkDeleted()` 后由 EF 删除。
  - 删除前继续捕获人员授权员工 ID，删除成功后继续清理 `DeviceAccessesByUser` 缓存。
  - ServiceLayer 守卫防止回退到 Dapper 删除路径、`CountTableAsync` 多次统计或 `ExecuteDeleteAsync` 分散删除。
- 验收命令：
  ```bash
  rg -n "SqlQuery<DeviceDeletionImpactRow>|edge_host_plc_runtime_states|edge_host_plc_bindings|edge_hosts|CountTableAsync|ExecuteDeleteAsync" IIoT.CloudPlatform/src/infrastructure/IIoT.EntityFrameworkCore/QueryServices/EfDeviceDeletionDependencyService.cs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter Device --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~ApplicationFlowGuardTests --no-restore --disable-build-servers
  ```

### C-11 ResponseCompression

- 严重级：MEDIUM
- 状态：已实施。
- 目标：减少普通 API/静态资源带宽占用，不影响上传、下载和已压缩包。
- 代码/文档落点：
  - `deploy/nginx/nginx.conf`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `src/hosts/IIoT.HttpApi/Program.cs`
  - `src/hosts/IIoT.HttpApi/DependencyInjection.cs`
  - `docs/云端规则.md`
- 修复动作：
  - 已选择 nginx 统一压缩，不在 HttpApi 增加 `AddResponseCompression` / `UseResponseCompression`，避免双重压缩。
  - nginx 已开启 `gzip on`、`gzip_min_length 1024`、`gzip_vary on`，压缩文本、JSON、JS/CSS、XML、SVG。
  - `gzip_types` 未包含 zip、nupkg、exe、octet-stream 等安装包或已压缩包类型；edge bundle 下载 endpoint 仍保持 `proxy_buffering off` 和大文件超时配置。
  - ConfigurationGuardTests 已解析 `gzip_types` 并守卫不加入安装包类型、不启用 HttpApi ResponseCompression。
- 验收命令：
  ```bash
  rg -n "gzip on|gzip_types|AddResponseCompression|UseResponseCompression" IIoT.CloudPlatform/deploy/nginx/nginx.conf IIoT.CloudPlatform/src/hosts/IIoT.HttpApi -g '*.cs' -g '*.conf'
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

## 4. 第三轮：Edge 状态和 AI 只读边界

### C-12 Edge PLC runtime state 专用接口

- 严重级：HIGH
- 状态：已实施；运行时代码已切换为设备身份 + 客户端上报 PLC 投影，已补专题契约和 ConfigurationGuardTests 守卫。
- 目标：Cloud 只接受 Edge 设备身份写 PLC runtime state，Human 只读，AI/Public 不得写。
- 代码/文档落点：
  - `docs/Edge上传与PLC状态接口契约.md`
  - `src/hosts/IIoT.HttpApi/Controllers/Edge/EdgeHostPlcRuntimeStateController.cs`
  - `src/hosts/IIoT.HttpApi/Controllers/Human/HumanEdgeHostController.cs`
  - `src/services/IIoT.ProductionService/Commands/Edge/EdgeHosts/ReportEdgeHostPlcRuntimeStates.cs`
  - `src/tests/IIoT.ProductionService.Tests/EdgeHostBehaviorTests.cs`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 已更新专题契约，写清 `DeviceId + ClientCode + PlcCode` 唯一、Cloud 不手工维护 PLC 绑定、客户端上报 PLC 可进入只读投影但不得反向改配置。
  - 已确认写入口只在 Edge controller：`POST /api/v1/edge/edge-hosts/plc-runtime-states`，使用 `RequireEdgeDeviceToken` 和 `EdgeHostPlcStateUpload` 限流。
  - 已确认写命令是 `IDeviceCommand`，只校验 `DeviceId + ClientCode` 设备身份链，不再要求旧 `EdgeHost` 配置记录存在。
  - 已确认 Human 查询只读：`GET /api/v1/human/edge-hosts/{deviceId}/plc-runtime-states` 使用 `EdgeHost.Read`，列表以可访问 `Device` 为主数据源并合并客户端状态和 PLC runtime state。
  - 已增加 ConfigurationGuardTests：引用写命令的 Controller 只能是 Edge controller，AI Read 目录不得出现 PLC runtime state 写入口。
- 验收命令：
  ```bash
  rg -n "plc-runtime-states|EdgeHostPlcRuntimeState|IEdgeHostPlcRuntimeStateStore" IIoT.CloudPlatform/src IIoT.CloudPlatform/docs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~EdgeHostBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

### C-13 设备客户端状态专用投影

- 严重级：HIGH
- 状态：已实施；既有运行时代码已确认，已补专题契约和 ConfigurationGuardTests 守卫。
- 目标：客户端安装状态、运行心跳、上传同步不能相互冒充。
- 代码/文档落点：
  - `docs/设备客户端状态投影契约.md`
  - `src/services/IIoT.ProductionService/Commands/Edge/ClientVersions/*`
  - `src/services/IIoT.ProductionService/Queries/Human/ClientReleases/GetDeviceClientVersionInventory.cs`
  - `src/services/IIoT.ProductionService/Queries/AiRead/AiReadQueries.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/AiReadBehaviorTests.cs`
  - `src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs`
  - `src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 修复动作：
  - 已新增专题契约，固化 `DeviceClientState` 是官方投影，按 `DeviceId + ClientCode` 唯一。
  - 已确认版本上报在写入 `DeviceClientVersionSnapshot` 后同步 `DeviceClientState.ApplyVersionReport`。
  - 已确认运行心跳在写入 `EdgeDeviceRuntimeHeartbeat` 后同步 `DeviceClientState.ApplyRuntimeHeartbeat`。
  - 已确认 Human 设备客户端状态清单和 AI Read 设备客户端状态都通过 `IDeviceClientStateStore.GetStatesByDevicesAsync` 读取官方投影。
  - 已增加 ConfigurationGuardTests：禁止回退到 `IRepository<DeviceClientState>` / `IReadRepository<DeviceClientState>` / 状态投影 Specification，守卫 Human/AI 不临时拼 snapshot/heartbeat。
- 验收命令：
  ```bash
  rg -n "DeviceClientState|IDeviceClientStateStore|device-client-states" IIoT.CloudPlatform/src IIoT.CloudPlatform/docs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~ClientReleaseBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

### C-14 AI 生产数据唯一只读入口

- 严重级：HIGH
- 状态：已实施；`production-records` 已作为唯一 AI 生产数据只读入口，专题契约和 ConfigurationGuardTests 已补强。
- 目标：AICopilot 读取生产数据只能通过 Cloud `/api/v1/ai/read/production-records`，不得直连生产库，不得新增 AI 专用 pass-station 旧语义入口。
- 代码/文档落点：
  - `docs/AI只读接口契约.md`
  - `src/hosts/IIoT.HttpApi/Controllers/AiRead/AiReadController.cs`
  - `src/services/IIoT.ProductionService/Queries/AiRead/AiReadQueries.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/AiReadBehaviorTests.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs`
  - `src/tests/IIoT.EndToEndTests/CloudProductionFlowTests.AiRead.cs`
- 修复动作：
  - 已新增 AI 只读接口契约，写明允许域、禁止写动作、权限点、审计、限行、时间窗、字段过滤和 device scope。
  - 已增加 guard：AI controller 不得有 `HttpPost`、`HttpPut`、`HttpPatch`、`HttpDelete`。
  - 已增加 guard：`GetAiRead*Query` 必须实现 `IAiReadQuery<>` 并带 `AuthorizeAiReadAttribute`；AI read 不得使用 Human 权限属性。
  - 已增加 guard：生产数据 AI read 不返回 raw `payload_jsonb`，不得恢复 `pass-stations/{typeKey}` AI 旧语义入口。
- 验收命令：
  ```bash
  rg -n "api/v1/ai/read|production-records|AuthorizeAiRead|payload_jsonb" IIoT.CloudPlatform/src IIoT.CloudPlatform/docs
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

## 5. 第四轮：部署文档和发布治理收口

### C-15 部署真实 IP、运行时提示和本机脚本默认值

- 严重级：HIGH
- 状态：已实施；后端 Dockerfile、运行时校验提示、Cloud 前端 placeholder、`build-and-push.sh`、`mirror-third-party-images.sh`、`verify-edge-installer-catalog.sh` 和 `InvokeEdgeInstallerPackageDownload.ps1` 已删除真实 IP 默认值，相关 README/GO_LIVE/RUNNER 示例已改占位。ConfigurationGuardTests 已全局扫描 `src`、`deploy`、`.github`，只允许 `deploy/README.md` 中标明“现场校准（不是模板默认值）”的历史记录保留真实 IP。
- 目标：仓库文档、脚本、运行时错误提示和 UI 占位不把 `10.98.90.154` 当成可复制默认生产值。
- 代码/文档落点：
  - `deploy/scripts/build-and-push.sh`
  - `deploy/scripts/mirror-third-party-images.sh`
  - `deploy/scripts/verify-edge-installer-catalog.sh`
  - `deploy/scripts/InvokeEdgeInstallerPackageDownload.ps1`
  - `src/services/IIoT.ProductionService/ClientReleases/EdgeInstallerPublicBaseUrl.cs`
  - `src/ui/iiot-web/src/features/client-releases/EdgeBindingDownloadPanel.vue`
  - `src/hosts/IIoT.HttpApi/Dockerfile`
  - `src/hosts/IIoT.Gateway/Dockerfile`
  - `src/hosts/IIoT.DataWorker/Dockerfile`
  - `src/hosts/IIoT.MigrationWorkApp/Dockerfile`
  - `deploy/README.md`
  - `deploy/EDGE_INSTALLER_GO_LIVE.md`
  - `deploy/RUNNER.md`
  - `docs/云端安全部署契约.md`
- 修复动作：
  - 脚本默认改为必须传入变量，或使用 `<cloud-host>` 占位并 fail-fast。
  - 运行时错误提示和 UI placeholder 使用泛化 `cloud-host` 示例，不携带真实现场 IP。
  - 现场值只允许放在真实 `.env`、操作者本机环境变量或明确的运维记录中。
  - 文档中历史现场校准可保留，但必须标注“现场记录，不是模板默认值”。
- 验收命令：
  ```bash
  ! rg -n "10\\.98\\.90\\.154|root@10\\.98\\.90\\.154" IIoT.CloudPlatform/deploy/scripts IIoT.CloudPlatform/deploy/.env.example IIoT.CloudPlatform/deploy/EDGE_INSTALLER_GO_LIVE.md IIoT.CloudPlatform/deploy/RUNNER.md IIoT.CloudPlatform/.github/workflows
  ! rg -n "10\\.98\\.90\\.154" IIoT.CloudPlatform/src/services/IIoT.ProductionService/ClientReleases/EdgeInstallerPublicBaseUrl.cs IIoT.CloudPlatform/src/ui/iiot-web/src/features/client-releases/EdgeBindingDownloadPanel.vue
  rg -n "2026-06-22 现场校准（不是模板默认值）.*10\\.98\\.90\\.154" IIoT.CloudPlatform/deploy/README.md
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests
  ```

### C-16 Cloud/AICopilot OIDC HTTP 口径

- 严重级：MEDIUM
- 状态：已实施；运行时只允许 Development loopback 或显式 `ALLOW_INTRANET_HTTP_OIDC=true` 下的 loopback/RFC1918 IPv4，拒绝公网 IP、普通域名和未知 host，并已补安全部署契约和部署 README 示例说明。
- 目标：允许内网 HTTP OIDC，但拒绝公网 HTTP 或域名 HTTP 伪装。
- 代码/文档落点：
  - `src/services/IIoT.Services.Contracts/Contracts/Identity/OidcProviderOptions.cs`
  - `src/tests/IIoT.ServiceLayer.Tests/CloudOidcProviderBehaviorTests.cs`
  - `docs/云端安全部署契约.md`
- 修复动作：
  - 已保留内网 IP HTTP 例外。
  - 已确认 `ALLOW_INTRANET_HTTP_OIDC=true` 时仍拒绝公网 IP、普通域名 HTTP、未知 host。
  - 已在安全部署契约说明 HTTP OIDC 只适用于当前受控内网，实际 OIDC HTTP 值必须使用 loopback/RFC1918 IPv4，不能使用模板域名。
  - 已在 `deploy/README.md` 中把可复制的生产 HTTP OIDC 示例改为 `<cloud-private-ip>` / `<aicopilot-private-ip>`，避免复制 `.internal.example` 后被 preflight 拒绝。
- 验收命令：
  ```bash
  dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~CloudOidcProviderBehaviorTests --no-restore --disable-build-servers
  rg -n "ALLOW_INTRANET_HTTP_OIDC|RFC1918|普通域名|公网 IP|<cloud-private-ip>|<aicopilot-private-ip>" IIoT.CloudPlatform/src IIoT.CloudPlatform/docs IIoT.CloudPlatform/deploy/README.md -g '*.cs' -g '*.md'
  ```

### C-17 发布 preflight 和 ops-check 补强

- 严重级：HIGH
- 状态：已实施；preflight 已补 root 执行拦截、bootstrap 关闭变量拦截、OIDC HTTP 边界、Edge 上传限流边界、文档示例镜像仓库拦截、应用镜像显式 registry、Docker Hub、compose、磁盘余量、OIDC certs、HTTP-only 补偿控制摘要和既有 runtime ops-check；本机构建、基础镜像 mirror、local release 和 Edge installer catalog 验收入口也已拒绝 `.example` 文档示例域；无副作用的模板值、公开地址、OIDC 边界、镜像和 compose 校验必须先于 OIDC PFX 生成，避免无效 `.env` 先落证书文件；post-deploy 已支持显式 OIDC token gate 和 Edge installer catalog / 静态下载 gate；直接执行的运维入口脚本已保持 git executable 位；`deploy-release.sh` 已在影响浏览器流量的发布中启动或重启 `nginx-gateway`；`cloud-ci` 快速门禁已加入 deploy shell/bash/PowerShell 脚本语法检查，防止脚本语法错误只到生产执行时暴露。
- 目标：生产发布前阻断危险配置，而不是靠人工记忆。
- 代码/文档落点：
  - `deploy/scripts/pre-deploy-check.sh`
  - `deploy/scripts/ops-check.sh`
  - `deploy/scripts/release-common.sh`
  - `.github/workflows/cloud-ci.yml`
  - `deploy/OPERATIONS.md`
- 修复动作：
  - preflight 已检查：secret、HTTP OIDC 内网边界、bootstrap secret 恒必需、Edge 上传限流变量、runner/root、文档示例镜像仓库、应用镜像显式 registry、Docker Hub、compose、磁盘、备份、OIDC certs。
  - `RATE_LIMIT_CAPACITY_UPLOAD_*`、`RATE_LIMIT_DEVICE_LOG_UPLOAD_*`、`RATE_LIMIT_PASS_STATION_UPLOAD_*` 必须是正整数，且不得超过 nginx `edge_upload_limit` 基线 12000 次/分钟。
  - `build-and-push.sh`、`mirror-third-party-images.sh`、`local-release.sh`、`verify-edge-installer-catalog.sh` 和灾备 workflow 也必须拒绝 `.example` / `internal.example` 文档示例域；`build-and-push.sh` 的 `REGISTRY`、`mirror-third-party-images.sh` 的 `MIRROR_REGISTRY` 和灾备 workflow 的 `OCI_REGISTRY` 还必须是显式 Harbor/内部 registry host，不能接受 `harbor` 这类非显式 registry；`HARBOR_PROJECT`、`MIRROR_NAMESPACE`、灾备 `OCI_NAMESPACE` 和 Harbor retention project 必须是单段小写 project/namespace，不能包含斜杠、空白、反斜杠或模板域；脚本帮助和可复制文档示例不得再使用会被自身拒绝的 `.example` 值。
  - `build-and-push.sh` 只有构建 `web` 或 `--all` 时才要求 `VITE_AICOPILOT_CHALLENGE_URL`，backend-only 构建不被 AICopilot 前端 URL 阻断。
  - preflight 顺序必须先完成模板值、公开地址、OIDC HTTP 边界、镜像和 compose 等无副作用校验，再调用 `ensure-oidc-signing-cert.sh`，最后运行 non-root readiness 探针。
  - `ops-check.sh` 保留 `/internal/healthz` HTTP 本机检查，并默认把 DataWorker Docker healthcheck 纳入运行态门禁；缺失 healthcheck、`starting` 或 `unhealthy` 都必须失败，不改 HTTPS。`pre-deploy-check.sh` 检查的是更新前旧当前版本，默认以 `REQUIRE_DATAWORKER_HEALTHCHECK=${PRE_DEPLOY_REQUIRE_DATAWORKER_HEALTHCHECK:-0}` 调用 `ops-check.sh`，避免旧 DataWorker 镜像缺少新 healthcheck 时阻断升级；这不得作为 C-06 关闭证据。
  - preflight 已输出 HTTP-only 风险和补偿控制检查结果；摘要必须反映实际运行态检查，已有 `current-release.env` 时打印 `healthz-http-local ops-check-runtime`，干净首部署或 post-clean rebuild 没有旧运行态时打印 `runtime-check-skipped-no-current-release`。
  - 运维文档的服务器本机 curl 示例必须读取 `.env` 中的 `GATEWAY_HTTP_PORT`，不得写死 `http://127.0.0.1:80/...`。
  - `post-deploy-check.sh` 默认检查服务、首页、`/internal/healthz`、OIDC discovery、JWKS、DataWorker Docker healthcheck 和 `ops-check.sh`；当 `POST_DEPLOY_VERIFY_OIDC_TOKEN=1` 时，必须使用真实 authorization code + PKCE verifier 调用 `/connect/token` 并验证 bearer access token；真实 code/verifier 必须通过 0600 类私有文件提交给 curl，脚本必须拒绝 group/other 有权限的文件，不能进入 shell history、进程环境或 curl 命令行参数；当 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1` 时，必须调用 `verify-edge-installer-catalog.sh` 验证 public catalog、installer artifact、静态 installer stub、Velopack `RELEASES` 和 `.nupkg` 下载；关闭 C-06 或验收上传链路时还必须传入 `POST_DEPLOY_EDGE_EXPECTED_VERSION`、`POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID` 和 `POST_DEPLOY_EDGE_EXPECTED_PLUGIN_VERSION`，把 catalog 证据绑定到本次真实上传的 host/plugin 版本。
  - `deploy-release.sh` 的服务选择仍只管理五个应用镜像，不得把基础设施 `nginx-gateway` 混入 release manifest；但当选中 `iiot-web` 或 `iiot-gateway` 时，必须在 `post-deploy-check.sh` 前启动缺失的 `nginx-gateway` 或重启已有容器刷新上游 DNS。
  - `deploy-release.sh`、`pre-deploy-check.sh`、`post-deploy-check.sh`、`ops-check.sh`、backup/restore/rollback/cleanup 等直接执行入口必须是 executable；`release-common.sh` 只作为 source 共享库。
  - 可复制运维模板和部署默认值必须使用当前标准目录：Cloud 部署目录为 `/data/iiot-platform/cloud/deploy`，Edge 更新包目录为 `/data/iiot-platform/edge-client/edge-updates`；`deploy/.env.example`、compose fallback、non-root readiness fallback、backup、restore verification 和 post-release cleanup cron 模板不得保留旧 `/srv/iiot-cloud/deploy` 或 `/srv/iiot/edge-updates` 示例。
  - `cloud-ci` 必须在快速门禁中运行 deploy shell/bash/PowerShell 脚本语法检查；ConfigurationGuardTests 必须守卫该 workflow 步骤。
- 验收命令：
  ```bash
  cd IIoT.CloudPlatform
  sh -n deploy/scripts/release-common.sh
  sh -n deploy/scripts/pre-deploy-check.sh
  sh -n deploy/scripts/post-deploy-check.sh
  sh -n deploy/scripts/ops-check.sh
  bash -n deploy/scripts/build-and-push.sh
  bash -n deploy/scripts/local-release.sh
  bash -n deploy/scripts/post-release-cleanup.sh
  pwsh -NoProfile -Command '$tokens = $null; $errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile("deploy/scripts/InvokeEdgeInstallerPackageDownload.ps1", [ref]$tokens, [ref]$errors); if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Error $_ }; exit 1 }'
  REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services httpapi --dry-run
  if REGISTRY=harbor deploy/scripts/build-and-push.sh --services httpapi --dry-run >/tmp/iiot-build-bad-registry.out 2>&1; then cat /tmp/iiot-build-bad-registry.out; exit 1; fi; rg -n "REGISTRY must include an explicit Harbor registry host" /tmp/iiot-build-bad-registry.out
  if MIRROR_REGISTRY=harbor deploy/scripts/mirror-third-party-images.sh >/tmp/iiot-mirror-bad-registry.out 2>&1; then cat /tmp/iiot-mirror-bad-registry.out; exit 1; fi; rg -n "MIRROR_REGISTRY must include an explicit Harbor registry host" /tmp/iiot-mirror-bad-registry.out
  if REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services web --dry-run >/tmp/iiot-build-web-no-challenge.out 2>&1; then cat /tmp/iiot-build-web-no-challenge.out; exit 1; fi; rg -n "VITE_AICOPILOT_CHALLENGE_URL is required for web image builds" /tmp/iiot-build-web-no-challenge.out
  test -x deploy/scripts/deploy-release.sh && test -x deploy/scripts/pre-deploy-check.sh && test -x deploy/scripts/post-deploy-check.sh && test -x deploy/scripts/ops-check.sh
  rg -n "POST_DEPLOY_VERIFY_OIDC_TOKEN|POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE|POST_DEPLOY_OIDC_CODE_VERIFIER_FILE|grant_type=authorization_code|code@\\$authorization_code_file|code_verifier@\\$code_verifier_file|POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG|POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID|POST_DEPLOY_EDGE_EXPECTED_PLUGIN_VERSION|verify-edge-installer-catalog.sh|require_healthy_service|openid-configuration|\\.well-known/jwks|ensure_nginx_gateway_if_needed|compose up -d nginx-gateway" deploy/scripts/post-deploy-check.sh deploy/scripts/ops-check.sh deploy/scripts/release-common.sh deploy/scripts/deploy-release.sh src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs
  rg -n "Validate deploy script syntax|sh -n deploy/scripts/release-common.sh|bash -n deploy/scripts/build-and-push.sh|pwsh -NoProfile -Command|InvokeEdgeInstallerPackageDownload.ps1" .github/workflows/cloud-ci.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs
  rg -n "ensure_rate_limit_values_bounded|RATE_LIMIT_PASS_STATION_UPLOAD_TOKEN_LIMIT|max_edge_upload_rate_per_minute=12000|ensure_image_values_not_template|ensure_app_images_have_explicit_registry|documentation example registry|explicit Harbor registry|runtime-check-skipped-no-current-release|healthz-http-local ops-check-runtime|gateway_http_port=\\$\\(sed -n 's/\\^GATEWAY_HTTP_PORT=//p' \\.env" deploy/scripts/release-common.sh deploy/scripts/pre-deploy-check.sh deploy/OPERATIONS.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs
  ! rg -n "/srv/iiot-cloud/deploy|/srv/iiot/edge-updates" deploy/.env.example deploy/docker-compose.prod.yml deploy/scripts/check-container-nonroot-readiness.sh deploy/cron
  rg -n "/data/iiot-platform/cloud/deploy|/data/iiot-platform/edge-client/edge-updates" deploy/.env.example deploy/docker-compose.prod.yml deploy/scripts/check-container-nonroot-readiness.sh deploy/cron src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs
  dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers
  ```

## 6. 推荐执行批次

### 批次 A：文档和守卫先行

- C-01、C-07、C-16 的文档和测试守卫。
- 目标：先防止后续继续走错 HTTPS/TLS 方向。
- 合并条件：ConfigurationGuardTests 通过。

### 批次 B：部署 secret 和 bootstrap

- C-02、C-03、C-17。
- 目标：先堵默认值和 bootstrap 绕过。
- 合并条件：ServiceLayer + EndToEnd bootstrap 测试通过，compose config 通过。

### 批次 C：runner/SSH/container 权限

- C-04、C-05、C-06、C-15。
- 目标：减少生产密钥和 root 权限暴露。
- 合并条件：workflow guard、Dockerfile guard、deploy dry-run 通过。

### 批次 D：身份授权

- C-08、C-09。
- 目标：多角色和 lockout 行为正确。
- 合并条件：AuthorizationAndIdentityBehaviorTests 和相关 EndToEnd 通过。

### 批次 E：数据和只读边界

- C-10、C-12、C-13、C-14。
- 目标：删除、Edge 状态、AI 只读边界闭环。
- 合并条件：ProductionService、AiRead、EdgeHost 定向测试通过。

### 批次 F：性能和最终收口

- C-11、文档引用检查、滚动复盘、部署验收清单。
- 目标：压缩策略、文档入口和发布前检查完整。
- 合并条件：全量快速门禁通过。

## 7. 分层验证

PR 前必须过：

```bash
dotnet build IIoT.CloudPlatform/IIoT.CloudPlatform.slnx -c Release
dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release
dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter ConfigurationGuardTests
cd IIoT.CloudPlatform/src/ui/iiot-web && npm run build
docker compose --env-file IIoT.CloudPlatform/deploy/.env.example -f IIoT.CloudPlatform/deploy/docker-compose.prod.yml config -q
```

合 main 前必须过：

```bash
dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release
dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~RateLimitingConfigurationGuardTests
dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~CloudProductionFlowTests.InternalHealthz_ShouldReturnOk|FullyQualifiedName~CloudProductionFlowTests.DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue|FullyQualifiedName~EdgeBootstrap_ShouldRequireSecret_AndResolveByCode|FullyQualifiedName~CloudOidc_Discovery_ShouldExposeCodeFlowPkceAndProviderEndpoints|FullyQualifiedName~AiReadToken_ShouldNotAccessHumanMutationOrEdgeUpload|FullyQualifiedName~HumanIdentity_Login_ShouldReturnTooManyRequestsAfterTenthRequestWithinWindow"
```

`CloudProductionFlowTests` 整类是重型 Aspire 回归，每个测试方法独立启动 fixture，本机合 main 前不得用 `CloudProductionFlowTests|RateLimitingConfigurationGuardTests` 这种全类过滤冒充稳定门禁；需要整类覆盖时作为手动重型回归单独排期执行，并按失败用例逐项收口。

发布前必须过：

```bash
cd IIoT.CloudPlatform
./deploy/scripts/pre-deploy-check.sh sha-<git-sha>
./deploy/scripts/post-deploy-check.sh
./deploy/scripts/ops-check.sh
curl -I http://<cloud-host>/internal/healthz
curl -I "http://<cloud-host>/api/v1/public/client-downloads/latest?channel=stable"
```

文档或规则整理后至少过：

```bash
rg -n "云端部署传输红线|内网受控 HTTP|云端安全部署契约|云端架构治理清单" IIoT.CloudPlatform/docs
rg -n "RequireSecret=false|AI只读接口契约|Edge上传与PLC状态接口契约|production-records" IIoT.CloudPlatform/docs IIoT.CloudPlatform/src
git -C IIoT.CloudPlatform diff --check
```

## 8. 停止条件

- 需要修改 `IIoT.EdgeClient`、`AICopilot` 或工作区顶层 `docs/总规则.md`，但用户本轮未明确授权。
- 需要改 HTTPS/TLS/443/HSTS 或采购/配置证书。
- 需要把 Cloud HTTP 入口暴露公网。
- 需要猜测生产 secret、管理员密码、Harbor token、SSH 凭据或现场真实 IP。
- 需要绕过 Cloud DB、审计或发布保留策略直接写 Edge 更新目录。
- 验证失败且原因不明。
- 改动会破坏已验收的 EdgeHost/PLC、AI Read、Edge Release API key、客户端发布或设备删除链路。

## 9. 文档沉淀要求

- 已写入长期规则：
  - `docs/云端规则.md`
  - `docs/云端安全部署契约.md`
  - `docs/云端架构治理清单.md`
- 已新增的专题契约：
  - `docs/Edge上传与PLC状态接口契约.md`
  - `docs/AI只读接口契约.md`
  - `docs/设备客户端状态投影契约.md`
- 已新增但未完成执行的专题契约：
  - `docs/云端容器非Root权限契约.md`；C-06 代码和发布前门禁已实施，仍需生产 PFX/Edge 更新目录权限和真实容器启动验收通过后再关闭生产状态。
- 每批完成后必须更新：
  - `docs/改动复盘与规则沉淀.md`
- 如果后续必须同步顶层 `docs/上传部署总览.md` 或 `docs/总规则.md`，需要用户在当前轮明确授权对应顶层目录写入。
