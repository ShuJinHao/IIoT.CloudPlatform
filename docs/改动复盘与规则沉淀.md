# 改动复盘与规则沉淀

本文档是 `IIoT.CloudPlatform` 的项目滚动复盘入口，最新记录放在最前。每次代码改动完成前必须更新本文件，并判断是否需要把长期结论沉淀到 `docs/云端规则.md`、专题契约或工作区 `../docs/总规则.md` / `../docs/历史核心记录.md`。

默认只更新本滚动文档，不为每个任务新增单独流水文档。只有形成可长期复用的业务规则、技术契约、部署口径或验收口径时，才新增专题文档。

没有形成长期规则时，复盘记录必须写明固定结论“无新增长期规则”及原因。

## 2026-07-17 测试架构审计后 Contracts 与跨仓 compatibility 重新闭合

- 改动范围：仅修改 Cloud 候选分支的 Analyzer 真实 csproj fixture、`DeploymentSourceGuardTests`、compatibility schema 4/baseline、`cloud-ci` 外部证据 checkout、云端规则/架构契约/治理清单和本复盘。未修改 `CLOUDARCH008/009` 规则、生产 Cloud/Edge 业务行为、数据库 schema、部署/CD 或任何生产入口。
- 改动原因与首红：审计证明 `Invalid009`/`ValidDelegateContext` 手写 `ICacheService` 与 `IPermissionProvider`，真实 Contracts 签名漂移可 fail-open；旧 compatibility 又把 PASS 绑成 Edge HEAD 精确相等，纯文档 commit 会误报。首次真实 fixture 运行先暴露沙箱 MSBuild IPC 无诊断超时，在沙箱外保留同一改动后又暴露真实签名不匹配和聚合分层诊断；修正为真实签名/真实 Core aggregate 后绿。`DeploymentSourceGuardTests` 首次以 51/52 失败拦截了 probe marker 文本断言错误，修正后 52/52。
- 实现与影响面：两个安全缓存 fixture 现以 `ProjectReference` 引用真实 `IIoT.Services.Contracts.csproj`，`Invalid009` 使用真实 `GetOrSetAsync<T>` / `GetPermissionsAsync` 且仍只因 `CLOUDARCH007` 失败，`ValidDelegateContext` 零诊断；新 binding probe 从真实 build output 对 Analyzer 的 9 个 Contracts metadata identity 逐个 exact-one 验证。compatibility schema 4 记录 Edge `7474b3a919d8e0058ac529fdda6e2d0cd720390d` 作为 reference identity、整个 tracked `src/` state `4f04f99809aad794f491bb41ae1b185b2571d7ab51aa6f73f9b05f4fd964d6b1`、两 consumer aggregate `7eedaa475125e3e6e59a66d3a98efe39f3889deb991fbabe03b675dd6d6b05f4`、逐文件 SHA/模式数/`RequestId` absence；HEAD 不同但 source states 相同时只报 `headMatchesReference=false` 并通过，任一 tracked `src/`、consumer 聚合或 dirty 状态仍 fail-closed。
- 验证命令与结果：`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers --nologo -noAutoResponse -property:RunAnalyzers=true -property:RunAnalyzersDuringBuild=true` 为 0 warning / 0 error；`bash scripts/tests/TestCloudArchitectureAnalyzerFixtures.sh` 通过，输出 `valid=8 invalid=15 callGraphBypass=20 suppressionBypass=3 contractsMetadataIdentities=9`；`pwsh -NoProfile -File scripts/tests/Test-CloudCompatibility.ps1 -EdgeRepositoryRoot /Users/shushu/Developer/产线系统架构升级/1/.codex-worktrees/edge-startup-exception-retirement-closure -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07 -ReportDirectory /private/tmp/cloud-compatibility-final-v2` 通过，`externalConsumers=2 externalVerified=2`；`dotnet test src/tests/IIoT.CloudPlatform.DeploymentTests/IIoT.CloudPlatform.DeploymentTests.csproj -c Release --no-restore --disable-build-servers --nologo -noAutoResponse` 通过 52/52；迁移账本 `529/656/currentDelta188/unresolved0/negativeFixtures7`、质量 baseline behavior 9/9、duplication 6 组均通过。source guard 最终增加四个 consumer 行为负例文本绑定后，已再次执行完整 solution build、`Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage` 和 `Test-CloudCoverage.ps1`；最终仍为 16 runner / 661 selected / 661 passed / 0 failed / 0 skipped，lines `11677/18488`、branches `2816/5619`、P0 `Passed`。新 Cloud HEAD 远端 `cloud-ci / build-test` 未通过前不冒充最终关单证据。
- 规则提炼：形成两条 Cloud 长期测试治理规则，已写入 `docs/云端规则.md`、`docs/contracts/cloud-architecture.md` 和 `docs/云端架构治理清单.md`：Analyzer 对 Contracts metadata 必须由真实 ProjectReference/build-output probe 绑定；跨仓 compatibility 用 clean tracked-source/consumer state 判定语义，reference HEAD 仅作不可变身份记录。无新增工作区级生产规则。

## 2026-07-16 Cloud required 远端关单与总进度单一入口纠偏

- 改动范围：仅更新 `docs/云端架构治理清单.md` 和本滚动复盘，移除 Cloud 清单中已过时的三项目总进度 `22%` 副本，并记录当前 Cloud required 远端证据；不修改生产代码、测试代码、workflow、质量 baseline、Cloud/Edge/AI 产品契约、数据库 schema、CD 或部署脚本。
- 远端证据：Draft PR #36 当前实现候选 `58bbf780b0cd5ff374f13dfd34886e6d0a624d55` 的 run `29492450883` 中 required `cloud-ci / build-test` success，16 个 required runner 精确 `661/661`、0 failed、0 skipped；Release build 0 warning / 0 error，coverage lines `11677/18488`、required Linux branches `2815/5619`，compatibility active 2 / unclassified 0 / external 2 / verified 2，duplication 6 scans、deployment behavior 33/33、Vitest 67/67、browser smoke 2/2 与 Web inventory 69/69 均通过。PR 保持 Draft / Open / MERGEABLE。
- 状态结论：Cloud 仓内 `CLOUD-CACHE-001`、`CLOUD-ARCH-001`、`CLOUD-TEST-ARCH-001` 及 required 质量门禁均已由当前候选和远端 required job 闭合；三项目总百分比、contract digest、duplication report 与非生产联合验收只在工作区唯一活动总计划和 clean-HEAD evidence 中维护，避免项目清单复制旧百分比后与总计划冲突。
- 本批验证：`gh pr view 36 --repo ShuJinHao/IIoT.CloudPlatform --json number,state,isDraft,mergeable,headRefOid,statusCheckRollup`；`rg -n "总计划完成度只由|58bbf780|29492450883|661/661" docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md`；`git diff --check`。本批为状态文档纠偏，不在本地重复运行已经由同一实现候选远端 required job 完整执行的 build/test。
- 影响面：只修正活动状态入口，Cloud 测试、运行时、发布与生产行为不变；生产、LiveExternal 和真实现场验收仍按原边界单独授权。

长期规则结论：无新增长期规则；工作区已有唯一活动总计划，Cloud 规则已定义 required 和质量门禁，本批只是删除过时进度副本并写回最终证据。

## 2026-07-16 Cloud-AI WorkspaceAlignment 证据路径漂移修复（CLOUD-TEST-ARCH-001 v11）

- 改动范围：仅修改 Cloud 的 `scripts/tests/Get-CloudAiWorkspaceEvidence.ps1`、既有 `DeploymentSourceGuardTests` 和本滚动复盘；未修改 Cloud/AI 产品契约、业务代码、数据库 schema、生产配置、CD 或部署脚本，未执行生产发布或部署。
- 首红证据与根因：工作区三仓非生产联合验收已先通过 Cloud provider contract 47/47、Edge consumer contract 87/87 和跨仓 contract digest，随后在 Cloud-AI WorkspaceAlignment evidence 生成处 fail closed，缺失路径为旧 `src/infrastructure/AICopilot.CloudReadClient/CloudAiReadClient.cs`。近期历史确认 Cloud 提交 `e99b92c` 建立 evidence 源集合后，AI 提交 `ca988a9` 将 typed client 100% 迁到 `src/infrastructure/AICopilot.Infrastructure/CloudRead/CloudAiReadClient.cs`，并将对应契约测试 99% 迁到 `src/tests/AICopilot.InProcessTests/CloudAiReadClientContractTests.cs`；失败属于精确源码集合的路径漂移，不是 provider/consumer 语义回退。
- 修复与影响面：evidence 源集合只把上述两个旧路径替换为当前 AI clean HEAD 的真实受管路径，保留 Cloud/AI 精确 git root、完整 HEAD、稳定排序路径与文件 bytes SHA-256 的既有 fail-closed 算法；没有增加 glob、目录猜测、旧路径 fallback 或双轨兼容。既有 Cloud CI source guard 同步锁定两个当前路径并拒绝两个旧路径，避免只修脚本而不保护当前候选源集合。影响仅限 WorkspaceAlignment 证据身份和测试治理。
- 验证命令与结果：`pwsh -NoProfile -File scripts/tests/Get-CloudAiWorkspaceEvidence.ps1 -CloudRepositoryRoot . -AiRepositoryRoot /Users/shushu/Developer/产线系统架构升级/1/.codex-worktrees/ai-phase0-closeout` 成功生成唯一 marker，AI HEAD 为 `a6692ab1b8ed862eb5bd12852f9f8d4500f55e37`、AI candidate source digest 为 `333ed01babc7f06bce6bea15018126bfed0906cf6b1e8c5a3de6cf306889360e`；定向 `CloudCiWorkflow_ShouldRunDynamicRequiredInventoryAndKeepManualRunnersSeparated` 1/1 通过；完整 `IIoT.CloudPlatform.DeploymentTests` 52/52、0 failed、0 skipped；`git diff --check` 通过。首次沙箱内 vstest 因回环 socket `Permission denied` 中止，沙箱外以相同命令通过，不计为产品失败。提交后的 clean-HEAD 三仓联合验收、contract digest 与 duplication 由工作区总计划重新执行并绑定最终三仓 HEAD，本记录不提前冒充该结果。
- 规则提炼：无新增长期规则。`docs/云端规则.md` 已明确 WorkspaceAlignment 必须绑定两仓精确 root、HEAD 和稳定排序候选源码 digest，任一侧源码、root 或 HEAD 变化后旧结果失效；本次只是 AI 已验收物理分层后同步 Cloud evidence 消费者与 source guard，不新增更宽或不同的合同。

## 2026-07-15 Cloud 外审阻断项与真实质量对账收口（CLOUD-TEST-ARCH-001 v10）

- 改动范围：仅写入 `IIoT.CloudPlatform`，收口 Redis claim/release 取消语义、`CLOUDARCH004` 跨程序集调用图、Dapper effect/Specification 执行边界、AppHost 测试 seam、coverage/duplication/compatibility/mutation 真实当前结果对账、Cloud CI、AnalyzerTests/Workflow/Deployment 测试、正式契约/规则/清单和本复盘。未写入 Edge、AICopilot 或工作区顶层规则；未执行生产 CD、发布、上传或部署。
- 首红证据：Redis 新增语义先证明 caller cancellation 会被内部 `TimeoutException` 覆盖；真实跨 csproj 负 fixture 先证明 Dapper 写、static initializer/field、默认接口、生命周期和伪造 effect summary 可逃逸。外审一度恢复 Dapper 既有 public 可见性，随后真实 `--no-incremental` production build 立即产生 10 个 `CLOUDARCH004`：Capacity/DeviceLog/AiProduction 读端口经 public `IDbConnectionFactory.CreateConnection()` 到达不可审计的开放分派。`ISpecification<T>` 被误当执行端口会造成 EF evaluator 假阳性；普通质量基线又会冻结工具、目标、普通抽象路径或 fingerprint 身份，偏离“真实调用方 + 真实结果”的本批口径。
- Redis 纠偏：claim/renew/complete 的 Redis script 等待绑定 caller token，并在 await 后再次检查取消；release 使用独立 2 秒有界 cleanup token，超时记录 `ReleaseCleanupTimeout`，其它 cleanup 失败只记录脱敏日志，始终保留原业务异常或 caller cancellation/token。`RedisCacheServiceSemanticsTests` 定向 47/47 通过。
- Analyzer 与数据库边界：新增带 schema、来源 csproj 身份、条目 digest 和 method/field effect 的跨程序集 summary；只消费当前受管 ProjectReference，旧 schema、重复/手写伪造 manifest、非受管预编译引用、损坏 digest 和开放分派全部 fail-closed。调用图补齐 implicit/lifecycle/default-interface/extern/static field；`IReadOnlyQueryPort` / `IAiReadScopeAccessor` 递归检查返回值、参数、属性、事件、字段、delegate/Task/集合和自定义 wrapper 中的 writable capability。外审逐项扫描证实 Dapper concrete 的生产构造只在本程序集 DI、三个宿主只调用 `AddDapper`、跨 Edge/AICopilot 零调用，且 `IIoT.Dapper` 不是对外发布的包。候选先恢复 public API 再由完整生产构建精确首红，因此最终只收口 raw DB factory 及必须携带该 internal 构造参数的 implementation/repository 闭包，仅保留 `IIoT.CloudPlatform.Persistence.PostgresTests` 一条编译必需 IVT；未新增重复 read factory/port，也未放宽开放分派门禁。AnalyzerTests 72/72，真实 fixture 精确为 `valid=8 / invalid=15 / callGraphBypass=20 / suppressionBypass=3`。
- Specification 与宿主 seam：`ISpecification<T>` 不再携带读端口标记；EF evaluator 只接受内部可执行 `Specification<T>` 基类，恶意任意接口实现在读取任何 getter 前被拒绝，标准 filter/include/order/page 行为保持。AppHost 的 dispatcher-disable 只在宿主真实 `Testing` 环境开启；测试 kit 只把父宿主设为 `Testing`，AppHost 再把 `builder.Environment.EnvironmentName` 动态传给子进程，没有硬编码子进程环境。首次完整 required 因未传递该环境让 DataWorker 触发 guard 并退出，修正后 ACK-lost 定向 1/1、完整 PostgreSQL 19/19；Contract/Filesystem/Deployment source tests 同步锁定该边界。另两处只为测试/覆盖率形成的 seam 已撤销：`SerilogExtensions` 恢复 private 常量/private 目录检查与内联 file sink，测试改从 public `AddSerilog` 真实写入并观察脱敏/滚动；`EdgeReleasePublishedFilePermissions` 恢复 origin/main 平台分支并删除 `Func<bool>` probe，coverage 不再通过改生产分支换绿。
- 质量脚本纠偏：共同 helper 拒绝零 SHA、candidate HEAD 和非祖先；变更前 commit 首次没有基线时，结构/身份事实必须与本次真实结果精确一致，质量 floor 必须来自 required CI 支持平台证据。PR/push 基线只取事件给出的真实变更前祖先，定时/按需 mutation lane 固定取 `HEAD^`，没有可填写基线输入。Coverage 候选精确当前 PDB/source/assembly/report 的结构身份，已覆盖行/分支与 rate 按不可下降 floor 对账；Duplication 候选精确当前 fingerprint/instance 且只按总量棘轮，合法身份 churn 不阻断。Compatibility 只阻止真实兼容项增长；普通 `Adapter` / `Wrapper` 可增删移动，但必须有唯一 production producer、独立 production caller、真实测试且无兼容语义。Mutation 候选精确当前 tool/target/blob/fingerprint/status，跨基线只比较 score/evaluated rate 与坏状态 rate，不按 mutant 绝对数量冻结。两个无调用方的建基线/增长审计开关及其影子路径已物理删除。
- 兼容与物理删除：既有无调用方缓存 alias/adapter/wrapper/fallback/双写/影子路径保持物理删除。当前兼容扫描在 Edge clean HEAD `1d25f9a6945b0b73224cfc75250203ae35be17be` / tree `55d8694b50358792c504ff93ff8f8b4e0c659dae` 上通过 `active=2 / unclassified=0 / retained=6 / retired=1 / external=2 / verified=2`，source-state digest 为 `7eedaa475125e3e6e59a66d3a98efe39f3889deb991fbabe03b675dd6d6b05f4`；普通抽象的路径和 ID 不再跨 commit 冻结，零调用方、测试调用冒充、自调用、兼容语义和注释/字符串 decoy 负例均 fail-closed。
- 远端 CI 回归修正：Draft PR #36 候选 `e99b92caaed5600b9202ec153918fce7a7651d53` 的 run `29427865690` 已通过 restore、增量部署策略、完整 build、Analyzer fixture 和迁移门禁，但 `Verify quality baseline behavior` 在打印 `scenarios=6` 全绿后仍以 1 退出。根因不是基线或反例失败，而是 `git rev-parse` / `merge-base` / `show` 的预期失败已被 PowerShell 转换为明确异常或 bootstrap 分支后，GitHub Actions dot-source wrapper 仍继承残留 `$LASTEXITCODE`。共享 helper 现只在已捕获并完成语义翻译的 native failure 处把全局退出码复位为 0；行为脚本新增“已处理 git failure 不得泄漏 native exit code”断言。直接 `-File` 和模拟 GitHub dot-source 两种入口均打印 `scenarios=6` 且 exit 0，未修改任何质量基线、阈值、生产代码或 CD。该修正无新增长期规则，因为 fail-closed 基线合同和 behavior 反例要求已存在，本次只是 PowerShell 宿主退出码可移植性缺陷修复。
- 远端覆盖率可移植性纠偏：候选 `d856d6eac96379a2f78f42e6c5e72faa02163a1e` 的 run `29428679213` 已通过 build、Analyzer fixture、迁移、质量基线行为、兼容、重复度和 661 个 required case，随后仅在 coverage exact 分支数失败。下载并逐条比对 16 份远端 Cobertura 与本地报告后，源码/PDB/assembly universe、有效行、有效分支和 line coverage 全部一致；唯一差异是 origin/main 原样存在的 `EdgeReleasePublishedFilePermissions.cs:25` 在 Mac 对 `!IsLinux() && !IsMacOS()` 记录 `3/4`、Linux 记录 `2/4`。生产文件保持零 diff，不恢复测试 seam。基线以 required Linux 实测 `2815/5619`、`0.50097882` 作为 branch floor，本地 Mac `2816/5619` 高于 floor 合法；结构身份仍 exact，line/branch floor 或 P0 阈值下降仍失败。行为脚本新增“高于 floor 通过、低于 floor 失败、结构变化失败”三条反例。该纠偏形成 Cloud 项目级长期质量规则，已更新 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`；未修改生产代码、部署 workflow、CD 或 P0 阈值。
- 远端 source guard 对账修正：候选 `7f69bff8b5e9a5f481d35a4f704aa3906cfb7285` 的 run `29430699450` 已通过 build、Analyzer fixture、迁移、9 条质量行为、兼容和重复度，required 执行中只有 `DeploymentSourceGuardTests` 1/52 失败；其仍要求行为脚本文本包含旧 `scenarios=6`。该 guard 现对账 `scenarios=9`，并同时锁定 `coverageImprovementAccepted`、`coverageRegressionRejected` 和 `coverageStructureRejected`，避免只改数字而漏掉三种语义。无新增长期规则，因为跨平台 coverage floor 的正式规则已在上一条沉淀；本次只是同一门禁消费者的计数与语义同步。
- 当前验证：Release solution `--no-incremental` build 0 warning / 0 error；AnalyzerTests 72/72，真实 csproj fixture `8/15/20/3`；质量基线行为 9/9；迁移账本 current 656 / delta 188 / unresolved 0；SharedKernelGuard 16/16；Redis 语义 47/47、真实 Redis 5/5、Workflow 68/68、WorkflowFilesystem 88/88、HTTP 26/26、PostgreSQL 19/19、Deployment 52/52。最终 16 个 required runner精确 discovered/executed/passed `661/661/661`、failed 0、skipped/notExecuted 0。旧 coverage 候选混入 AppHost 改动前 runner 二进制产生的 10 条 stale sequence-point 行和 6 个分支，虽为绿结果仍被候选精确对账拒绝；门禁现于 runner 启动前校验生产 DLL/PDB SHA-256，以 PDB document hash 逐文件核验当前生产源码 bytes，并要求每份 Cobertura production assembly 与采集身份精确同集，空 `<packages />` 的测试-only report 也作为合法零生产程序集输入处理。以锁定 merge-base `88c41109fbcf0b87b18939a139e0bff751e03d07` 对最终候选重建后，Coverage 为 lines `11677/18488`、Mac branches `2816/5619`、required Linux branch floor `2815/5619`、line rate `0.63159887`、branch floor `0.50097882`、files 376、production assemblies `21/21`、new P0 `Passed`。Duplication 6 scans 通过；mutation created 27 / tested 21 / killed 21 / survived 0 / timeout 0 / noCoverage 0 / ignored 4 / compileError 2，score 100%、evaluated rate `0.77777778`；compatibility 为 active 2 / unclassified 0 / external 2 / verified 2。JSON/YAML/本地链接/尾随空白和最终 diff 检查见本记录列出的验证命令。
- 验证命令：`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --no-incremental`；`dotnet test src/tests/IIoT.CloudPlatform.AnalyzerTests/IIoT.CloudPlatform.AnalyzerTests.csproj -c Release --no-restore`；`bash scripts/tests/TestCloudArchitectureAnalyzerFixtures.sh`；`pwsh scripts/tests/Test-CloudQualityBaselineProtectionBehavior.ps1`；`pwsh scripts/tests/Test-CloudTestMigration.ps1`；`pwsh scripts/tests/Test-CloudCompatibility.ps1 -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07 -EdgeRepositoryRoot ../../IIoT.EdgeClient`；`pwsh scripts/tests/Test-CloudDuplication.ps1 -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07`；`pwsh scripts/tests/Invoke-CloudMutation.ps1 -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage -ResultsDirectory artifacts/test-results-v10-scope-final-v6`；`pwsh scripts/tests/Test-CloudCoverage.ps1 -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07 -ResultsDirectory artifacts/test-results-v10-scope-final-v6`；完整 DeploymentTests、JSON/YAML/Markdown 本地链接/尾随空白和 `git diff --check`。
- 规则提炼：形成 Cloud 项目级长期技术规则，已写入 `docs/云端规则.md` 6.1、`docs/contracts/cloud-architecture.md` 和 `docs/云端架构治理清单.md`：受管跨程序集 effect summary、递归 writable capability、Specification 可执行闭包、AppHost Testing seam、首次结构身份精确基线、required CI 覆盖率 floor 与后续指标棘轮、采集时 production DLL/PDB 身份对账、普通抽象按当前真实调用方验证。本批无工作区级新规则，因为这些均是 Cloud Analyzer、测试 CI 和缓存合同细节；三项目总计划完成度仍按 22%，错误治理调查与清理不计完成度。

## 2026-07-15 Cloud 缓存 caller、失效 fence 与物理去重终审收口（CLOUD-TEST-ARCH-001 v9，已被 v10 纠偏）

- 改动范围：仅写入 `IIoT.CloudPlatform`。收口 `ICacheService`/`RedisCacheService`、10 个生产 caller、产能查询同域策略、Application/Workflow/真实 Redis 测试、duplication/coverage/compatibility 基线、Cloud CI、正式缓存契约、Cloud 规则/清单和本滚动复盘。未写入 Edge、AICopilot 或工作区顶层规则；未运行 `Deploy-Changed.ps1`、发布、上传或生产 CD。
- 改动原因：终审证明旧合同仍同时存在三类错误口径：分离 `GetAsync`/`SetAsync` 使 caller 可绕过单次 factory/admission；可空 options 和默认 TTL 仅被测试便利保活，没有生产调用方；普通 remove 在 generation bump 失败时仍返回成功，与“失效后旧 factory 不得回填”相互矛盾。另外 Human/Edge 四个产能 handler 复制同一 key/admission/TTL 策略，新增缓存测试也使 duplication 棘轮增长。
- 红灯证据：新的确定性 Workflow 反例先让 factory 捕获 generation=1 并阻塞，再使 ordinary exact-key bump 抛出同一 `RedisConnectionException`。修复前定向 1/1 失败：期望该异常，实际 `RemoveAsync` 返回成功，执行物理 remove 后释放 factory 仍将旧值回填。该证据否定了“bump 失败可降级且仍保证 fence”的旧声明。
- 实现与物理删除：`ICacheService` 只保留 `GetOrSetAsync`/`RemoveAsync`/`RemoveByPatternAsync`；分离 Get/Set、兼容 overload、默认 predicate、默认 TTL 和 constructor options fallback 已物理删除。`shouldCache` 与正值 TTL 是必填参数；options wrapper 与其 `Value` 均由生产 DI 必填注入并在构造时 exact null fail closed，不把 null 延迟为 `NullReferenceException`。ordinary exact/pattern generation bump 现为失效成功的必要条件；Redis 连接/超时在物理 remove/endpoint scan 前 fail closed，不再返回伪成功。bump 成功后的普通值物理删除/scan 白名单降级边界保持不变。
- caller 语义：权限 1、工序 1、设备 1、配方 2、产能 5 共 10 个生产 caller 全部迁移到唯一 `GetOrSetAsync` 合同，显式给出 null/空 admission、TTL 和 caller token。产能 4 个 Human/Edge summary/range handler 在同一 `ProductionService/Queries/Capacities` bounded context 内共用 `CapacityQueryCache`，共有 4 个真实 caller，仅收敛 key/factory/admission/TTL；Human 权限校验仍在共享策略前执行，不是 alias、adapter、wrapper 或兼容层。
- 测试与账本：当前资产为 18 runner / 710 case、16 required / 646 case、5 support / 0 case；Analyzer 61、Application 157、Workflow 68、Postgres 19、真实 Redis 5。`CLOUD-CACHE-001` 为 SQLite 2 + Application caller matrix 10 + Redis 5 + Workflow 47 = 64 case。pre-task 529 条旧声明当前账本为 current 641 / delta 173 / retained 0 / moved 468 / replacement 42 / Analyzer 14 / retired duplicate 5 / unresolved 0。caller matrix 直接执行 10 个 handler，锁定合法空值缓存、null/空 range 拒绝缓存、授权顺序、cache hit 二次授权与 token 传递。
- 重复代码治理：产能同域策略、Application 重复 handler setup/空结果断言和 Workflow 重复 Fusion mock 已收敛为有多个真实调用方的 helper。未放宽数值棘轮：production strict 292 < 294、mild 50 < 52；test strict 278 = 278 且 duplicated lines 2641 < 2701 / tokens 22665 < 23347，mild 53 < 60。严格 fingerprint 只在物理去重且全部数值不增长后建立，随后 no-update 复验 6 scans 通过。
- 当前验证：Release solution build 0 warning / 0 error；16 required runner 精确 discovered/executed/passed `646/646/646`、failed 0、notExecuted/Skip 0，Workflow 68/68、真实 Redis 5/5、PostgreSQL 19/19。旧 coverage 基线先以 source-universe mismatch fail closed；建立后的 no-update 为 16/16 report、376 文件、11624/18409 行（`0.63143028`）、2810/5607 分支（`0.50115927`）、21/21 production assembly，P0 changed code 为 627/661 行（`0.94856278`）和 142/154 分支（`0.92207792`）。干净提交首次复算还精确暴露 post-write fence cleanup 的 5 行只在测试调度恰好超过内部超时时才命中；现有同一 Workflow case 改为等待 stale-entry remove 这一可观察结果后再释放阻塞读，使 cleanup timeout 分支确定执行，未新增 case、未下调基线。Analyzer fixture valid 6 / invalid 14 / suppression bypass 3、迁移账本 unresolved 0、duplication 6 scans、部署 policy/脚本语法/behavior 33/compose、Web type-check/Vitest 67/browser smoke 2/Web inventory 69、solution direct/transitive vulnerability 0、JSON/YAML/本地链接/尾随空白和 `git diff --check` 全部通过。跨仓 compatibility 已在 Edge clean HEAD `caf5004420d02e714f66df6679dabb97edc255b8` / tree `d7654c2b732e537a491b24fe3e3c428d3a3df344` 上精确得到 active 2 / unclassified 0 / external 2 / verified 2，两份调用源码与 source-state digest 均已固定。
- 验证命令：`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore -warnaserror`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage -ResultsDirectory artifacts/test-results-v9-clean-final`；`pwsh scripts/tests/Test-CloudCoverage.ps1 -BaseRef HEAD^ -ResultsDirectory artifacts/test-results-v9-clean-final`；`pwsh scripts/tests/Test-CloudCompatibility.ps1 -EdgeRepositoryRoot /Users/shushu/Developer/产线系统架构升级/1/IIoT.EdgeClient`；Analyzer fixture、migration、duplication、deployment policy/脚本语法/behavior、Web type-check/Vitest/build/browser smoke/Web inventory、production compose、活动治理遗产扫描和 `git diff --check`。
- 规则提炼：形成 Cloud 项目级长期规则，已写入 `docs/云端规则.md` 6.1/6.2、`docs/contracts/caching-resilience.md` 和 `docs/云端架构治理清单.md`：所有 caller 显式 admission + 正值 TTL、options wrapper/`Value` 构造时 exact non-null 且无 fallback、ordinary generation bump 失败不得返回成功、以及只允许有多个真实调用方的同 bounded-context 策略共享。本批无工作区级新规则，因为上述均是 Cloud 缓存合同与门禁细节。

## 2026-07-15 Cloud 第二轮独立终审与缓存事务补偿收口（CLOUD-TEST-ARCH-001 v8，已被 v9 纠偏）

- 新增反例与账本审计：外审新增的 release-failure Theory 在修复前 2/2 精确失败：原 `OperationCanceledException`/caller token 和原业务 `InvalidOperationException` 均被 secondary `RedisConnectionException` 覆盖；改为 nested best-effort compare-delete 后 2/2 通过并保留原实例/token。该 Theory 新增 1 条声明/2 case。迁移脚本不提供可更新 current baseline，因 pre-task 529 是不可变基线，current/delta 由脚本动态生成；对比旧 CI 精确断言 628/160/45 与 no-update 脚本新输出 629/161/46 后，只显式更新 CI 断言，不改 pre-task baseline，再以 no-update `Test-CloudTestMigration.ps1` 复验 `unresolved=0`。
- 改动范围：本轮继续只写入 `IIoT.CloudPlatform`，补强 Domain Event 缓存失效、Analyzer/AnalyzerTests、DataWorker/Outbox 测试组合、required inventory/coverage 门禁、Cloud CI 计数、正式规则/契约/清单/本复盘和既有 deploy 验收文档。未写入 Edge、AICopilot 或工作区顶层规则；未运行 `Deploy-Changed.ps1`、发布、上传、部署、nginx reload 或生产 CD。本地终审修正尚未 commit/push；既有 Draft PR #36 仍停在旧 head `82d0719619334df22d4499f4b9417da868f6ffad`，不代表当前候选。
- 改动原因：第二轮独立审计证明，先前 ACK-lost/Outbox 已保证 transport/inbox 幂等，但同一 Domain Event 在 EF execution-strategy 重试中仍可重复执行 Redis 缓存失效，业务 pattern 也可命中系统 receipt namespace。外部复核进一步用真实 Redis 证明 `GetOrSetAsync` 的 in-flight factory 会在 `InvalidateOnceAsync` remove 完成后把旧值重新写回；只在调用方正常返回后清理还会被 caller cancellation 或 Fusion synthetic timeout/background completion 绕过。Analyzer 对外部 callback receiver、`DynamicInvoke` 和 quoted SQL 边界仍可存在绕过；DataWorker 单调用方组合 helper 与无真实运行时调用方的旧缓存 alias 不应因测试或覆盖率保留。
- 实现与物理删除：新增独立 `IIdempotentCacheInvalidationService`，以稳定 `OutboxMessage.Id + handler scope` 生成 Redis receipt，使用 claim/renew/complete/release compare-script 冻结事务重试中的单次逻辑失效。设备/配方失效只由 Domain Event handler 触发；命令直接删除、旧 non-Once 方法、alias/fallback/双写已物理删除。`__iiot_system:` 整个 namespace 禁止业务 key/pattern/glob 命中。严格失效在 remove 前推进 exact-key/pattern generation；Fusion 2.6 factory wrapper 在交付写回前二次核对，变化或不可验证时动态设置 `SkipMemoryCacheWrite`、`SkipDistributedCacheWrite` 和 skip backplane SET，并禁用 timed-out factory background completion；正常 provider 完成后再以独立 2 秒 cleanup token 二次 remove，覆盖 check→write 窗口。caller cancellation 仍以原 token 退出，但不能取消 fence 清理。严格失效的断连、取消、未知异常和 owner 丢失都传播；失败以 best-effort compare-delete 释放 claim，release 自身失败只记录 EventId 2401 的稳定 operation/异常类型，不遮蔽原业务异常实例或 caller cancellation/token；崩溃由 lease 过期允许补偿重放，未宣称崩溃 exactly-once。
- 组合与 Analyzer：DataWorker 正式 MassTransit/EF inbox/outbox 组合已回到唯一 composition root，无独立生产调用方的 `DataWorkerMassTransitConfiguration` 已物理删除；测试专用 worker-disable 只能在精确 `Testing` 环境显式开启，其它环境配置 true 在宿主构造时 fail closed，production compose 固定 `DOTNET_ENVIRONMENT: Production` 且不存在该开关。Analyzer 现跟踪 contextual callback 和外部 receiver、拒绝 `DynamicInvoke`，并精确处理 quoted identifier/string/comment；AnalyzerTests 由 52 增至 61，真实 fixture 由 valid 5 增至 6，invalid 14/suppression 3 不变。
- 测试与账本（v8 过程口径，已作废）：该阶段资产为 18 runner / 698 case、16 required / 634 case、5 support / 0 case；Analyzer 61、Contract 47、Application 147、Workflow 68、Postgres 19、真实 Redis 3。该阶段 `CLOUD-CACHE-001` 为 SQLite 2 + Redis 3 + Workflow 47 = 52 case。pre-task 529 条旧声明该阶段对账为 current 629 / delta 161 / retained 0 / moved 468 / replacement 42 / Analyzer 14 / retired duplicate 5 / unresolved 0，7 个负 fixture 保持通过。当前口径只认 v9 的 710/646/CACHE64/Redis5/current641/delta173。
- 首轮失败与纠偏：Inventory 首先因真实 PostgreSQL Outbox 中两次有界 `Task.Delay(100, cancellationToken)` 未进入精确 allowlist 而失败，只按文件 + 文本 + 次数 2 + 60s 取消证据放行。首次 required 又因选择性 build 留下旧 Deployment DLL 而 2/51 失败。后续旧 coverage 数字与 P0 将 Cobertura 已覆盖的 Redis/Outbox 行误合并为 0，根因是 16 个 runner 输出中混用了不同源码映射的旧 DLL/PDB。上述 stale/ghost 结果均作废，不得作为缺测或基线下调证据；全 solution Release rebuild 后 P0 恢复通过。重复度终审还发现 production strict 296 超过 295，以四个真实配方 handler 共用执行器和 Redis 测试 `SetupKeys` 消除重复后降至 294，未放宽阈值。外部并发反例修复前真实 Docker Redis 定向结果为 1/1 failed：失效后仍读到 `stale-before-invalidation`；generation fence 后同一用例连同 exact、pattern、caller cancel/ignored token 和 synthetic timeout 全部通过。
- 外部审核裁决：`Publish -> Save/Commit` transient 不应改成 at-most-once；发送事务回滚后以持久化 `OutboxMessage.Id` 重发是预期 at-least-once transport，真实 receiver 依靠 `MessageId + ConsumerId` inbox 收敛为 exactly-once business side effect，既有 ACK-lost Postgres 用例已证明 publish 2 / handler 1、stable ID、inbox consumed/delivered 和 cleanup。新增的三个 MassTransit 表与 DataWorker/EF DI 只服务该 receiver inbox/outbox 闭环，是上述授权缓存/事务/异常语义的最小 schema/composition，不承载额外业务模型或第二套路径。worker-disable 的标准生产路径不能绕过：运行时代码拒绝非 Testing，production compose 显式固定 Production 且 deployment guard 拒绝测试开关；具备主机配置权限的人若故意把整个进程伪装为 Testing 已超出应用配置可防御边界，不作为生产 fallback。
- v8 过程验证证据（已作废）：该阶段 Release solution build 0 warning / 0 error；Inventory `baseline=698 required=634 regressionIds=18 regressionZero=0 support=5 skipped=0`；Required+CollectCoverage 为 634/634。该阶段 Coverage source universe 为 375 文件 / 18366 可执行行 / 5621 分支，global 为 11389/18366 行（`0.62011325`）和 2784/5621 分支（`0.49528554`）。这些数字只保留为 v8 过程证据；v9 已修改 production/test source，因此不得复用该 coverage digest 或将其写成当前结论。
- 验证命令：`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers --nologo -noAutoResponse -property:RunAnalyzers=true -property:RunAnalyzersDuringBuild=true`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Inventory -Configuration Release -NoBuild`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage`；`pwsh scripts/tests/Test-CloudCoverage.ps1 -BaseRef HEAD`；Analyzer fixture、migration、compatibility、duplication、mutation、Node、deploy、Web、compose、JSON/YAML/Markdown 与 `git diff --check` 门禁。
- 规则提炼：形成 Cloud 项目级长期规则，已写入 `docs/云端规则.md` 6/6.1/6.2、`docs/contracts/cloud-architecture.md`、`docs/contracts/caching-resilience.md` 和 `docs/云端架构治理清单.md`：Domain Event 缓存失效的稳定 operation/scope、系统 namespace 隔离、事务重试单次逻辑失效与崩溃补偿边界，Analyzer 的 callback/`DynamicInvoke`/quoted SQL fail-closed，以及覆盖率必须先同步全 runner 产物。本批未新增工作区级规则，因为上述均是 Cloud 内部实现与门禁细节。

## 2026-07-15 Cloud 第一轮独立终审阻断项收口（CLOUD-TEST-ARCH-001 v7，已被 v8 纠偏）

- 改动范围：本批只修改 `IIoT.CloudPlatform` 的 Analyzer/AnalyzerTests、inventory/迁移/兼容/覆盖率门禁、Cloud CI 计数、Outbox 与 DataWorker MassTransit 组合、EF inbox migration、真实 PostgreSQL 行为测试、文件权限平台分支、真实浏览器身份验收、Cloud 正式规则/契约/清单/本复盘及已存在的部署验收文档。未修改 Edge、AICopilot 或工作区顶层规则，未运行 `Deploy-Changed.ps1`、发布、上传、远端部署或生产 CD。该阶段本地修正尚未 commit/push；既有 Draft PR #36 仍停在旧 head `82d0719619334df22d4499f4b9417da868f6ffad`，不代表 v8 当前候选。本条是对 2026-07-14 “最终收口”记录的终审增量纠偏，下方旧计数只作过程证据。
- 改动原因：独立终审确认 9 个 P1 与 1 个 P2 阻断仍可使“结果为绿”与“真实高风险语义已验证”分离：OS 分支使基线跨平台漂移；Outbox 重试没有强制稳定 transport ID；receiver 缺真实 inbox 幂等证据；Analyzer 可被存储委托、泛型约束、raw SQL 和未分类生产项目绕过；inventory 可漏 Import/target-time 图变更与伪 Node consumer；coverage 可漏 production assembly；跨仓兼容证据可非 clean 或不精确；浏览器证据可由手工 fetch 自证；CI 当前计数未纳入新的 ACK-lost case。P2 为无真实生产调用方的 generic publisher overload，必须物理删除而不是标 optional。
- 终审修复 1–3（过程记录，v10 已纠偏）：该阶段曾为 `EdgeReleasePublishedFilePermissions` 加入可注入平台 probe，v10 审计确认它只为覆盖率改写生产分支后已完整撤销。`IEventPublisher` 现只保留显式 `Guid messageId` 的唯一合同，无调用方 generic overload 已物理删除；`OutboxMessageDispatcher` 传入持久化 `OutboxMessage.Id`，`MassTransitEventPublisher` 拒绝 empty ID 并设置 transport `MessageId`。DataWorker 的三个真实 endpoint 由唯一 `DataWorkerMassTransitConfiguration` 启用 PostgreSQL EF inbox/outbox；新增正式 migration `20260714224929_AddIntegrationEventConsumerInbox`，以 `MessageId + ConsumerId` 唯一约束、7 天 duplicate window 和 1 秒 query delay 固定消费幂等与清理边界。
- 终审修复 4：真实 PostgreSQL ACK-lost 测试在发送方 transport publish 后、事务 commit 前注入 SQLSTATE `40001`，以同一持久化 ID 发送两次；接收方使用真实 MassTransit EF inbox、`IReceiveObserver` barrier 和真实业务审计副作用，精确证明 receive 2 / handler 1 / inbox consumed+delivered，并检查 cleanup options 和注册。测试不使用固定延时，还直接执行 DataWorker 正式组合的有/无连接字符串分支。Postgres runner 因此从 17 增至 18 case。
- 终审修复 5：`CLOUDARCH004/007` 调用图在 compilation end 解析字段、属性、局部、参数存储委托，并正确识别 `IAiReadRequest<>` 泛型约束与直接 lambda caller。新的 `IIoT.*` 生产 assembly 无法归层时以 `CLOUDARCH001` fail-closed。AI Read 内 EF `ExecuteSql*` 始终是写 sink；Dapper `Query*` 与 ADO reader/scalar 只在编译期常量、词法闭合、单语句且严格只读 `SELECT/CTE` 时放行，动态、未闭合、多语句、DML/DDL 和写 CTE 均失败。AnalyzerTests 保持 52/52，真实 csproj fixture 由 valid 4 / invalid 13 提升为 5 / 14，suppression bypass 保持 3。
- 终审修复 6：inventory 以 evaluated MSBuild + `dotnet msbuild -preprocess` 展开完整 Import 闭包，并在 target-time 检测 `Compile/Reference/ProjectReference/PackageReference/Analyzer/MSBuild/Csc` 图注入；全仓 csproj 的 runner/support/production 归属、solution membership、引用闭包和零发现都必须精确对账。WebE2ETestKit 的外部调用改由真实 JS spawn parser 确认，注释、字符串、template 和同名 decoy 均不能保活 support。当前资产为 18 runner / 674 case、16 required / 610 case、5 support / 0 case，18 个 RegressionId floor 无归零。
- 终审修复 7：coverage 现从五个 production root 的 evaluated project 与 portable PDB 组合源码全集，基线为 374 文件 / 18018 可执行行 / 5507 分支，source digest `9e13d1f8…bfb48d2`；21 个 production assembly digest 为 `d705735d…59e39b0`。实际 coverage package 与 evaluated assembly 在每次运行中必须精确 21/21，missing/unexpected/duplicate 一律失败，候选基线也必须与当次真实报告精确一致。changed-code 白名单只允许精确路径的 `non-executable-source` 与 EF `generated-migration-source`，不允许 glob；本批 P0 新代码 73/74 行、4/4 分支，line `0.98648649`、branch `1.0`，全量为 11092/18018 行、2679/5507 分支。远端 run `29371660617` / job `87216117685` 在候选 `82d0719` 上的 build 和当时 required 609/609 已通过，但 Ubuntu branch 2676/5513 低于 Mac 基线 2677/5513，精确暴露 OS short-circuit 漂移；该远端失败只作问题证据，不冒充当前候选远端验收。
- 终审修复 8：compatibility schema 升为 v3，跨仓证据要求显式 clean Edge root、精确 HEAD、source-state digest、文件 SHA-256、可执行调用模式和调用数；当前精确为 active 2 / unclassified 0 / external 2 / verified 2。不成立且无其它调用方的 alias、adapter、wrapper、fallback、双写或影子路径仍必须物理删除。CI 中的 Edge HEAD 仅是跨仓候选临时固定值，必须在 Edge 最终提交后由主任务更新，不得把中间 SHA 写成最终交付。
- 终审修复 9：真实浏览器测试不再使用自证式 dependency chain 或手工 fetch。合法登录后导航 `/devices`，必须观测真实应用 XHR `GET /api/v1/human/devices` 的 200、精确 bearer 和响应驱动 DOM；伪造签名 token 经同一应用入口必须返回 `401/403` 并显示登录 DOM。workflow、deploy 文档与 source guard 已同步 674/610、Postgres 18 和 fixture 5/14；计数修正不改变 EndToEnd 63、WorkspaceAlignment 1、Vitest 67、browser smoke 2、真实浏览器 E2E 1 或 deployment behavior 33 的独立边界。
- 迁移/重复/退役对账：pre-task 529 条旧声明在当前 606 条声明中对账为 retained 0 / moved 469 / replacement 41 / Analyzer 14 / retired duplicate 5 / unresolved 0，current delta 137，7 个负 fixture 通过。重复度棘轮为 production strict 295 / mild 52、support strict 4 / mild 2、test strict 269 / mild 62、cross-context 22，全部未回归；mutation 为 created 27 / tested 21 / killed 21 / survived 0 / timeout 0 / noCoverage 0 / score 100%。
- 本地已完成验证：Release restore 成功，最终 solution build 0 warning / 0 error；AnalyzerTests 52/52 随 required 通过，fixture valid 5 / invalid 14 / suppression bypass 3；inventory 为 baseline 674 / required 610 / support 5 / skipped 0；required 发现 = 执行 = 通过 = 610，failed = skipped = notExecuted = 0，16 份 coverage 聚合通过；coverage 全集与 P0 changed-code 通过；ACK-lost Postgres 定向 1/1、Windows/Unix 文件权限定向 1/1；compatibility 在临时 clean Edge SHA 上为 2/2；部署策略、PowerShell/shell 语法、33/33 deployment behavior；Web type-check、Vitest 67、production build、browser smoke 2 和 required inventory 69/69 全部通过。同一轮后端 EndToEnd 按每 Fact 独立 Aspire 环境运行 44 分 45 秒，TRX 在 `2026-07-15T08:13:15+08:00`–`08:58:01+08:00` 精确为 total = executed = passed = 63、failed = notExecuted = skipped = 0；未重启、未沿用旧 TRX。真实浏览器 EndToEnd 1/1、unexpected = skipped = 0，真实应用设备 XHR 先 200、伪签名 token 后 401；Web EndToEnd inventory 为 1 runner / 1 file / 1 case / 0 failed / 0 skipped。production Compose `config -q`、JSON/YAML/本地链接/尾随空白/核心口径检索和 `git diff --check` 均通过；本批 Aspire/DCP/容器无残留。唯一尚未封板项是 Edge rebase 后的最终 clean SHA 刷新与 compatibility 2/2 复验，当前中间 SHA 不冒充最终跨仓证据。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx --disable-parallel`；`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers -property:RunAnalyzers=true -property:RunAnalyzersDuringBuild=true`；`bash scripts/tests/TestCloudArchitectureAnalyzerFixtures.sh`；`pwsh scripts/tests/Test-CloudTestMigration.ps1`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Inventory -Configuration Release -NoBuild`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage`；`pwsh scripts/tests/Test-CloudCoverage.ps1 -BaseRef 82d0719`；`pwsh scripts/tests/Test-CloudCompatibility.ps1 -EdgeRepositoryRoot <temporary-clean-edge-root>`；`pwsh scripts/tests/Test-CloudDuplication.ps1`；`pwsh scripts/tests/Test-CloudMutation.ps1`；Postgres ACK-lost 与 UnitFilesystem 精确 `dotnet test --filter`；`pwsh deploy/tests/TestDeploymentPolicy.ps1`；部署 PowerShell/shell 语法；`bash deploy/tests/deployment-behavior.sh`；`pwsh scripts/tests/Test-CloudWebInventory.ps1 -Mode Required`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode EndToEnd -Configuration Release -NoBuild`；`xmllint --huge` 解析当前 EndToEnd TRX；`PLAYWRIGHT_JSON_OUTPUT_FILE=../../../artifacts/test-results/playwright-real-e2e.json npm run test:e2e-real`；`pwsh scripts/tests/Test-CloudWebInventory.ps1 -Mode EndToEnd`；`docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`；JSON/YAML/Markdown 本地链接与尾随空白检查；核心兼容/计数/治理遗产 `rg`；`git diff --check`。
- 规则提炼：形成 Cloud 项目级长期技术规则，已写入 `docs/云端规则.md` 6/6.1、`docs/contracts/cloud-architecture.md` 和 `docs/云端架构治理清单.md` 的 `CLOUD-ARCH-001` / `CLOUD-TEST-ARCH-001`：稳定 Outbox transport ID 与真实 receiver inbox、stored delegate/generic/raw SQL/unknown layer fail-closed、preprocessed evaluated graph 与真实 spawn consumer、PDB source universe 和 21/21 assembly 双向对账、clean 跨仓兼容证据、真实应用 XHR/DOM 浏览器证据、以及无调用方兼容/抽象路径物理删除。这些均为 Cloud 实现与门禁细节，本批不在工作区 `../docs/总规则.md` 重复制造第二入口。

## 2026-07-14 Cloud 测试架构治理最终收口（CLOUD-TEST-ARCH-001）

- 改动范围：仅写入 `IIoT.CloudPlatform` 的 solution、Analyzer/AnalyzerTests、18 个物理测试 runner 与 5 个零 case support 项目、高风险行为测试及其暴露的 Cloud 实现收口、inventory/迁移/覆盖率/重复度/mutation/兼容性工具、`cloud-ci`、Cloud Web 测试与 Vite 环境输出收口、Cloud 规则/契约/清单/本复盘及 3 份 deploy 验收文档。5 个 support 分别是 `TestKit`、`IntegrationTestKit`、`PortFakes`、`FilesystemTestKit` 与 `WebE2ETestKit`；FilesystemTestKit 只承载跨 Filesystem runner 共用的仓根解析，WebE2ETestKit 承载真实浏览器 Aspire host，均不包含测试 case。兼容退役还物理删除了零调用方生产 `POST /api/v1/edge/process-records`、`ProcessRecordUploadRequest/Mapper` 和无调用方 `scripts/verify-cloud-baseline.ps1`，并同步移除 `deploy/nginx/nginx.conf` 对该旧 route 的专用限流 location；Edge 零调用方由 compatibility baseline 固定。这是源码/模板退役，不是生产变更执行，本批未运行 nginx reload、`Deploy-Changed.ps1`、发布、上传或部署。未写入 `IIoT.EdgeClient`、`AICopilot` 或工作区顶层规则；本 worktree 在记录时未 commit、push 或创建 PR。
- 改动原因与影响面：旧测试大桶、filter 和文档分类不能证明物理边界，也无法保证旧语义搬迁后不丢失。本批将 Architecture/Analyzer、Unit、Aggregate、Application、Contract、Persistence SQLite/Postgres、Integration Redis、Workflow、HTTP、Deployment、EndToEnd、WorkspaceAlignment 及 Filesystem 运行时边界落到独立 csproj，并使 CI 直接对账物理 runner、发现数、执行数、失败数和 Skip；独立终审进一步修正取消传播、Outbox 并发事务、schema upgrade、PortFakes 漏检和 Web E2E 凭据失败路径。下方同日早期记录仅是过程快照，不得作为当前入口。
- 物理清单与必跑对账：最终候选 Release discovery 为 18 runner / 673 case，`RegressionId` 673/673 非空、18 个 floor 无归零；push/PR required 为 16 runner / 609 case。主 Agent 发现 raw csproj/浅层 Import 可漏掉条件、隐式注入的测试依赖或编译源码后，将 inventory 改为读取当前 Configuration 的 evaluated MSBuild 属性、TargetFramework、ProjectReference、PackageReference 与 Compile，双向锁定 runner/support/production 身份、仓内全部 csproj 分类和 solution membership，拒绝 multi-target 未对账、Compile Link/跨项目源码、runner 间引用、imported test package、target-time graph mutation 和无消费 support，并检查 Pure/Filesystem/SQLite 的传递 TestKit 闭包；内置隐式递归 Import + compound condition、multi-target、Compile Link、传递 support、reachability、imported package 与动态 target 正反 fixture。真实浏览器 Aspire host 原藏在 `scripts/tests`、未进入任何物理层且其固定轮询逃过 TestKit 检查，现已物理迁到第五个零 case support `WebE2ETestKit`，由 Node orchestrator 唯一消费并把轮询纳入精确有界 allowlist。生成代码、callback/dynamic 与兼容证据防伪收口后，全新 `artifacts/test-results/root-final-required-coverage-v6` 权威复验为 discovered = total = executed = passed = 609、failed = skipped = notExecuted = 0、support 5；v5 及更早 609/609 均只保留为过程证据。EndToEnd 63 和 WorkspaceAlignment 1 保持独立显式 lane；其余四个 support 为 TestKit、IntegrationTestKit、PortFakes、FilesystemTestKit，没有 filter 假分层、Compile Link 影子 runner或旧名 wrapper。Architecture、ContractFilesystem 与 Deployment 原先各自复制的仓根查找已物理删除并收敛到唯一 `FilesystemTestKit.CloudRepositoryPath`。
- Analyzer 与高风险语义：三层依赖/DDD/数据库 owner/AI Read/请求授权元数据/生产依赖测试资产/安全读取缓存/JWT/退役命名空间/连接常量由 `CLOUDARCH001`–`CLOUDARCH010` 默认 Error 且 `NotConfigurable` 的独立 Analyzer 守卫；生产项目 evaluated graph 必须精确携带 Analyzer 且 CI 显式启用，callback/lambda/存储委托进入调用图，dynamic fail-closed，生成代码同样执行分析并上报。AnalyzerTests 52/52，真实 csproj fixture valid 4 / invalid 13 / suppression bypass 3，其中 `<auto-generated/>` 负例证明生成标记不能绕过；`PortFakes` 现在由明确 assembly 判定为 test support，并从生产 Analyzer attach 集合排除。`CLOUD-CACHE-001` 精确 40 case（SQLite 2 + Redis 1 + Workflow 37），密码锁定和取消测试不再虚标缓存；真实 Redis 1/1 通过两个独立生产 `AddInfrastructures` 容器解析各自的缓存服务、FusionCache 与连接，reader 先把同 key 的旧值写入自身 L1，writer 更新后 reader 必须由 backplane 失效并读到新值，删除 backplane 接线会使该测试失败。PostgreSQL/Aspire 保持 17/17。Outbox 用两个真实 worker/barrier 证明 `SKIP LOCKED` 和事务，caller cancellation 传播且回滚；DeviceIdentity 预取消/lock-wait 取消与 PermissionProvider 真实 SQLite await-barrier 均传播；legacy device/identity schema 在真实 PostgreSQL 上证明冲突阻断、规范化、唯一索引、旧列删除和 drift repair。另以 required 契约 + 真实 Aspire 证明 DeviceBinding token/device 不匹配返回 403，以及 human/edge/bootstrap/ai-read 四份 OpenAPI 真实生成并隔离。
- 旧声明迁移账本：真正 pre-task 基线固定为 commit `88c41109fbcf0b87b18939a139e0bff751e03d07`、path `src/tests`、tree `604fc2c1f2b9e4746657f314191a4514ccf75530`、声明 digest `d75d5529e418be31819676abeb210a79550149e14896fbe1857c30a83149c77c`、旧 `[Fact]` / `[Theory]` 529 条。逐条结果为 retained 0 / moved 469 / replacement 41 / Analyzer 14 / retired duplicate 5 / unresolved 0；当前声明 605，current delta 136，其中 AnalyzerTests 52 和 `RedisCacheServiceSemanticsTests` 37 单列；7 个负例证明错 commit/path/tree/digest/count、缺失替代目标和非法 Analyzer 诊断均会失败。
- 重复与兼容治理：与测试架构无关的错误活动资产及前置已物理删除，不以 optional 或同义机制保留；通用 PowerShell/JSON LF 规则保留。兼容扫描要求每个 alias/adapter/wrapper/fallback/legacy/shadow/dual-write 候选都有唯一处置；除 Web array fallback 外，本批按零调用方证据删除旧 process-record API/DTO/mapper 和可吞 solution build 失败的 baseline fallback 脚本。有真实调用方的剩余项保留 producer/consumer/replacement/deletion condition 证据。终审发现 `human-process-select-api` 的一条 consumer 原来只命中文档注释，现已改成 `useRecipes` 的真实执行调用，并由词法状态门禁证明注释、块注释和字符串同名均不能冒充可执行 producer/consumer；冻结 Edge clean HEAD `0330880…` 上 active 2 / unclassified 0 / external verified 2 通过。Cloud 对 Edge 两个外部调用方的最终 clean HEAD 与 source SHA-256 将在 Edge 最终提交固定后单独刷新，本记录不把 dirty/中间态冒充最终跨仓证据。
- CI 与质量结果：`cloud-ci / build-test` 保持 25 分钟，同一 required job 必跑 solution build、Analyzer fixture、迁移账本、兼容/重复度门禁、16 runner 执行与 16 份 coverage、部署门禁、Web 和 compose；不允许 `continue-on-error`。最终 solution build 为 0 warning / 0 error；line coverage 为 11006/17962、branch coverage 为 2677/5513，changed P0 gate 为 Passed。为防止移除低覆盖文件或漏报告后比率虚高，coverage v2 baseline 同时锁定 372 个 production 文件、17962 个 sequence-point 行、5513 个分支及逐文件指标 digest `eb96065dff5a99400c0fc2bcb95319090645accdece6735eab13a4908430e1bb`，内置缺文件负 fixture。最终 duplication 为 production strict `295/2902/21485`、mild `52/915/5340`，support strict `4/79/605`、mild `2/60/295`，test strict `270/2668/23106`、mild `62/1035/7354`，cross-context groups 22；mutation 为 created 27、tested 21、killed 21、survived/timeout/no-coverage 0、ignored 4、compile-error 2。Web real E2E 的明文一次性测试凭据已从 artifacts 移到 0700 的进程专属 OS temp 目录，host 拒绝仓内路径并将文件设为 0600，artifact 上传另显式排除 state/credential 文件。
- EndToEnd 与 Web：最终候选已无 filter 重跑完整后端 EndToEnd，`artifacts/test-results/root-final-endtoend-v2` 对账 63/63、0 failed、0 skipped，耗时 45 分 10 秒；测试完成后 Aspire/DCP 与 DataWorker/Gateway/HttpApi 子进程均无残留。限流用例消耗全局 human login bucket、身份用例会停用 seed admin，因此继续每 case 独立 Aspire fixture，不引入顺序污染。Cloud Web 最终对账 Vitest 67/67、browser smoke 2/2、production build 通过；真实 Gateway/HttpApi/auth/PostgreSQL 浏览器 EndToEnd 为 1/1、0 failed、0 skipped，Playwright JSON 未包含一次性凭据或敏感值。首次浏览器复验发现的 Vite 环境枚举泄漏已通过物理删除 `Object.entries(process.env)` 与所有 target/environment console 输出收口，并由 ContractFilesystem 负例守卫。
- 独立审计与首个失败：独立审计先后发现 UseCase cancellation 被翻译为 500、3 个密码测试虚标 `CLOUD-CACHE-001`、Outbox `SKIP LOCKED` 只有源码 token guard、schema compatibility 无真实升级路径、PortFakes Analyzer fail-open、E2E credential artifact fail-open、安全读取取消缺口、无调用方 baseline fallback，以及 Serilog/Redis DI/DeviceBinding/ForwardedHeaders/OpenAPI/Aspire proxy/AppHost 参数等旧 wiring 语义被错误退役或用相邻测试替代。最终复核又发现 Redis 仅验证两个实例读同一分布式值而未把旧值压入 reader L1、endpoint lookup 后取消无独立观察点、OpenAPI 与 Serilog 生产入口接线断言不足、DeviceCascade 仍可能被同名假 store/内存计数替代、Deployment 间接引用不兼容 IntegrationTestKit、真实浏览器进程退出缺少 SIGKILL 上限，以及三个 Filesystem runner 复制仓根 helper；均以生产入口/真实 DI/精确源码结构、取消检查、有限进程收口和单一 FilesystemTestKit 修正。主 Agent 终审再发现 Roslyn 默认跳过生成代码、AI Read 写调用图漏 callback/lambda/存储委托与 dynamic，以及兼容 consumer 可由注释同名保活；均以生成代码 Analyze+Report、委托边与 dynamic fail-closed、可执行代码词法证据和真实 consumer 替换修正。真实浏览器复验还直接暴露 Vite 用 `/iiot|vite|PORT/i` 枚举环境时把 `OTEL_EXPORTER_*` header 当作端口变量输出；立即终止源码正在变化的两条 Aspire 长跑，物理删除全部环境/API target 输出并把负例并入既有 ContractFilesystem case，避免增加计数或另建旁路门禁。修正过程中的首轮失败包括 MigrationWorkApp project reference 产生 10.1/10.7 assembly warning、直接访问受保护 Identity `Store` 编译失败、遗漏 `Xunit` using、迁移账本漏映射旧 Outbox success 声明、inventory 拒绝未列入 allowlist 的固定 `Task.Delay`、真实 OpenAPI 首跑发现 human 文档混入 ai-read 路径、误删 Application 两个必要 TestKit namespace导致 305 个编译错误、Deployment 旧计数断言仍为 61/旧 support 数、重复度 strict 拒绝三份同义仓根 helper、修正 `Assert.Single` 后触发 xUnit2031 warning-as-error、WebE2ETestKit 首次加入 solution 后按 `--no-restore` build 缺少新的 assets file，以及 dirty Edge 已改变外部调用源码而被 clean-HEAD 兼容门禁正确拒绝。最终分别以精确 package 统一、显式注入 store ports、补 using/replacement、`TaskCompletionSource.WaitAsync` barrier、精确 controller prefix 匹配、只恢复必要 namespace、同步计数、物理共享 helper、predicate overload、先完整 restore 后 0 warning/0 error build和等待 Edge 清洁终态刷新证据修正，未降低任何门禁。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx --disable-build-servers --nologo -noAutoResponse`；`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers --nologo -noAutoResponse -property:RunAnalyzers=true -property:RunAnalyzersDuringBuild=true`；`bash scripts/tests/TestCloudArchitectureAnalyzerFixtures.sh`；`pwsh scripts/tests/Test-CloudTestMigration.ps1 -OutputPath artifacts/test-results/root-final-cloud-test-migration-v3.json`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild -CollectCoverage -ResultsDirectory artifacts/test-results/root-final-required-coverage-v6`；`pwsh scripts/tests/Test-CloudCoverage.ps1 -ResultsDirectory artifacts/test-results/root-final-required-coverage-v6 -BaseRef 88c41109fbcf0b87b18939a139e0bff751e03d07`；`pwsh scripts/tests/Test-CloudCompatibility.ps1 -EdgeRepositoryRoot <final-clean-edge-root>`；`pwsh scripts/tests/Test-CloudDuplication.ps1`；`pwsh scripts/tests/Invoke-CloudMutation.ps1`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode EndToEnd -Configuration Release -NoBuild -ResultsDirectory artifacts/test-results/root-final-endtoend-v2`；Cloud Web `npm run test:unit` / browser smoke / real E2E / `npm run build`；deployment behavior/policy/PowerShell 语法、JSON/YAML/链接/遗产检索、活动部署文档旧测试桶零引用与 `git diff --check`。最终 WorkspaceAlignment 和 Edge 外部兼容证据只能在 AI/Edge 最终清洁提交后补跑，未补跑前不称跨仓完成。
- 长期规则结论：形成 Cloud 测试物理分层、旧声明迁移账本、编译型 Analyzer、高风险缓存/事务/异常矩阵、CI 精确对账、coverage production universe 锁定、E2E 隔离与精确不可执行覆盖率白名单等长期规则，已写入 `docs/云端规则.md`、`docs/contracts/cloud-architecture.md`、`docs/contracts/caching-resilience.md` 和 `docs/云端架构治理清单.md`。未修改工作区 `../docs/总规则.md`，因为本批新结论是 Cloud 项目级执行契约。三项目总计划完成度仍按 22%，错误治理调查、遗产删除和对账工具收口不计入完成度。

## 2026-07-14 Cloud 测试单元物理分层与动态 CI 对账

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 solution、Cloud 测试/support 项目、测试源码归属、Analyzer 测试程序集识别、测试 inventory/执行器、`cloud-ci`、WorkspaceAlignment evidence、相关守卫、Cloud Web 过期 processes array 兼容路径以及 Cloud 规则/清单/本复盘。未修改 Cloud 业务语义、数据库 schema/migration、生产配置或 CD；未修改 `IIoT.EdgeClient`、`AICopilot` 和工作区顶层规则。本批未执行发布、部署或任何生产操作。
- 改动原因：旧 ServiceLayer/ProductionService/Infrastructure/RedisIntegration/EndToEnd 大桶使测试分类只存在文档和 filter 中，不能从物理项目、运行时资源与 CI 调度看出边界；硬编码项目列表和定向 filter 也容易在新增/移动 case 时漏跑。本批将分类落到物理测试单元、case-level 清单和动态执行对账，同时保持 `CLOUD-CACHE-001`、Analyzer 和已验收业务链语义不变。
- 物理分层：665 条 discovery 已分入 13 个独立 runner：Analyzer 35、Architecture 81、Unit 39、Aggregate 11、Application 166、Contract 23、Persistence 62、Integration 1、Workflow 144、HTTP 21、Deployment 20、EndToEnd 61、WorkspaceAlignment 1。旧五个测试桶及 csproj 物理删除；共享纯 helper 与集成运行 helper 分别收敛到零 case `TestKit` / `IntegrationTestKit`，没有 `Compile Link`、影子 runner 或旧名 wrapper。测试程序集内部可见性和 production Analyzer 接线已同步到新边界。
- 清单与 CI：`src/tests/cloud-test-inventory.json` 固化 runner/support allowlist、预期数、运行时依赖、执行组和 case-level 元数据；`Invoke-CloudTestInventory.ps1` 对每个 runner 真实 `--list-tests`，再按源文件映射产生 JSON。required 由它动态执行 11 runner / 603 case，Pure 组项目级并行，Filesystem/Database/Container 等资源组隔离串行；每个 TRX 必须 discovered = executed = passed = expected、0 failed、0 not-executed。过期 CI filter、条件 Skip、自定义 Workspace Fact、旧仓 helper/baseline 引用和额外定向重复计数入口均已物理删除。
- 高风险语义保留：缓存 37 条确定性语义、真实 Redis 1 条、PostgreSQL persistence 5 条继续在 required 中真实运行，Persistence 分类显式标记 SQLite/Postgres/Aspire。WorkspaceAlignment 拆为独立 LiveExternal runner；执行前证据脚本固化 Cloud/AI 精确 git root、HEAD SHA 和稳定排序源码 digest，测试在 Aspire 启动前验证 marker/root 归属，不再用条件 Skip 或猜测 checkout。Cloud Web `/human/master-data/processes` 只接受分页响应，无真实调用方的旧 array fallback 与对应测试已删除，`/all` 独立 typed API 不受影响。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx --disable-parallel`；`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers`；`bash scripts/tests/TestCloudArchitectureAnalyzerFixtures.sh`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Inventory -Configuration Release -NoBuild`；`pwsh scripts/tests/Invoke-CloudTestInventory.ps1 -Mode Required -Configuration Release -NoBuild`；Cloud Web `npm run test:unit` / `npm run build`；`bash deploy/tests/deployment-behavior.sh`；`pwsh deploy/tests/TestDeploymentPolicy.ps1`；production compose config；JSON/YAML 解析、遗产/死链 `rg`、`git diff --check`。
- 验证结果：Release solution build 0 warning / 0 error；13 runner discovery 665，required 11 runner 精确 603/603、0 failed、0 Skip；Analyzer fixture valid 4 / invalid 8；Cloud Web 16 文件/67 case 与 production build、deployment behavior 33/33、部署策略、compose 和格式/遗产门禁在本批最终结果中对账。Workspace evidence 生成与 marker 结构已使用显式 AI 根验证；未实际执行手动 EndToEnd 61 或 LiveExternal WorkspaceAlignment 1，未运行远程 CI、真实上传或生产部署，本地结果不冒充这些验收。
- 长期规则结论：形成 Cloud 项目长期测试分层、动态 inventory/CI 对账和 Workspace evidence 规则，已写入 `docs/云端规则.md` 6.1/7/8，执行事实同步到 `docs/云端架构治理清单.md`。未提升到工作区 `../docs/总规则.md`，因为本批只约束 Cloud 测试执行与跨仓对齐证据。

## 2026-07-14 Cloud 编译型架构门禁与独立 AnalyzerTests 收口（CLOUD-ARCH-001）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的根 Analyzer 接线、solution、独立 Analyzer/AnalyzerTests 项目、真实 csproj fixture、`cloud-ci`、ConfigurationGuard、Cloud 规则/专题契约/治理清单和本滚动复盘。未修改 Cloud 业务运行时、数据库 schema/migration、缓存实现、部署/CD 实现或生产环境；未修改 `IIoT.EdgeClient`、`AICopilot` 和工作区顶层规则，未 commit、push、PR、发布或部署。
- 改动原因：原测试执行只能证明已运行行为，不能在编译期阻断分层反向依赖、伪聚合/通用仓储、数据库 owner 越界、AiRead 隐藏写链、request-kind/授权元数据混用和生产依赖测试资产。需要用独立测试单元验证 Roslyn 语义规则，并让真实 Cloud 生产项目和 push/PR required lane 使用同一 Analyzer，而不是恢复已物理删除的错误治理前置。
- Analyzer 实现：新增独立 `netstandard2.0` `IIoT.CloudPlatform.Analyzers`，精确使用 `Microsoft.CodeAnalysis.CSharp 5.6.0`，由根 `Directory.Build.props` 仅以 `OutputItemType=Analyzer` 附加到 `IIoT.*` 生产项目。`CLOUDARCH001`–`CLOUDARCH006` 全部默认 Error，分别以引用程序集图、Roslyn symbol/generic constraint、`IOperation`、同编译单元调用图与继承/接口分派检查 layer、DDD/聚合和通用仓储、EF/Dapper/Npgsql/ADO.NET owner、AiRead 写路径、request-kind/授权元数据及 Tests/Testing/TestKit/test framework 生产引用；没有保留 regex 源码扫描、alias、adapter、wrapper、fallback 或新旧双轨。
- 例外边界：数据库项目级例外只保留职责完整且专用于 migration/schema compatibility/seed 的 `IIoT.MigrationWorkApp`；其它例外固定为 `IIoT.DataWorker::Program`、`IIoT.HttpApi::IIoT.HttpApi.DesignTimeDbContextFactory`、`IIoT.HttpApi::IIoT.HttpApi.Infrastructure.PostgresReadinessHealthCheck` 三个 `assembly::fully-qualified-type`。DataWorker/HttpApi 相邻类型仍由真实 invalid fixture 证明失败，禁止扩大为整仓放行。
- 测试与 CI：新增独立 `IIoT.CloudPlatform.AnalyzerTests` 35 条 required 测试，覆盖 6 个稳定 ID/default Error、正反例、alias、generic/local resolver、跨文件 helper、接口分派、inheritance、授权特性符号缺失 fail-closed、裸 `AiRead.` 权限前缀和 no-false-positive；fixture 门禁创建临时真实 csproj，对账 valid 4 成功、invalid 8 只以目标诊断失败。`cloud-ci / build-test` 在业务测试前直接执行两层门禁并精确对账 Analyzer TRX/fixture 日志，不设条件失活或 `continue-on-error`。
- 首轮失败与修正：首次真实 solution build 暴露 `.globalconfig` 自定义键被 Roslyn 规范为小写且分号值只读取首项，导致合法 migration/host owner 产生 26 个 `CLOUDARCH003`；改为读取小写键、逗号列表并收窄为上述精确例外后全解通过。ConfigurationGuard 首次执行又因通用文件查找命中 `src/tests/Directory.Build.props` 而 1/81 失败；改为从唯一 solution 路径解析仓根后读取根 props/config，复跑 81/81。终审继续发现 `CLOUDARCH005` 会在授权 attribute symbol 不可见时整体早退、且裸 `AiRead.` 被前缀判断放行；已改为 symbol 缺失按空特性集合 fail-closed，并要求 `AiRead.` 后存在非空实际后缀。终审同时发现 `CLOUDARCH002` 只检查声明符号会漏掉 local generic resolver；已把 invocation/object-creation 结果纳入同一聚合约束，并以非法 Projection/合法 aggregate local resolution 正反例冻结；合法正例首跑把聚合声明在 Service 而被 owner 规则正确拒绝，移到 Core fixture 后才作为有效 no-false-positive 通过。失败没有通过降级 severity、扩大项目例外、删测或 Skip 换绿。
- 验证命令：Release solution build；六个 xUnit 项目 `--list-tests`；AnalyzerTests、fixture、ServiceLayer、ProductionService、缓存语义、真实 Redis、`Category=PostgresPersistenceIntegration`、ConfigurationGuard、DeploymentGuard 的 required 执行及 TRX 对账；Cloud Web unit/build；deployment policy/behavior/脚本语法；production compose；solution direct/transitive vulnerability scan；Analyzer fixture 脚本语法；架构关键词/链接检索与 `git diff --check`。
- 验证结果：Release solution build 0 warning/0 error。六项目 Release discovery 精确 665（原五项目 630 + AnalyzerTests 35）；required 后端 577/577、0 failed、0 Skip（原业务/运行时 542 + AnalyzerTests 35）。原 542 已包含 `PostgresPersistenceIntegration` 5 条，其中 DeviceIdentity 3 条、ClientRelease recovery 2 条；独立 `postgres-recovery.trx` 2/2 只可作为相同 recovery 的额外定向证据，不得再次累计。Analyzer fixture valid 4/invalid 8 精确通过；Cloud Web 16 文件/67 case、production build、deployment behavior 33/33、部署策略/语法、compose 和 solution direct/transitive vulnerability scan均通过。未运行手动完整 Aspire EndToEnd、LiveExternal 跨仓对齐、远端 CI、真实上传或生产部署，本地结果不冒充这些验收。
- 长期规则结论：形成 Cloud 项目长期技术规则，已写入 `docs/云端规则.md` 6.1、专题契约 `docs/contracts/cloud-architecture.md` 和 `docs/云端架构治理清单.md` 的 `CLOUD-ARCH-001`。未提升到工作区 `../docs/总规则.md`，因为本批只约束 Cloud 编译边界、例外和 Cloud required 执行口径。

## 2026-07-14 Cloud 普通值缓存韧性、直接授权读取与 required 门禁收口（CLOUD-CACHE-001，已被 v9 纠偏）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 `ICacheService`/Redis-FusionCache 普通值缓存实现及调用点、Identity/设备范围/设备身份直接读取、设备删除中已失去用途的授权缓存预取字段、两个独立测试项目、solution、`cloud-ci`、Cloud 规则/专题契约/治理清单/本滚动复盘，并物理清理错误测试治理资产及其活动引用。未修改 Redis 分布式锁/lease、幂等登记、Outbox、数据库 schema/migration、部署实现或生产环境；未修改 `IIoT.EdgeClient`、`AICopilot` 和工作区顶层文件，未 commit、push 或部署。
- 改动原因：原 `RedisCacheService` 会用宽泛 catch 吞掉 caller cancellation、serialization/未知错误，并可能在 `GetOrSetAsync` provider/factory 竞态后重复执行 factory。更严重的是用户权限、设备访问范围和设备身份曾复用“故障可降级”的普通值缓存，缓存陈旧或 Redis 故障会影响授权/身份判断。仓内旧错误模型还以与测试架构无关的活动前置阻断合法新增测试，不能替代当前任务授权、真实 required 执行和 0 Skip 对账。
- 历史运行时口径：该阶段普通值缓存只对白名单中的 Fusion distributed/backplane/synthetic timeout 与 Redis connection/timeout 受控降级；未知、序列化、factory 业务异常和所有 caller cancellation 原样传播。`GetOrSetAsync` 以一个 TCS/exception capture 复用同一 factory execution，provider 重入、竞态、write-back 失败或 caller cancellation 均不产生第二次回源；outer provider 抛取消前也先检查已捕获 factory failure，因此 provider 捕获 factory 原始取消后改抛新取消时仍传播 factory 原实例且只执行一次。该阶段旧 `SetAsync(null)` 合同曾映射到 remove；v9 已因无真实调用方物理删除整个分离 `SetAsync`，该语义不再是现行合同。pattern 删除在 endpoint、server、key 枚举和逐 key remove 各层传播取消。降级日志固定 `ValueCacheInfrastructureDegraded(2401)`，只记 operation/异常类型名。
- 安全敏感读取与调用点：`PermissionProvider` 每次从 Identity store 合并用户/角色 claims，`DevicePermissionService` 每次从 EF 正式存储查询范围，`DeviceIdentityQueryService` 每次从 Dapper 正式存储查身份；三者不再注入 `ICacheService`。相关 permission/device-access/device-identity cache keys、配置、写路径失效和设备删除员工 ID 预取/返回字段均物理删除。保留的 19 个生产调用文件只缓存权限定义展示目录、工序/设备/配方/产能查询 DTO 及其失效；权限定义目录不参与授权判定。设备身份另以真实 PostgreSQL + Dapper 覆盖 missing/current、直接更新 `devices.client_code` 后第二次读取立即看到新值；取消测试为目标连接设置唯一 `ApplicationName`，有界轮询 `pg_stat_activity + pg_locks` 确认查询已进入真实 Lock wait 后才取消，不使用固定延时或源码字符串自证。当前 xUnit 2.9.3 不提供 xUnit v3 的 `TestContext.Current.CancellationToken`，普通 await 因此统一使用显式 30 秒测试 token，readiness backoff 绑定同一 token，清理另有 5 秒硬上限。
- 测试与 CI：新增 `IIoT.Infrastructure.Tests` 37 条确定性语义测试和 `IIoT.RedisIntegrationTests` 1 条真实 Redis 故障恢复测试，均直接进入 push/PR required lane。真实测试固定 `redis@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99`，覆盖 pause/unpause、stop/start、断连降级与同 multiplexer 恢复、factory/fallback 单次执行和 pattern 删除故障；Docker/Redis 不可用失败，不 Skip。既有两条 ClientRelease post-commit recovery 与新增三条设备身份 Dapper 测试共享一次真实 PostgreSQL fixture，以 `PostgresPersistenceIntegration` 5/5 进入 required。CI 对账同步修正因删除 2 条旧 PermissionCache 测试而变化的 ServiceLayer 291。
- 测试治理清理：仓内与测试架构无关的错误活动资产及其执行引用已物理删除。保留 `failSkips=true`、NuGet `true/all/low` 审计、直接 required 测试与结果对账；分层/DDD/数据库访问等静态架构门禁留给后续编译型 Analyzer + AnalyzerTests，不用计数清册冒充。
- 首轮失败与修正：第一次 Release build 仅因移除缓存依赖时误删了仍提供 `IHumanCommand` 的 `IIoT.Services.Contracts` using 而编译失败；恢复真实类型依赖后全解通过。真实 Redis 首版动态 host port 候选在 Docker restart 后端口变化，最终固定为预留 loopback port 并以实际跨 runtime L2 可见性轮询恢复，未用连接对象状态冒充数据恢复。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx --disable-build-servers --nologo -noAutoResponse`；Release solution build；五个 xUnit 项目 `--list-tests`；ServiceLayer、ProductionService、缓存语义、真实 Redis、`Category=PostgresPersistenceIntegration`、ConfigurationGuard、DeploymentGuard required 执行与 TRX 对账；Cloud Web unit/build；deployment policy/behavior/syntax；production compose；solution direct/transitive vulnerability scan；变更 C# 定向 format；活动治理/缓存键/死链 `rg`；`git diff --check`。
- 验证结果：Release solution build 0 warning/0 error。五项目 Release discovery 精确 630（EndToEnd 194、ProductionService 107、ServiceLayer 291、Infrastructure 37、RedisIntegration 1）；required 后端 542/542、0 failed、0 Skip（291+107+37+1+5+81+20）。Cloud Web 16 文件/67 case、production build、deployment behavior 33/33、部署策略/语法、compose、TRX/JSON/log reconciliation 和 solution direct/transitive vulnerability scan均通过。未运行手动完整 Aspire EndToEnd、LiveExternal 跨仓对齐、远端 CI、真实上传或生产部署，本地结果不冒充这些验收。
- 长期规则结论：形成 Cloud 项目长期规则，已写入 `docs/云端规则.md` 6.1/6.2 和专题契约 `docs/contracts/caching-resilience.md`，治理状态同步到 `docs/云端架构治理清单.md`。未提升到工作区 `../docs/总规则.md`，因为本批只约束 Cloud 的普通值缓存、直接授权读取和 Cloud required 执行口径。

## 2026-07-12 Cloud 错误测试治理模型事故记录（已退役，非现行入口）

- 历史事实：该批曾引入与测试架构无关的错误活动资产和执行前置，虽然产生过一次远端绿灯证据，但会阻断当前任务已授权的合法测试新增或删除，也不能证明架构正确性。
- 退役事实：2026-07-14 已物理删除上述资产和活动入口，只保留 `failSkips=true`、NuGet audit、真实 required 测试及执行结果精确对账；历史 run、digest 和计数不再是现行规则或合并前置。
- 后续边界：分层、DDD、聚合、数据库访问、AI Read 与授权元数据等静态架构约束由后续编译型 Analyzer + AnalyzerTests 建立，不恢复已删除的错误模型。


## 2026-07-11 Cloud ClientRelease 机械能力唯一实现收口（C-DUP-P2）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 ClientRelease 无状态/文件系统 helper、Host/Plugin 发布 handler、catalog/客户端状态查询、删除/安装包生成调用点、两项 ProductionService 测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改 Host/Plugin 聚合、manifest、artifact、发布事务、提交观测、回滚、保留/删除业务策略、HTTP API/DTO、数据库 schema/migration、Redis 租约、部署脚本、生产配置或前端，也未修改 `IIoT.EdgeClient`、`AICopilot` 和工作区顶层文档；未部署。
- 外部审计纠偏与去重边界：外部审计指出 ClientRelease Host/Plugin 双轨存在重复，这个方向部分成立，但不能把两条发布链机械合成一个通用 handler/base。源码复核后只抽取结果、错误文案和副作用都相同的机械能力；Host/Plugin 的 artifact、manifest、retention、rollback、download、commit recovery 和审计结果继续分离。上传读取/限速/staging 已由既有 `ClientReleaseUploadCoordinator` 唯一实现，本批没有复制或重写第二套。
- 唯一机械实现：Host wrapper 与 Plugin wrapper 的 ZIP 解包、entry 规范化、zip-slip 与 reparse 拒绝收敛到 `ClientReleaseZipArchive`，Plugin 内包扫描也调用同一 entry 规范化；SHA-256 格式和发布磁盘阈值进入唯一 `ClientReleaseFileFacts`；installer/plugin URL、路径段编码和 artifact 相对路径进入 `ClientReleaseArtifactBuilder`；edge-updates 根解析进入 `EdgeInstallerArtifactOptions`。channel/可空文本规范化由 `ClientReleaseText` 唯一提供，ClientRelease 审计 actor GUID 解析由 `ClientReleaseAuditActor` 唯一提供，原 handler/query 私有副本全部物理删除。
- 文件清理与 Velopack：归档 Plugin 目录删除收敛到无状态 `ClientReleaseArchivedFileCleanup`；Host 清理复用首次读取的组件快照，物理删除第二次相同 repository 查询，独立 Plugin 发布仍在 retention 后读取一次最新快照。Velopack stable manifest 名称、发布事务 channel-manifest 判定、删除保护判定和“manifest 是否引用文件”的共同扫描进入独立 `ClientReleaseVelopackPaths`；发布清理只看 stable manifests + `RELEASES`，物理删除仍保护所有 `releases.*` / `assets.*`，两种策略以不同命名保留，未用一个模糊 bool 偷换语义。
- 冗余与垃圾清理：删除两套 wrapper ZIP extract/normalize、两套 SHA regex、两套磁盘检查、两套 Plugin download URL、三套路径段编码、两套 edge root resolver、六套 `NormalizeOptional`、三处 channel default、四套 ClientRelease audit actor parse、两套 archived Plugin 目录循环、Host 的重复组件查询、重复 stable manifest 字面量和两套 manifest 内容扫描。没有保留 adapter、legacy helper、fallback、feature flag 或新旧双轨；`PublishEdgeReleaseBundleHandler` 与 `PublishEdgePluginPackageHandler` 仍是两个真实业务用例。
- 测试分类与首轮红测：既有 Host zip traversal 行为测试继续冻结第一个调用方；新增 Plugin wrapper zip traversal 行为测试冻结第二调用方接线，`ClientReleaseFileFactsTests` 只补 SHA 大小写/长度/非十六进制纯算法边界，不复制 handler manifest 测试。首轮 Release build 暴露 9 个编译错误：Plugin 内包扫描和 Host 当前版本 nupkg 检查仍引用已删除私有 helper；随后分别接到共享 entry normalize 与共享 manifest reference scan，复跑构建通过。该失败作为去重调用图不完整的证据保留，没有隐藏、删测或恢复旧 helper。
- 规则提炼：形成 Cloud 项目级长期代码规则，已写入 `docs/云端规则.md` 5.1，并在 `docs/云端架构治理清单.md` 新增 C-27 / 批次 N。没有提升到工作区 `../docs/总规则.md`，原因是本规则只约束 Cloud ClientRelease 的内部机械 helper 边界。
- 验证命令：`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers --nologo -v:minimal`；ProductionService / ServiceLayer 全量 `dotnet test -c Release --no-restore --disable-build-servers`；EndToEnd `ConfigurationGuardTests + ClientReleasePublishExceptionHandlerTests + DistributedLockExceptionHandlerTests + ClientReleaseUploadBoundaryTests` 定向；真实 PostgreSQL `ClientReleaseCommitRecoveryPostgresTests`；旧 ZIP/SHA/normalize/URL/root/cleanup helper 零命中、规则关键词/死链 `rg`、`git diff --check`。
- 验证结果：修正首轮编译调用图遗漏后，Release solution build 0 warning / 0 error，构建中的 Cloud deployment policy architecture test 通过；ProductionService 全量 107/107、ServiceLayer 全量 293/293、ConfigurationGuard + typed ClientRelease/lock HTTP + upload boundary 定向 92/92 通过；真实 PostgreSQL Plugin singleton + Host batch post-commit simulations 2/2、0 Skip、进程退出码 0。远端 `cloud-ci` 和 annotations 结果在本批 push 后继续核对；未运行完整重型 EndToEnd、真实上传或生产部署，本地结果不冒充远端/生产验收。

## 2026-07-11 Cloud Host bundle 提交观测与 ACK-lost 恢复（4C2b）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 ClientRelease expected-set observation core port/EF reader、唯一 expected matcher/file-facts/受控目录链、Host bundle handler、Host installer/generated Plugin/Velopack 文件事务、Host/Plugin 分层测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改管理员手工 upsert、retention/delete 业务、数据库 schema/migration、上传 API/DTO、Redis 租约实现、部署脚本、生产配置或前端，也未修改 `IIoT.EdgeClient`、`AICopilot` 和工作区顶层文档；未 push、未部署。
- 改动原因与外部审计纠偏：外部代码审计关于“Host 保存响应丢失后按失败回滚”“Host manifest/Velopack artifact 未记录真实文件事实”“反射字符串判断 EF concurrency”“string rollback target + 无条件递归 rollback”在本批基线中成立；关于 Controller 仍传 `Request.Body`、仍使用本地文件锁、Plugin ACK-lost 未治理等结论已经分别由 C-21/C-23/C-25 收口，属于过期事实，未重复制造第二套修复。本批遵守 Cloud HTTP-only、发布身份、共享 Redis 租约和无 schema 变更红线，只处理 Host 4C2b。
- expected-set 提交观测：`IClientReleaseVersionObservationReader` 改为唯一批量合同并物理删除单 identity overload；EF reader 每次创建 fresh `IIoTDbContext`，将 Host 和本 bundle 新建的全部 Plugin identities 用 `AsNoTracking + filtered Include + AsSingleQuery` 在一条 SQL/一个快照读取，再在内存按完整 identity 精确投影。Plugin recovery 改为 singleton expected set，不复制 reader。Host/Plugin 仅共享 `ClientReleaseExpectedVersionState`、唯一 exact matcher、file-facts 和 warning combiner，两条恢复 workflow/结果/审计/保留策略继续分离。
- Save 边界与固定结果：Host 在调用 `SaveChangesAsync` 前设置 invoked；此后异常/取消绝不触发文件 rollback。保存非取消异常只做一次独立 `5s` 观测：全部 DB version/artifact 与 installer、全部 Velopack 文件和本包 generated Plugin package 同时 exact 才返回恢复成功，并明确 retention/cleanup 未执行；任一 observed DB mismatch 抛 typed `409 client_release_publish_conflict`，missing/partial/timeout/read failure/static mismatch 抛 typed `503 client_release_commit_unknown`。取消同样独立观测并写固定 `CommittedResponseCancelled/CommitConflict/CommitUnknown` 审计后原样重抛；raw DB message、绝对路径和 secret 不进入响应或审计。
- 精确文件事务：installer source 先在 upload staging 校验 reserved marker 并写入 32-byte ASCII ownership marker，marker 成功后才移动到 target-parent private 并以 `Directory.Move` 原子占位；marker create/flush 失败时所有文件仍留在 upload staging，不产生无主 private。提交前 installer rollback 只删除 snapshot 中 exact 目录/相对路径/hash/size/marker 且零 extra 的本次 owned tree。generated Plugin 直接复用 C-25 文件事务，不再保留 `Directory.Create + File.Move` 和 string rollback target 双轨。Velopack `.nupkg` 目标已存在时只允许 hash/size exact 复用且不取得回滚所有权，不同包直接冲突；可变 manifest 先 exact backup，再经目标同目录私有文件原子替换，rollback 只在当前发布事实与 backup 都 exact 时恢复。Installer、Plugin、Velopack 的 source/private/target/backup 都复用唯一物理目录链校验器，edge root 到路径的每个既有 ancestor 有 reparse/symlink 即在外部写入前 fail closed；snapshot 改为显式 top-level 逐目录遍历，先拒绝 reparse child 再进入。
- artifact 与冗余清理：Host 顶层 `DownloadUrl/Sha256/PackageSize` 继续保持“manifest URL + installer stub hash/size”既有 catalog 语义；`ManifestFile` 改为真实 `installer-artifact.json` hash/size，installer stub 增加真实 `PackageFile`，每个 `VelopackFile` 保存真实 hash/size且 builder 不再保留 nullable/default 参数。目录 hash/size 与单文件 SHA/regular exact 收敛到 `ClientReleaseFileFacts`，32-byte marker create/write/match 收敛到唯一 `ClientReleaseOwnershipMarker`，物理目录 containment/创建/观测收敛到唯一 `ClientReleaseControlledDirectory`；三个文件事务调用方的重复 lexical child 检查、Host 私有 `IsChildPath` 与 Host handler、Plugin transaction、测试中的重复实现均物理删除。Host manifest 另在任何文件写入前拒绝 null/empty module 集合、null module、normalized ModuleId/PluginDirectory 重复和非 SemVer module version，消除字典覆盖造成的不可追踪 orphan。旧 `commitPointReached`、installer/string rollback targets、`VelopackFilePublishTransaction/VelopackFileChange`、无条件 Host rollback delete/copy-overwrite、反射 EF concurrency helper 和 Plugin 私有 warning combiner均已删除，不保留 adapter、fallback、feature flag、migration 或旧新双轨。
- 测试分类：ProductionService Host 行为覆盖 not-observed unknown、Host+generated Plugin exact batch recovered、persisted mismatch conflict、static mismatch unknown、Save cancellation 观测/审计/重抛、Save 后 `InvalidDataException` 不误回滚、pre-save infrastructure unavailable 精确回滚、所有真实 artifact facts，以及 duplicate ID/directory、null modules/item、module version traversal 五类 preflight 零 published target；文件事务专项覆盖 installer 原子竞争、marker write/flush failure、exact rollback、extra/hash 篡改、Installer/Plugin target symlink ancestor、source snapshot reparse child、Velopack exact restore、immutable nupkg 复用、不同 nupkg 冲突、发布文件篡改和 reparse ancestor。ServiceLayer 用 SQLite + `DbCommandInterceptor` 证明 Host+Plugin 两个 identity 只发一条包含 artifacts 的 SQL。EndToEnd 把真实 PostgreSQL 环境改为共享 class fixture，以两个明确的 `SavedChangesAsync` post-commit exception simulations 分别验证 Plugin singleton 和 Host+generated Plugin batch recovery；Host 命令 interceptor 额外锁定一条 observation SQL。
- 规则提炼：形成 Cloud 项目级长期技术规则，已写入 `docs/云端规则.md` 5.1，并在 `docs/云端架构治理清单.md` 新增 C-26 / 批次 M，同时把 C-23/C-24 的 Host 待办状态更新为已由 C-26 收口。没有提升到工作区 `../docs/总规则.md`，原因是本规则只约束 Cloud ClientRelease Host 发布提交边界。manual upsert、通用 exact retry、retention/delete、大文件 IO、schema/migration 和真实部署仍是未实施边界。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx -p:NuGetAudit=false`；`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；ProductionService / ServiceLayer 全量 `dotnet test -c Release --no-build --no-restore -p:NuGetAudit=false`；EndToEnd `ConfigurationGuardTests + ClientReleasePublishExceptionHandlerTests + DistributedLockExceptionHandlerTests + ClientReleaseUploadBoundaryTests` 定向；真实 PostgreSQL `ClientReleaseCommitRecoveryPostgresTests`；旧 Host/Plugin 合同与 rollback/hash helper 零命中、规则关键词/死链 `rg`、`git diff --check`。
- 验证结果：Release solution build 0 warning / 0 error，构建中的 Cloud deployment policy architecture test 通过；ProductionService 全量 105/105、ServiceLayer 全量 293/293、ConfigurationGuard + typed ClientRelease/lock HTTP + upload boundary 定向 92/92 通过；真实 Aspire PostgreSQL post-commit simulations 2/2、0 Skip、进程退出码 0，其中 Host+generated Plugin expected set 由 interceptor 证明一条 SQL。Host/Plugin 文件事务组合 20/20（Host 12、Plugin 8）、manifest 五类 preflight 5/5、SQLite observation reader 3/3 通过；本批全部变更 C# 文件的 `dotnet format --verify-no-changes --include ...` 通过。仓库级 format verify 仍报告未触碰的 `ClientReleaseComponentConfiguration.cs` 与旧 `CloudProductionFlowTests*` 空白基线，本批未越权机械改写无关文件。未运行完整重型 EndToEnd、远端 CI、Harbor/SSH、真实上传或生产部署，本地结果不冒充远端/生产验收。

## 2026-07-11 Cloud Plugin 发布提交观测与 ACK-lost 恢复（4C2a）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 Plugin package 发布 handler、ClientRelease fresh observation core port/EF reader、无状态文件事实 helper、Plugin 专用文件所有权与 commit recovery、typed HTTP 异常、分层测试、常规 `cloud-ci`、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改 Host bundle handler、管理员手工 upsert、Redis 租约实现、数据库 schema/migration、fingerprint/idempotency 表、通用 exact retry、部署脚本或生产环境，也未修改 `IIoT.EdgeClient`、`AICopilot` 或工作区顶层规则；未 push、未部署。
- 改动原因与审计纠偏：旧 Plugin handler 用 `databaseSaved` 表示 `SaveChangesAsync` 是否正常返回；数据库已提交但 ACK/响应异常时会把 target 目录递归删除，并把真实成功误报为失败。提交异常后同 scoped repository 也不能证明提交事实。4C2a 只修正 Plugin 的 ACK-lost，不把“fresh exact”扩展成普通后续重复请求幂等，也不借机抽取会掩盖 Host artifact 差异的通用 Host/Plugin base。
- fresh 提交观测：新增 `IClientReleaseVersionObservationReader`，EF 实现每次用 `DbContextOptions<IIoTDbContext>` 新建/释放 context，通过 `AsNoTracking + filtered Include + AsSingleQuery` 一条 SQL 读取组件、目标版本和 artifacts；保存异常后只允许一个与 caller token 无关的 `5s` 观测。数据库和 owned 静态包同时 exact 才恢复成功；persisted mismatch 为固定 `409 client_release_publish_conflict`；未观测、超时、reader failure 或静态 mismatch 为固定 `503 client_release_commit_unknown`。普通 duplicate 仍在 preflight 返回 `409`。
- 文件所有权与回滚：发布包先进入 target 同父级 GUID 私有目录，目录内只有固定 32-byte ASCII marker 和唯一包，再用 `Directory.Move(private,target)` 原子占位；只有 move 成功才设置 target owned。Save invoked 前回滚同时验证受控根、目录/marker regular file、当前 token、唯一文件名、sha256、size、零 extra 和非 reparse；竞争目录、marker/包篡改或 extra entry 均 fail closed。Save invoked 后任何异常/取消都不删除包；marker 成功删除后记录状态，重复 cleanup 幂等。旧无条件 `TryDeleteDirectory` 已物理删除。
- 取消、审计和时间：Save 抛 `OperationCanceledException` 或 Save 返回后 caller/lease token 被取消，都先用独立 token 完成必要观测/固定审计，再原样重抛。confirmed commit 写成功 `CommitRecovered` / `CommittedResponseCancelled`；conflict/unknown 写固定脱敏失败原因，均不记录原始 DB message、绝对路径或 secret。manifest `createdAtUtc` 无 offset 时 fail-fast `400`；UTC 原样、明确 offset 转 UTC；fresh matcher 双方按 PostgreSQL `1µs` 精度严格截断比较，sha256 大小写不敏感，身份/path/kind/size 保持严格。
- 冗余与测试架构：物理删除 Plugin `databaseSaved`、提交异常后的无条件 target delete helper/分支和 `SaveChangesException` 模糊 fake，未保留 adapter、fallback、feature flag、同 scoped probe 或轮询；`ClientReleaseFileFacts` 成为 Plugin handler、文件事务及相关测试唯一的 strict child path / SHA-256 / regular-file exact 实现，文件事务内的受控目录、精确条目和 package hash/size 校验也收敛为唯一内部链。Host 严格未修改，其历史同义 helper 留给既定 C-DUP-P2，本批没有新增第三套。ProductionService 覆盖 exact/mismatch/not-observed/cancellation、普通 duplicate、uppercase hash、UTC/offset、原子竞争、owned rollback、extra/hash/marker 篡改和 marker 幂等；ServiceLayer 抽取一个 file-backed SQLite EF fixture 供审计/reader 共用，并用 DbCommandInterceptor + TCS 冻结每次新 context/单 SQL及命令边界 mutation；EndToEnd 直接冻结 typed ProblemDetails code/脱敏，并用真实 PostgreSQL `SavedChangesAsync` interceptor 做明确的 post-commit exception simulation。`ConfigurationGuard` 对本批新增的 ProductionService/PG recovery 常规步骤和手动 full-E2E 改为按 YAML named-step block 结构化定位，同步冻结 command/filter/timeout/if/no-continue-on-error，没有扩大全文 `Contains` 测试债；workflow trigger 和历史脚本守卫的旧全文断言不属本批，未引入新 YAML 依赖，另用 Ruby YAML parser 做语法验收。最终行级变更统计为 `+2322/-244，净增 2078`：运行时 `+1162/-139`、测试 `+1111/-100`、文档/CI `+49/-5`；净增来自 fresh reader、所有权事务、typed 错误和分层门禁，不是保留旧新双轨。
- CI 与成本：常规 push/PR 现运行 ProductionService 全量和过滤后的 `ClientReleaseCommitRecoveryPostgresTests`，真实 PostgreSQL 门禁设置 `8` 分钟 fail-closed 超时；Docker/Postgres 不可用不 Skip。完整 EndToEnd 仍只在手动 dispatch。Release 本机真实门禁 1/1 用时 44 秒，预计托管 runner 增加约 1–3 分钟，远端实际耗时待本提交 push 后由 CI 事实确认。
- 规则提炼：形成 Cloud 项目级长期技术规则，已写入 `docs/云端规则.md` 5.1，并在 `docs/云端架构治理清单.md` 新增 C-25 / 批次 L。没有提升到工作区 `../docs/总规则.md`，原因是该规则只约束 Cloud Plugin release 的 EF/静态文件提交边界。Host ACK-lost、Host manifest/Velopack artifact hash、manual upsert 与租约关系及通用同包重试仍是后续独立批次。
- 验证命令：`dotnet restore IIoT.CloudPlatform.slnx -p:NuGetAudit=false`；`dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；ProductionService / ServiceLayer 全量 `dotnet test -c Release --no-build --no-restore -p:NuGetAudit=false`；EndToEnd `ConfigurationGuardTests + ClientReleasePublishExceptionHandlerTests + DistributedLockExceptionHandlerTests` 定向；Release 真实 PostgreSQL `ClientReleaseCommitRecoveryPostgresTests`；Ruby `YAML.parse_file` 语法验收；旧 `databaseSaved` / 无条件 Plugin target delete / `SaveChangesException` /新增重复 hash/path helper 零命中、规则关键词/死链 `rg`、`git diff --check`。
- 验证结果：Release solution build 0 warning / 0 error，构建中的 Cloud deployment policy architecture test 通过；ProductionService 全量 82/82、ServiceLayer 全量 292/292、ConfigurationGuard + typed HTTP/lock exception 定向 87/87 通过；真实 Aspire PostgreSQL post-commit exception simulation 1/1 通过、0 Skip、耗时 44 秒，并用非整微秒 ticks 验证 fresh exact recovery。Cloud CI YAML 语法解析通过。完整 EndToEnd 未执行，符合手动重型门禁规则；未运行远端 CI、未访问生产或执行部署，本地结果不冒充远端/生产验收。

## 2026-07-11 Cloud 审计持久化 DbContext 隔离（4C1）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 EF 审计实现、EntityFrameworkCore 测试可见性、ServiceLayer 测试依赖/审计隔离测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改 `IAuditTrailService` 合同或 Scoped DI、ClientRelease Host/Plugin handler、artifact/hash、文件落盘/rollback、HTTP 状态、commit probe、数据库 migration/schema、部署脚本、生产环境、`IIoT.EdgeClient` 或 `AICopilot`；未 push、未部署。
- 改动原因与审计纠偏：`EfAuditTrailService` 原先注入请求 scoped `IIoTDbContext`，写审计时调用同一个 `SaveChangesAsync`，会把业务 context 中所有 pending aggregate 一并提交；失败后 Detach 单条审计记录也不能隔离或恢复业务 context。4C1 只先移除这条隐式提交边界，为后续 ClientRelease 提交歧义治理提供可信前置，不把“独立审计 context”误称为已解决 ACK-lost 或 exact retry。
- 影响面与实现：`EfAuditTrailService` 改为注入宿主已经配置和验证的 `DbContextOptions<IIoTDbContext>`；每次 `TryWriteAsync` 创建并 `await dispose` 一个新 `IIoTDbContext`，只 Add/保存当次 `AuditTrailRecord`。旧 `dbContext.Entry(record).State = Detached` 补丁物理删除，未新增 ClientRelease 专用审计 adapter、factory/service locator、fallback 或双轨。调用方 token 已取消时继续抛 `OperationCanceledException`；其他异常仍 best-effort，但日志只写稳定 `PersistenceFailed(4301)` 和 `ErrorType`，不传 exception 对象，也不写原始 message、target/path 或 secret。
- 测试架构：ServiceLayer 新增唯一职责文件 `AuditTrailPersistenceIsolationTests`，使用 SQLite in-memory 真实关系型 EF provider；通过 `SaveChangesInterceptor` 验证 pending ClientRelease aggregate 不会被审计顺带提交、审计保存失败后业务 context 的 Added 状态保留且仍可独立保存、连续两次审计使用不同 context、caller cancellation 继续传播，以及完整审计字段正常落库。没有用 EF InMemory provider 或源码字符串断言冒充持久化行为。
- 规则提炼：形成 Cloud 项目级长期技术规则，已写入 `docs/云端规则.md` 5.1，并在 `docs/云端架构治理清单.md` 新增 C-24 / 批次 K。没有提升到工作区 `../docs/总规则.md`，原因是本规则只约束 Cloud 的 EF 审计持久化实现。Host/Plugin `commitPointReached/databaseSaved` 的 ACK-lost 误判、fresh commit probe、Host manifest/Velopack artifact hash、lease-loss 条件回滚和 exact retry `200/409/503` 仍是未解决项，必须继续拆成后续独立可回滚批次。
- 验证命令：`dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release -p:NuGetAudit=false --filter FullyQualifiedName~AuditTrailPersistenceIsolationTests`；ServiceLayer 全量 `dotnet test -c Release --no-build --no-restore -p:NuGetAudit=false`；ProductionService 全量 `dotnet test -c Release --no-restore -p:NuGetAudit=false`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore -p:NuGetAudit=false --filter FullyQualifiedName~ConfigurationGuardTests`；`dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；变更范围、旧 Detach/共享 context 和规则关键词 `rg`；`git diff --check`。
- 验证结果：SQLite 审计隔离定向 5/5、ServiceLayer 全量 290/290、ProductionService 全量 67/67、ConfigurationGuard 81/81 通过；配置门禁和 AppHost 构建过程中 Cloud deployment policy architecture test 同时通过；AppHost Release warnings-as-errors build 0 warning / 0 error。未执行真实远程 PostgreSQL、ClientRelease 上传、远程 CI 或生产部署，本地结果不冒充远程/生产验收。

## 2026-07-11 Cloud ClientRelease 共享 Redis 发布租约迁移（4B）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 ClientRelease Host/Plugin marker 命令与 handler、上传协调器/会话、Human Controller、ClientRelease ProductionService 测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改 API 路由/请求上限/返回 DTO、数据库 schema/migration、Redis 实现/配置、部署脚本、生产环境、`IIoT.EdgeClient` 或 `AICopilot`；未 push、未部署、未连接真实生产 Redis。
- 改动原因与审计纠偏：4A 已证明 Redis 租约所有权丢失可 fail closed，而 ClientRelease 仍使用单机 `.edge-release-upload.lock`，在跨实例/非共享文件系统下不能保证 Host/Plugin 互斥。本批确认本地文件锁异常释放本身没有已知 managed path 缺陷，真实问题是锁的分布式边界不足。同时拒绝在本批用同一 scoped `DbContext/IRepository` 猜测提交结果；提交歧义与同包幂等确认保留为需要 fresh DbContext/基础设施读端的独立 4C。
- 租约与顺序：Host bundle 和 Plugin package marker 共用内部单一常量 `iiot:lock:client-release:publish`，获取总预算为非零 `5s`，由现有 `DistributedLockBehavior` 覆盖完整 handler。未授权行为测试把 `AuthorizationBehavior` 组合在租约 Behavior 外层，证明 lock acquire、handler 和 body source 读取均为 0；发布竞争继续沿用 4A 的 `409`，Redis 不可用/所有权丢失继续沿用 `503`。
- 冗余清理和会话合同：物理删除 `.edge-release-upload.lock`、`TryAcquireUploadLock`、锁用 `FileStream/DeleteOnClose`、`ConcurrentUpload`、nullable session、两个 handler 空分支、Controller 重复 409 特判、锁释放 EventId/日志和所有测试死引用。`TryBeginAsync` 收口为唯一同步非空 `Begin`，`ClientReleaseUploadSession` 只实现 `IDisposable`，`Dispose` 只幂等同步删 staging；未保留 `ValueTask`、`GC.SuppressFinalize`、adapter、fallback 或 feature flag。
- 取消与提交点：handler 在上传、解包、验证、文件发布、数据库提交和提交后保留/清理相位继续传播 Behavior 联结 token；保留 Host `commitPointReached` 和 Plugin `databaseSaved` 自然提交点。提交前 caller cancellation/所有权丢失会回滚本次新建产物后重抛，提交后取消不回滚已提交产物。本批没有大面积改写 zip/hash 算法，没有改变发布唯一冲突和文件/数据库补偿语义。
- 测试架构：删除混合文件 `ClientReleaseUploadBehaviorTests`，收敛为 `ClientReleaseUploadSessionTests` 和 `ClientReleasePublishLeaseTests`；原 1600+ 行 `ClientReleaseBehaviorTests` 只机械更新 session cleanup 断言，未继续塞入新锁测试。共享一个可控 singleton lease fake，使用 `TaskCompletionSource` 和两个 DI scope 覆盖真异步 acquire、Host/Plugin 同资源/`5s` 预算、竞争、释放后重入、ownership loss、caller cancellation 和 staging 清理。生产 MediatR 注册清单提取为宿主/测试共用的唯一内部 helper，EndToEnd 直接检查真实 service descriptors 顺序；未复制测试顺序常量，也未使用猜测性 `Task.Delay`、源码字符串断言或真实 Redis。
- 规则提炼：形成长期技术规则，已写入 `docs/云端规则.md` 5.1 和 `docs/云端架构治理清单.md` C-21/C-22/C-23 及批次 J：ClientRelease 共享租约必须包围完整 handler，权限先于 lease/body，本地锁不得回归，会话必须保持同步非空/单一释放合同，提交歧义不得用同一 scoped repository 伪确认。
- 验证命令：`dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore -p:NuGetAudit=false --filter 'FullyQualifiedName~ClientReleaseUploadSessionTests|FullyQualifiedName~ClientReleasePublishLeaseTests|FullyQualifiedName~ClientReleaseBehaviorTests'`；ProductionService 和 ServiceLayer 全量 `dotnet test -c Release --no-restore -p:NuGetAudit=false`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore -p:NuGetAudit=false --filter 'FullyQualifiedName~ClientReleaseUploadBoundaryTests|FullyQualifiedName~DistributedLockExceptionHandlerTests|FullyQualifiedName~ConfigurationGuardTests'`；`dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；旧文件锁/伪异步/并发分支与规则关键词 `rg`；`git diff --check`。
- 验证结果：ClientRelease 定向 50/50、ProductionService 全量 67/67、ServiceLayer 全量 285/285 通过；ClientRelease 边界 + 分布式锁异常 + ConfigurationGuard 端到端定向 89/89 通过，构建过程中 Cloud deployment policy architecture test 同时通过；AppHost Release warnings-as-errors build 0 warning / 0 error。旧运行时/测试锁路径、`TryBeginAsync`、`ConcurrentUpload`、上传 session `IAsyncDisposable/ValueTask/GC.SuppressFinalize`、Controller 特判和锁日志零命中；文档旧入口与规则关键词检查通过，`git diff --check` 无输出。未执行真实上传、远程 CI 或生产部署，本地结果不冒充远程/生产验收。

## 2026-07-11 Cloud Redis 分布式锁租约完整性收口（4A）

- 改动范围：仅修改 `IIoT.CloudPlatform` 的分布式锁合同、Redis 租约实现、MediatR 分布式锁 Behavior、HttpApi 异常映射、三个宿主配置、ServiceLayer/ProductionService/EndToEnd 测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。本批未修改 ClientRelease `.edge-release-upload.lock`、Host/Plugin 发布业务、数据库 schema、部署脚本、生产环境、`IIoT.EdgeClient` 或 `AICopilot`；未 push、未部署。
- 改动原因与审计纠偏：外部审计关于“续租失败只记 warning，调用方仍可成功”和“Redis/续租收口没有显式硬超时”的结论成立；对 GUID owner token、`SET NX + TTL`、compare-renew / compare-delete Lua 不安全的泛化质疑不成立，这些原子性边界已保留。对 ClientRelease 本地文件锁“异常时可能不释放”的结论也缺乏当前 managed path 证据；该锁在同主机共享 inode 下可互斥，但不能覆盖跨主机/非共享文件系统，因此作为独立 4B 迁移而不在本批猜测删改。
- 合同与冗余清理：`IDistributedLockService.AcquireAsync` 直接改为唯一 `Task<IDistributedLockLease>` 合同，租约暴露 `OwnershipLost` 并负责异步释放。旧 `Task<IAsyncDisposable>` 签名已删除，未保留 overload、adapter、feature flag 或兼容层；17 个生产 `[DistributedLock]` 写用例继续统一通过 Behavior 执行，测试 fake 一次性对齐新合同。
- 租约与异常语义：续租返回 0、Redis 异常或操作超时会幂等触发 `OwnershipLost` 并结束续租循环。Behavior 将调用方 token 和所有权 token 联结：调用方取消保持 `OperationCanceledException`，租约丢失即使 handler 忽略取消也拒绝返回成功。handler 本身异常与 Dispose 异常同时出现时保留原 handler 异常，成功路径上的解锁失败则显式向上抛出。
- 超时、竞态与脱敏：新增可配置 `OperationTimeoutMilliseconds` 和 `RenewalShutdownTimeoutMilliseconds`，与 lease/cadence 一起 `ValidateOnStart`，并硬性要求 `cadence + 2 * operation timeout < lease`。`acquireTimeout` 作为 Redis 尝试和 retry wait 的单调总预算；每次 Redis `WaitAsync` 取剩余预算与 operation timeout 较小值，零定义为一次不等待尝试。总预算截止映射 `409`，独立 Redis operation timeout 映射 `503`。acquire/renew/unlock 和 renewal shutdown 均有 `WaitAsync` 上限；超时/取消后底层 acquire 晚到成功会用原 token compare-delete，晚到 fault 被观测。阻塞或抛异常的 `OwnershipLost` 回调不会阻塞续租线程或 Dispose。日志和响应不包含锁 key、原始 Redis message 或 stack。
- 测试架构：ServiceLayer 租约专项按 Redis lease service、Redis primitive 原子脚本、MediatR behavior、options/DI 拆成四个测试类，公共可控 primitive/delay/logger 只保留一份 `DistributedLockTestSupport`，不复制 fixture。测试使用可控 `TaskCompletionSource` 和可注入 delay，不使用猜测性 `Task.Delay`；除原竞争、取消、续租失效、Lua/token、回调隔离、Dispose 幂等和异常优先级外，明确覆盖零时不等待尝试、短总预算不被长 operation timeout 穿透、retry wait 总预算与 `409/503` 分界。EndToEnd 使用真实 `UseCaseExceptionHandler` 冻结 `409/503` ProblemDetails 和脱敏，宿主配置测试覆盖三个 appsettings。没有新增 NoWarn、空 `catch` 或仅依赖源码字符串的锁行为断言。
- 规则提炼：形成长期技术规则，已写入 `docs/云端规则.md` 第 6 节和 `docs/云端架构治理清单.md` C-22：租约所有权必须可观测且 fail closed，Redis/收口有硬超时与启动安全窗验证，原子 token/Lua 不得回退，错误稳定分类并脱敏，不得无锁降级。
- 验证命令：`dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore -p:NuGetAudit=false --filter 'FullyQualifiedName~RedisDistributedLockServiceTests|FullyQualifiedName~RedisDistributedLockPrimitiveTests|FullyQualifiedName~DistributedLockBehaviorTests|FullyQualifiedName~DistributedLockOptionsTests'`；ServiceLayer 和 ProductionService 全量 `dotnet test -c Release --no-restore -p:NuGetAudit=false`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore -p:NuGetAudit=false --filter 'FullyQualifiedName~DistributedLockExceptionHandlerTests|FullyQualifiedName~ConfigurationGuardTests'`；`dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；旧合同/兼容入口、旧续租 warning 和规则核心词 `rg`；`git diff --check`。
- 验证结果：按职责拆分后的租约定向 28/28、ServiceLayer 全量 285/285、ProductionService 全量 65/65 通过；`DistributedLockExceptionHandlerTests + ConfigurationGuardTests` 84/84 通过，构建过程中 Cloud deployment policy architecture test 同时通过；AppHost Release warnings-as-errors build 0 warning/0 error。旧 `Task<IAsyncDisposable>` 合同、带伪参数的 cadence resolver、旧 `Configure<DistributedLockOptions>`、旧续租 warning、锁相关空 `catch` 和新租约测试中的 `Task.Delay` 均零命中；17 个生产锁用例可检索，ClientRelease 运行时目录无本批 diff，规则核心词可检索，`git diff --check` 无输出。未连接生产 Redis，未 push、未部署；本地结果不冒充远端 CI 或生产验收。

## 2026-07-11 Cloud ClientRelease 上传边界与重复入口收口

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 Human ClientRelease Controller、HttpApi 请求适配器/Scoped DI、ProductionService 上传端口/协调器/Host 与 Plugin 发布 handler、ProductionService 与 EndToEnd 测试、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本滚动复盘。未修改 API 路由、数据库 schema、部署脚本、生产配置、`IIoT.EdgeClient`、`AICopilot` 或工作区顶层文档；未 push、未部署、未触发远端 workflow。
- 改动原因：源码复核确认外部审计关于 Controller 把 `Request.Body`、长度、ContentType 和来源 IP 直接塞入应用 Command 的判断成立，也确认 Host/Plugin 两个 handler 各自复制发布根解析、同一文件锁、1 MiB 限速 copy、声明长度判断和暂存清理。外部审计把 Host/Plugin 双入口整体视为应删除重复业务则不成立：二者共用同一 release 聚合，但 manifest、产物、回滚与保留策略不同，不能误删任一能力。
- 影响面：两个上传 Command 改为保留 `ClientRelease.Publish` 的无参数 marker；HttpApi 通过 `CurrentClientReleaseUploadSource` 把当前请求适配为传输中立端口；`ClientReleaseUploadCoordinator/Session` 统一 source → staging、Host/Plugin 共用发布锁、限速、最大体积、长度核对和异步清理。Host/Plugin handler 继续分别负责自己的 manifest/hash/落盘/聚合/回滚/保留策略/审计。路由、请求上限、默认 `1000 Mbps`、发布目录、静态下载、权限和返回 DTO 未改变。
- 冗余清理：物理删除两个 handler 内的两套 root resolver、两套 lock helper、两套 copy/限速循环、两套声明长度分支、两套暂存 finally 和重复并发错误常量；Controller 两段 409 映射合并为一个泛型方法；测试使用一个 Host/Plugin 共用上传源 fake，不保留旧 Stream 构造器、兼容重载或双轨入口。
- 异常与权限：AuthorizationBehavior 测试证明未授权 Host/Plugin 请求不会进入 handler，上传源 `ReadCount=0`，且不创建 staging 或 lock。两个 handler 均在通用异常分支前显式捕获取消，执行必要回滚后继续抛出；测试证明 staging 清空、发布锁释放且可重新获取。声明长度不一致和超过上限仍返回已审查验证提示；未知 `InvalidDataException`、IO、数据库、保留策略和回滚异常不再把原始 message、绝对路径或 secret 写入 Result/审计。统一诊断只记录 EventId、稳定操作/目标和异常类型；清理失败不再使用空 `catch`。
- 测试：原 `ClientReleaseBehaviorTests` 全部迁移到 marker 命令并保持通过；新增上传行为用例覆盖权限顺序、Host/Plugin 共锁、长度、上限、取消传播、session 清理/解锁和审计来源，并增加数据库/未知 InvalidData/IO/保留策略 sentinel 不泄漏断言，ClientRelease 定向结果为 48/48；新增反射式架构/适配器用例 4/4 通过，没有新增源码字符串断言。最终全量 ProductionService、ServiceLayer、ConfigurationGuard、AppHost build 和静态死引用检查见本条最终验证结果。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 5.1 和 `docs/云端架构治理清单.md` C-21：应用上传命令/端口不得泄漏 HTTP/Stream；Host 与 Plugin 保留真实业务差异但必须共用唯一上传协调器；权限必须先于读取/暂存/锁；取消和异常必须异步释放，清理失败不得静默。
- 验证命令：`dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore -p:NuGetAudit=false`；`dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore -p:NuGetAudit=false`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore -p:NuGetAudit=false --filter 'FullyQualifiedName~ClientReleaseUploadBoundaryTests|FullyQualifiedName~ConfigurationGuardTests'`；`dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj -c Release --no-restore -p:NuGetAudit=false -p:TreatWarningsAsErrors=true`；上传旧 helper/旧参数 `rg` 零命中检查；规则核心词和死链 `rg`；`git diff --check`。
- 验证结果：ClientRelease 定向 48/48、ProductionService 65/65、ServiceLayer 257/257 全量通过；ClientReleaseUploadBoundaryTests 4/4 与 ConfigurationGuardTests 81/81 合并执行为 85/85 通过，构建过程中 Cloud deployment policy architecture test 同时通过；AppHost Release build 0 warning/0 error。旧 helper/旧 Stream 参数、应用端口传输术语、原始异常 message/路径进入结果与审计、空 `catch`、服务定位器和尾随空白静态检查均零命中；ProductionService 项目引用仅为 Core.Production、Services.Contracts、Services.CrossCutting，不反向依赖 HttpApi；规则核心词可检索，`git diff --check` 无输出。未执行真实上传、远端 CI 或生产部署，本地结果不冒充生产发布验收。

## 2026-07-11 Cloud CI GitHub Actions Node 运行时升级

- 改动范围：仅修改 `.github/workflows/cloud-ci.yml`，把 `actions/checkout` 从 `v4` 升到 `v7`、`actions/setup-dotnet` 从 `v4` 升到 `v5`、`actions/setup-node` 从 `v4` 升到 `v6`，并更新本滚动复盘。未修改 `cloud-deploy`、`cloud-image`、`cloud-routine-request`、`cloud-admin-repair` 等其它 workflow，未修改部署脚本、触发条件、缓存策略、业务代码或生产环境。
- 改动原因：当前 `cloud-ci` run `29136701275` 的所有 job 已通过，但仍有一条旧 Node 运行时弃用注解。经官方当前 major/runtime 信息核对，所选三个 Action major 均使用 Node 24，可在不改变 Cloud CI 业务步骤的前提下消除该 Node 20 注解。
- 影响面：只更新 `cloud-ci` 三个基础 Action 的 major；`.NET SDK 10.0.301`、Node.js 22、npm 缓存路径、build/test/compose 步骤、workflow 触发范围和端到端测试开关均保持不变。本批不把其它 workflow 顺带升级，避免越过部署链独立审查与验证边界。
- 验证命令：`ruby -e 'require "yaml"; YAML.parse_file(ARGV.fetch(0))' .github/workflows/cloud-ci.yml`；`rg -n 'uses: actions/(checkout|setup-dotnet|setup-node)@' .github/workflows/cloud-ci.yml`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers --logger 'console;verbosity=minimal'`；`git diff --check`。
- 验证结果：YAML 语法解析通过；静态核对仅得到 `checkout@v7`、`setup-dotnet@v5`、`setup-node@v6`；`ConfigurationGuardTests` 81/81 通过，构建过程中的 Cloud deployment policy architecture test 同时通过；`git diff --check` 无输出。新提交尚未 push，因此新的 GitHub `cloud-ci` 终态和 Node 20 注解清零仍由主任务在 push 后持续验收，本地结果不冒充远端 CI 结果。
- 规则提炼：无新增长期规则。原因是 GitHub Action major 与其 Node 运行时会随官方上游演进，不应把具体 major 冻结成业务长期规则；现有依赖安全、CI 验证和变更复盘规则已能约束后续升级。

## 2026-07-11 Cloud Web 登录页与生产概览数据真实性收口

- 改动范围：仅修改 `src/ui/iiot-web` 的登录页、认证前端切片、Dashboard feature、i18n 和对应 Vitest；同步更新 `docs/云端架构治理清单.md` 与本滚动复盘。未修改 Cloud 后端 API、身份/权限、设备、数据库、部署脚本、生产配置、`IIoT.EdgeClient`、`AICopilot` 或工作区顶层文档。
- 改动原因：未登录页写死 `36` 台在线设备、`932` 今日产量、`98.6%` 合格率、`2` 条告警、十根固定柱，并宣称“车间在线”“网关刚刚上报”“数据同步正常”；Dashboard 在真实小时产能为空时补固定周趋势，并展示固定甲班组/质检岗/维修岗。这些内容违反禁止伪造生产数据、设备在线、Cloud 同步和告警状态的既有红线。
- 影响面：未登录页不再读取、展示或推测业务运行数据，只保留平台能力说明和原登录行为。原登录必填校验、`loginApi`、session 写入、安全本地 return URL、OIDC `/connect/` 跳转和分类错误展示保持并由组件测试冻结。Dashboard 以四个现有真实 Human API 为来源，显式区分 `loading / empty / error / ready`；任一必需请求失败会清空旧值并提供安全重试。真实小时产能为空时趋势显示空态，产量与合格率显示 `--`；只有 API 返回至少一个真实小时槽且槽值全为零时才显示 `0 / 0.0%`。未改变 API 契约、权限判断或数据计算口径。
- 冗余清理：2026-06-24 模块化后，旧 `src/components/dashboard/` 下 `DeviceStatusDonut`、`EventStream`、`HeroBanner`、`ProductionTrendChart`、`QuickAccessGrid` 已无静态 import、动态 import、测试或样式引用。本批经两轮 `rg`/`git grep` 复核后删除五个文件，不保留旧新双轨；Login 页面壳降至 61 行，登录行为和纯能力说明拆成单一职责组件。
- 测试：新增/扩展 Login 与 Dashboard 组件测试，覆盖未认证页面无运行声明、登录关键行为、return URL/OIDC 分支、错误脱敏，以及 Dashboard loading、empty、error、retry、ready、真实小时值、device-only ready 的产能无数据占位、真实全零小时槽和趋势空态。`npm run test:unit` 为 16 files / 67 tests 全部通过；`npm run build` 完成 `vue-tsc -b && vite build`，3245 modules transformed，构建成功；Cloud `ConfigurationGuardTests` 81/81 已通过。
- CI 基线：本批从 `main` 提交 `68ea4d0` 开始。截图所指旧 run `29134659774` 的唯一失败是 `update-deploy-env.sh` 缺 Unix UserExecute；该问题已由当前 HEAD 修复，后续 run `29134735165` 全绿。本批未重复修改 deploy；终审确认 `deploy/scripts/update-deploy-env.sh` Git mode 仍为 `100755`，Cloud deployment policy architecture test 也随 ConfigurationGuard 构建通过。新提交由主任务负责 push 并等待新的 `cloud-ci` 终态，本批不越权 push。
- 视觉验收：本地 Vite 真实运行后使用 Playwright headed Chromium 检查 `1920x1080`、`1366x768`、`1024x768`。首轮发现 `1366x768` 的能力页脚和左侧说明超出容器，补 `min-height` 和矮屏紧凑布局后复验通过；三个视口的 document scroll 宽高均与 viewport 相同，无横纵溢出，控制台 0 error/0 warning，页面扫描未发现原固定 KPI、车间在线、同步正常或网关上报文案。截图与 Playwright 缓存仅用于本地检查并已清理，没有进入仓库。
- 未验范围：没有合法本地 Cloud 测试账号和可用后端，本轮未进入真实已登录 Dashboard 做浏览器截图；严格禁止用伪造账号、token、mock 业务数据或视觉测试数据替代。四状态和真实值映射已由组件测试覆盖，真实已登录浏览器验收留给具备合法身份/后端的后续验收批次。本轮未 push、未部署、未访问生产环境。
- 规则提炼：无新增长期规则。原因是“禁止伪造生产数据、设备在线、Cloud 同步和告警”“UI 必须接真实业务链路，无真实数据必须空态”“可见 UI 必须真实运行验收”已经分别存在于工作区 `docs/总规则.md`、`docs/云端规则.md` 和 `src/ui/iiot-web/AGENTS.md`；本次只落实既有规则并把实施状态记录到 C-20，不重复制造新的长期规则入口。
- 验证命令：`cd src/ui/iiot-web && npm run test:unit`；`npm run build`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`；`git ls-files -s deploy/scripts/update-deploy-env.sh`；Playwright `open/resize/screenshot/eval/console` 三视口检查；旧 Dashboard 静态/动态引用 `rg`/`git grep`；伪数据/假状态模式 `rg`；`git diff --check`。

## 2026-07-11 Cloud 自动增量 CI/CD 与编译级防回退门禁

- 改动范围：为 `cloud-routine-request.yml` 增加只读生产状态 inspect 与 allowlist artifact；新增 `deploy/tests/TestDeploymentPolicy.ps1`、`Directory.Build.targets` 并接入 `cloud-ci`；同步 Cloud 规则、README、OPERATIONS、Edge 上线说明和工作区 `Deploy-Changed.ps1` / 影响分析 / 总规则。未修改 Cloud 业务源码、数据库 schema、生产 `.env`、Compose 或 nginx，本轮未再次发布生产容器。
- 改动原因：日常部署不能再由操作者手工选全量或逐服务猜测，也不能因 SSH 不通反复编译。自动化需要先 push Git、读取生产当前 SHA，再按 Git 改动和 .NET 项目依赖闭包只构建、推送 Harbor、部署受影响镜像；影响不明确必须停止。
- 影响面：Cloud routine workflow 可在 non-root self-hosted Runner 上只读导出 `GIT_SHA/STATUS/SERVICES` 等无敏感字段，inspect 不 checkout 仓库，避免只读规划被 Git 拉取卡住；统一入口据此计算生产到候选的影响。部署策略测试在 HttpApi build 和 Cloud CI 前执行；删除 inspect、显式服务集合或增量红线会直接让构建失败。部署文档不再把 `Deploy-All.ps1`、脏工作树或手工服务选择写成日常标准。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`deploy/README.md`、`deploy/OPERATIONS.md`、工作区 `AGENTS.md`、`docs/总规则.md` 和 `docs/上传部署总览.md`：Cloud 日常只认自动 Git push + 生产基线 + 依赖闭包增量发布；无法归属禁止全量 fallback。
- 验证命令：`pwsh -File deploy/tests/TestDeploymentPolicy.ps1`；工作区 `pwsh -File deploy/tests/TestIncrementalDeploymentPolicy.ps1`、`pwsh -File deploy/tests/TestDeploymentImpact.ps1`；`dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj -c Release --no-restore -p:BuildInParallel=false --disable-build-servers`；Ruby YAML 解析；`git diff --check`。
- 验证结果：Cloud 策略测试通过；影响测试证明仅被共享项目依赖的 HttpApi 被命中；Release build 0 warning/0 error，构建日志实际打印策略门禁通过。只读生产 inspect run `29134258077` 成功导出 `GIT_SHA=0b4f3e4...` / `STATUS=healthy`，部署请求步骤明确 skipped。增量入口尚未用新的单服务生产改动执行真实发布，因此本条不冒充增量生产 E2E 已验收。
- CI 修复：push 后 `cloud-ci` 的新策略门禁和 solution build 均通过，但旧 `ConfigurationGuardTests.StandardDeploymentDocs_ShouldUseLocalBuildPushAndSshDeployPath` 仍强制 `Deploy.ps1` / 手工 Edge target 文案。已把 guard、Cloud 灾备 workflow 提示和 OPERATIONS/上线说明同步到 `Deploy-Changed.ps1`，保留 `Deploy.ps1` 仅作为内部执行器/恢复入口；这不是放宽门禁，而是让旧门禁约束新的唯一口径。同步后 `ConfigurationGuardTests` 81/81 通过。
- Linux mode 修复：后续 CI 继续由既有 Unix executable guard 发现 `deploy/scripts/update-deploy-env.sh` 在 Git index 中是唯一的 `100644`，而该脚本被文档和部署链直接执行。已只把该文件 mode 恢复为 `100755`，未修改脚本内容或放宽 guard；本地复跑 `ConfigurationGuardTests` 81/81。

## 2026-07-11 Cloud 一键发布与 SSH 不可达自动兜底

- 改动范围：新增 `.github/workflows/cloud-routine-request.yml` 与 pinned `deploy/runner/iiot-release-runner.sh`，并更新工作区 `deploy/Deploy.ps1`、新增 `deploy/Deploy-All.ps1`，同步 Cloud 部署规则、README、OPERATIONS、RUNNER 现场目录和工作区总规则/部署总览；未修改 Cloud 业务源码、数据库 schema、生产 `.env`、Compose 或 nginx。
- 改动原因：生产 HTTP/Harbor 与 self-hosted Runner 正常，但 VPN 到 `10.98.90.154:22` 的新 SSH TCP 连接超时，导致镜像已推送后无法交付远端请求；要求操作者手工找 SSH、重复构建或直接运行旧灾备 workflow 都不符合一键发布目标。
- 影响面：`RemoteTransport=Auto` 先检查 SSH TCP；可达时保持单次 SSH，不可达时把同一份非敏感、digest-bound `request.env` 通过生产 GitHub self-hosted job 交给服务器 `runner/iiot-release-runner.sh`。workflow 校验 invocation、外层 SHA-256、目标、non-root 用户和请求大小；服务器 Runner 完全缺失时从当前提交一次性安装 pinned 副本，已有 Runner 永不覆盖。该路径不同步其它 support、不覆盖 `.env`、不调用旧 `cloud-deploy`。`Deploy-All.ps1` 顺序发布 Cloud 与 AICopilot。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md`、`deploy/README.md`、`deploy/OPERATIONS.md`、工作区 `docs/总规则.md` 和 `docs/上传部署总览.md`：统一入口不变，传输失败只允许把同一不可变请求切换到生产 self-hosted Runner，不能切回旧直调脚本。
- 验证命令：PowerShell parser 校验 `deploy/Deploy.ps1` / `deploy/Deploy-All.ps1`；Ruby YAML 解析 `cloud-routine-request.yml`；`git diff --check`；真实 `Deploy.ps1 -Target Cloud -Deploy -ResumeInvocation 20260710T161703Z-cloud-0b4f3e47b072`；Cloud HTTP/OIDC/Edge catalog 与远端 routine state 验收。
- 验证结果：PowerShell parser、YAML 解析、Shell 语法和 diff 检查通过；三个 Runner 副本 SHA-256 同为 `286d189051cbb53d2edb56f71c07c6b384313abf1e6ab5e3618642365563e01e`；`TestRoutineDeploy.sh` 真实走完隔离 Cloud/AI 构建、单 SSH、migration 备份、失败回滚、无重建 resume、dry-run 和不兼容候选 fail-fast。生产 run `29132517581` 从 invocation `20260710T161703Z-cloud-0b4f3e47b072` 完成 Runner bootstrap、PostgreSQL 备份、migration、DataWorker/Gateway/HttpApi/Web rollout，远端 healthz 200 并写入 `releases/routine-current.env`；外部 healthz、OIDC discovery、stable 客户端 catalog 均返回 200。

## 2026-07-10 Cloud 可信工作站日常发布链收敛

- 改动范围：本轮只新增工作区 `deploy/Deploy.ps1`、稳定 Runner、隔离回归，并更新 Cloud 部署文档、规则、部署契约及部署门禁口径；未修改 Cloud 业务源码、数据库 schema 或运行配置，未连接 Harbor/SSH/生产服务器，未 commit/push。
- 改动原因：旧日常路径把候选冻结、support 同步、多层锁、事务取证、cleanup 和深度检查全部叠入一次应用发布，导致已经可用的本机 SSH key 仍表现为慢、卡和权限问题；同时 clean 工作树门禁会阻断其它窗口正在进行的业务迭代。
- 影响面：Cloud 日常发布改为以 `origin/main` tip 创建临时 detached worktree，只构建选中镜像，生成 OCI digest 后以一次 SSH 请求稳定 non-root Runner。Runner 使用单 `flock`、`--no-deps` 更新、migration 前 PostgreSQL 备份、健康检查和失败镜像回滚；support/Runner/Compose/nginx 升级、cleanup 和 GC 拆成维护任务。远端失败可用 invocation 直接续传，不重建镜像。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`deploy/README.md`、`deploy/OPERATIONS.md` 及工作区 `docs/总规则.md` / `docs/上传部署总览.md`。本地工作树允许脏但未推送内容不得发布；日常应用发布不得替换服务器执行代码；root 只允许一次性修 owner/mode。
- 验证命令：`bash -n deploy/server/iiot-release-runner.sh deploy/tests/TestRoutineDeploy.sh`（工作区路径）；`bash deploy/tests/TestRoutineDeploy.sh`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-build --no-restore --filter "FullyQualifiedName~DeploymentGuardTests" --disable-build-servers --logger "console;verbosity=minimal"`；`git diff --check`。
- 验证结果：隔离回归已通过 Cloud 脏工作树/远端 SHA/一次 SSH/不可变镜像、失败健康检查回滚、同一请求无重建续传和 dry-run 零 SSH。真实生产闭环尚未执行，不能称已完成生产验收。

## 2026-07-10 Cloud 部署事务持久化、严格配置互斥与进程组终审

- 改动范围：仅修改 Cloud `deploy/` 下发布实现、fake 行为测试、`DeploymentGuardTests.cs` / `ConfigurationGuardTests.cs` 及 Cloud 部署文档和规则。未修改 Cloud 业务代码、AICopilot、EdgeClient、数据库或工作区统一编排器；未联网、push 或执行真实生产部署。
- 改动原因：既有隔离回归虽覆盖了 support 恢复和部分失败，仍不能证明父进程 SIGKILL/OOM/重启、子 rollout kill-9、父 TERM、组长先退出但子孙仍存活、dotenv 控制键注入、数值 payload、状态文件中断或并发配置更新下不会静默重试、覆盖操作者数据或留下半文件。
- 影响面：父事务在 support 变更前写原子 marker，获锁前扫描 marker/lock/staging/recovery。无 promotion proof 时 durable blocked；有 proof 后 cleanup 绝不恢复旧 support，下一轮只清理旧事务证据。installer/release 独立进程组执行，信号直接检查 PGID，组长退出后仍对存活子孙走 TERM grace/KILL，确认整组消失才 restore/release lock。local precheck 对 stale/malformed lock 只读，blocked evidence 记录真实旧 invocation/marker/backup/staging/lock。dotenv 错误仅 file/line/category。deploy/rollback 不再替换 `.env`，而是与 `update-deploy-env.sh` 共用严格配置锁；镜像状态独立原子晋升到 `current-images.env`。
- 规则提炼：形成长期 Cloud 部署规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`deploy/README.md` 和 `deploy/OPERATIONS.md`：promotion proof 是 support 恢复的不可逆边界；子孙进程未收敛禁止 restore/释锁；precheck 不得破坏旧证据；dotenv 不得泄漏 key/value/token；操作员配置与 release 镜像状态必须分离，所有受支持配置写入入口共用严格锁，严格锁永不 stale 自动清理。
- 验证命令：`bash deploy/tests/deployment-behavior.sh`；`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --filter "FullyQualifiedName~DeploymentGuardTests" --disable-build-servers --logger "console;verbosity=minimal"`；编译后使用 `--no-build --no-restore` 分别复跑 `DeploymentGuardTests` 和 `FullyQualifiedName~Deploy`；所有修改 shell 脚本的 `sh -n` / `bash -n`；`git diff --check`。
- 验证结果：fake 部署行为 33/33 通过，新增覆盖正式 local release/cleanup/config stale/malformed lock 只读、promotion marker 后父进程 kill-9 cleanup-only、卡死 installer、忽略 TERM 的 release child + grandchild、组长先退出但子孙仍被清理、deploy/rollback 最终阶段的受支持配置更新竞争失败且操作员值不丢失；恶意 dotenv 测试删除输入文件后扫描全部输出/状态，未发现 secret 泄漏。`DeploymentGuardTests` 20/20、名称含 `Deploy` 的门禁 39/39 通过。本结果仍是隔离行为与源码契约验收，未执行 Harbor/SSH/生产容器 deploy/history/cleanup/健康闭环，不得称生产发布完成。

## 2026-07-10 AICopilot OpenAPI 依赖升级后的 X0 重新配对

- 改动范围：Cloud 生产源码和测试数据契约未变；仅更新本复盘，记录 AICopilot 修复 `Microsoft.OpenApi` 高危依赖后的真实联合复验。未修改 Cloud 业务代码、部署脚本、数据库、EdgeClient 或生产环境。
- 改动原因：AICopilot 的 `src/hosts/AICopilot.HttpApi/AICopilot.HttpApi.csproj` 从可传递 `Microsoft.OpenApi 2.0.0` 显式固定到安全版本 2.7.5，导致上一组 AICopilot 候选源码 digest 和 X0 证据按规则失效，必须重新运行真实 provider ↔ typed consumer 验收。
- 配对候选：`CloudBaselineSha=e7835a62e7ee06c7a180c879ce4a8db0af8643be`；`CloudCandidateSourceDigest=sha256:51f58843aff8b4f8616c6c4560e39aa54b299aedff14553174f2d15fa50a2afc`；`AICopilotBaselineSha=9164d869cd2f58794eb3e3a2b5fb8320576a0ac3`；`AICopilotCandidateSourceDigest=sha256:f6faca901b01519bcd798cad07fa8605e3bc75bc5d6e064d7ad536e9c7db789b`；`ExecutedAtUtc=2026-07-10T08:48:58Z`；`CloudEnvironment=local-aspire-isolated`；`Result=Passed`。
- 权威源码提交对：Cloud `bb4184ca107b0e35a4293748594364686f864f18`；AICopilot `b66e472685cffb4c65fa30ce54d1fca1b589dd3a`。两笔提交在上述 X0 通过后由同一候选工作树创建，未改变已配对的 `src/` 字节或源码 digest；本条复盘回写属于纯文档提交。
- 规则提炼：无新增长期规则；本次严格执行既有“任一侧生产源码变化后 X0 失效并重跑”的跨仓规则。
- 验证命令：`IIOT_RUN_CLOUD_AI_WORKSPACE_ALIGNMENT=1 dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter 'FullyQualifiedName~CurrentCloudAndAICopilotTypedClient_ShouldPassWorkspaceAlignment'`。
- 验证结果：真实 Cloud Aspire provider ↔ 当前 AICopilot typed client 1/1 通过（约 50 秒）；已创建上述本地源码提交，未 push、部署或访问生产环境。


## 2026-07-10 Cloud ↔ AICopilot 整体契约对齐与真实 live 验收

- 改动范围：在当前 Cloud C0/C1 与部署加固候选工作树上收敛 delegated scope、AiRead 查询/审计、运行心跳幂等并发与部署失败码，新增显式联合验收入口 `CloudProductionFlowTests.WorkspaceAlignment.cs`，并更新 AI Read、状态投影、云端规则和本复盘。未修改 EdgeClient、数据库 migration、Cloud 前端、工作区部署调度器或生产环境；未提交、未推送、未部署。
- 改动原因：Cloud 与 AI 分窗修复后，单仓测试仍不能证明当前 AICopilot typed client 能消费当前 Cloud 信封、严格字段、Missing/Stale 与权限边界；malformed/mixed delegated claim、同时间戳冲突心跳、未来时钟和部署退出码也需要固定为回归行为。
- 影响面：delegated scope 显式分为 `Global/Delegated/Invalid`，empty、非法、合法+非法混合均 fail-closed；设备端点在数据库侧完成 scope、AND 过滤、count、排序和分页。运行心跳按 `DeviceId + ClientCode` 串行化，区分 Applied/Idempotent/Stale/Conflict，未来时间最多容忍 5 分钟；软件状态严格 `>24h` 才 stale。联合测试用真实 Aspire PostgreSQL/RabbitMQ/HttpApi/Gateway 准备三类状态和八端点数据，再只经子进程环境向 AI live-test 传临时 BaseUrl/token；token 不进入参数、输出、复盘或仓库。
- 配对候选：`CloudBaselineSha=e7835a62e7ee06c7a180c879ce4a8db0af8643be`；`CloudCandidateSourceDigest=sha256:51f58843aff8b4f8616c6c4560e39aa54b299aedff14553174f2d15fa50a2afc`；`AICopilotBaselineSha=9164d869cd2f58794eb3e3a2b5fb8320576a0ac3`；`AICopilotCandidateSourceDigest=sha256:4185c4abc0ec0e0478ae57d6721aabd887620cbd4c3674bead185d381fe13419`；`ExecutedAtUtc=2026-07-10T08:16:19Z`；`CloudEnvironment=local-aspire-isolated`；`Result=Passed`。两仓仍是 baseline HEAD + 未提交候选工作树；任一侧 `src/` 变化后结果失效。
- 规则提炼：形成长期跨仓验收规则，已写入 `docs/AI只读接口契约.md`、`docs/设备客户端状态投影契约.md` 和 `docs/云端规则.md`：单仓绿测或 Stub 不等于整体对齐；必须使用真实 provider ↔ typed consumer、成对 SHA/源码 digest、临时 token 环境传递和源码变化失效规则。无需提升到工作区总规则，原因是该规则专属于 Cloud AiRead/AICopilot consumer 契约。
- 验证命令：`dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers`；`dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore --disable-build-servers`；ConfigurationGuard + DeploymentGuard；AiRead HTTP E2E；HttpApi/AppHost Release build；`IIOT_RUN_CLOUD_AI_WORKSPACE_ALIGNMENT=1 dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-build --no-restore --disable-build-servers --filter 'FullyQualifiedName~CurrentCloudAndAICopilotTypedClient_ShouldPassWorkspaceAlignment'`；shell syntax；`git diff --check`。
- 验证结果：ServiceLayer 257/257、ProductionService 50/50、ConfigurationGuard + DeploymentGuard 101/101 通过；AiRead HTTP 全套首轮 21/22，唯一失败为独立 Aspire eventbus 启动瞬态且未进入业务断言，同一实例重跑 1/1 通过；HttpApi/AppHost 0 warning、0 error，shell 语法和 diff 检查通过。真实 Cloud ↔ AI live 联合矩阵 1/1 通过（约 53 秒），覆盖八端点非空/空集/截断、严格属性、专用状态权限、Running/Missing/Stale、scope/审计脱敏和稳定 403。未执行真实生产发布、SSH、Harbor、migration 或 push。


## 2026-07-10 Cloud 部署隔离行为回归与并发/中断加固

- 改动范围：只修改 Cloud 部署脚本、默认 CI 部署守卫、工作区统一入口和部署规则；未修改或回退当前 AiRead/状态投影业务改动，未连接生产服务器。
- 改动原因：固定提交的第一次完整模拟发布虽然 build、support sync 和 SSH 均成功，统一入口仍被遗留的 `sleep 180/2400` 看门狗占住输出管道；普通失败被误报为 timeout 124，并发 support 安装没有持有 release lock。继续并发审查还确认，本地固定 `cloud-built-services.txt` 可被另一 dry-run 覆盖，clean 检查后的可变工作树也可能把新内容构建进旧 SHA。
- 影响面：timeout watchdog 现在清理自身 sleep 与受监督本地进程树且保留真实退出码；support sync 与 release 共用互斥锁并绑定 manifest SHA-256；HUP/INT/TERM 显式退出并释放锁；正式 Cloud 发布从已推送 HEAD 的临时 detached worktree 构建和同步，每次使用独立 `artifacts/deploy/runs/<run-id>/` 服务清单；CI 默认运行 DeploymentGuardTests。工作区入口同时增加独立步骤 supervisor、交互取消 130、步骤硬超时、stdout/stderr 公平读取、正式发布禁用 `-AllowDirty` 和 EdgePlugin 必须显式 ModuleId。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 及工作区 `docs/总规则.md` / `docs/上传部署总览.md`：看门狗不得遗留继承输出管道的孤儿；普通失败不得冒充 124；support sync/release 必须互斥并绑定 digest；信号必须释放锁后退出；正式发布必须固定提交快照并隔离每次运行的服务清单；这些行为必须进入默认 CI。
- 验证命令：`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter 'FullyQualifiedName~DeploymentGuardTests|FullyQualifiedName~ConfigurationGuardTests'`；Cloud shell syntax；固定 commit clean clone + fake Docker/SSH/remote 的成功、exit 42、heartbeat、Ctrl-C、timeout、并发、live/stale lock、digest mismatch、同 SHA 重跑、TERM、构建期间主工作树变脏和并行 dry-run 覆盖共享清单测试。
- 验证结果：同步部署文件时当前并行业务改动工作区 DeploymentGuard + ConfigurationGuard 曾为 88/88。标准模拟发布正常收口；Ctrl-C 返回 130 并写摘要；2 秒 timeout 约 3.35 秒返回 124；第二个并发发布约 0.37 秒以 75 失败；release/cleanup TERM 返回 143 并释放锁；同 SHA 两次成功；`.env`、`certs/`、`releases/`、`backups/` 哈希不变。正式模拟发布实际使用 detached worktree；构建期间修改主工作树仍按原 SHA 成功；另一 dry-run 将共享清单覆盖为 `gateway` 后，本次独立清单仍保持并只部署 `httpapi`。文档终审后的 `--no-build --no-restore` 复核中，部署专用门禁 7/7、名称含 `Deploy` 的静态门禁 26/26 通过；88 项组合为 87/88，唯一失败是并行窗口正在修改的 AiRead 业务源码与既有断言暂时不同步，本轮未修改。真实生产部署未执行，因此本轮只称“隔离行为回归通过”。

## 2026-07-10 Cloud Goal C0/C1a/C1b/C1c：部署门禁、AiRead 精确查询、状态语义和审计脱敏

- 改动范围：仅修改 `IIoT.CloudPlatform`。C0 更新 Cloud 部署文档、灾备 workflow 提示和门禁；C1a 增加 AiRead 设备专用 EF 查询服务、设备/工序精确参数、严格设备四字段 DTO 和已知误导参数 400；用户确认 G0 后，C1b 增加显式时钟的共享软件状态判定器，让 Human 上位机/Human 版本清单/AI 状态查询共用心跳语义，并将 AI `device-client-states` 改为授权 Device 主集、精确 AND 过滤、缺失投影补 Missing 行和真实 count/page；C1c 把 QueryScope 改为显式类型化构造，将自由字符串脱敏为 `present`，失败审计收敛为 ResultStatus/异常类型。同步更新 `docs/AI只读接口契约.md`、`docs/设备客户端状态投影契约.md`、`docs/云端规则.md` 和本复盘。
- 改动原因：部署提交 `e7835a6` 后仍有门禁把项目实现脚本写成外部标准入口，Cloud 文档也保留目录扫描补 catalog 的旧语义。AiRead 主数据缺少正式精确过滤，误导参数会被静默忽略，QueryScope/失败审计仍可能保存自由文本。G0 确认前，Human 两处软件状态各自读取 `DateTime.UtcNow`，AI 又只以已有投影为主集，会造成同一响应时间漂移、无心跳设备消失、分页截断失真，也容易误用版本/投影更新时间解释运行新鲜度。
- 影响面：Cloud 仓内操作者标准入口统一为工作区 `deploy/Invoke-WorkspaceDeploy.ps1`，catalog 版本集合只来自 Cloud release 记录。AiRead `/devices` 和 `/device-client-states` 支持 `deviceId/deviceCode/processId/keyword/maxRows` AND 过滤并在数据库侧完成 delegated scope、count、稳定排序和分页；`/processes` 支持 `processId`。状态端点无投影时返回 `softwareStatus=MissingRuntimeHeartbeat`且不伪造 `updatedAtUtc`；有投影时仅按当前 `Device.Code` 匹配。`runtimeStatus` 保留心跳原值，`lastRuntimeHeartbeatAtUtc` 是唯一新鲜度时间；版本上报不再可能冒充运行心跳。Human 现有状态字符串和中文 Issue 原样保留。QueryScope 自由字符串只记 `present`，失败审计不保存原始错误文本。
- 冻结链路：未修改 Cloud 前端、Human API DTO/路由/权限、PLC 状态、Cloud/MES 上传、bootstrap、发布鉴权、catalog/首装/下载运行时代码、数据库 schema/migration、EdgeClient 或 AICopilot。Human 仅把重复的软件状态计算替换为公共判定器，已用原字符串和 Issue 断言冻结。本轮未部署、未推送、未连接 SSH/Harbor/生产数据库。
- 规则提炼：C0 无新增长期规则，原因是纠正既有唯一入口和 release-record catalog 契约漂移。C1a/C1c 形成长期 AI Read 契约：精确条件与 delegated scope 必须在数据库侧相交，已知误导参数不得静默忽略，自由字符串 QueryScope 只记 `present`，FailureReason 只记安全分类。C1b 形成长期状态契约：`softwareStatus` 只由显式时钟和最新运行心跳解析，恰好 24 小时不 stale，Human 共用判定器，AI 以授权 Device 为主集并对缺失投影返回 Missing 行。已写入 `docs/AI只读接口契约.md`、`docs/设备客户端状态投影契约.md` 和 `docs/云端规则.md`；未提升到工作区总规则，因为该语义属于 Cloud 状态投影与 AI Read 专用契约。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName~ConfigurationGuardTests' --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName~AiReadBehaviorTests|FullyQualifiedName~AiReadDeviceQueryServiceTests|FullyQualifiedName~AiReadProcessQueryServiceTests' --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName~DeviceClientSoftwareStatusResolverTests' --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore --disable-build-servers --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName~AiRead' --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName~CloudProductionFlowTests' --logger 'console;verbosity=minimal'`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --disable-build-servers --filter 'FullyQualifiedName=IIoT.EndToEndTests.CloudProductionFlowTests.AiReadProcesses_ShouldSupportExactProcessId' --logger 'console;verbosity=minimal'`
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj -c Release --no-restore --disable-build-servers --nologo -v:minimal`
  - `rg -n "Invoke-WorkspaceDeploy.ps1|Cloud release 记录|不得扫描目录补出未登记版本|deviceId|deviceCode|processId|ScopeText|FailureReason" deploy docs .github src/tests src/services`
  - `if rg -n "IRepository<DeviceClientState|IReadRepository<DeviceClientState|DeviceClientState.*Specification" src -g '*.cs' -g '!src/tests/**'; then exit 1; fi`
  - `if rg -n "\\[(HttpPost|HttpPut|HttpPatch|HttpDelete)" src/hosts/IIoT.HttpApi/Controllers/AiRead -g '*.cs'; then exit 1; fi`
  - `git diff --check`
- 验证结果：C0 修改前在 `e7835a6` 上真实为 78/79，修正后 ConfigurationGuard 79/79；加入 C1a/C1c 守卫后为 81/81，最终 ConfigurationGuard + DeploymentGuard 86/86。C1b 共享状态判定器与 AiRead 行为定向测试 45/45，Human EdgeHost/版本清单冻结测试 13/13。ServiceLayer 全量 252/252、ProductionService 全量 45/45 通过。AiRead HTTP E2E 最终 26/26 通过（14 分 48 秒），覆盖精确 AND 过滤、delegated scope、专用权限矩阵、Missing 行、真实截断、严格字段、运行心跳 Running、版本上报不替换心跳新鲜度、误导参数 400 和审计脱敏。完整 CloudProductionFlow 59 个实例首轮 58/59（42 分 20 秒），唯一失败在该实例进入业务断言前的独立 Aspire `eventbus` 健康启动阶段；同一 `AiReadProcesses_ShouldSupportExactProcessId` 实例单独重跑 1/1 通过（40 秒），且已包含在 AiRead 26/26 通过结果中，确认为 RabbitMQ/Aspire 单 fixture 启动瞬态失败，不是业务回归失败。HttpApi 与 AppHost Release build 均为 0 warning/0 error；运行时代码无通用 `DeviceClientState` 仓储/Specification，AiRead Controller 无写方法，前端无 diff，`git diff --check` 通过。未部署、未推送、未访问 Harbor/SSH/生产数据库。

## 2026-07-10 Cloud 标准部署真实链路失真修复

- 改动范围：修复工作区统一入口 `../deploy/Invoke-WorkspaceDeploy.ps1` 的实时 stdout/stderr、30 秒 heartbeat 和子进程树中止；修改 Cloud `deploy/scripts/build-and-push.sh`、`local-release.sh`、`release-common.sh`、`pre-deploy-check.sh`、`deploy-release.sh`、`post-release-cleanup.sh`，补齐 Unicode 路径可读命令输出、support files 白名单 staging/SHA-256 同步、全局 release lock、cleanup lock stale 识别、fail-fast 和 cleanup 实时输出；修复 `check-container-nonroot-readiness.sh`、`check-release-state-access.sh` executable 位；新增 `DeploymentGuardTests.cs` 并补 CI 语法门禁；同步更新工作区与 Cloud 部署文档、规则和安全部署契约。未修改 Cloud 业务 API、数据库 schema、PLC runtime-state 业务改动、EdgeClient 或 AICopilot。
- 改动原因：2026-07-09 曾以 dry-run + validate-only 宣称“任何 AI 可完成标准部署”，但没有验证真实 build/push/support sync/cleanup/中止链路。现场同一 SHA 被重复触发，容器虽已运行且健康，远端 compose 仍保留旧 `100 Mbps` 默认值；统一入口全程隐藏输出，cleanup 锁又静默等待 15 分钟，导致 AI 误判卡死、反复停止并绕开唯一入口请求额外权限。
- 影响面：工作区入口现在实时显示长任务阶段；Cloud 发布把镜像和版本化 support files 作为同一提交交付，生产 `.env`、证书、release state 和备份不进入同步包；release/cleanup 活锁立即失败，已证明 stale 的锁才能清理；cleanup 结果实时输出并在失败时明确区分“运行态健康、收口失败”，阻止盲目整轮重发。当前工作区同时存在用户进行中的 PLC runtime-state 改动，本次部署修复不覆盖、不回退、不纳入其提交。
- 规则提炼：形成长期部署规则，已写入工作区 `../docs/总规则.md`、`../docs/上传部署总览.md`、`../docs/历史核心记录.md`、Cloud `docs/云端规则.md` 和 `docs/云端安全部署契约.md`：真实 deploy 才能证明标准发布能力；统一入口必须实时可观测；support files 与镜像同批交付且保护生产持久化数据；并发锁 fail-fast、stale 锁受控清理；部分成功先查状态、不得重复发布。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh deploy/scripts/pre-deploy-check.sh deploy/scripts/deploy-release.sh deploy/scripts/check-container-nonroot-readiness.sh deploy/scripts/check-release-state-access.sh deploy/scripts/post-deploy-check.sh deploy/scripts/ops-check.sh && bash -n deploy/scripts/build-and-push.sh deploy/scripts/local-release.sh deploy/scripts/post-release-cleanup.sh deploy/scripts/harbor-retention.sh`
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter 'FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~DeploymentGuardTests' --no-restore --disable-build-servers --nologo -v:minimal`
  - `pwsh -NoProfile -File ../deploy/Invoke-WorkspaceDeploy.ps1 -Target Cloud -Services httpapi,dataworker -DryRun`
  - managed lock 合成测试：同一 PID owner 允许、其他调用者 live lock 返回 75、无效 PID stale lock 原子清理。
- 验证结果：shell 语法、compose config、PowerShell parser/profile、Unicode 路径 dry-run、实时 step 输出和 managed lock 合成测试通过；并发 Cloud 改动出现前 ConfigurationGuard + DeploymentGuard 84/84 通过。随后用户正在进行的 `AiReadQueries.cs` 改动暂时导致 Cloud 编译失败，因此最终 .NET 回归、真实标准部署、远端 `1000 Mbps` 运行值、support manifest、history summary 和 ops-check 仍待该业务改动恢复可编译并获得发布边界确认后执行；当前不得称为部署完成。

## 2026-07-10 PLC runtime 完整快照替换语义对齐

- 改动范围：仅修改 `IIoT.CloudPlatform` 的 `IEdgeHostPlcRuntimeStateStore`、EF 专用 store、`ReportEdgeHostPlcRuntimeStatesHandler`、ProductionService/EndToEnd 测试、`docs/云端规则.md`、`docs/Edge上传与PLC状态接口契约.md` 和本复盘。未修改 Cloud 前端、设备身份链、Human 查询结构、部署脚本、生产配置、`AICopilot` 或现场客户端 UI。
- 改动原因：Cloud 原实现只按 `DeviceId + ClientCode + PlcCode` 新增或替换本次上报项，不删除本次已经缺失的旧 PLC；Edge 删除全部 PLC 后又不会发送空列表，导致 Cloud 长期保留陈旧投影。上一轮只补了列表性能和 `RuntimeStatus` 兜底，客户端稳定编码、明确状态分类和完整快照语义仍未闭环。
- 影响面：每次合法上报现在作为当前设备身份的完整 PLC 配置快照处理；本次出现的编码继续新增/替换，本次缺失的旧编码在同一 `SaveChanges` 单元删除，合法空列表会清空该身份下全部旧投影。身份、字段或重复编码校验失败时仍在读取/写入投影前返回，不会部分修改。`PlcCode` 明确为 EdgeClient 持久化且改名不变的稳定身份，`ReportedPlcName` 仅用于展示；Cloud 仍保留旧客户端缺省 `RuntimeStatus` 兜底。
- 规则提炼：形成长期跨端契约，已写入 `docs/云端规则.md` 和 `docs/Edge上传与PLC状态接口契约.md`：PLC runtime 上报是完整快照而非增量事件；合法空列表清空旧投影；`PlcCode` 与可变展示名称分离；客户端必须明确分类 `Connected/Faulted/Unknown/Disconnected`。没有提升为工作区总规则，因为该约束只属于 Edge PLC runtime 专用接口。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~EdgeHostBehaviorTests" --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~InfrastructureBehaviorTests|FullyQualifiedName~ApplicationFlowGuardTests|FullyQualifiedName~AuthorizationAndIdentityBehaviorTests|FullyQualifiedName~AggregateBehaviorTests" --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers --nologo -v:minimal`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~EdgeHostPlcRuntimeState_ShouldKeepDedicatedEdgeWriteAndHumanReadBoundary" --no-restore --disable-build-servers --nologo -v:minimal`
  - `git diff --check`
- 验证结果：AppHost build 通过，0 warning/0 error；`EdgeHostBehaviorTests` 11/11、ServiceLayer 定向守卫 160/160、本轮 PLC 契约 ConfigurationGuard 1/1 通过。Release EndToEnd 配置/限流全套为 77/79：失败的 `StandardDeploymentDocs_ShouldUseLocalBuildPushAndSshDeployPath` 与 `DeployTemplates_ShouldDocumentSingleNodeSecretsAndSmokePolicy` 都是当前 HEAD 已存在的 `deploy/README.md` 文案与旧断言不一致，本轮没有修改 `deploy/`，未越权顺带改部署口径。Cloud 前端未改，因此未重复执行 npm build/test；未部署、未连接生产环境。

## 2026-07-09 工作区唯一部署入口与 Cloud 对外口径收口

- 改动范围：新增工作区 `../deploy/Invoke-WorkspaceDeploy.ps1`、`../deploy/README.md`、`../deploy/profiles/template-rules.json`、`../deploy/profiles/current-production.json`，并更新工作区 `../AGENTS.md`、`../docs/总规则.md`、`../docs/上传部署总览.md`、`../docs/历史核心记录.md`，同时修改 Cloud `deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md` 和本复盘。未修改 Cloud 业务 API、数据库 schema、运行时业务流程或服务器生产配置。
- 改动原因：虽然 Cloud/AICopilot/Edge 各自已有可运行脚本，但对外仍然需要人工或 AI 自己猜入口、拼参数、区分标准路径与实现脚本，达不到“把任务扔给任何 AI 就能直接按标准路径完成部署”的要求。
- 影响面：Cloud 的对外标准入口现在统一收口到工作区根 `deploy/Invoke-WorkspaceDeploy.ps1 -Target Cloud ...`；Cloud 项目内 `deploy/scripts/local-release.sh`、`build-and-push.sh` 和服务器 `deploy-release.sh` 继续作为被调度实现脚本。工作区机器可读部署口径拆成两层：长期模板/规则在 `../deploy/profiles/template-rules.json`，当前生产现场事实在 `../deploy/profiles/current-production.json`。统一入口已产出 Cloud dry-run 摘要 `../artifacts/deploy/workspace/latest-cloud.md` 和 validate-only 摘要 `../artifacts/deploy/workspace/history/20260709-162642-cloud-validate-only.md`。
- 规则提炼：形成长期部署规则，已写入工作区 `../AGENTS.md`、`../docs/总规则.md`、`../docs/上传部署总览.md`、`../docs/历史核心记录.md` 和 Cloud `docs/云端规则.md`：工作区唯一对外标准部署执行入口是 `deploy/Invoke-WorkspaceDeploy.ps1`；`deploy/profiles/template-rules.json` 与 `deploy/profiles/current-production.json` 是机器可读部署口径源；项目内脚本不再各自充当外部统一入口。
- 验证命令：
  - `pwsh -NoProfile -File ../deploy/Invoke-WorkspaceDeploy.ps1 -Target Cloud -Services web -DryRun`
  - `pwsh -NoProfile -File ../deploy/Invoke-WorkspaceDeploy.ps1 -Target Cloud -Services web -ValidateOnly`
  - `rg -n "Invoke-WorkspaceDeploy\\.ps1|current-production\\.json|template-rules\\.json|latest-summary\\.md" ../AGENTS.md ../docs/总规则.md ../docs/上传部署总览.md deploy/README.md deploy/OPERATIONS.md docs/云端规则.md ../deploy/README.md`
- 验证结果：通过。统一入口 dry-run 成功调度 Cloud `local-release.sh --dry-run` 并输出标准 `ssh iiot-server-runner` 命令；validate-only 成功通过同机真实服务器 `/data/iiot-platform/cloud/deploy` 的 non-root preflight，摘要记录了 `check-release-state-access.sh`、OIDC PFX 可读性、Edge 更新目录 non-root readiness、`/internal/healthz` 和 `ops-check-runtime` 全部通过。检索确认 Cloud 对外文档、工作区规则和机器可读 profile 都已统一指向新的唯一入口。

## 2026-07-09 同机双部署根口径写实与 Cloud 现场 non-root 路径修复

- 改动范围：更新工作区 `../docs/上传部署总览.md`、`../docs/总规则.md`、`../docs/历史核心记录.md`，并修改 Cloud `deploy/scripts/local-release.sh`、`deploy/README.md`、`deploy/EDGE_INSTALLER_GO_LIVE.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端架构治理清单.md`、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs` 和本复盘。同时直接修复同机 Cloud 真实部署根 `/data/iiot-platform/cloud/deploy` 的 `certs/`、`releases/*` 和 `cloud-oidc-signing.pfx` owner/mode，并把最新 deploy support scripts 同步到服务器。未修改 Cloud 业务 API、数据库 schema 或运行时业务流程。
- 改动原因：上一轮虽然补了仓库门禁，但对外表述仍容易把 Cloud 与 AICopilot 讲成两套无关环境；继续按标准 `github-runner` 路径回归时，又依次暴露出同机 Cloud 根的 `releases/*`、`certs/` 和 `cloud-oidc-signing.pfx` 权限漂移，以及旧复盘里仍残留可复制 root 发布示例。
- 影响面：工作区总入口、Cloud 部署文档和 Cloud 帮助示例现在统一写明“Cloud 与 AICopilot 同机、共享 `github-runner` 非 root 发布人、各自独立部署根”；Cloud 本机发布帮助示例改为 `github-runner@<shared-host>`。真实服务器上的 `releases/*` 已恢复为 `github-runner:github-runner 600/755`，`cloud-oidc-signing.pfx` 已恢复为 `github-runner:10001 640`，标准 `github-runner` 路径下的 `./scripts/pre-deploy-check.sh <current-release-tag>` 已重新通过。
- 规则提炼：形成长期部署规则，已写入工作区 `../docs/总规则.md`、`../docs/历史核心记录.md`、Cloud `docs/云端规则.md` 和 `docs/云端安全部署契约.md`：当 Cloud 与 AICopilot 同机时，必须同时写清共享宿主机事实、共享标准发布人和两个独立部署根；Cloud 根的 root-owned 漂移不得被误写成整机通用故障；root 应急路径写入的 release state / cert 文件必须恢复到标准 non-root + 容器可读口径。
- 验证命令：
  - `sh -n deploy/scripts/pre-deploy-check.sh deploy/scripts/check-release-state-access.sh deploy/scripts/local-release.sh deploy/scripts/ensure-oidc-signing-cert.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~ConfigurationGuardTests" --no-restore --disable-build-servers`
  - `ssh root@<shared-host> 'chown -R github-runner:github-runner /data/iiot-platform/cloud/deploy/certs /data/iiot-platform/cloud/deploy/releases && find /data/iiot-platform/cloud/deploy/certs -type d -exec chmod 755 {} + && find /data/iiot-platform/cloud/deploy/certs -type f -exec chmod 600 {} + && find /data/iiot-platform/cloud/deploy/releases -type d -exec chmod 755 {} + && find /data/iiot-platform/cloud/deploy/releases -type f -exec chmod 600 {} +'`
  - `ssh root@<shared-host> 'chgrp 10001 /data/iiot-platform/cloud/deploy/certs/cloud-oidc-signing.pfx && chmod 640 /data/iiot-platform/cloud/deploy/certs/cloud-oidc-signing.pfx'`
  - `ssh iiot-server-runner 'cd /data/iiot-platform/cloud/deploy && release_tag=$(sed -n "s/^DEPLOY_RELEASE_ID=//p" releases/current-release.env | tail -n 1) && ./scripts/check-release-state-access.sh && ./scripts/ensure-oidc-signing-cert.sh && ./scripts/pre-deploy-check.sh "$release_tag"'`
  - `rg -n "github-runner@<shared-host>|同一台生产宿主机|共享标准发布人" deploy/README.md deploy/EDGE_INSTALLER_GO_LIVE.md docs/云端规则.md docs/云端安全部署契约.md docs/云端架构治理清单.md`
- 验证结果：通过。Cloud `ConfigurationGuardTests` 79/79 通过；同机真实服务器上的 `releases/*`、`certs/` 和 OIDC PFX 权限恢复后，`check-release-state-access.sh`、`ensure-oidc-signing-cert.sh` 和 `pre-deploy-check.sh` 均通过。`preflight_compensation_controls` 输出已重新回到标准 non-root 路径，说明这次不是只修文档，现场也已收口。

## 2026-07-09 三项目部署口径收口与 Cloud non-root release-state 门禁

- 改动范围：更新工作区 `../docs/总规则.md`、`../docs/上传部署总览.md`、`../docs/历史核心记录.md`，并修改 Cloud `deploy/scripts/pre-deploy-check.sh`、新增 `deploy/scripts/check-release-state-access.sh`、更新 `deploy/README.md`、`deploy/OPERATIONS.md`、`deploy/RUNNER.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md` 和本复盘。保留同日已改好的 Cloud Edge 上传默认值 `1000 Mbps` 口径，并与 Edge 发起端默认值同步到同一标准。未修改 Cloud 业务 API、数据库 schema、运行时业务流程或真实服务器文件 owner。
- 改动原因：复核当前部署失败时确认，问题不只是 `certs/` 目录权限，而是 root/non-root 混跑后把 `releases/current-release*`、`staged-release*`、`previous-release*` 和 `current-release.summary.md` 也污染成 root-owned；同时顶层入口、Cloud/Edge/AICopilot 部署文档、脚本默认值和当前生产现场口径没有一起维护，导致部署速度、权限边界和标准路径反复回退。
- 影响面：Cloud 标准 preflight 现在会在 OIDC 证书检查前先验证 `releases/*` owner/mode 对标准 non-root 部署用户是否仍可读可写，提前拦住 root 应急路径留下的状态文件；Cloud 部署文档、runner 文档和安全契约同步写明当前标准目录 `/data/iiot-platform/cloud/deploy`、标准用户 `github-runner`、root 应急后必须恢复 owner/mode；顶层入口也同步写明 Edge/Cloud 当前统一的 `1000 Mbps` 口径和 AICopilot 的真实慢路径。
- 规则提炼：形成长期部署规则，已写入工作区 `../docs/总规则.md`、`../docs/历史核心记录.md`、Cloud `docs/云端规则.md` 和 `docs/云端安全部署契约.md`：部署文档必须拆成“长期模板/规则”和“当前生产现场口径”两层；root 应急路径不得留下 root-owned `certs/` 或 `releases/*` 后直接收口；改 deploy/默认值/当前现场目录时必须同步更新顶层入口、项目部署文档和项目规则。
- 验证命令：
  - `sh -n deploy/scripts/pre-deploy-check.sh deploy/scripts/check-release-state-access.sh deploy/scripts/ensure-oidc-signing-cert.sh`
  - `rg -n "check-release-state-access|github-runner:github-runner|长期模板/规则|当前生产现场口径|1000 Mbps" deploy docs ../docs`
  - `ssh iiot-server-runner 'cd /data/iiot-platform/cloud/deploy && ls -ld certs releases releases/history && ls -l releases/current-release.env releases/previous-release.env releases/staged-release.env releases/current-release.summary.md'`
  - `ssh iiot-server-runner 'cd /data/iiot-platform/cloud/deploy && { test -w certs || echo certs-not-writable; for f in releases/current-release.env releases/previous-release.env releases/staged-release.env releases/current-release.summary.md; do [ ! -e \"$f\" ] || test -w \"$f\" || echo not-writable:$f; done; }'`
  - `git diff --check -- deploy/scripts/pre-deploy-check.sh deploy/scripts/check-release-state-access.sh deploy/README.md deploy/OPERATIONS.md deploy/RUNNER.md docs/云端规则.md docs/云端安全部署契约.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`sh -n` 通过，说明 Cloud 新增的 `check-release-state-access.sh` 和 preflight 改动没有语法问题；规则检索命中新的双层口径、`github-runner` 和 release-state 门禁。远端只读 SSH 验证确认当前现场正是本轮要拦的回退形态：`certs/` 为 `root:10001 750`，`releases/current-release.env`、`previous-release.env`、`staged-release.env`、`current-release.summary.md` 为 `root:root 600`，`github-runner` 对这些路径均不可写。`git diff --check` 通过。当前结论是：仓库门禁已补齐，真实服务器 owner 仍需后续按标准恢复命令修正。

## 2026-07-09 Cloud Edge 发布上传默认限速提高到 1000 Mbps

- 改动范围：仅修改 `IIoT.CloudPlatform`。将 `EdgeReleaseUploadOptions`、`IIoT.HttpApi/appsettings.json` 和 `deploy/docker-compose.prod.yml` 中的 Cloud Edge 发布上传默认限速从 `100 Mbps` 提高到 `1000 Mbps`；同步更新 `deploy/README.md`、`deploy/EDGE_INSTALLER_GO_LIVE.md` 和本复盘中的 Cloud 口径。未修改 `IIoT.EdgeClient`、AICopilot、nginx `client_max_body_size`、上传请求频率限流、数据库 schema 或 API 路由。
- 改动原因：当前 Cloud 服务端在接收 Edge 宿主 bundle / 插件包时，默认用 `EdgeReleaseUpload:MaxUploadMbps=100` 对流复制做限速，日常内网发布等待时间过长；现场要求把服务端默认上限提升到 `1000 Mbps`。
- 影响面：Cloud 服务端在未显式配置 `EDGE_RELEASE_UPLOAD_MAX_MBPS` 时，默认按 `1000 Mbps` 处理 Edge 发布上传；如果服务器 `.env` 显式设置了 `EDGE_RELEASE_UPLOAD_MAX_MBPS`，则仍以显式配置为准。该口径已在 2026-07-09 同日的跨项目部署收口中同步到 `IIoT.EdgeClient` 发起端脚本和部署文档，当前标准端到端默认值已统一为 `1000 Mbps`。
- 规则提炼：无新增长期规则，原因是本次只是调整现有限速默认值，没有改变发布链路、权限边界、HTTP-only 补偿控制或部署红线。
- 验证命令：
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~ClientReleaseBehaviorTests.EdgeReleaseUploadOptions_ShouldDefaultToControlledLargeBundleUpload" --no-restore --disable-build-servers`
  - `rg -n "MaxUploadMbps.: 1000|EDGE_RELEASE_UPLOAD_MAX_MBPS:-1000|Cloud 服务端默认限速 \`1000 Mbps\`|-UploadRateLimitMbps 1000|Assert.Equal\\(1000, options.MaxUploadMbps\\)" src/hosts/IIoT.HttpApi/appsettings.json src/services/IIoT.ProductionService/ClientReleases/EdgeReleaseUploadOptions.cs deploy/docker-compose.prod.yml deploy/README.md deploy/EDGE_INSTALLER_GO_LIVE.md src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs docs/改动复盘与规则沉淀.md`
  - `git diff --check -- src/services/IIoT.ProductionService/ClientReleases/EdgeReleaseUploadOptions.cs src/hosts/IIoT.HttpApi/appsettings.json deploy/docker-compose.prod.yml deploy/README.md deploy/EDGE_INSTALLER_GO_LIVE.md src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs docs/改动复盘与规则沉淀.md`
- 验证结果：待执行。本轮需先运行定向测试、检索和 diff whitespace 检查，再决定是否进入服务器 `.env` 覆盖值调整或后续 EdgeClient 同步。

- 改动范围：更新工作区 `docs/上传部署总览.md` 中 Cloud/AICopilot 的日常发布示例和说明，统一改成专用部署用户 `github-runner@10.98.90.154`；补充 Cloud root 应急变量 `ALLOW_ROOT_SSH_DEPLOY=emergency` 与 AICopilot root 应急变量 `ALLOW_ROOT_SSH_DEPLOY=true` 的差异说明。同时在当前工作站补齐 `~/.ssh/config` 的 `iiot-server-runner` alias，并把现有服务器专用公钥加入远端 `github-runner` 的 `authorized_keys`，验证非 root BatchMode SSH 可用。未修改 Cloud 运行时代码、部署脚本、EdgeClient、AICopilot 或服务器业务配置。
- 改动原因：统一部署入口当时仍保留 `DEPLOY_SSH_TARGET=root@<shared-host>` 这类 root 示例，而 Cloud/AICopilot `local-release.sh` 当前都默认拒绝 root SSH。文档和脚本门禁不一致，会把后续发布人直接带到错误路径。
- 影响面：后续 Cloud/AICopilot 日常本机发布应走 `github-runner` 或等价非 root alias；root 只保留脚本约束的应急路径。当前工作站已经可以直接通过非 root SSH 验证服务器部署目录，不需要再把 root 作为默认发布账号。
- 规则提炼：无新增长期规则，原因是“日常部署必须使用专用非 root 用户、root 只允许应急”已经写入工作区和项目现有规则；本次只是清理统一入口中的旧示例，并补齐当前工作站的非 root SSH 通道。
- 验证命令：
  - `ssh -o BatchMode=yes -i "$HOME/.ssh/iiot_server_root_ed25519" github-runner@10.98.90.154 'printf ssh-ok'`
  - `ssh -o BatchMode=yes iiot-server-runner 'printf ssh-ok'`
  - `ssh -o BatchMode=yes iiot-server-runner 'cd /data/iiot-platform/cloud/deploy && docker compose --env-file .env -f docker-compose.prod.yml config -q && echo deploy-config-ok'`
  - `! rg -n "DEPLOY_SSH_TARGET=root@10\\.98\\.90\\.154" ../docs/上传部署总览.md`
  - `rg -n "github-runner@10\\.98\\.90\\.154|ALLOW_ROOT_SSH_DEPLOY=emergency|ALLOW_ROOT_SSH_DEPLOY=true" ../docs/上传部署总览.md docs/改动复盘与规则沉淀.md`
  - `! rg -n '[[:blank:]]$' ../docs/上传部署总览.md docs/改动复盘与规则沉淀.md`
- 验证结果：通过。`github-runner@10.98.90.154` 和本机 alias `iiot-server-runner` 都能在 BatchMode 下直接登录；非 root 路径下 `docker compose --env-file .env -f docker-compose.prod.yml config -q` 通过，说明部署目录和 `.env` 读取权限满足日常发布要求。统一入口文档里已经没有 `DEPLOY_SSH_TARGET=root@<shared-host>` 这类可复制 root 默认示例残留，`github-runner` 与 Cloud/AI 两套 root 应急开关差异都可检索；两份文档空白扫描无命中。

## 2026-07-09 上位机状态列表性能和 RuntimeStatus 兜底补强

- 改动范围：仅修改 `IIoT.CloudPlatform`。新增 `IEdgeHostOverviewQueryService` 及 EF 读模型实现，让上位机状态列表在数据库侧完成授权设备过滤、设备字段/PLC 字段 keyword、count 和分页；`GetEdgeHostPagedListHandler` 只为当前页设备批量读取 `DeviceClientState` 和 `EdgeHostPlcRuntimeState`。Cloud 端 `EdgeHostPlcRuntimeState.ReplaceReport` 在 `RuntimeStatus` 缺省且 `IsConnected=false`、`LastError` 非空时兜底为 `Faulted`。补充 ProductionService 和 ConfigurationGuardTests 守卫，更新 `docs/云端规则.md`、`docs/Edge上传与PLC状态接口契约.md` 和本复盘。
- **客户端改动大标记：本轮没有修改 `IIoT.EdgeClient`。`RuntimeStatus` 的源头语义仍由 EdgeClient 明确上报；如果后续要调整 EdgeClient 的 PLC 状态分类、`Disconnected/Faulted/Unknown` 边界或稳定 `PlcCode` 字段，必须新开 EdgeClient 窗口单独处理，不得夹在 Cloud 修复里顺手改。**
- 改动原因：CC 审核指出上位机列表页仍先拉全部授权设备、全部客户端状态和全部 PLC 状态，再在内存里 keyword 过滤和分页，设备量上来后存在慢查询和内存风险；同时 Cloud 对缺省 `RuntimeStatus` 只按 `IsConnected` 推断，会把带错误的连接失败误归为普通断开。
- 影响面：Human `/api/v1/human/edge-hosts` 列表语义不变，仍以可访问 `Device` 为主数据源并合并客户端状态/PLC 状态；性能路径改为数据库 count/page，PLC 编码、上报名称、协议、地址 keyword 仍可命中设备。状态上报兼容旧 payload：明确上报 `RuntimeStatus` 时 Cloud 尊重上报值；未上报且有 `LastError` 时展示为 `Faulted`。前端 API/页面结构未改，EdgeClient/AICopilot 未改。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/Edge上传与PLC状态接口契约.md`：上位机列表 count、分页和 keyword 过滤必须在数据库侧完成，只允许为当前页设备读取状态投影；EdgeClient 应明确上报 `RuntimeStatus`，Cloud 只做缺省兜底。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~EdgeHostBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~InfrastructureBehaviorTests|FullyQualifiedName~ApplicationFlowGuardTests|FullyQualifiedName~AuthorizationAndIdentityBehaviorTests|FullyQualifiedName~AggregateBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers`
  - `cd src/ui/iiot-web && npm run build`
  - `cd src/ui/iiot-web && npm run test:unit -- edge-hosts`
- 验证结果：通过。后端 build 0 warning/0 error；`EdgeHostBehaviorTests` 9/9 通过，新增覆盖 PLC keyword 命中、只加载当前页状态、Cloud 缺省状态兜底为 `Faulted`；ServiceLayer 定向守卫 160/160 通过；EndToEnd 配置和限流守卫 79/79 通过，新增源码守卫防止回退到全量 `DevicePagedSpec(0, 0...)` 内存分页；前端 `npm run build` 通过，保留既有 `@vueuse/core` PURE annotation Rollup warning；edge-hosts 前端单测 5/5 通过。

## 2026-07-09 上位机与 PLC 状态语义收口

- 改动范围：Cloud 后端删除旧 `EdgeHost` 手工配置聚合、PLC 绑定写命令、MigrationWorkApp `EdgeHostSeeds` 和 Human 写接口；`edge_host_plc_runtime_states` 改为 `DeviceId + ClientCode + PlcCode` 投影；新增迁移清理旧表、旧外键和重复投影；设备删除影响、权限清单、角色模板、查询 DTO、Controller、前端 `/edge-hosts` 页面和测试同步改为只读状态展示；更新 `docs/云端规则.md`、`docs/Edge上传与PLC状态接口契约.md`、`docs/云端架构治理清单.md` 和本复盘。未修改 `IIoT.EdgeClient`、`AICopilot`、部署脚本或工作区顶层规则。
- 改动原因：现场语义中客户端实际就是上位机，Cloud 之前把“设备客户端状态”和“上位机 PLC 绑定”拆成两套对象，并提供新增、编辑、删除、启用、禁用 PLC 的入口，容易让交付误解 Cloud 可以控制 PLC。实际边界应该是：Cloud 只注册设备身份、展示 Edge 客户端运行状态和客户端上报的 PLC 清单/状态，不维护现场 PLC 主配置。
- 影响面：Human `/api/v1/human/edge-hosts` 只保留列表、详情和 `{deviceId}/plc-runtime-states` 读取；列表以当前人员可访问 `Device` 为主数据源，左连 `DeviceClientState` 与 PLC runtime state，不再以旧 `edge_hosts` 表为基准。Edge 上报 `POST /api/v1/edge/edge-hosts/plc-runtime-states` 仍是唯一写入口，只校验 `DeviceId + ClientCode` 身份链，不再要求旧 `EdgeHost` 配置记录存在。`EdgeHost.Manage`、旧 Human 写 API、旧 PLC 绑定 DTO/前端表单和产能绑定汇总入口已移除。设备删除只统计和清理 `EdgeHostPlcRuntimeState`，不再依赖旧配置表级联。
- 数据库迁移策略：新增迁移先按 `DeviceId + UPPER(ClientCode) + UPPER(PlcCode)` 去重并保留最近上报记录，再统一大小写，随后删除 runtime state 上的 `EdgeHostId` / `PlcBindingId` 外键和列，最后 drop 废弃的 `edge_host_plc_bindings` 与 `edge_hosts`。Down 只重建空旧表和兼容列，不恢复已废弃配置数据。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/Edge上传与PLC状态接口契约.md`，并同步 `docs/云端架构治理清单.md`：Cloud 上的“上位机”是设备身份 + Edge 客户端运行状态 + 客户端上报 PLC 投影；Cloud 不得恢复上位机/PLC 新增、修改、删除、启用、禁用入口；`EdgeHost.Manage` 不得恢复；runtime state 不得依赖 `EdgeHostId` 或 `PlcBindingId`。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~EdgeHostBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~InfrastructureBehaviorTests|FullyQualifiedName~ApplicationFlowGuardTests|FullyQualifiedName~AuthorizationAndIdentityBehaviorTests|FullyQualifiedName~AggregateBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers`
  - `cd src/ui/iiot-web && npm run build`
  - `cd src/ui/iiot-web && npm run test:unit -- edge-hosts`
  - `rg -n 'EdgeHost\.Manage|上位机 PLC 绑定|新增上位机|禁用上位机|启用上位机|EdgeHostSeeds|EdgeHostByDeviceIdentitySpec|PlcBindingId|EdgeHostPlcBinding|edge_host_plc_bindings|edge_hosts' src docs/云端规则.md docs/Edge上传与PLC状态接口契约.md docs/云端架构治理清单.md -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260702072655_AddEdgeHosts.cs' -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260702072655_AddEdgeHosts.Designer.cs' -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260703005532_AddEdgeHostPlcRuntimeStates.cs' -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260703005532_AddEdgeHostPlcRuntimeStates.Designer.cs' -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260709021000_AlignEdgeHostPlcRuntimeStateWithClientProjection.cs' -g '!src/infrastructure/IIoT.EntityFrameworkCore/Migrations/IIoTDbContextModelSnapshot.cs'`
- 验证结果：通过。后端 build 0 warning/0 error；`EdgeHostBehaviorTests` 7/7 通过；ServiceLayer 定向守卫 160/160 通过；EndToEnd 配置与限流守卫 79/79 通过；前端 `npm run build` 通过，保留既有 `@vueuse/core` PURE annotation Rollup warning；edge-hosts 前端单测 5/5 通过。残留检索只剩规则文档中的“不得恢复/废弃表”说明和测试反向断言，运行时代码和前端没有旧写入口。

## 2026-07-08 Edge 下载链路部署硬门禁和 Published 前校验

- 改动范围：`deploy/scripts/deploy-release.sh`、`deploy/scripts/post-deploy-check.sh`、`src/services/IIoT.ProductionService/ClientReleases/EdgeReleasePublishedFilePermissions.cs`、`src/services/IIoT.ProductionService/Commands/Human/ClientReleases/PublishEdgeReleaseBundle.cs`、`src/services/IIoT.ProductionService/Commands/Human/ClientReleases/PublishEdgePluginPackage.cs`、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs`、`docs/云端规则.md`、工作区 `../docs/上传部署总览.md`、工作区 `../docs/总规则.md` 和本复盘。未修改 EdgeClient、AICopilot、HTTPS/TLS/443/HSTS、数据库 schema、API 路由或真实生产 `.env`。
- 改动原因：复盘生产部署耗时过长时确认，Cloud/Edge 下载链路验收脚本早已存在，但部署入口没有强制调用，导致 `nginx` 权限、Velopack `RELEASES`、`.nupkg` 静态下载等问题被串行手工暴露。只在文档中写“必须跑”仍然属于假门禁；需要让部署脚本和服务端发布状态都 fail-closed。
- 影响面：`deploy-release.sh` 选中 `iiot-httpapi`、`iiot-gateway`、`iiot-web` 或全量发布时，自动向 `post-deploy-check.sh` 注入 `POST_DEPLOY_REQUIRE_EDGE_INSTALLER_CATALOG=1` 和 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1`；`post-deploy-check.sh` 在 required 场景看到 `post_deploy_edge_installer_catalog=skipped` 会直接非零退出。宿主 bundle 和独立插件包在写入 `Published` 前，服务端会校验 installer artifact、安装器、Velopack `RELEASES`、channel manifests、当前版本 `.nupkg`、插件 zip 均位于受控 `edge-updates`、非空、无符号链接/重解析点且 nginx/static 可读；当前版本 `.nupkg` 还必须被 Velopack manifests 引用。失败时不写 DB Published，并回滚已复制文件。
- 规则提炼：形成长期部署和发布规则，已写入 `docs/云端规则.md`、工作区 `../docs/上传部署总览.md` 和工作区 `../docs/总规则.md`：涉及 Cloud 下载中心的发布必须强制 Edge catalog/Velopack 验收，`skipped` 等同失败；Cloud Published release API 必须在服务端校验静态文件链路后才能写入 `Published`。
- 验证命令：
  - `sh -n deploy/scripts/deploy-release.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~ConfigurationGuardTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~ClientReleaseBehaviorTests" -p:BuildInParallel=false --disable-build-servers`
  - `rg -n "POST_DEPLOY_REQUIRE_EDGE_INSTALLER_CATALOG|post_deploy_edge_installer_catalog=skipped required=1|拒绝置为 Published|Edge catalog/Velopack|Published 前" deploy/scripts src/services src/tests docs ../docs`
  - `git diff --check -- deploy/scripts/deploy-release.sh deploy/scripts/post-deploy-check.sh src/services/IIoT.ProductionService/ClientReleases/EdgeReleasePublishedFilePermissions.cs src/services/IIoT.ProductionService/Commands/Human/ClientReleases/PublishEdgeReleaseBundle.cs src/services/IIoT.ProductionService/Commands/Human/ClientReleases/PublishEdgePluginPackage.cs src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs docs/云端规则.md docs/改动复盘与规则沉淀.md`
  - `cd .. && if rg -n '[[:blank:]]$' docs/上传部署总览.md docs/总规则.md; then exit 1; fi`
- 验证结果：通过。脚本语法检查通过；`ConfigurationGuardTests` 82/82 通过；`ClientReleaseBehaviorTests` 28/28 通过，新增坏 Velopack `.nupkg` 行为测试确认服务端会在 DB `Published` 前失败并回滚；Cloud 子仓库 `git diff --check` 通过；工作区顶层 `docs/上传部署总览.md` 和 `docs/总规则.md` 空白扫描通过；部署硬门禁、required skipped 失败、Published 前服务端校验和规则沉淀关键词均可检索。

## 2026-07-08 Edge 发布上传和静态下载闭环修复

- 改动范围：`src/core/IIoT.Core.Production/Aggregates/ClientReleases/ClientReleaseComponent.cs`、`src/core/IIoT.Core.Production/Specifications/ClientReleases/ClientReleaseComponentSpecs.cs`、`src/services/IIoT.ProductionService/Commands/Human/ClientReleases/PublishEdgeReleaseBundle.cs`、`src/infrastructure/IIoT.EntityFrameworkCore/Configuration/Production/ClientReleaseComponentConfiguration.cs`、`src/tests/IIoT.ProductionService.Tests/ClientReleaseBehaviorTests.cs`、`docs/云端规则.md` 和本复盘。未修改客户端发布 API 路由、DTO、上传包格式、数据库 schema、EdgeClient 包内容、AICopilot 或 HTTPS/TLS/443/HSTS。
- 改动原因：生产清空重部署已导回客户端更新历史，复用 EdgeClient `1.0.25` 发布包上传 Cloud 时先返回 `DbUpdateConcurrencyException`，上传前置文件回滚且数据库未生成 `1.0.25`。修复上传后继续验收发现静态 `/edge-updates/...` 全部 404，实际根因是 `edge-updates` 目录由 HttpApi 容器用户创建为 `750`，nginx gateway 用户不可遍历；同时 Edge 打包输出为 `RELEASES-stable`，Cloud 验收和 Velopack 更新源要求 channel 目录下存在标准 `RELEASES`。
- 影响面：宿主和插件组件元数据只有实际变化才更新；追加新的宿主/插件版本只插入 `ClientReleaseVersion` 和 `ClientReleaseArtifact`，不更新父组件 `UpdatedAtUtc`；上传包路径先用无跟踪存在性查询判断版本是否已发布，再只加载组件根，避免把导入的历史 `Versions.Artifacts` 全量挂入当前 SaveChanges。发布聚合三个实体的 Id 由领域构造函数显式生成，EF 映射改为 `ValueGeneratedNever()`，避免新建 `ClientReleaseArtifact` 因非默认 Guid 被误判为 `Modified`。宿主 bundle 上传和独立插件上传落盘后统一收敛目录/文件权限为 nginx 可读、不可 world-writable；bundle 上传如果只提供 `RELEASES-{channel}`，Cloud 归一复制为 `RELEASES`。覆盖已有版本、组件元数据实际变化、状态变更、归档和删除仍会更新组件时间。发布 catalog、首装生成、保留策略和受控文件清单契约不变。
- 规则提炼：形成长期发布规则，已写入 `docs/云端规则.md`：客户端发布追加新版本不得顺手更新未变化的 `ClientReleaseComponent` 元数据或 `UpdatedAtUtc`；只有元数据变化、已有版本更新、状态变更、归档或删除才允许触碰组件更新时间；上传发布文件必须由服务端设置 nginx gateway 可读权限；Velopack channel 目录必须存在标准 `RELEASES`。
- 验证命令：
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter "FullyQualifiedName~ClientReleaseBehaviorTests" -p:BuildInParallel=false --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~InfrastructureBehaviorTests" -p:BuildInParallel=false --disable-build-servers`
  - `ssh <deploy-user>@<cloud-host> 'find /data/iiot-platform/edge-client/edge-updates -type d -exec chmod 755 {} + && find /data/iiot-platform/edge-client/edge-updates -type f -exec chmod 644 {} +'`
  - `ssh <deploy-user>@<cloud-host> 'cp /data/iiot-platform/edge-client/edge-updates/velopack/stable/RELEASES-stable /data/iiot-platform/edge-client/edge-updates/velopack/stable/RELEASES && chmod 644 /data/iiot-platform/edge-client/edge-updates/velopack/stable/RELEASES'`
  - `ssh <deploy-user>@<cloud-host> 'for url in http://127.0.0.1:81/edge-updates/installers/stable/1.0.25/installer-artifact.json http://127.0.0.1:81/edge-updates/installers/stable/1.0.25/IIoT.Edge.Setup.exe http://127.0.0.1:81/edge-updates/velopack/stable/RELEASES http://127.0.0.1:81/edge-updates/velopack/stable/releases.stable.json http://127.0.0.1:81/edge-updates/velopack/stable/assets.stable.json http://127.0.0.1:81/edge-updates/velopack/stable/IIoT.EdgeClient-1.0.25-stable-full.nupkg; do code=$(curl -sS -o /dev/null -w "%{http_code}" "$url"); echo "$code $url"; done'`
  - `dotnet publish src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj -c Release -o /tmp/iiot-httpapi-publish-5caf241 /p:UseAppHost=false --no-restore --disable-build-servers`
  - `docker buildx build --platform linux/amd64 --push --file /tmp/iiot-httpapi-runtime.*/Dockerfile --tag <harbor-registry>/iiot/iiot-httpapi:sha-5caf241 /tmp/iiot-httpapi-runtime.*`
  - `ssh <deploy-user>@<cloud-host> "cd /data/iiot-platform/cloud/deploy && ALLOW_ROOT_DEPLOY_PREFLIGHT=emergency DEPLOY_GIT_SHA=5caf24138a88328a8e00f461d921a31c05a29032 DEPLOY_TRIGGERED_BY=local ./scripts/deploy-release.sh sha-5caf241 --services httpapi"`
  - `pwsh -NoProfile -Command '<Resolve-EdgeReleaseCloudToken 后查询 /api/v1/human/client-releases/catalog?channel=stable&targetRuntime=win-x64&includeArchived=true>'`
- 验证结果：通过。`ClientReleaseBehaviorTests` 27/27 通过，新增断言覆盖宿主追加版本不 touch、已有版本更新 touch、插件元数据无变化和追加版本不 touch、插件元数据变化和已有版本更新 touch、发布文件 nginx 可读权限和 `RELEASES-{channel}` 归一到 `RELEASES`；`InfrastructureBehaviorTests` 41/41 通过。生产 EdgeClient `1.0.25` 上传成功，Cloud catalog 返回 `1.0.25` 且历史版本保留，状态为 `Published`，downloadUrl 为 `/edge-updates/installers/stable/1.0.25/installer-artifact.json`；服务器静态下载验收 6 个 URL 均返回 `200`。HttpApi 已部署到现场 Harbor `iiot/iiot-httpapi:sha-5caf241`，容器 `deploy-iiot-httpapi-1` 为 `healthy`，`/internal/healthz` 返回 `200`。标准 `build-and-push.sh --services httpapi` 在 Docker build 的 `dotnet publish` 阶段触发 900 秒超时；已用本地 `dotnet publish` 产物按原 Dockerfile final stage 等价构建 runtime 镜像并推送。部署脚本返回码为 1 的剩余原因是 Harbor retention/GC 缺凭据，运行态和 post-deploy 检查已通过。

## 2026-07-08 Cloud nginx-gateway 临时目录非 root 修复

- 改动范围：`deploy/nginx/nginx.conf` 和本复盘。未修改 Cloud 业务代码、数据库 schema、API 契约、Web 静态资源、HTTPS/TLS/443/HSTS、EdgeClient 或 AICopilot。
- 改动原因：继续生产清空重部署时，`iiot-web` 已正常启动，但外层 `nginx-gateway` 以非 root 用户启动失败，日志显示 `mkdir() "/tmp/nginx/client_temp" failed (2: No such file or directory)`。官方 nginx 镜像可在 `/tmp` 下创建一级临时目录，但不会自动创建中间目录 `/tmp/nginx`。
- 影响面：外层 nginx 的 client/proxy/fastcgi/uwsgi/scgi 临时目录改为 `/tmp/client_temp`、`/tmp/proxy_temp`、`/tmp/fastcgi_temp`、`/tmp/uwsgi_temp`、`/tmp/scgi_temp`；仍保持 pid 位于 `/tmp/nginx.pid`、容器内监听 `8080`、HTTP-only 基线和现有反代/静态下载规则。
- 规则提炼：无新增长期规则，原因是 Cloud nginx 非 root、临时目录收敛到 `/tmp` 已属于 `docs/云端容器非Root权限契约.md` 的既有要求；本次是外层 nginx 配置的具体路径修复。
- 验证命令：
  - `git diff --check -- deploy/nginx/nginx.conf docs/改动复盘与规则沉淀.md`
  - `docker run --rm --user 101:101 -v "$PWD/deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro" <harbor-registry>/mirror/nginx:1.27-alpine nginx -t`
  - `scp deploy/nginx/nginx.conf <deploy-user>@<cloud-host>:/data/iiot-platform/cloud/deploy/nginx/nginx.conf`
- 验证结果：待执行。需在服务器重启 `nginx-gateway`、`/internal/healthz` 和 Cloud post-deploy 通过后补充结果。

## 2026-07-08 Cloud Web nginx pid 多空格格式修复

- 改动范围：`src/hosts/IIoT.AppHost/iiot-web.Dockerfile` 和本复盘。未修改 Cloud 业务代码、数据库 schema、API 契约、前端静态构建、HTTPS/TLS/443/HSTS、EdgeClient 或 AICopilot。
- 改动原因：继续生产清空重部署时，`iiot-web` 仍以非 root 用户重启失败，镜像内 `nginx.conf` 实际为 `pid        /run/nginx.pid;`，中间包含多个空格；上一轮固定字符串替换没有命中，导致 nginx 仍尝试写 `/run/nginx.pid`。
- 影响面：Web 镜像构建时用 sed 正则匹配 `pid` 后任意空白，将 `/var/run/nginx.pid` 和 `/run/nginx.pid` 都收敛到 `/tmp/nginx.pid`；不改变 Web 静态资源、反代路径、外层 nginx-gateway 或端口映射。
- 规则提炼：无新增长期规则，原因是“非 root nginx 的 pid/temp 目录必须收敛到 `/tmp`”已经写入 `docs/云端容器非Root权限契约.md`；本次是匹配实现补强。
- 验证命令：
  - `printf 'pid        /run/nginx.pid;\\npid /var/run/nginx.pid;\\n' | sed -e 's#^pid[[:space:]][[:space:]]*/var/run/nginx.pid;#pid /tmp/nginx.pid;#' -e 's#^pid[[:space:]][[:space:]]*/run/nginx.pid;#pid /tmp/nginx.pid;#'`
  - `git diff --check -- src/hosts/IIoT.AppHost/iiot-web.Dockerfile docs/改动复盘与规则沉淀.md`
  - `TAG=sha-<new-cloud-sha> REGISTRY=<harbor-registry> HARBOR_PROJECT=iiot VITE_AICOPILOT_CHALLENGE_URL=http://<ai-host>:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --services web`
- 验证结果：待执行。需在新 Web 镜像推送、服务器重启和 Cloud post-deploy 验收后补充结果。

## 2026-07-08 Cloud 后端镜像 publish 禁止重复 restore

- 改动范围：`src/hosts/IIoT.HttpApi/Dockerfile`、`src/hosts/IIoT.Gateway/Dockerfile`、`src/hosts/IIoT.DataWorker/Dockerfile`、`src/hosts/IIoT.MigrationWorkApp/Dockerfile` 和本复盘。未修改 Cloud 业务代码、数据库 schema、API 契约、前端、HTTPS/TLS/443/HSTS、EdgeClient 或 AICopilot。
- 改动原因：继续生产清空重部署时，`iiot-gateway` 镜像在 `dotnet publish` 阶段超过脚本 900 秒超时线。四个后端 Dockerfile 已在前一层执行 `dotnet restore`，但 publish 阶段仍默认再次 restore，引入重复网络依赖和超时风险。
- 影响面：四个后端镜像仍先显式 restore，再用 `dotnet publish --no-restore` 发布，镜像运行时入口、非 root 用户、healthcheck、端口和业务输出不变；构建链路减少重复 restore，提高生产 build/push 可预测性。
- 规则提炼：无新增长期规则，原因是生产构建必须 fail-fast、不得被重复网络 restore 长时间卡住已属于 `docs/上传部署总览.md` 的发布治理口径；本次是 Dockerfile 落地修复。
- 验证命令：
  - `git diff --check -- src/hosts/IIoT.HttpApi/Dockerfile src/hosts/IIoT.Gateway/Dockerfile src/hosts/IIoT.DataWorker/Dockerfile src/hosts/IIoT.MigrationWorkApp/Dockerfile docs/改动复盘与规则沉淀.md`
  - `rg -n "dotnet publish .*--no-restore" src/hosts -g 'Dockerfile'`
  - `TAG=sha-<new-cloud-sha> REGISTRY=<harbor-registry> HARBOR_PROJECT=iiot VITE_AICOPILOT_CHALLENGE_URL=http://<ai-host>:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --all`
- 验证结果：待执行。需在本轮 Cloud 全量镜像构建推送和生产部署完成后补充结果。

## 2026-07-07 Cloud Web 非 root nginx pid 路径修复

- 改动范围：`src/hosts/IIoT.AppHost/iiot-web.Dockerfile` 和本复盘。未修改 Cloud 业务代码、数据库 schema、API 契约、HTTPS/TLS/443/HSTS、EdgeClient 或 AICopilot。
- 改动原因：生产清空重部署时 `iiot-web` 容器以 `USER 101:101` 启动后反复重启，日志显示 nginx 仍尝试写 `/run/nginx.pid` 并因权限不足失败。现有 Dockerfile 只替换 `/var/run/nginx.pid`，没有覆盖当前 `nginx:1.27-alpine` 基础镜像的 `/run/nginx.pid` 默认值。
- 影响面：Cloud Web 镜像构建时同时收敛 `/var/run/nginx.pid` 和 `/run/nginx.pid` 到 `/tmp/nginx.pid`，保持非 root nginx 监听 `8080` 的既有安全基线；不改变外层 `nginx-gateway`、端口映射、Cloud API、数据库或前端静态资源内容。
- 规则提炼：无新增长期规则，原因是“Cloud nginx 容器非 root、监听 8080、pid/temp 目录收敛到 `/tmp`”已写入 `docs/云端容器非Root权限契约.md` 和 `docs/云端规则.md`；本次是对基础镜像实际 pid 路径差异的代码补齐。
- 验证命令：
  - `git diff --check -- src/hosts/IIoT.AppHost/iiot-web.Dockerfile docs/改动复盘与规则沉淀.md`
  - `TAG=sha-7049f6bd541bdbfde3427cb9e899937acbf3ca80 REGISTRY=<harbor-registry> HARBOR_PROJECT=iiot VITE_AICOPILOT_CHALLENGE_URL=http://<ai-host>:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --all`
  - `ALLOW_ROOT_DEPLOY_PREFLIGHT=emergency DEPLOY_GIT_SHA=7049f6bd541bdbfde3427cb9e899937acbf3ca80 DEPLOY_TRIGGERED_BY=local ./scripts/deploy-release.sh sha-7049f6bd541bdbfde3427cb9e899937acbf3ca80`
- 验证结果：待执行。需在本轮生产重部署完成后补充 `iiot-web` 容器健康、`/internal/healthz` 和 post-deploy 结果。

## 2026-07-07 生产清空重部署和 JWT secret 运行时下限补齐

- 改动范围：`src/infrastructure/IIoT.Infrastructure/Authentication/JwtSettings.cs`、`src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs`、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`deploy/scripts/export-client-release-history.sh`、`deploy/scripts/import-client-release-history.sh`、`docs/三项目生产清空重部署对齐手册.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本复盘。未修改 HTTPS/TLS/443/HSTS，未写入真实密码、真实数据库连接串或真实模型内网地址。
- 改动原因：按 CC 补充意见补齐 G2/G3/G4，并按用户生产清空重部署要求明确“唯一保留客户端更新历史”。此前 deploy preflight 已检查 `JWTSETTINGS__SECRET` 长度，但 `JwtSettings.Validate()` 运行时没有下限；生产全清空后如果不单独导出 release history，Cloud 下载中心/客户端发布管理历史版本会丢失。
- 影响面：Cloud 已配置 JWT secret 低于 32 字符时运行时直接失败；开发环境缺失 secret 仍由 `JwtSecretResolver` 生成临时值。新增 release history 导出/导入脚本只覆盖 `edge_client_release_components`、`edge_client_release_versions`、`edge_client_release_artifacts`、`edge_client_release_retention_policies`；其它业务数据、AI 数据和消息队列数据默认不恢复。HMAC 对称 JWT 风险已挂到后续 `RS256/JWKS` 专项，不在本轮伪装为完成。
- 规则提炼：形成长期部署/安全规则，已写入 `docs/云端规则.md`、`docs/云端架构治理清单.md`、`docs/三项目生产清空重部署对齐手册.md`，并同步工作区 `../docs/总规则.md`：生产清空重部署只默认保留客户端更新历史元数据；真实 secret 只进服务器 `.env`、密钥系统或本机私密手册；`JwtSettings:Secret` 已配置时运行时下限 32 字符；`RS256/JWKS` 作为后续专项持续跟踪。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --filter "FullyQualifiedName~InfrastructureBehaviorTests.JwtSettings" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~ConfigurationGuardTests.ProductionRedeployGuide" --no-restore --disable-build-servers`
  - `sh -n deploy/scripts/export-client-release-history.sh && sh -n deploy/scripts/import-client-release-history.sh`
  - `git diff --check`
  - `rg -n "AICOPILOT_PRIVATE_MODEL_CONTEXT_TOKENS|RS256/JWKS|客户端更新历史|JwtSettings.Validate\\(\\)|ProductionRedeployGuide" IIoT.CloudPlatform -g '!**/bin/**' -g '!**/obj/**'`
- 验证结果：通过。`InfrastructureBehaviorTests.JwtSettings` 2/2 通过；`ProductionRedeployGuide` 1/1 通过；release history 导出/导入脚本 `sh -n` 通过；`git diff --check` 通过；关键文档、测试和治理清单检索均有命中。本轮未连接真实生产 PostgreSQL 执行导出/导入，也未执行真实服务器清空、正式部署或线上下载中心 UI 验收；这些必须在生产窗口按手册执行。

## 2026-07-07 云端 Harbor project/namespace 单段门禁收口

- 改动范围：`deploy/scripts/build-and-push.sh`、`deploy/scripts/mirror-third-party-images.sh`、`deploy/scripts/harbor-retention.sh`、`.github/workflows/cloud-image.yml`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时代码、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-04/C-17 镜像发布门禁时发现，registry 已要求显式 Harbor/内部 host，但 `HARBOR_PROJECT`、`MIRROR_NAMESPACE`、灾备 `OCI_NAMESPACE` 和 Harbor retention project 仍可能被配置成包含斜杠、空白、反斜杠或 `.example` 模板值，最终拼进镜像路径或 Harbor retention API。这个口径和“生产镜像必须进入明确 Harbor project/namespace”的要求不一致。
- 影响面：本机构建、基础镜像 mirror、灾备 `cloud-image` 和 Harbor retention 现在都要求 project/namespace 为单段小写值，只允许小写字母、数字、`.`、`_`、`-`；`iiot/prod`、`mirror/example`、模板域、空白和反斜杠都会 fail-fast。backend-only 构建仍不要求 AICopilot challenge URL，HTTP-only 基线不变。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Harbor registry 必须显式，project/namespace 必须是单段小写 Harbor project/namespace，不能用斜杠、空白、反斜杠或模板域。
- 验证命令：
  - `bash -n deploy/scripts/build-and-push.sh && sh -n deploy/scripts/mirror-third-party-images.sh && bash -n deploy/scripts/harbor-retention.sh`
  - `REGISTRY=harbor.local:5000 HARBOR_PROJECT=iiot deploy/scripts/build-and-push.sh --services httpapi --dry-run`
  - `if REGISTRY=harbor.local:5000 HARBOR_PROJECT='iiot/prod' deploy/scripts/build-and-push.sh --services httpapi --dry-run >/tmp/iiot-build-bad-project.out 2>&1; then cat /tmp/iiot-build-bad-project.out; exit 1; fi; rg -n "HARBOR_PROJECT must be a single Harbor project segment" /tmp/iiot-build-bad-project.out`
  - `if MIRROR_REGISTRY=harbor.local:5000 MIRROR_NAMESPACE='mirror/example' deploy/scripts/mirror-third-party-images.sh >/tmp/iiot-mirror-bad-namespace.out 2>&1; then cat /tmp/iiot-mirror-bad-namespace.out; exit 1; fi; rg -n "MIRROR_NAMESPACE must be a single Harbor project/namespace segment" /tmp/iiot-mirror-bad-namespace.out`
  - `if HARBOR_URL=harbor.local:5000 HARBOR_PROJECT='iiot/prod' HARBOR_USERNAME=user HARBOR_PASSWORD=pass deploy/scripts/harbor-retention.sh --dry-run iiot-httpapi >/tmp/iiot-retention-bad-project.out 2>&1; then cat /tmp/iiot-retention-bad-project.out; exit 1; fi; rg -n "HARBOR_PROJECT/OCI_NAMESPACE must be a single Harbor project segment" /tmp/iiot-retention-bad-project.out`
  - `ruby -e 'require "yaml"; %w[.github/workflows/cloud-image.yml .github/workflows/cloud-deploy.yml .github/workflows/cloud-admin-repair.yml].each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }'`
  - `rg -n "HARBOR_PROJECT must be a single Harbor project segment|MIRROR_NAMESPACE must be a single Harbor project/namespace segment|OCI_NAMESPACE must be a single Harbor project/namespace segment|HARBOR_PROJECT/OCI_NAMESPACE must be a single Harbor project segment|单段小写" deploy/scripts .github/workflows/cloud-image.yml docs/云端规则.md docs/云端架构治理清单.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；`HARBOR_PROJECT=iiot` 的 backend-only dry-run 通过；`HARBOR_PROJECT=iiot/prod`、`MIRROR_NAMESPACE=mirror/example` 和 Harbor retention 的 `HARBOR_PROJECT=iiot/prod` 均按预期 fail-fast；三个灾备 workflow YAML 解析通过；新增 project/namespace 规则和测试守卫可检索；ConfigurationGuardTests 81/81 通过；diff whitespace 检查通过。

## 2026-07-07 云端灾备 deploy/repair registry 显式性收口

- 改动范围：`.github/workflows/cloud-deploy.yml`、`.github/workflows/cloud-admin-repair.yml`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时代码、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-04/C-17 灾备 workflow 时发现，`cloud-image` 已收紧 `OCI_REGISTRY`，但 `cloud-deploy` 和 `cloud-admin-repair` 仍只检查 `OCI_REGISTRY` 非空，随后直接 `docker login`。如果 secret 误填 `harbor.example.com` 或 `harbor`，灾备部署/修复路径仍可能绕过本机构建和 preflight 的显式 Harbor registry 门禁。
- 影响面：`cloud-deploy` 和 `cloud-admin-repair` 现在都拒绝 `.example` / `internal.example` 文档域，并要求 `OCI_REGISTRY` 至少包含 `.`、`:` 或等于 `localhost`；ConfigurationGuardTests 固化两个 workflow 的 registry 错误提示。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：所有灾备 workflow 的 `OCI_REGISTRY` 都必须是显式 Harbor/内部 registry host，不能接受文档域或短 registry。
- 验证命令：
  - `ruby -e 'require "yaml"; %w[.github/workflows/cloud-deploy.yml .github/workflows/cloud-admin-repair.yml].each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }'`
  - `rg -n "OCI_REGISTRY still uses the documentation example domain|OCI_REGISTRY must include an explicit Harbor registry host" .github/workflows/cloud-deploy.yml .github/workflows/cloud-admin-repair.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：两个 workflow YAML 解析通过；`cloud-deploy` 和 `cloud-admin-repair` 的 `OCI_REGISTRY` 文档域/显式 registry 守卫可检索；ConfigurationGuardTests 81/81 通过；diff whitespace 检查通过。

## 2026-07-07 云端灾备 cloud-image challenge 模板域拦截补齐

- 改动范围：`.github/workflows/cloud-image.yml`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时代码、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-17 灾备构建路径时发现，`cloud-image` 的 `OCI_REGISTRY` 已拒绝 `.example` / `internal.example`，但 `VITE_AICOPILOT_CHALLENGE_URL` 仍只拒绝 `internal.example`，会放过 `aicopilot.example.com` 这类文档示例域。该行为和本地 `build-and-push.sh` 的 Web challenge URL 门禁不一致。
- 影响面：`cloud-image` 的 AICopilot challenge URL 现在统一拒绝 `.example` / `internal.example`；ConfigurationGuardTests 要求 `cloud-image` 中文档示例域拦截模式至少覆盖 `OCI_REGISTRY` 和 `VITE_AICOPILOT_CHALLENGE_URL` 两处。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：灾备 `cloud-image` 的 registry 和 AICopilot challenge URL 都不得使用 `.example` / `internal.example` 文档示例域。
- 验证命令：
  - `rg -n 'VITE_AICOPILOT_CHALLENGE_URL still uses the documentation example domain|\\*\\.example\\*\\|\\*internal\\.example\\*|cloud-image must reject documentation example domains' .github/workflows/cloud-image.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：`cloud-image` 中 `OCI_REGISTRY` 和 `VITE_AICOPILOT_CHALLENGE_URL` 的 `.example` / `internal.example` 拦截模式均可检索；ConfigurationGuardTests 81/81 通过；diff whitespace 检查通过。

## 2026-07-07 云端构建 registry 显式性收口

- 改动范围：`deploy/scripts/build-and-push.sh`、`deploy/scripts/mirror-third-party-images.sh`、`.github/workflows/cloud-image.yml`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时代码、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-17 发布前门禁时发现，preflight 已要求应用/基础设施镜像带显式 Harbor registry，但本机构建入口 `build-and-push.sh`、基础镜像 mirror 入口 `mirror-third-party-images.sh` 和灾备 `cloud-image` workflow 只拒绝空值或 `.example`，仍可能接受 `REGISTRY=harbor` / `MIRROR_REGISTRY=harbor` / `OCI_REGISTRY=harbor` 这种非显式 registry。Docker 会把这类值解释成 Docker Hub namespace 或非明确 registry，和“生产镜像必须进 Harbor/内部 registry”的口径不一致。
- 影响面：`build-and-push.sh` 的 `REGISTRY`、`mirror-third-party-images.sh` 的 `MIRROR_REGISTRY` 和灾备 `cloud-image` 的 `OCI_REGISTRY` 现在必须至少包含 `.`、`:` 或等于 `localhost`；否则 fail-fast。ConfigurationGuardTests 固化本地脚本和灾备 workflow 的显式 registry 错误提示。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Cloud 本机构建、基础镜像 mirror 和灾备 `cloud-image` 的 registry 参数必须是显式 Harbor/内部 registry host，不能接受 `harbor` 这类非显式 registry 值。
- 验证命令：
  - `bash -n deploy/scripts/build-and-push.sh`
  - `sh -n deploy/scripts/mirror-third-party-images.sh`
  - `REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services httpapi --dry-run`
  - `if REGISTRY=harbor deploy/scripts/build-and-push.sh --services httpapi --dry-run >/tmp/iiot-build-bad-registry.out 2>&1; then cat /tmp/iiot-build-bad-registry.out; exit 1; fi; rg -n "REGISTRY must include an explicit Harbor registry host" /tmp/iiot-build-bad-registry.out`
  - `if MIRROR_REGISTRY=harbor deploy/scripts/mirror-third-party-images.sh >/tmp/iiot-mirror-bad-registry.out 2>&1; then cat /tmp/iiot-mirror-bad-registry.out; exit 1; fi; rg -n "MIRROR_REGISTRY must include an explicit Harbor registry host" /tmp/iiot-mirror-bad-registry.out`
  - `rg -n "OCI_REGISTRY still uses the documentation example domain|OCI_REGISTRY must include an explicit Harbor registry host" .github/workflows/cloud-image.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；`cloud-image` 的 `OCI_REGISTRY` 显式 registry 守卫和测试守卫可检索；`REGISTRY=harbor` 和 `MIRROR_REGISTRY=harbor` 均按预期 fail-fast；ConfigurationGuardTests 81/81 通过；diff whitespace 检查通过。

## 2026-07-07 云端灾备 workflow `.env` 权限收口

- 改动范围：`.github/workflows/cloud-deploy.yml`、`.github/workflows/cloud-admin-repair.yml`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时代码、部署脚本行为、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-04 self-hosted runner 生产密钥治理时发现，灾备 workflow 使用 `umask 077` 后重定向写入 `$DEPLOY_TARGET_DIR/.env`，但如果服务器 `.env` 已存在，重定向会保留旧文件权限，不能证明 `DEPLOY_ENV_FILE` 以 0600 写入；这和治理清单“runner 密钥最小权限”的口径不一致。
- 影响面：`cloud-deploy` 和 `cloud-admin-repair` 在写入服务器 `.env` 后显式执行 `chmod 600 "$DEPLOY_TARGET_DIR/.env"`；ConfigurationGuardTests 固化两个灾备 workflow 都必须具备该权限收敛，并补上 `cloud-admin-repair` 的显式修复确认、非 root runner 和临时 env 0600 守卫。
- 规则提炼：形成长期部署规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：灾备 workflow 写入 `DEPLOY_ENV_FILE` 后必须显式 `chmod 600`，不得只依赖 `umask`。
- 验证命令：
  - `rg -n 'chmod 600 "\\$DEPLOY_TARGET_DIR/.env"|CloudAdminRepairWorkflow_ShouldProtectDeployEnvFileAndRequireExplicitRepair|CloudDeployWorkflow_ShouldUseReleaseTagInputAndSharedDeployScript' .github/workflows/cloud-deploy.yml .github/workflows/cloud-admin-repair.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：新增 workflow 权限收敛和测试守卫可检索；ConfigurationGuardTests 81/81 通过；diff whitespace 检查通过。

## 2026-07-07 云端部署默认目录和 cron 模板收口

- 改动范围：`deploy/.env.example`、`deploy/docker-compose.prod.yml`、`deploy/scripts/check-container-nonroot-readiness.sh`、`deploy/cron/iiot-backup.cron.example`、`deploy/cron/iiot-backup-verify.cron.example`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计生产部署和备份链路时发现，`.env.example`、compose fallback、non-root readiness fallback、backup / restore verification cron 模板仍混用旧 `/srv/iiot-cloud/deploy` 或 `/srv/iiot/edge-updates`，而当前云端标准部署目录是 `/data/iiot-platform/cloud/deploy`，Edge 更新包目录是 `/data/iiot-platform/edge-client/edge-updates`。如果运维复制旧模板，会导致证书、更新包、备份和恢复验证落到错误目录。
- 影响面：OIDC 证书目录、Edge 更新包目录和 backup / restore verification cron 模板统一到当前 `/data/iiot-platform/...` 标准；ConfigurationGuardTests 增加正向目录断言和旧 `/srv` 目录负向守卫。HTTP-only 生产基线不变，不新增证书、443、HSTS 或 HTTPS redirection。
- 规则提炼：形成长期部署模板规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Cloud 可复制运维模板和部署默认值必须使用 `/data/iiot-platform/cloud/deploy` 与 `/data/iiot-platform/edge-client/edge-updates`，不得继续使用旧 `/srv/iiot-cloud/deploy` 或 `/srv/iiot/edge-updates` 示例。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`
  - `! rg -n "/srv/iiot-cloud/deploy|/srv/iiot/edge-updates" deploy/.env.example deploy/docker-compose.prod.yml deploy/scripts/check-container-nonroot-readiness.sh deploy/cron`
  - `rg -n "/data/iiot-platform/cloud/deploy|/data/iiot-platform/edge-client/edge-updates" deploy/.env.example deploy/docker-compose.prod.yml deploy/scripts/check-container-nonroot-readiness.sh deploy/cron src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：ConfigurationGuardTests 80/80 通过；`sh -n` 和 `docker compose config -q` 通过；模板/脚本文件中旧 `/srv/iiot-cloud/deploy`、`/srv/iiot/edge-updates` 已无残留；当前标准目录和测试守卫可检索；diff whitespace 检查通过。C-06 仍需生产真实启动和 post-deploy 证据后才能关闭。

## 2026-07-07 云端 OPERATIONS 本机端口示例收口

- 改动范围：`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时代码、部署脚本行为、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计发布验收文档时发现，`OPERATIONS.md` 的可复制 curl 示例写死 `http://127.0.0.1:80/...`，但 `.env.example` 和 ConfigurationGuardTests 明确生产入口 `GATEWAY_HTTP_PORT=81`，部署脚本也使用 `http://127.0.0.1:${GATEWAY_HTTP_PORT:-80}`。文档写死 80 会误导生产验收。
- 影响面：`OPERATIONS.md` 的 healthz 和 Velopack 静态下载本机 curl 示例改为从 `.env` 读取 `GATEWAY_HTTP_PORT`，缺失时才回落 `80`。ConfigurationGuardTests 增加 `OPERATIONS.md` 不得再出现 `http://127.0.0.1:80/` 的守卫。云端规则和治理清单同步写明本机 curl 示例必须跟随 `GATEWAY_HTTP_PORT`。
- 规则提炼：形成长期运维文档规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：服务器本机 curl 示例必须读取 `.env` / `GATEWAY_HTTP_PORT`，不得写死 `127.0.0.1:80`。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `! rg -n "http://127\\.0\\.0\\.1:80/" deploy/OPERATIONS.md`
  - `rg -n "gateway_http_port=\\$\\(sed -n 's/\\^GATEWAY_HTTP_PORT=//p' \\.env|http://127\\.0\\.0\\.1:\\$\\{gateway_http_port\\}/internal/healthz|http://127\\.0\\.0\\.1:\\$\\{gateway_http_port\\}/edge-updates" deploy/OPERATIONS.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：ConfigurationGuardTests 80/80 通过；`OPERATIONS.md` 不再包含写死的 `http://127.0.0.1:80/`；端口读取示例、测试守卫、云端规则和治理清单均可检索；diff whitespace 检查通过。

## 2026-07-07 云端 pre-deploy 首部署摘要口径修正

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-17 发布 preflight 时发现，干净首部署没有 `current-release.env` 时，`pre-deploy-check.sh` 会合理跳过旧运行态 `/internal/healthz` 和 `ops-check.sh`；但 `print_http_only_preflight_summary` 固定输出 `healthz-http-local ops-check-runtime`，会把未执行的旧运行态检查说成已通过，和“按源码生成计划、前后口径一致”的要求不一致。
- 影响面：`print_http_only_preflight_summary` 改为接收运行态摘要参数；`pre-deploy-check.sh` 默认输出 `runtime-check-skipped-no-current-release`，只有存在 `current-release.env` 且 healthz/ops-check 实际执行后才输出 `healthz-http-local ops-check-runtime`。部署文档、云端规则和治理清单同步写明首部署与已有当前版本的差异。
- 规则提炼：形成长期发布规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：preflight 的 HTTP-only 补偿控制摘要必须反映实际执行过的运行态检查；不得把首部署跳过的旧运行态 healthz/ops-check 说成已通过。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "runtime-check-skipped-no-current-release|healthz-http-local ops-check-runtime|runtime_controls=\\$\\{1:-runtime-check-not-declared\\}|print_http_only_preflight_summary \"\\$runtime_preflight_controls\"" deploy/scripts/release-common.sh deploy/scripts/pre-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端架构治理清单.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：release-common 和 pre-deploy 脚本语法检查通过；ConfigurationGuardTests 80/80 通过；首部署跳过标记、已有当前版本运行态标记、摘要参数、规则文档和治理清单均可检索；diff whitespace 检查通过。

## 2026-07-07 云端复盘旧部署命令脱敏

- 改动范围：本复盘中旧批次的可复制部署命令示例，以及 ConfigurationGuardTests 的复盘命令脱敏守卫。未修改运行时代码、部署脚本、专题契约、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按 C-15 审计真实 IP 和 root 部署口径时发现，本复盘旧记录仍保留历史 `root@<现场地址>`、真实 HTTP 地址和 SSH 查询命令。它们属于历史验收记录，但 Markdown 中仍是可复制命令，和当前“真实 IP 不作为默认示例、root SSH 默认拒绝”的治理口径不够一致。
- 影响面：旧记录中的部署命令统一改为 `<legacy-root-ssh-target>` / `<cloud-private-ip>` 占位；保留“当时验证过 machine token、发布表和 dry-run”的事实，不再暴露可复制现场地址或鼓励 root SSH。新增 `RollingReview_ShouldNotKeepCopyableSiteIpDeployCommands`，只拦截复盘中可复制的真实 IP/root 部署命令，不禁止历史说明文本提到已清理过的真实 IP。
- 规则提炼：无新增长期规则，原因是 `docs/云端规则.md` 和 `docs/云端架构治理清单.md` 已经规定真实 IP 不得作为可复制默认值、root SSH 默认拒绝；本次只是清理旧复盘记录中的残留示例。
- 验证命令：
  - `! rg -n 'root@10\\.98\\.90\\.154|DEPLOY_SSH_TARGET=.*10\\.98\\.90\\.154|--ssh-target .*10\\.98\\.90\\.154|curl .*http://10\\.98\\.90\\.154|ssh root@10\\.98\\.90\\.154' docs/改动复盘与规则沉淀.md`
  - `rg -n '<legacy-root-ssh-target>|<cloud-private-ip>' docs/改动复盘与规则沉淀.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：旧复盘中可复制 root/IP 部署命令已脱敏；占位符可检索；ConfigurationGuardTests 80/80 通过并守卫复盘命令脱敏；diff whitespace 检查通过。

## 2026-07-07 云端 deploy-release nginx-gateway 启动门禁

- 改动范围：`deploy/scripts/deploy-release.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 HTTP-only 发布链路时发现，`post-deploy-check.sh` 明确要求 `nginx-gateway`、`iiot-gateway`、`iiot-httpapi`、`iiot-dataworker`、`iiot-web` 均处于 running，但 `deploy-release.sh` 全量首部署只启动五个应用服务；原 `restart_nginx_gateway_if_needed` 只在容器已经存在时重启，缺失时不会启动，导致首次发布或缺失 nginx 容器时在 post-deploy 前后口径不一致。
- 影响面：`deploy-release.sh` 的 release manifest 和应用镜像选择仍只覆盖五个应用镜像，不把基础设施 `nginx-gateway` 混入应用发布列表；当本次发布选中 `iiot-web` 或 `iiot-gateway` 时，脚本会在 post-deploy 前重启已有 `nginx-gateway` 刷新上游 DNS，或在容器缺失时执行 `compose up -d nginx-gateway` 启动 HTTP 入口。README、OPERATIONS 和治理清单同步声明该发布行为。
- 规则提炼：形成长期发布契约，已写入 `docs/云端架构治理清单.md`：post-deploy 既然要求 `nginx-gateway` running，deploy-release 在影响浏览器流量的发布中必须保证 nginx-gateway 已启动或已重启；不写入工作区总规则，原因是这是 Cloud HTTP 发布脚本契约，不是三项目通用规则。
- 验证命令：
  - `sh -n deploy/scripts/deploy-release.sh && sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n 'ensure_nginx_gateway_if_needed|compose up -d nginx-gateway|Starting nginx-gateway because the selected release affects browser traffic|启动或重启 .*nginx-gateway|start or restart .*nginx-gateway' deploy/scripts/deploy-release.sh src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs deploy/OPERATIONS.md deploy/README.md docs/云端架构治理清单.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：deploy-release 和 post-deploy 脚本语法检查通过；ConfigurationGuardTests 79/79 通过；nginx-gateway 启动/重启逻辑、测试守卫和文档口径均可检索；diff whitespace 检查通过。

## 2026-07-07 云端 pre-deploy 旧 DataWorker healthcheck 兼容

- 改动范围：`deploy/scripts/ops-check.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-06 DataWorker healthcheck 门禁时发现，`pre-deploy-check.sh` 会在更新容器前复用 `ops-check.sh` 检查当前线上版本。若当前线上 DataWorker 镜像还没有新 `--healthcheck` 支持或 compose healthcheck 尚未生效，刚加的严格 health gate 会阻断升级到修复版本，形成升级死锁。
- 影响面：`ops-check.sh` 默认仍 `REQUIRE_DATAWORKER_HEALTHCHECK=1`，日常巡检和 post-deploy 严格要求 DataWorker Docker healthcheck 定义且为 `healthy`；只有 pre-deploy 的旧当前版本检查显式传入 `REQUIRE_DATAWORKER_HEALTHCHECK=${PRE_DEPLOY_REQUIRE_DATAWORKER_HEALTHCHECK:-0}`，允许从旧 DataWorker 升级到新版本。非法 `REQUIRE_DATAWORKER_HEALTHCHECK` 值会 fail-fast。
- 规则提炼：形成长期验收口径，已写入 `docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：pre-deploy 的兼容跳过只适用于更新前旧版本检查，不能作为 C-06 关闭证据；post-deploy 和日常 ops-check 必须严格要求 DataWorker healthcheck。
- 验证命令：
  - `sh -n deploy/scripts/ops-check.sh && sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/release-common.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "REQUIRE_DATAWORKER_HEALTHCHECK|PRE_DEPLOY_REQUIRE_DATAWORKER_HEALTHCHECK|dataworker_healthcheck=skipped|旧 DataWorker|升级到修复版本|不得作为 C-06 关闭证据" deploy/scripts/ops-check.sh deploy/scripts/pre-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端架构治理清单.md docs/云端容器非Root权限契约.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；ConfigurationGuardTests 79/79 通过；`ops-check.sh` 默认严格、pre-deploy 旧版本兼容开关、post-deploy 不使用兼容开关、文档“不得作为 C-06 关闭证据”和复盘关键词均可检索；diff whitespace 检查通过。

## 2026-07-07 云端 CI 部署脚本语法门禁

- 改动范围：`.github/workflows/cloud-ci.yml`、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端架构治理清单.md` 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计完整云端治理计划时发现，`cloud-ci` 快速门禁已经运行 ConfigurationGuardTests 和 compose config，但没有执行 `sh -n` / `bash -n`，随后又确认 PowerShell 下载脚本 `InvokeEdgeInstallerPackageDownload.ps1` 也未进入 CI 语法门禁。部署脚本语法错误只能靠人工验证或生产执行时暴露，不符合 C-17 “生产发布前阻断危险配置和脚本问题”的口径。
- 影响面：`cloud-ci` 在 ConfigurationGuardTests 后新增 `Validate deploy script syntax` 步骤，覆盖所有 shell/bash 部署入口、共享库和 PowerShell 下载脚本 AST 解析；ConfigurationGuardTests 增加 workflow 断言，防止后续删除该 CI 步骤。OPERATIONS、README 和治理清单同步把部署脚本语法检查列入快速门禁。
- 规则提炼：形成长期验收口径，已写入 `docs/云端架构治理清单.md`：Cloud 快速 CI 必须包含 deploy shell/bash/PowerShell 脚本语法检查，并由 ConfigurationGuardTests 守卫 workflow。未写入工作区总规则，原因是这是 Cloud 部署 CI 门禁，不是三项目通用规则。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/ops-check.sh && sh -n deploy/scripts/check-container-nonroot-readiness.sh && sh -n deploy/scripts/deploy-release.sh && sh -n deploy/scripts/ensure-oidc-signing-cert.sh && sh -n deploy/scripts/mirror-third-party-images.sh && sh -n deploy/scripts/postgres-backup.sh && sh -n deploy/scripts/postgres-restore.sh && sh -n deploy/scripts/postgres-verify-backup.sh && sh -n deploy/scripts/rollback-release.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh && bash -n deploy/scripts/build-and-push.sh && bash -n deploy/scripts/harbor-retention.sh && bash -n deploy/scripts/local-release.sh && bash -n deploy/scripts/post-release-cleanup.sh`
  - `pwsh -NoProfile -Command '$tokens = $null; $errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile("deploy/scripts/InvokeEdgeInstallerPackageDownload.ps1", [ref]$tokens, [ref]$errors); if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Error $_ }; exit 1 }'`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "Validate deploy script syntax|sh -n deploy/scripts/release-common.sh|bash -n deploy/scripts/build-and-push.sh|pwsh -NoProfile -Command|InvokeEdgeInstallerPackageDownload.ps1|部署脚本语法检查|Cloud 快速 CI" .github/workflows/cloud-ci.yml src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs deploy/OPERATIONS.md deploy/README.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：所有 deploy shell/bash 脚本语法检查通过；PowerShell 下载脚本 AST 解析通过；ConfigurationGuardTests 79/79 通过；`cloud-ci` 的 `Validate deploy script syntax` 步骤、workflow 守卫断言、部署脚本语法检查文档和治理清单关键词均可检索；diff whitespace 检查通过。

## 2026-07-07 云端 DataWorker healthcheck 发布门禁

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/post-deploy-check.sh`、`deploy/scripts/ops-check.sh`、`deploy/OPERATIONS.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-06 生产关闭条件时确认 `iiot-dataworker` 已有 Docker healthcheck，但 post-deploy 和日常 ops-check 只显式检查容器 running 状态；初版 health gate 又会在容器缺失 healthcheck 时跳过。这会让 DataWorker 缺失 healthcheck 或处于 `starting` / `unhealthy` 时仍可能通过发布脚本的服务运行检查，和“DataWorker healthcheck 必须通过”验收口径不一致。
- 影响面：新增共享 `require_healthy_service`，对带 Docker healthcheck 的服务等待 `healthy`；如果服务缺失 Docker healthcheck，则 fail-fast。`post-deploy-check.sh` 和 `ops-check.sh` 均对 `iiot-dataworker` 调用该门禁。OPERATIONS、治理清单和非 root 契约同步写明 DataWorker Docker healthcheck 是默认门禁；HTTP-only 基线仍保持 `no-tls`、`no-hsts`、`no-https-redirection`，不引入 443/TLS/HSTS。
- 规则提炼：形成长期验收口径，已写入 `docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：C-06 关闭和日常运维检查不得只看 DataWorker running，必须定义并通过 Docker healthcheck。未写入工作区总规则，原因是这是 Cloud 容器部署契约，不是三项目通用规则。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/ops-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "require_healthy_service|DataWorker Docker healthcheck|Service does not define a Docker health check|Service did not become healthy|\\.State\\.Health\\.Status|POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID|EXPECTED_PLUGIN_MODULE_ID|http-only|no-tls|no-hsts|no-https-redirection" deploy/scripts/release-common.sh deploy/scripts/post-deploy-check.sh deploy/scripts/ops-check.sh deploy/OPERATIONS.md docs/云端架构治理清单.md docs/云端容器非Root权限契约.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；ConfigurationGuardTests 79/79 通过；DataWorker health gate、C-06 Edge expected plugin 版本绑定和 HTTP-only 红线关键词均可检索；diff whitespace 检查通过。

## 2026-07-07 云端 C-06 Edge 上传验收版本绑定

- 改动范围：`deploy/scripts/verify-edge-installer-catalog.sh`、`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-06 生产关闭条件时发现，文档要求“Edge installer/plugin 上传”，但现有 post-deploy gate 只验证 public catalog 和“至少一个插件版本存在”，无法把验收证据绑定到本次真实上传的插件包。临时 HTTP fixture 还复现出脚本对 public catalog 中相对 `downloadUrl` 直接 `curl` 会失败；而源码测试和生产契约都允许 `/edge-updates/installers/.../installer-artifact.json` 这种相对路径。
- 影响面：`verify-edge-installer-catalog.sh` 新增可选 `EXPECTED_PLUGIN_MODULE_ID` + `EXPECTED_PLUGIN_VERSION`，必须成对传入；传入后会同时验证 catalog 中存在该插件模块/版本，并验证 installer artifact 的 modules 中包含同一模块和版本。脚本现在会把相对 `downloadUrl` 按 `BASE_URL` 归一化后再下载 installer artifact。`post-deploy-check.sh` 透传 `POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID` 和 `POST_DEPLOY_EDGE_EXPECTED_PLUGIN_VERSION`。OPERATIONS、治理清单和非 root 契约同步要求：关闭 C-06 前先通过 Cloud 受控发布 API 上传真实 host/plugin 包，再用期望 host/plugin 版本跑 post-deploy gate。
- 规则提炼：无新增长期规则，原因是“Edge installer bundle 上传、plugin package 上传、public catalog 和 Velopack 静态下载必须通过”已经写入 `docs/云端容器非Root权限契约.md`；本次是把该验收口径从泛称上传补成可执行的版本绑定证据。
- 验证命令：
  - `sh -n deploy/scripts/verify-edge-installer-catalog.sh && sh -n deploy/scripts/post-deploy-check.sh`
  - `mktemp + python3 SimpleHTTPRequestHandler 临时 HTTP fixture，构造相对 downloadUrl、installer-artifact.json、installer stub、Velopack RELEASES / releases.stable.json / assets.stable.json / .nupkg，分别验证 EXPECTED_PLUGIN_MODULE_ID=Homogenization EXPECTED_PLUGIN_VERSION=2.3.4 成功、catalog 插件版本不匹配失败、installer artifact 模块版本不匹配失败、只传 EXPECTED_PLUGIN_MODULE_ID 失败`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "EXPECTED_PLUGIN_MODULE_ID|EXPECTED_PLUGIN_VERSION|POST_DEPLOY_EDGE_EXPECTED_PLUGIN_MODULE_ID|POST_DEPLOY_EDGE_EXPECTED_PLUGIN_VERSION|expected plugin module not found|expected plugin version mismatch|Cloud 受控发布 API|期望 host/plugin 版本" deploy/scripts/verify-edge-installer-catalog.sh deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md docs/云端架构治理清单.md docs/云端容器非Root权限契约.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；临时 HTTP fixture 中相对 `downloadUrl` 成功归一化到 `BASE_URL`，installer artifact、stub、Velopack `RELEASES`、channel manifests 和 `.nupkg` 均通过下载验收；catalog 插件版本匹配和 installer artifact 模块版本匹配成功，catalog 版本不匹配、artifact 模块版本不匹配和只传 moduleId 三个负向分支均按预期失败；ConfigurationGuardTests 79/79 通过；期望 plugin module/version、post-deploy 透传、Cloud 受控发布 API 上传后再验收、相对 downloadUrl 归一化和复盘关键词均可检索；diff whitespace 检查通过。

## 2026-07-07 云端合 main E2E 门禁口径修正

- 改动范围：`docs/云端架构治理清单.md` 和本复盘。未修改运行时代码、测试代码、部署脚本、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：当前规则和复盘已经确认 `CloudProductionFlowTests` 是重型 Aspire 回归，整类过滤会让每个测试方法独立启动 fixture，耗时长且不便定位；但治理清单第 7 节仍写着合 main 前运行 `CloudProductionFlowTests|RateLimitingConfigurationGuardTests`，和刚验证过的窄过滤门禁前后不一致。
- 影响面：C-03 bootstrap 验收命令从全类 `CloudProductionFlowTests` 改为两个 bootstrap 精确用例；合 main 前 E2E 从全类过滤改为 `RateLimitingConfigurationGuardTests` 加 healthz、DeviceLog deadletter、bootstrap、OIDC discovery、AI read token 和 Human login 限流的精确过滤。整类 `CloudProductionFlowTests` 仍保留为手动重型回归，不作为本机稳定门禁。
- 规则提炼：无新增长期规则，原因是“重型 E2E 本机冒烟优先窄过滤”已经写入 `docs/云端规则.md`；本次只是修正治理清单和实际验证证据之间的口径不一致。
- 验证命令：
  - `rg -n "CloudProductionFlowTests\\|RateLimitingConfigurationGuardTests|整类是重型 Aspire 回归|EdgeBootstrap_ShouldAcceptBootstrapSecretHeader_WhenSecretIsRequired|DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue" docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：治理清单中的 C-03 精确 bootstrap 过滤、合 main 前精确 E2E 过滤、整类 CloudProductionFlowTests 禁止冒充稳定门禁说明和本复盘关键词均可检索；diff whitespace 检查通过。

## 2026-07-06 云端 DeviceLog deadletter 窄 E2E 验证

- 改动范围：本次只更新本复盘，未修改运行时代码、测试代码、部署脚本、专题契约、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续执行完整云端治理计划的合 main 前证据时，直接使用 `CloudProductionFlowTests|RateLimitingConfigurationGuardTests` 会把大量重型 Aspire 用例混进同一命令，出现过 DeviceLog deadletter 用例超时且后续进程长时间未退出。需要把失败点单独复核，并改用按治理项命名的窄过滤作为可复现门禁证据。
- 影响面：单独验证 `CloudProductionFlowTests.DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue`，确认 DataWorker 对不存在 `DeviceId` 的日志事件抛出预期业务异常后，MassTransit 会把消息移入 `iiot-device-logs_error`；再组合 RateLimitingConfigurationGuardTests 验证限流配置门禁。没有改变生产消息消费、重试或 deadletter 行为。
- 规则提炼：无新增长期规则，原因是 DataWorker deadletter、限流 guard 和重型 E2E 分层已经属于既有治理口径；本次只是补充验证证据，并明确本机合 main 前不要用 `CloudProductionFlowTests` 全类过滤替代按风险点拆分的窄过滤。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~CloudProductionFlowTests.DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue --no-restore --disable-build-servers --logger "console;verbosity=normal"`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~CloudProductionFlowTests.DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
- 验证结果：DeviceLog deadletter 单用例 1/1 通过，总耗时约 53 秒，日志中可见预期的 `Persist failed: DeviceId ... does not exist` 后进入 MassTransit `R-FAULT`；DeviceLog deadletter + RateLimiting 窄过滤 4/4 通过，总耗时约 52 秒。

## 2026-07-06 云端关键 E2E 链路补充验证

- 改动范围：本次只更新本复盘，未修改运行时代码、测试代码、部署脚本、专题契约、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按“完整云端修复计划”补强合 main 前证据。`CloudProductionFlowTests` 每个测试方法都会独立启动一套 Aspire fixture，整类过滤会耗时过长且不便定位；因此先按治理清单选取安全红线相关的代表性真实 HTTP 链路逐条验证，避免用配置扫描替代真实链路。
- 影响面：本轮验证覆盖四条真实 Aspire/HTTP 链路：bootstrap secret 强制校验、Cloud OIDC discovery/code+PKCE 元数据、AI read token 不得访问 Human mutation 或 Edge upload、Human 登录限流；同时补跑 RateLimitingConfigurationGuardTests。没有产生生产环境副作用。
- 规则提炼：无新增长期规则，原因是本次只是补充执行既有治理清单的代表性 E2E 证据；CloudProductionFlowTests 整类仍属于重型手动验证，不应误当成每次本机快速门禁。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --no-restore --disable-build-servers --list-tests`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~RateLimitingConfigurationGuardTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter "FullyQualifiedName~EdgeBootstrap_ShouldRequireSecret_AndResolveByCode|FullyQualifiedName~CloudOidc_Discovery_ShouldExposeCodeFlowPkceAndProviderEndpoints|FullyQualifiedName~AiReadToken_ShouldNotAccessHumanMutationOrEdgeUpload|FullyQualifiedName~HumanIdentity_Login_ShouldReturnTooManyRequestsAfterTenthRequestWithinWindow" --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `rg -n "云端关键 E2E 链路补充验证|bootstrap secret 强制校验|Cloud OIDC discovery|AI read token|Human 登录限流|RateLimitingConfigurationGuardTests" docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：测试清单枚举成功；RateLimitingConfigurationGuardTests 3/3 通过；四条真实 HTTP 链路 4/4 通过，总耗时约 2 分 49 秒；复盘关键词可检索；diff whitespace 检查通过。

## 2026-07-06 云端 E2E healthz 启动等待收口

- 改动范围：`src/tests/IIoT.EndToEndTests/IIoTAppFixture.cs`、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`docs/云端规则.md` 和本复盘。未修改生产运行时代码、部署脚本、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续执行合 main 前端到端过滤门禁时，`CloudProductionFlowTests.InternalHealthz_ShouldReturnOk` 首次请求通过 Gateway 打到 `/internal/healthz` 返回 503。日志显示 HttpApi 资源已被 Aspire 判定 healthy，但 MassTransit bus 刚启动，健康检查短时间仍报 `Not ready: not started`。这说明测试 fixture 把 ResourceNotifications 的资源状态误当成真实 HTTP healthz 通过，存在启动验收 race。
- 影响面：`IIoTAppFixture.StartAsync` 在创建 `iiot-gateway` HttpClient 后新增 `WaitForGatewayHealthzAsync`，在既有 3 分钟启动超时内轮询 `/internal/healthz`，只有返回 `HttpStatusCode.OK` 才放行测试；连接异常会短暂重试。`ConfigurationGuardTests` 增加源代码守卫，防止后续删除真实 healthz 等待。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：Aspire 端到端测试不能只依赖 ResourceNotifications healthy；测试 fixture 必须等待 Gateway `/internal/healthz` 真实返回 200，避免 bus、Redis 或下游健康检查启动 race 造成首个请求偶发 503。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~CloudProductionFlowTests.InternalHealthz_ShouldReturnOk --no-restore --disable-build-servers --logger "console;verbosity=minimal"`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "WaitForGatewayHealthzAsync|GetAsync\\(\"/internal/healthz\"|HttpStatusCode.OK|ResourceNotifications 报告资源 healthy|首个请求偶发 503|E2E healthz 启动等待收口" src/tests/IIoT.EndToEndTests/IIoTAppFixture.cs src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：`CloudProductionFlowTests.InternalHealthz_ShouldReturnOk` 1/1 通过；ConfigurationGuardTests 79/79 通过；fixture healthz 等待、门禁测试、规则文档和复盘关键词均可检索；diff whitespace 检查通过。

## 2026-07-06 云端核心门禁本机验证

- 改动范围：本次只更新本复盘，未修改运行时代码、部署脚本、专题契约、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按“完整云端修复计划”做完成度审计，不能只依赖单项定向测试。需要用当前工作树实际运行结果证明身份权限、设备删除、AI/Edge 状态、部署模板、脚本和前端构建在本机门禁层面一致。
- 影响面：本机验证覆盖 Release build、ServiceLayer 全量、ProductionService 全量、EndToEnd ConfigurationGuardTests、compose 生产模板、部署脚本语法、前端 build、本机构建 dry-run 分支和非 root readiness 临时目录分支。`build-and-push.sh --dry-run` 会刷新 `artifacts/deploy/cloud-built-services.txt` 和 `artifacts/deploy/cloud-images.env`，该目录是生成物，不作为规则入口。
- 规则提炼：无新增长期规则，原因是本次只是把既有 C-01 至 C-17 的本机门禁重新跑全并记录结果；C-06 生产真实容器验收仍需在服务器完成，不能用本机验证替代。
- 验证命令：
  - `dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/ops-check.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/check-container-nonroot-readiness.sh && sh -n deploy/scripts/ensure-oidc-signing-cert.sh && sh -n deploy/scripts/mirror-third-party-images.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh && bash -n deploy/scripts/build-and-push.sh && bash -n deploy/scripts/local-release.sh && bash -n deploy/scripts/post-release-cleanup.sh`
  - `cd src/ui/iiot-web && npm run build`
  - `REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services httpapi --dry-run`
  - `if REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services web --dry-run >/tmp/iiot-build-web-no-challenge.out 2>&1; then cat /tmp/iiot-build-web-no-challenge.out; exit 1; fi; rg -n "VITE_AICOPILOT_CHALLENGE_URL is required for web image builds" /tmp/iiot-build-web-no-challenge.out`
  - `REGISTRY=harbor.local:5000 VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.local:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --services web --dry-run`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`
  - `git diff --check && git diff --cached --check`
- 验证结果：Release build 成功且 0 warning；ServiceLayer 227/227 通过；ProductionService 41/41 通过；ConfigurationGuardTests 79/79 通过；compose config、脚本语法检查、前端 build、httpapi dry-run、web 缺少 challenge URL 负向门禁、web 显式 challenge URL dry-run 和临时 PFX/Edge 更新目录的 non-root readiness 均通过；前端 build 仅输出 Rollup 对第三方 `@vueuse/core` pure annotation 的提示，未阻断构建；diff whitespace 检查通过。

## 2026-07-06 云端 C-07 JWT 验签门禁补齐

- 改动范围：`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本复盘。未修改 JWT 运行时代码、OIDC 授权流、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：按治理清单继续审计 JWT/`ReadJwtToken` 风险时确认，生产代码已经通过 JwtBearer `TokenValidationParameters` 验证 issuer、audience、lifetime 和 signing key，且生产运行时代码没有 `ReadJwtToken`；但原先 C-07 只有清单口径，没有可执行门禁挡住未来未验签 claims 参与认证/授权。
- 影响面：新增 `JwtAuthentication_ShouldValidateBearerTokensAndAvoidUnsignedClaimParsing`，守卫 HttpApi 必须保留 JwtBearer 默认认证、完整 token validation 参数、零 `ClockSkew`、非 Development 缺失 JWT secret fail-fast，并扫描 `src/hosts`、`src/services`、`src/infrastructure`、`src/shared` 生产运行时代码不得出现 `ReadJwtToken(`。测试项目里合法生成/解析 token 的断言不受影响。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：JWT 认证判断必须走 JwtBearer 验签管线；生产运行时代码不得用 `ReadJwtToken` 读取未验签 claims 参与认证、授权、身份链或权限判断；后续如要新增非授权诊断展示，必须先说明用途并调整门禁。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "JwtAuthentication_ShouldValidateBearerTokensAndAvoidUnsignedClaimParsing|ReadJwtToken|ValidateIssuer = true|ValidateAudience = true|ValidateLifetime = true|ValidateIssuerSigningKey = true|ClockSkew = TimeSpan.Zero|生产运行时代码没有这类例外|未验签 claims" src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md src/hosts/IIoT.HttpApi/DependencyInjection.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：ConfigurationGuardTests 79/79 通过；JWT bearer 验签配置、生产运行时代码零 `ReadJwtToken` 门禁、规则文档和治理清单关键词均可检索；diff whitespace 检查通过。

## 2026-07-06 云端 OIDC token gate 源文件权限门禁

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 OIDC 服务端授权流、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 `POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE` / `POST_DEPLOY_OIDC_CODE_VERIFIER_FILE` 后发现，脚本要求文件输入但只校验可读、非空，若操作者传入 group/other 可读的文件，一次性 code/verifier 仍可能被同机其它用户读取。文件输入必须同时具备权限门禁，才和“不得进入日志、文档、shell history、进程环境或 curl 参数”的目标一致。
- 影响面：`post-deploy-check.sh` 新增 portable `stat -c '%a'` / `stat -f '%Lp'` 权限解析，`require_post_deploy_file` 会拒绝 group/other 有任意权限的 code/verifier 源文件，并提示先 `chmod 600`。通过校验后仍会复制到 0600 临时文件并用 `code@file` / `code_verifier@file` 调用 `/connect/token`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：OIDC token gate 的 code/verifier 文件必须是可读、非空、仅当前用户可访问的 0600 类私有文件；脚本必须拒绝 group/other 有权限的源文件。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "must not grant group or other permissions|use chmod 600|0600 类私有文件|stat -f '%Lp'|mode_triplet|POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE|POST_DEPLOY_OIDC_CODE_VERIFIER_FILE" deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端安全部署契约.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：post-deploy 脚本语法检查通过；ConfigurationGuardTests 78/78 通过；源文件 0600 类私有权限门禁、文档规则和测试守卫均可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 OIDC token gate 文件输入收口

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 OIDC 服务端授权流、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：上一轮已避免把 OIDC code/verifier 放进 curl 参数，但脚本仍通过 `POST_DEPLOY_OIDC_AUTHORIZATION_CODE` / `POST_DEPLOY_OIDC_CODE_VERIFIER` 直接环境变量取值；直接值变量可能进入 shell history 或进程环境，和新写入的“不进入 shell history、进程环境或 curl 进程参数”规则不一致。
- 影响面：`post-deploy-check.sh` 现在只接受 `POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE` 和 `POST_DEPLOY_OIDC_CODE_VERIFIER_FILE`，并要求文件存在、可读、非空；直接值变量会 fail-fast。脚本会把源文件内容去除 CR/LF 后复制到 0600 临时文件，再通过 `code@file` / `code_verifier@file` 调用 `/connect/token`。OPERATIONS 示例改成 `read -rsp` 读取真实值并写入临时文件，不把真实 code/verifier 写入命令历史。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：OIDC token gate 必须通过文件传入 code/verifier，不得通过直接环境变量、日志、文档、shell history、进程环境或 curl 参数暴露真实值。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n 'POST_DEPLOY_OIDC_AUTHORIZATION_CODE_FILE|POST_DEPLOY_OIDC_CODE_VERIFIER_FILE|OIDC code/verifier must be passed as files|process environment|read -rsp|code@\\$authorization_code_file|code_verifier@\\$code_verifier_file|authorization_code=\\$\\{POST_DEPLOY_OIDC_AUTHORIZATION_CODE|code_verifier=\\$\\{POST_DEPLOY_OIDC_CODE_VERIFIER' deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端安全部署契约.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：post-deploy 脚本语法检查通过；ConfigurationGuardTests 78/78 通过；文件输入、直接值 fail-fast、OPERATIONS `read -rsp` 示例和“不得进入进程环境”规则均可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 OIDC token gate 隐私参数收口

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 OIDC 服务端授权流、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计刚补的 `POST_DEPLOY_VERIFY_OIDC_TOKEN=1` gate 时发现，原实现虽然不打印 code/verifier，但把 `--data-urlencode "code=$authorization_code"` 和 `--data-urlencode "code_verifier=$code_verifier"` 放在 curl 命令行参数里，生产服务器上可能被进程列表短暂看到，不符合 OIDC code/verifier 不外泄的红线。
- 影响面：token gate 仍只在显式打开时执行；真实 authorization code 和 PKCE verifier 改为写入 0600 临时文件，再通过 `--data-urlencode code@<file>` / `code_verifier@<file>` 提交给 curl，成功和失败路径都会删除临时文件。文档同步要求 code/verifier 不得进入日志、文档、shell history 或 curl 进程参数。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：真实 OIDC code/verifier 不得进入日志、文档、shell history 或 curl 进程参数。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n 'code@\\$authorization_code_file|code_verifier@\\$code_verifier_file|curl 进程参数|shell history|chmod 600 "\\$token_response_file"' deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端安全部署契约.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：post-deploy 脚本语法检查通过；ConfigurationGuardTests 78/78 通过；code/verifier 通过 0600 临时文件提交、禁止进入 curl 进程参数的规则口径和测试守卫均可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-09 重置密码不解锁回归测试

- 改动范围：`src/tests/IIoT.ServiceLayer.Tests/AuthorizationAndIdentityBehaviorTests.cs`、`docs/云端架构治理清单.md` 和本复盘。未修改运行时代码、部署脚本、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按治理清单做源码级审计时确认 C-09 已实现 Identity lockout，但原测试只覆盖“失败锁定”和“成功登录清零失败计数”，没有覆盖云端规则中的“管理员重置密码不得隐式清除 lockout”。虽然当前 `IdentityPasswordService.ResetPasswordAsync` 没有清锁逻辑，但缺少回归测试容易后续回退。
- 影响面：新增行为测试 `IdentityPasswordService_ResetPassword_ShouldNotUnlockLockedUser`，先用 5 次错误密码触发 lockout，再执行 `ResetPasswordAsync`，断言密码重置成功但账号仍处于 lockout。治理清单 C-09 同步记录该回归证据。
- 规则提炼：无新增长期规则，原因是“管理员重置密码不得隐式清除 lockout”已写入 `docs/云端规则.md`；本次只是把既有规则补成可执行回归测试。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --filter FullyQualifiedName~AuthorizationAndIdentityBehaviorTests --no-restore --disable-build-servers`
  - `rg -n "IdentityPasswordService_ResetPassword_ShouldNotUnlockLockedUser|ResetPasswordAsync.*lockout|重置密码不解锁" src/tests/IIoT.ServiceLayer.Tests/AuthorizationAndIdentityBehaviorTests.cs docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md`
  - `git diff --check && git diff --cached --check`
- 验证结果：AuthorizationAndIdentityBehaviorTests 33/33 通过；重置密码不解锁回归测试、治理清单和复盘关键词可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-06 post-deploy OIDC token 显式验收

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 OIDC 服务端授权流、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续关闭 C-06 生产验收口径时发现，上一轮只把 OIDC discovery/JWKS 作为默认 post-deploy 证据，但 C-06 关闭条件仍要求 token 发行验收。源码确认 Cloud OIDC 只支持 Authorization Code + PKCE，不能用脚本伪造密码流或 client_credentials；因此需要显式生产 gate，由操作者提供真实一次性 authorization code 和匹配 PKCE verifier 后验证 `/connect/token`。
- 影响面：`post-deploy-check.sh` 默认行为保持 HTTP 首页、healthz、OIDC discovery/JWKS 和 ops-check；新增 `POST_DEPLOY_VERIFY_OIDC_TOKEN=1` 可选 gate，缺少 `POST_DEPLOY_OIDC_REDIRECT_URI`、`POST_DEPLOY_OIDC_AUTHORIZATION_CODE` 或 `POST_DEPLOY_OIDC_CODE_VERIFIER` 时 fail-fast，成功时只确认返回 bearer access token，不打印 code/verifier/token。Edge installer catalog gate 仍由 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1` 独立打开。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：OIDC token 发行验收只能显式使用真实 authorization-code + PKCE，不能伪造凭据，也不能用 discovery/JWKS 冒充 token 成功。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "POST_DEPLOY_VERIFY_OIDC_TOKEN|POST_DEPLOY_OIDC_AUTHORIZATION_CODE|POST_DEPLOY_OIDC_CODE_VERIFIER|grant_type=authorization_code|post_deploy_oidc_token=verified|真实 authorization-code|真实授权码|PKCE" deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端安全部署契约.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：post-deploy 脚本语法检查通过；ConfigurationGuardTests 78/78 通过；OIDC token 显式 gate、真实 authorization-code + PKCE 口径、禁止伪造 token 验收和复盘关键词均可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-06 post-deploy OIDC 公共端点验收

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-06 生产关闭条件时发现，清单要求真实容器启动验收覆盖 OIDC discovery/token，但 `post-deploy-check.sh` 默认只验证首页和 `/internal/healthz`。token 发行需要真实 client/user 凭据，通用脚本不能伪造；但 OIDC discovery 和 JWKS 是公开端点，可以作为默认 post-deploy 证据，证明 Gateway 到 HttpApi 的 OIDC 公共链路和签名键发布没有断。
- 影响面：`post-deploy-check.sh` 默认新增 `/.well-known/openid-configuration` 和 `/.well-known/jwks` HTTP 200 探测；Edge installer catalog gate 仍由 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1` 显式打开。文档同步说明：token 发行验证必须在生产使用真实凭据单独执行，不得用 discovery/JWKS 冒充 token 成功。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：post-deploy 默认必须验证 OIDC discovery/JWKS；token 发行验证需要真实凭据，不能伪造。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "openid-configuration|\\.well-known/jwks|OIDC discovery|JWKS|post-deploy OIDC 公共端点验收" deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md docs/云端规则.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：post-deploy 脚本语法检查通过；ConfigurationGuardTests 78/78 通过；OIDC discovery/JWKS 验收关键词可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-06 nginx 内部端口 readiness 正向门禁

- 改动范围：`deploy/scripts/check-container-nonroot-readiness.sh`、`docs/云端规则.md`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-06 非 root 生产关闭条件时发现，readiness 脚本会对部分容器内 80 回退设置 `HAS_RISK=1`，但实现偏弱：只匹配少数 `80` 字符串，且没有正向要求 `listen 8080`、`proxy_pass http://iiot-web:8080` 和 compose target `8080` 同时存在。这样不能充分证明“nginx 内部端口未回退”。
- 影响面：`check_nginx_low_port_blocker` 改为反向阻断和正向确认并行：发现 nginx 监听 80、反代 web:80 或 compose target 80 时 fail-fast；缺少 nginx `listen 8080`、`proxy_pass http://iiot-web:8080` 或 compose target 8080 时也 fail-fast。通过时仍输出 `nonroot_readiness_nginx_internal_port=8080`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：C-06 readiness 必须正向证明 nginx 内部端口保持 8080，并对任何容器内 80 回退 fail-fast。
- 验证命令：
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `tmp_dir=$(mktemp -d); mkdir -p "$tmp_dir/certs" "$tmp_dir/edge/installers" "$tmp_dir/edge/plugins" "$tmp_dir/edge/velopack"; : > "$tmp_dir/certs/cloud-oidc-signing.pfx"; chmod 600 "$tmp_dir/certs/cloud-oidc-signing.pfx"; chmod 700 "$tmp_dir/edge" "$tmp_dir/edge/installers" "$tmp_dir/edge/plugins" "$tmp_dir/edge/velopack"; env_file="$tmp_dir/deploy.env"; printf 'OIDC_PROVIDER_CERTS_DIR=%s\nOIDC_PROVIDER_SIGNING_CERTIFICATE_PATH=/app/certs/cloud-oidc-signing.pfx\nEDGE_UPDATES_DIR=%s\n' "$tmp_dir/certs" "$tmp_dir/edge" > "$env_file"; COMPOSE_ENV_FILE="$env_file" CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh; rc=$?; rm -rf "$tmp_dir"; exit "$rc"`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "nginx-gateway must not listen on container port 80|nginx-gateway must listen on container port 8080|nginx-gateway compose port target must be container port 8080|nonroot_readiness_nginx_internal_port=8080|nginx 内部端口 readiness 正向门禁" deploy/scripts/check-container-nonroot-readiness.sh docs/云端规则.md docs/云端容器非Root权限契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：shell 语法检查通过；临时 PFX/Edge 更新目录权限探针通过，并输出 `nonroot_readiness_nginx_internal_port=8080`、`nonroot_readiness_log_volume_httpapi_logs=not-created`、`nonroot_readiness_log_volume_dataworker_logs=not-created` 和 `nonroot_readiness=passed`；ConfigurationGuardTests 78/78 通过；正向门禁关键词可检索；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-17 本机构建示例域与 Web Challenge 门禁收口

- 改动范围：`deploy/scripts/build-and-push.sh`、`deploy/scripts/local-release.sh`、`deploy/scripts/mirror-third-party-images.sh`、`deploy/scripts/verify-edge-installer-catalog.sh`、`deploy/scripts/InvokeEdgeInstallerPackageDownload.ps1`、`deploy/README.md`、`deploy/OPERATIONS.md`、`deploy/EDGE_INSTALLER_GO_LIVE.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-17 本机发布入口时发现，脚本帮助和 README/GO_LIVE 中仍有 `harbor.internal.example`、`cloud.internal.example`、`aicopilot.internal.example` 等可复制示例，但这些值会被脚本自身的 `.example` 拦截拒绝。`build-and-push.sh` 还把 `VITE_AICOPILOT_CHALLENGE_URL` 默认成会被自己拒绝的文档域名，导致 backend-only 构建也被 AICopilot 前端 URL 阻断，和“按服务构建”的目标不一致。随后负向检索又发现 `mirror-third-party-images.sh`、PowerShell 下载脚本和 README `PUBLIC_BASE_URL` 示例仍残留同类模板域。
- 影响面：脚本帮助和可复制文档示例改为 `<harbor-registry>`、`deploy@<cloud-host>`、`http://<cloud-host>:81`、`http://<aicopilot-browser-reachable-host>:82/...` 这类不会被脚本识别为文档示例域的占位。`build-and-push.sh` 现在只有在选中 `web` 或 `--all` 时才要求显式 `VITE_AICOPILOT_CHALLENGE_URL`，backend-only dry-run/build 不再需要该变量；Web 构建仍拒绝空值和 `.example` / `internal.example`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：本机构建、local release 和 installer catalog 验收的脚本帮助/文档示例不得使用会被脚本自身拒绝的 `.example` 值；Web challenge URL 只在构建 Web 镜像时必需，backend-only 构建不得被前端 URL 阻断。
- 验证命令：
  - `bash -n deploy/scripts/build-and-push.sh && bash -n deploy/scripts/local-release.sh && sh -n deploy/scripts/mirror-third-party-images.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh`
  - `REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services httpapi --dry-run`
  - `if REGISTRY=harbor.local:5000 deploy/scripts/build-and-push.sh --services web --dry-run >/tmp/iiot-build-web-no-challenge.out 2>&1; then cat /tmp/iiot-build-web-no-challenge.out; exit 1; fi; rg -n "VITE_AICOPILOT_CHALLENGE_URL is required for web image builds" /tmp/iiot-build-web-no-challenge.out; rm -f /tmp/iiot-build-web-no-challenge.out`
  - `REGISTRY=harbor.local:5000 VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.local:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --services web --dry-run`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "VITE_AICOPILOT_CHALLENGE_URL is required for web image builds|deploy@<cloud-host>|BASE_URL=http://<cloud-host>:81|<aicopilot-browser-reachable-host>|本机构建示例域与 Web Challenge 门禁收口" deploy/scripts deploy/README.md deploy/OPERATIONS.md deploy/EDGE_INSTALLER_GO_LIVE.md docs/云端规则.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `if rg -n "REGISTRY=harbor\\.internal\\.example|MIRROR_REGISTRY=harbor\\.internal\\.example|deploy@cloud\\.internal\\.example|BASE_URL=http://cloud\\.internal\\.example|PUBLIC_BASE_URL=http://cloud\\.internal\\.example|VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot\\.internal\\.example" deploy/scripts deploy/README.md deploy/OPERATIONS.md deploy/EDGE_INSTALLER_GO_LIVE.md; then exit 1; fi`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；backend-only `httpapi` dry-run 不要求 `VITE_AICOPILOT_CHALLENGE_URL` 并成功输出 `IIOT_HTTPAPI_IMAGE`；`web` dry-run 缺少 challenge URL 时输出 `VITE_AICOPILOT_CHALLENGE_URL is required for web image builds` 并失败；传入 `http://aicopilot.local:82/api/identity/cloud-oidc/challenge` 后 `web` dry-run 通过并输出 `IIOT_WEB_IMAGE`；ConfigurationGuardTests 78/78 通过；正向检索可找到 `<harbor-registry>`、`deploy@<cloud-host>`、`http://<cloud-host>:81`、`<aicopilot-browser-reachable-host>` 和本复盘；负向检索确认可复制脚本/README/GO_LIVE/OPERATIONS 中不再出现 `REGISTRY=harbor.internal.example`、`MIRROR_REGISTRY=harbor.internal.example`、`deploy@cloud.internal.example`、`BASE_URL=http://cloud.internal.example`、`PUBLIC_BASE_URL=http://cloud.internal.example`、`VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.internal.example`；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-16 HTTP OIDC 生产示例收口

- 改动范围：`deploy/README.md`、`docs/云端规则.md`、`docs/云端安全部署契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、部署脚本行为、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按源码反向审计 C-16 时发现，`release-common.sh` 和 `OidcProviderOptions` 已要求 HTTP OIDC 只允许 loopback/RFC1918 IPv4，但 `deploy/README.md` 的可复制生产示例仍写 `OIDC_PROVIDER_ISSUER=http://cloud.internal.example:81` 和 AICopilot `.internal.example` callback/logout。该示例会被 preflight 拒绝，和运行时契约不一致。
- 影响面：README 的生产 HTTP OIDC 示例改为 `<cloud-private-ip>` / `<aicopilot-private-ip>` 占位，并明确 `.internal.example` 只能是模板域名，不能作为生产 HTTP OIDC 值上线。新增 ConfigurationGuardTests 防止 README 回退到模板域名示例。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端安全部署契约.md` 和 `docs/云端架构治理清单.md`：生产 HTTP OIDC 文档示例必须使用私网 IPv4 占位，不得把 `.internal.example` 模板域名写成可上线示例。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "HTTP OIDC|loopback 或 RFC1918 私网 IPv4|OIDC_PROVIDER_ISSUER=http://<cloud-private-ip>:81|AICOPILOT_OIDC_REDIRECT_URI=http://<aicopilot-private-ip>:82|DeployReadme_ShouldNotUseTemplateDomainsForProductionHttpOidcExample" deploy/README.md docs/云端规则.md docs/云端安全部署契约.md docs/云端架构治理清单.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：ConfigurationGuardTests 78/78 通过；README、规则、契约、清单和 guard 中可检索到私网 IPv4 占位口径；README 中 `OIDC_PROVIDER_ISSUER=http://cloud.internal.example`、`AICOPILOT_OIDC_REDIRECT_URI=http://aicopilot.internal.example`、`AICOPILOT_OIDC_POST_LOGOUT_REDIRECT_URI=http://aicopilot.internal.example` 负向检索无结果；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-17 Edge 上传限流变量边界门禁

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 HTTP-only 补偿控制时确认，运行时 `HttpApiRateLimitingOptions` 会校验限流值必须大于 0，nginx 也有 `edge_upload_limit=12000r/m` 粗限流；但生产 `.env` 可以把 `RATE_LIMIT_*` 上传限流调成极大值，preflight 不会拦，等同削弱应用侧补偿控制。
- 影响面：新增 `ensure_rate_limit_values_bounded`，对 `RATE_LIMIT_CAPACITY_UPLOAD_*`、`RATE_LIMIT_DEVICE_LOG_UPLOAD_*`、`RATE_LIMIT_PASS_STATION_UPLOAD_*` 六个变量要求正整数且不超过 12000 次/分钟。该校验在 OIDC PFX 生成前执行，仍允许在 12000/min 以内按现场压测调整。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Edge 上传限流变量不能为 0、非数字或超过 nginx `edge_upload_limit` 基线 12000/min；如需更大值，必须同步调整 nginx 与应用限流契约，而不能只改 `.env`。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sed -i.bak -e 's#RATE_LIMIT_PASS_STATION_UPLOAD_TOKEN_LIMIT=3000#RATE_LIMIT_PASS_STATION_UPLOAD_TOKEN_LIMIT=12001#' "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_rate_limit_values_bounded' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_env.bak" "$tmp_out"; test "$rc" -eq 64`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sed -i.bak -e 's#RATE_LIMIT_CAPACITY_UPLOAD_TOKEN_LIMIT=600#RATE_LIMIT_CAPACITY_UPLOAD_TOKEN_LIMIT=fast#' "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_rate_limit_values_bounded' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_env.bak" "$tmp_out"; test "$rc" -eq 64`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；`RATE_LIMIT_PASS_STATION_UPLOAD_TOKEN_LIMIT=12001` 输出 `Deployment numeric value exceeds the allowed maximum` 并以 exit 64 失败；`RATE_LIMIT_CAPACITY_UPLOAD_TOKEN_LIMIT=fast` 输出 `Deployment numeric value must be a positive integer` 并以 exit 64 失败；ConfigurationGuardTests 77/77 通过；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-02/C-17 secret 最小长度门禁

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-02/C-17 secret fail-fast 链路时发现，preflight 已能拒绝空值、模板值和少数已知弱值，但没有最小长度要求；`JWTSETTINGS__SECRET=short-secret` 或 `PG_PASSWORD=abcdefg` 这类明显弱但不在已知弱值列表里的 secret 仍可能通过，不符合 HTTP-only 补偿控制里的“强 secret”口径。
- 影响面：新增 `require_min_secret_length`，`JWTSETTINGS__SECRET` 至少 32 字符；`PG_PASSWORD`、`RABBITMQ_DEFAULT_PASS`、`SEQ_ADMIN_PASSWORD`、`SEED_ADMIN_PASSWORD` 至少 12 字符。`deploy/README.md`、`deploy/OPERATIONS.md` 和治理清单同步说明过短 secret 会被 preflight fail-fast。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Cloud 发布 preflight 必须拒绝空值、模板值、已知弱值和过短 secret，JWT secret 至少 32 字符，其它生产密码至少 12 字符。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sed -i.bak -e 's#__REPLACE_POSTGRES_PASSWORD__#ShortPg1#g' -e 's#__REPLACE_RABBITMQ_PASSWORD__#LongRabbitPass123#g' -e 's#__REPLACE_JWT_SECRET__#12345678901234567890123456789012#g' -e 's#__REPLACE_SEQ_PASSWORD__#LongSeqPass123#g' -e 's#__REPLACE_ADMIN_PASSWORD__#LongAdminPass123#g' "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_required_secret_values_changed' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_env.bak" "$tmp_out"; test "$rc" -eq 64`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sed -i.bak -e 's#__REPLACE_POSTGRES_PASSWORD__#LongPostgresPass123#g' -e 's#__REPLACE_RABBITMQ_PASSWORD__#LongRabbitPass123#g' -e 's#__REPLACE_JWT_SECRET__#short-jwt-secret#g' -e 's#__REPLACE_SEQ_PASSWORD__#LongSeqPass123#g' -e 's#__REPLACE_ADMIN_PASSWORD__#LongAdminPass123#g' "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_required_secret_values_changed' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_env.bak" "$tmp_out"; test "$rc" -eq 64`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；短 `PG_PASSWORD` 负向用例输出 `Required secret is too short: PG_PASSWORD must be at least 12 characters` 并以 exit 64 失败；短 `JWTSETTINGS__SECRET` 负向用例输出 `Required secret is too short: JWTSETTINGS__SECRET must be at least 32 characters` 并以 exit 64 失败；ConfigurationGuardTests 77/77 通过；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-17 本机发布入口文档示例域拦截

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/build-and-push.sh`、`deploy/scripts/mirror-third-party-images.sh`、`deploy/scripts/local-release.sh`、`deploy/scripts/verify-edge-installer-catalog.sh`、`deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：上一轮补了服务器 preflight 的镜像模板值拦截后，继续审计本机构建和发布入口发现，`build-and-push.sh`、`mirror-third-party-images.sh`、`verify-edge-installer-catalog.sh` 只拒绝 `internal.example`，`harbor.example.com`、`cloud.example.com` 这类同样是文档保留域的值仍可能通过；`local-release.sh` 也没有拒绝 `deploy@cloud.internal.example` 这类文档 SSH 目标。这样会把文档占位值推进到 build、mirror、SSH 或下载验收阶段。
- 影响面：本机构建 registry、AICopilot challenge URL、第三方镜像 mirror registry、local release SSH target、Edge installer catalog BASE_URL 和 `release-common.sh` 公共地址模板值现在统一拒绝 `.example` / `internal.example` 文档示例域。README 和 OPERATIONS 同步要求运行前替换成真实内网 registry、SSH 目标、AICopilot challenge URL 和 Cloud Gateway URL。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：本机构建、基础镜像 mirror、local release、Edge installer catalog 验收等部署入口必须拒绝 `.example` / `internal.example` 文档示例域，不能让占位值进入 build、push、SSH 或下载验收阶段。
- 验证命令：
  - `bash -n deploy/scripts/build-and-push.sh && bash -n deploy/scripts/local-release.sh && sh -n deploy/scripts/mirror-third-party-images.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh && sh -n deploy/scripts/release-common.sh`
  - `REGISTRY=harbor.example.com VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.local:82/api/identity/cloud-oidc/challenge deploy/scripts/build-and-push.sh --services httpapi --dry-run`
  - `MIRROR_REGISTRY=harbor.example.com sh deploy/scripts/mirror-third-party-images.sh`
  - `REGISTRY=harbor.local:5000 VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.local:82/api/identity/cloud-oidc/challenge deploy/scripts/local-release.sh --services web --ssh-target deploy@cloud.internal.example --dry-run`
  - `BASE_URL=http://cloud.example.com CHANNEL=stable TARGET_RUNTIME=win-x64 deploy/scripts/verify-edge-installer-catalog.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：语法检查通过；四个负向用例均在进入外部 Docker/SSH/HTTP 操作前以 exit 64 拒绝文档示例域，分别输出 registry、mirror registry、SSH target 和 BASE_URL 的 documentation example domain 错误；ConfigurationGuardTests 77/77 通过；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-17 应用镜像显式 registry 门禁

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-17 镜像发布链路时发现，基础设施镜像已有显式 registry / Docker Hub 拦截，但应用镜像只要求非空和非 `:latest`。如果 `.env` 写成 `IIOT_HTTPAPI_IMAGE=iiot-httpapi:sha-*`，`resolve_release_images` 会保留这个无 registry 仓库名，后续 `docker compose pull` 可能隐式走 Docker Hub 或本地命名，和“本机构建应用镜像、推 Harbor、服务器拉 Harbor”标准路径不一致。
- 影响面：新增 `ensure_app_images_have_explicit_registry`，对五个应用镜像统一要求显式 Harbor/内部 registry，并拒绝 `docker.io/*` / `registry-1.docker.io/*`。`pre-deploy-check.sh` 在 release tag 重写和 OIDC PFX 生成之前执行该门禁；preflight 摘要增加 `image-registry-checked`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：应用镜像和基础设施镜像都必须带显式 Harbor/内部 registry，生产 preflight 不接受无 registry 短镜像名或 Docker Hub 引用。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sed -i.bak -e 's#harbor.example.com/iiot/iiot-httpapi#iiot-httpapi#' -e 's#harbor.example.com#harbor.local:5000#g' "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_image_values_not_template; ensure_app_images_have_explicit_registry' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_env.bak" "$tmp_out"; test "$rc" -eq 64`
  - `rg -n "ensure_app_images_have_explicit_registry|Application image must include an explicit Harbor registry|image-registry-checked|应用镜像显式 registry|无 registry 的短镜像名" deploy/scripts/release-common.sh deploy/scripts/pre-deploy-check.sh src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md deploy/OPERATIONS.md deploy/README.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
- 验证结果：两个 shell 脚本语法检查通过；POSIX `sh` 负向用例把 `IIOT_HTTPAPI_IMAGE` 改成无 registry 的 `iiot-httpapi:sha-*` 后输出 `Application image must include an explicit Harbor registry` 并以 exit 64 失败；脚本、测试、规则和文档关键词可检索；ConfigurationGuardTests 77/77 通过。

## 2026-07-06 云端 C-17 preflight 示例镜像仓库拦截

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/README.md`、`deploy/OPERATIONS.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改运行时业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-17 fail-fast 链路时发现，preflight 只检查镜像变量非空、应用镜像不是 `:latest`、基础设施镜像不是 Docker Hub；如果操作者复制 `.env.example` 后只替换 secret 和公开地址，却漏改 `harbor.example.com/...` 镜像仓库，会晚到 `docker compose pull` 阶段才失败，不符合部署前危险配置先阻断的口径。
- 影响面：`pre-deploy-check.sh` 现在在生成 OIDC PFX 之前调用 `ensure_image_values_not_template`，对应用镜像和基础设施镜像统一拒绝 `.example` / `internal.example` 文档示例 registry。`deploy/README.md`、`deploy/OPERATIONS.md` 和治理清单同步说明 `harbor.example.com` 只是文档占位，生产 `.env` 必须替换成真实 Harbor registry。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：Cloud 发布 preflight 必须拒绝仍指向文档示例仓库的应用镜像或基础设施镜像，不能等到 pull 阶段才暴露模板值未替换问题。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/pre-deploy-check.sh`
  - `tmp_env=$(mktemp); tmp_out=$(mktemp); cp deploy/.env.example "$tmp_env"; sh -c 'DEPLOY_DIR="$1"; env_file="$2"; . "$DEPLOY_DIR/scripts/release-common.sh"; load_dotenv "$env_file"; ensure_image_values_not_template' sh "$PWD/deploy" "$tmp_env" >"$tmp_out" 2>&1; rc=$?; cat "$tmp_out"; rm -f "$tmp_env" "$tmp_out"; test "$rc" -eq 64`
  - `rg -n "ensure_image_values_not_template|documentation example registry|文档示例镜像仓库|\\.example registry|harbor.example.com" deploy/scripts/release-common.sh deploy/scripts/pre-deploy-check.sh src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs docs/云端规则.md docs/云端架构治理清单.md deploy/OPERATIONS.md deploy/README.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
- 验证结果：两个 shell 脚本语法检查通过；POSIX `sh` 负向用例对 `.env.example` 中 `IIOT_HTTPAPI_IMAGE=harbor.example.com/...` 输出 `Image value still uses a documentation example registry` 并以 exit 64 失败；脚本、测试、规则和文档关键词可检索；ConfigurationGuardTests 77/77 通过。

## 2026-07-06 云端 C-17 preflight 无副作用顺序收口

- 改动范围：`deploy/scripts/pre-deploy-check.sh`、`deploy/README.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改应用业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续审计 C-17 preflight 链路时发现，`pre-deploy-check.sh` 原先在校验模板 secret、公开地址、OIDC HTTP 边界、镜像和 compose 之前先调用 `ensure-oidc-signing-cert.sh`。这会让复制模板或其它无效 `.env` 也先生成 PFX 文件，和“发布前 fail-fast、危险配置先阻断”的口径不一致。
- 影响面：preflight 现在先完成无副作用校验：secret 模板值、公开地址模板值、bootstrap 关闭变量、OIDC HTTP 边界、应用/基础镜像变量、Docker Hub 阻断、release tag 镜像解析和 compose config；这些通过后才生成/校验 OIDC PFX，再运行 non-root readiness。`deploy/README.md` 同步说明 OIDC PFX 是本地 token 签名证书，不是 HTTPS 证书；生成权限按 `600/640` 策略处理，目录 owner 示例改为专用部署用户。
- 规则提炼：形成长期规则，已写入 `docs/云端架构治理清单.md`：Cloud preflight 必须先做无副作用配置校验，再产生 OIDC PFX 等持久化文件，最后跑 non-root readiness。
- 验证命令：
  - `sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/ensure-oidc-signing-cert.sh && sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "无副作用|ensure-oidc-signing-cert.sh|600|640|deploy:deploy|不是公网 HTTPS 证书" deploy/README.md docs/云端架构治理清单.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：三个 shell 脚本语法检查通过；ConfigurationGuardTests 77/77 通过；README、治理清单和测试守卫中可检索无副作用顺序、`ensure-oidc-signing-cert.sh`、`600/640` 权限策略、`deploy:deploy` 专用部署用户示例和“不是公网 HTTPS 证书”说明；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-06 OIDC PFX 生成权限修正

- 改动范围：`deploy/scripts/ensure-oidc-signing-cert.sh`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改应用业务代码、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：审计 C-06 preflight 调用链时发现，`pre-deploy-check.sh` 会先调用 `ensure-oidc-signing-cert.sh` 生成 PFX，再调用 `check-container-nonroot-readiness.sh` 验证 PFX 可读。但生成脚本原来固定 `chmod 600`，如果部署用户 UID 不是默认容器 UID `10001`，新证书会立即被 readiness 判定为容器不可读，形成“preflight 自己生成、自己失败”的权限断点。
- 影响面：既有 PFX 现在也会先校验是否可被目标容器 UID/GID 读取且不 world-readable；生成新 PFX 时，若部署用户 UID 等于 `CLOUD_CONTAINER_UID`，仍使用 `600`；否则脚本尝试把 PFX group 改为 `CLOUD_CONTAINER_GID` 并设置 `640`，使 HttpApi 非 root 容器可读且不 world-readable。若无法 `chgrp` 到目标容器 GID，脚本 exit 73，提示运维先修 owner/group/mode；新生成但不可授权的 PFX 会被删除，避免下次运行被“文件已存在”短路。
- 规则提炼：形成长期规则，已写入 `docs/云端容器非Root权限契约.md` 和 `docs/云端架构治理清单.md`：既有或新生成 OIDC PFX 必须按目标容器 UID/GID 满足最小可读权限；不得用 world-readable 绕过 readiness。
- 验证命令：
  - `sh -n deploy/scripts/ensure-oidc-signing-cert.sh && sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=99999 CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/ensure-oidc-signing-cert.sh`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=99999 CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=99999 CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/ensure-oidc-signing-cert.sh`（PFX 已存在时，期望输出 already exists and is container-readable）
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：脚本语法检查通过；临时环境中 `ensure-oidc-signing-cert.sh` 以 `CLOUD_CONTAINER_UID=99999`、`CLOUD_CONTAINER_GID=$(id -g)` 首次生成 `-rw-r-----` PFX；第二次运行输出 `OIDC signing certificate already exists and is container-readable`；随后 `check-container-nonroot-readiness.sh` 以同一容器 UID/GID 读取 PFX 并通过；ConfigurationGuardTests 77/77 通过；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-15 真实 IP 全局守卫补强

- 改动范围：`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`deploy/README.md`、`docs/云端架构治理清单.md` 和本复盘。未修改运行时业务逻辑、EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：上一轮已清理运行时提示和 UI placeholder 中的真实现场 IP，但 ConfigurationGuardTests 仍是点状检查，后续仍可能在其它源码、部署脚本或 workflow 中重新写入 `10.98.90.154`。同时 `deploy/README.md` 的历史现场校准记录虽然允许保留真实 IP，但原文没有明确“不是模板默认值”，不够防复制。
- 影响面：新增配置守卫递归扫描 `src`、`deploy`、`.github` 的文本文件，跳过 `bin/obj/node_modules/dist` 等生成目录和 `src/tests`，只允许 `deploy/README.md` 中标明“2026-06-22 现场校准（不是模板默认值）”的历史记录保留真实 IP。`deploy/README.md` 的现场校准行已补充“不是模板默认值”。
- 规则提炼：无新增长期规则，原因是“真实现场 IP 不得作为运行时提示、UI placeholder、脚本默认值、示例 env 或 workflow 默认值”已经写入 `docs/云端规则.md`；本次是把规则落实为全局扫描守卫并补清历史记录例外标识。
- 验证命令：
  - `rg -n "10\\.98\\.90\\.154" src deploy .github -g '!**/bin/**' -g '!**/obj/**' -g '!src/tests/**'`
  - `rg -n "2026-06-22 现场校准（不是模板默认值）.*10\\.98\\.90\\.154" deploy/README.md docs/云端架构治理清单.md`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `git diff --check && git diff --cached --check`
- 验证结果：非测试 `src`、`deploy`、`.github` 中只有 `deploy/README.md` 的“2026-06-22 现场校准（不是模板默认值）”历史记录保留 `10.98.90.154`；治理清单和 README 的例外标识可检索；ConfigurationGuardTests 77/77 通过；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端治理清单已实施项措辞收口

- 改动范围：`docs/云端架构治理清单.md` 和本复盘。未修改运行时代码、部署脚本、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续做计划与源码一致性审计时发现，清单顶部仍写着 AI Read 和 EdgeHost PLC runtime state “后续要固化/补契约”，C-08 修复动作仍写“保留 Role 兼容属性建议删除”。但当前源码和专题契约显示 C-12/C-14 已固化，`ICurrentUser.Role` 也已删除；旧措辞会误导后续执行，把已完成项当成待办或建议保留兼容层。
- 影响面：治理清单现在明确 `/api/v1/ai/read/production-records` 已作为唯一 AI 生产数据只读入口，并由 `docs/AI只读接口契约.md` 和 ConfigurationGuardTests 守卫；EdgeHost PLC runtime state 已由 `docs/Edge上传与PLC状态接口契约.md` 和 ConfigurationGuardTests 守卫；C-08 明确 `ICurrentUser.Role` 已删除且不得恢复单 role 授权判断。
- 规则提炼：无新增长期规则，原因是本次只修正治理清单状态描述，相关长期规则已分别沉淀在 `docs/云端规则.md`、`docs/AI只读接口契约.md` 和 `docs/Edge上传与PLC状态接口契约.md`。
- 验证命令：
  - `rg -n "production-records.*已固化|EdgeHost PLC runtime state 已固化|已删除 `ICurrentUser.Role`|后续要把它固化|后续要补专题契约|保留 `Role` 兼容属性" docs/云端架构治理清单.md`
  - `rg -n "string\\?\\s+Role\\s*\\{|IReadOnlyCollection<string> Roles" src/services/IIoT.Services.Contracts/Contracts/Identity/ICurrentUser.cs`
  - `git diff --check && git diff --cached --check`
- 验证结果：治理清单已显示 AI Read、EdgeHost PLC runtime state 和 `ICurrentUser.Role` 删除事实；旧“后续固化/补契约/保留 Role 兼容属性”措辞不再出现；`ICurrentUser` 仅保留 `IReadOnlyCollection<string> Roles`；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-15 运行时提示真实 IP 清理

- 改动范围：`src/services/IIoT.ProductionService/ClientReleases/EdgeInstallerPublicBaseUrl.cs`、`src/ui/iiot-web/src/features/client-releases/EdgeBindingDownloadPanel.vue`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 EdgeClient、AICopilot、HTTPS/TLS/443/HSTS 配置或真实部署 `.env`。
- 改动原因：继续按源码审计 C-15 时发现，脚本和 `.env.example` 已删除 `10.98.90.154` 默认值，但客户端首装包生成的后端校验提示和 Cloud 前端 placeholder 仍显示 `http://10.98.90.154:81`。这不是标明的历史校准记录，而是可复制的运行时提示，和“真实 IP 不作为默认生产值”不一致。
- 影响面：首装包云端地址校验失败提示和前端输入占位现在使用 `http://cloud-host:81` 泛化示例；真实输入仍可填写受控内网 HTTP Gateway origin，校验规则未改变。ConfigurationGuardTests 增加守卫，防止运行时提示和 UI placeholder 回退到现场 IP。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：运行时错误提示、UI placeholder、脚本默认值、示例 `.env` 和 workflow 默认值不得硬编码真实现场 IP；测试用例和标明“现场记录，不是模板默认值”的历史校准记录可以保留。
- 验证命令：
  - `! rg -n "10\\.98\\.90\\.154" src/services/IIoT.ProductionService/ClientReleases/EdgeInstallerPublicBaseUrl.cs src/ui/iiot-web/src/features/client-releases/EdgeBindingDownloadPanel.vue`
  - `rg -n "http://cloud-host:81" src/services/IIoT.ProductionService/ClientReleases/EdgeInstallerPublicBaseUrl.cs src/ui/iiot-web/src/features/client-releases/EdgeBindingDownloadPanel.vue src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `npm run build`（`src/ui/iiot-web`）
  - `git diff --check && git diff --cached --check`
- 验证结果：运行时提示和前端 placeholder 中已无 `10.98.90.154`；`http://cloud-host:81` 泛化示例可检索；ConfigurationGuardTests 76/76 通过；Cloud 前端 `npm run build` 通过，Vite 仅输出依赖包 pure annotation 提示；`git diff --check && git diff --cached --check` 通过。

## 2026-07-06 云端 C-06 Edge 更新子目录权限探针补齐

- 改动范围：`deploy/scripts/check-container-nonroot-readiness.sh`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时业务代码、compose、EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续审计 C-06 非 root 生产关闭条件时发现，文档要求核对 `EDGE_UPDATES_DIR` 下既有 `installers`、`plugins`、`velopack` 子目录权限，但 readiness 脚本只检查根目录。历史发布目录如果由 root/runner 创建，根目录可写也不能保证 HttpApi 能写入 installer、plugin 或 Velopack 静态文件。
- 影响面：readiness 探针现在会在根目录通过基础检查后逐项检查既有 `installers`、`plugins`、`velopack` 子目录。缺失子目录输出 `nonroot_readiness_edge_updates_subdir_<name>=missing` 且不阻断空发布目录；既有子目录不可被目标 UID/GID 写入或带 world write bit 时输出 `failed` 并 exit 2，发布前 fail-fast。成功场景输出 `checked`，方便生产验收留痕。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端架构治理清单.md` 和 `docs/云端容器非Root权限契约.md`：Cloud 容器非 root 发布前不仅要验证 `EDGE_UPDATES_DIR` 根目录，还要验证既有 `installers/plugins/velopack` 子目录权限；缺失子目录是空目录诊断，既有子目录权限不满足必须阻断发布。
- 验证命令：
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（既有 `installers/plugins/velopack` 均 `750`，期望通过）
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（`plugins` chmod `500`，期望 exit 2 和 `nonroot_readiness_edge_updates_subdir_plugins=failed`）
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（`velopack` chmod `772`，期望 exit 2 和 `must not be world-writable`）
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（空 `EDGE_UPDATES_DIR`，期望 missing 诊断且通过）
- 验证结果：脚本语法检查通过；ConfigurationGuardTests 76/76 通过；既有三类子目录 `750` 时 readiness 输出三条 `checked` 并通过；`plugins` 不可写时 exit 2 并输出 `nonroot_readiness_edge_updates_subdir_plugins=failed`；`velopack` world-writable 时 exit 2 并输出 `nonroot_readiness_edge_updates_subdir_velopack=failed`；空发布目录输出三条 `missing` 并通过。

## 2026-07-06 云端 Edge catalog gate 补齐 Velopack 静态下载

- 改动范围：`deploy/scripts/verify-edge-installer-catalog.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时业务代码、compose、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：上一轮已把 `post-deploy-check.sh` 接到 `verify-edge-installer-catalog.sh`，但继续审计发现该脚本只验证 public catalog、installer artifact 和 installer stub，没有真实访问 `/edge-updates/velopack/<channel>/RELEASES`、`releases.<channel>.json`、`assets.<channel>.json` 或 `.nupkg`。这和 C-06 生产关闭条件里的“Velopack 静态下载”不一致。
- 影响面：`verify-edge-installer-catalog.sh` 现在会下载 Velopack `RELEASES`、channel releases/assets manifest，从这些文件中解析一个 `.nupkg` URL，并对该 `.nupkg` 执行 HEAD Content-Length 和 GET 非零字节检查。`post-deploy-check.sh` 开启 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1` 后会覆盖 public catalog、installer artifact、installer stub、Velopack manifest 和 `.nupkg` 静态下载。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端架构治理清单.md` 和 `docs/云端容器非Root权限契约.md`：关闭 Cloud 容器非 root 生产验收或验收 Edge 安装/更新发布时，必须证明 Velopack `RELEASES` / channel manifests / `.nupkg` 静态下载链路可用。
- 验证命令：
  - `sh -n deploy/scripts/verify-edge-installer-catalog.sh && sh -n deploy/scripts/post-deploy-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `BASE_URL=http://127.0.0.1:<temp-port> CHANNEL=stable TARGET_RUNTIME=win-x64 EXPECTED_VERSION=1.2.0 deploy/scripts/verify-edge-installer-catalog.sh`（临时 HTTP 目录模拟 public catalog、installer artifact、installer stub、Velopack `RELEASES`、channel manifests 和 `.nupkg`）
  - `rg -n "Velopack RELEASES|releases\\.\\$CHANNEL\\.json|assets\\.\\$CHANNEL\\.json|No Velopack \\.nupkg URL found|check_get_download \"\\$velopack_nupkg_url\"|Velopack `RELEASES`|channel manifests" deploy/scripts/verify-edge-installer-catalog.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端架构治理清单.md docs/云端容器非Root权限契约.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 验证结果：脚本语法检查通过；ConfigurationGuardTests 76/76 通过；临时 HTTP 集成验证通过，输出了 `Checking Velopack RELEASES`、`Checking Velopack releases manifest`、`Checking Velopack assets manifest`、`Checking Velopack package URL` 和 `Edge installer catalog verification passed`；Velopack gate 规则和测试守卫可检索。

## 2026-07-06 云端 post-deploy Edge catalog gate 补强

- 改动范围：`deploy/scripts/post-deploy-check.sh`、`deploy/OPERATIONS.md`、`deploy/README.md`、`docs/云端规则.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时业务代码、compose、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续审计 C-06 生产关闭条件时，清单要求验证 public download catalog、Velopack 静态下载和 Edge installer/plugin 上传链路，但 `post-deploy-check.sh` 只检查服务运行、首页和 `/internal/healthz`，容易把“Cloud-only 冒烟通过”误当成“Edge 下载链路也通过”。
- 影响面：`post-deploy-check.sh` 默认仍只做 Cloud-only 冒烟并打印 `post_deploy_edge_installer_catalog=skipped`，避免无 Edge release 的纯 Cloud 部署被误阻断；当设置 `POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG=1` 时，会以本机 `public_base_url` 调用 `verify-edge-installer-catalog.sh`，验证 public catalog、installer artifact 和 installer stub 静态下载。可选变量包括 `POST_DEPLOY_EDGE_CHANNEL`、`POST_DEPLOY_EDGE_TARGET_RUNTIME`、`POST_DEPLOY_EDGE_EXPECTED_VERSION`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`、`docs/云端架构治理清单.md` 和 `docs/云端容器非Root权限契约.md`：关闭 Cloud 容器非 root 生产验收或验收 Edge 安装/更新发布时，不得只依赖 `/internal/healthz`，必须开启 post-deploy Edge catalog gate 或运行等价 catalog 验证。
- 验证命令：
  - `sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "POST_DEPLOY_VERIFY_EDGE_INSTALLER_CATALOG|POST_DEPLOY_EDGE_CHANNEL|POST_DEPLOY_EDGE_TARGET_RUNTIME|POST_DEPLOY_EDGE_EXPECTED_VERSION|verify-edge-installer-catalog.sh|public catalog" deploy/scripts/post-deploy-check.sh deploy/OPERATIONS.md deploy/README.md docs/云端规则.md docs/云端架构治理清单.md docs/云端容器非Root权限契约.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
- 验证结果：post-deploy 和 catalog 验证脚本语法检查通过；ConfigurationGuardTests 76/76 通过；显式 Edge catalog gate、文档规则和测试守卫均可检索。

## 2026-07-06 云端部署入口脚本 executable 位收口

- 改动范围：`deploy/scripts/*.sh` 的入口脚本 executable 文件模式、`src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`、`docs/云端规则.md`、`docs/云端架构治理清单.md` 和本复盘。未修改 Cloud 运行时业务代码、compose、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续审计真实发布链路时确认 `deploy-release.sh`、`pre-deploy-check.sh`、`post-deploy-check.sh`、`ops-check.sh` 等脚本会被文档、workflow 或其它脚本以 `./scripts/<name>.sh` / `"$SCRIPT_DIR/<name>.sh"` 直接执行，但 git 文件模式仍是 `100644`。生产服务器 checkout 后会出现 permission denied，导致发布链路在 preflight、backup、post-deploy 或 cleanup 阶段失败。
- 影响面：直接执行的运维入口脚本已调整为 executable；`release-common.sh` 保持非 executable，只作为 source 共享库。由于本机 `core.filemode=false`，已使用 `git update-index --chmod=+x` 显式记录 tracked 脚本的 `100755` 模式；新增 `check-container-nonroot-readiness.sh` 已设置 executable。ConfigurationGuardTests 新增 Unix 文件模式守卫，防止入口脚本回退为不可执行。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端架构治理清单.md`：`deploy/scripts` 中被直接执行的运维入口脚本必须在 git 中保持 executable 位；`release-common.sh` 只作为 source 共享库，不作为入口脚本。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh && sh -n deploy/scripts/check-container-nonroot-readiness.sh && sh -n deploy/scripts/deploy-release.sh && sh -n deploy/scripts/ensure-oidc-signing-cert.sh && sh -n deploy/scripts/mirror-third-party-images.sh && sh -n deploy/scripts/ops-check.sh && sh -n deploy/scripts/post-deploy-check.sh && sh -n deploy/scripts/postgres-backup.sh && sh -n deploy/scripts/postgres-restore.sh && sh -n deploy/scripts/postgres-verify-backup.sh && sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/rollback-release.sh && sh -n deploy/scripts/verify-edge-installer-catalog.sh && bash -n deploy/scripts/build-and-push.sh && bash -n deploy/scripts/harbor-retention.sh && bash -n deploy/scripts/local-release.sh && bash -n deploy/scripts/post-release-cleanup.sh`
  - `test -x deploy/scripts/build-and-push.sh && test -x deploy/scripts/check-container-nonroot-readiness.sh && test -x deploy/scripts/deploy-release.sh && test -x deploy/scripts/ensure-oidc-signing-cert.sh && test -x deploy/scripts/harbor-retention.sh && test -x deploy/scripts/local-release.sh && test -x deploy/scripts/mirror-third-party-images.sh && test -x deploy/scripts/ops-check.sh && test -x deploy/scripts/post-deploy-check.sh && test -x deploy/scripts/post-release-cleanup.sh && test -x deploy/scripts/postgres-backup.sh && test -x deploy/scripts/postgres-restore.sh && test -x deploy/scripts/postgres-verify-backup.sh && test -x deploy/scripts/pre-deploy-check.sh && test -x deploy/scripts/rollback-release.sh && test -x deploy/scripts/verify-edge-installer-catalog.sh && test ! -x deploy/scripts/release-common.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
- 验证结果：POSIX shell 脚本 `sh -n` 通过，bash 脚本 `bash -n` 通过；直接执行入口脚本均具备 executable 位，`release-common.sh` 不可执行；ConfigurationGuardTests 76/76 通过，未再出现 CA1416 warning。

## 2026-07-06 云端 C-06 world-writable 权限位判定修正

- 改动范围：`deploy/scripts/check-container-nonroot-readiness.sh`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时业务代码、Dockerfile、compose、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续审计 C-06 readiness 探针时发现 `world-writable` 判断复用了目录“可写且可进入”的 `digit_has_write_execute`，这会漏掉 other 只有 write bit 的模式，例如 `772`。虽然这种目录权限不一定可实际创建文件，但从安全语义上仍属于 world write bit，不应通过 `EDGE_UPDATES_DIR` 的生产权限探针。
- 影响面：新增 `digit_has_write`，`EDGE_UPDATES_DIR` 的 world-writable 阻断和日志卷 `world_writable` 诊断现在只要 other 有 write bit 就判定为 true；容器自身目录可写判断仍保持写+执行要求，避免把不可进入目录误判为可写。
- 规则提炼：无新增长期规则，原因是 `EDGE_UPDATES_DIR` 不得 world-writable、readiness 探针失败不得发布已经写入 `docs/云端容器非Root权限契约.md` 和 `docs/云端规则.md`；本次只是修正权限位实现。
- 验证命令：
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（`EDGE_UPDATES_DIR` chmod `772` 负向探针，期望 exit 2）
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`（`EDGE_UPDATES_DIR` chmod `750` 正向探针）
- 验证结果：shell 语法检查通过；ConfigurationGuardTests 75/75 通过；`EDGE_UPDATES_DIR` chmod `772` 时 readiness 输出 `must not be world-writable` 并按预期阻断；chmod `750` 时输出 `nonroot_readiness=passed`。

## 2026-07-06 云端 C-06 日志卷 readiness 诊断补强

- 改动范围：`deploy/scripts/check-container-nonroot-readiness.sh`、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md`、ConfigurationGuardTests 和本复盘。未修改 Cloud 运行时业务代码、Dockerfile `USER`、compose 端口映射、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续审计完整云端修复计划时发现 C-06 契约写了生产 readiness 需要覆盖日志卷，但脚本只检查了 OIDC PFX、Edge 更新目录和 nginx 非特权端口。由于文件日志已被设计为非阻断能力，正确补法不是让日志卷权限阻断发布，而是输出可见诊断，供生产验收判断是否需要调整卷权限。
- 影响面：readiness 探针现在输出 `nonroot_readiness_log_volume_httpapi_logs` 和 `nonroot_readiness_log_volume_dataworker_logs`。卷未创建、Docker Desktop 宿主侧 mountpoint 不可访问或不可写时只输出诊断，不设置阻断失败；PFX 不可读、Edge 更新目录不可写、nginx 回退容器内 80 仍然 fail-fast。
- 规则提炼：无新增长期规则，原因是“文件日志不可阻断启动”和“C-06 必须通过 readiness 探针输出生产权限事实”已经写入 `docs/云端规则.md` 与 `docs/云端容器非Root权限契约.md`；本次是补齐探针输出和验收口径。
- 验证命令：
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "nonroot_readiness_log_volume_httpapi_logs|nonroot_readiness_log_volume_dataworker_logs|log_volume_writability_diagnostic|该诊断不得替代应用启动验收" deploy/scripts/check-container-nonroot-readiness.sh docs/云端容器非Root权限契约.md docs/云端架构治理清单.md src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`
- 验证结果：shell 语法检查通过；ConfigurationGuardTests 75/75 通过；日志卷诊断关键词可检索；临时 PFX/Edge 更新目录探针通过并输出 `nonroot_readiness_log_volume_httpapi_logs=not-created`、`nonroot_readiness_log_volume_dataworker_logs=not-created`、`nonroot_readiness_nginx_internal_port=8080` 和 `nonroot_readiness=passed`。

## 2026-07-06 云端 Swagger/OpenAPI 依赖收敛

- 改动范围：`src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj`、`docs/云端规则.md` 和本复盘。未修改 Cloud 运行时业务代码、部署脚本、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：完整云端治理计划宽范围验证后仍遗留 `Microsoft.OpenApi` 2.3.0/2.7.5 MSB3277 warning。追溯 `project.assets.json` 后确认来源是 `Swashbuckle.AspNetCore.Swagger 10.1.0` 依赖 `Microsoft.OpenApi 2.3.0`，而 Cloud 直接引用 `Microsoft.OpenApi 2.7.5` 是 2026-07-02 为规避 `2.3.0` 高危漏洞而落下的安全修复，不能通过降级消警告。
- 影响面：只把 HttpApi 的 `Swashbuckle.AspNetCore` 从 `10.1.0` 升到 `10.2.3`，使 Swagger/SwaggerGen/SwaggerUI 子包统一到 `10.2.3`，且 `Swashbuckle.AspNetCore.Swagger 10.2.3` 依赖 `Microsoft.OpenApi 2.7.5`。未升级 `Microsoft.OpenApi` 到历史已验证不兼容的 3.x。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：修复 NuGet 版本冲突必须先追溯依赖图和历史复盘，不得为了消 warning 回退到已知漏洞版本或历史已验证不兼容版本；`Microsoft.OpenApi` 不得降级到 `2.3.0`，也不得未验证 source generator 兼容就升到 3.x。
- 验证命令：
  - `dotnet restore IIoT.CloudPlatform.slnx`
  - `dotnet list src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj package --include-transitive | rg "Swashbuckle|Microsoft.OpenApi"`
  - `dotnet list src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj package --vulnerable --include-transitive`
  - `rg -n "Swashbuckle.AspNetCore/(10|[0-9])|Swashbuckle.AspNetCore.Swagger/[0-9]|\"Microsoft.OpenApi\":" src/hosts/IIoT.HttpApi/obj/project.assets.json`
  - `dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
- 验证结果：HttpApi 包清单显示 `Microsoft.OpenApi 2.7.5`、`Swashbuckle.AspNetCore 10.2.3`、Swagger 子包 `10.2.3`；漏洞扫描显示 HttpApi 无 vulnerable package；assets 显示 `Swashbuckle.AspNetCore.Swagger 10.2.3` 依赖 `Microsoft.OpenApi 2.7.5`；Release build 0 warning / 0 error；ConfigurationGuardTests 75/75 通过。

## 2026-07-06 云端治理计划宽范围验证与 EndToEnd 启动超时

- 改动范围：`src/tests/IIoT.EndToEndTests/IIoTAppFixture.cs`、ConfigurationGuardTests、`docs/云端规则.md` 和本复盘。未修改 Cloud 运行时代码、部署脚本、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续执行完整云端修复计划的完成度审计时，`CloudProductionFlowTests|RateLimitingConfigurationGuardTests` 组合过滤在本机超过三分钟无输出，问题集中在 Aspire 端到端环境启动反馈不足。需要让重型 E2E 启动有明确超时，避免后续把“无限等待”误判为通过或业务失败。
- 影响面：`IIoTAppFixture.StartAsync` 现在用 3 分钟总启动超时包住 Aspire builder、Build、Start 和 Postgres/RabbitMQ/HttpApi/Gateway/DataWorker 就绪等待；超时会抛出明确的 `TimeoutException`，提示检查 Docker 和相关资源。配置守卫已固定该超时和错误信息。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：Aspire 端到端测试启动必须有明确超时和错误信息；本机冒烟优先跑窄过滤，例如 `CloudProductionFlowTests.InternalHealthz_ShouldReturnOk`。
- 验证命令：
  - `dotnet build IIoT.CloudPlatform.slnx -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -c Release --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -c Release --no-restore --disable-build-servers`
  - `npm run build`（`src/ui/iiot-web`）
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~RateLimitingConfigurationGuardTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj -c Release --filter FullyQualifiedName~CloudProductionFlowTests.InternalHealthz_ShouldReturnOk --no-restore --disable-build-servers`
  - `git diff --check`
- 验证结果：Release build 通过；ServiceLayer.Tests 226/226 通过；ProductionService.Tests 41/41 通过；前端 build 通过；ConfigurationGuardTests 75/75 通过；RateLimitingConfigurationGuardTests 3/3 通过；最小 CloudProductionFlow healthz 1/1 通过，耗时 41 秒。组合过滤 `CloudProductionFlowTests|RateLimitingConfigurationGuardTests` 在加超时前超过三分钟无输出，已手动取消，不计为通过。上述 .NET 命令仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端容器非 root 代码和发布门禁收口

- 改动范围：`src/hosts/IIoT.HttpApi/Dockerfile`、`deploy/scripts/pre-deploy-check.sh`、`deploy/nginx/nginx.conf`、`deploy/docker-compose.prod.yml`、`src/hosts/IIoT.AppHost/iiot-web.Dockerfile`、`src/hosts/IIoT.AppHost/iiot-web.nginx.conf`、`deploy/scripts/check-container-nonroot-readiness.sh`、ConfigurationGuardTests、`docs/云端规则.md`、`docs/云端架构治理清单.md`、`docs/云端容器非Root权限契约.md` 和本复盘。未修改 EdgeClient、AICopilot 或 HTTPS/TLS/443/HSTS 配置。
- 改动原因：继续执行 C-06。上一轮已把 Gateway/DataWorker/MigrationWorkApp 切到非 root，但 nginx/web 仍靠容器内 `80`，HttpApi 仍以 root 运行；如果只切 HttpApi 不加发布前权限探针，生产 PFX 和 Edge 更新目录权限问题会变成上线后故障，因此必须同时接入 readiness gate。
- 影响面：外部仍使用 HTTP 和 `${GATEWAY_HTTP_PORT}`；`iiot-httpapi` 镜像创建固定 `10001:10001` 业务用户并设置 `USER ${APP_UID}:${APP_GID}`；`pre-deploy-check.sh` 在 compose config 后调用 `check-container-nonroot-readiness.sh`，PFX 不可读、Edge 更新目录不可写或 nginx 内部端口回退时会 fail-fast；`nginx-gateway` 容器内部改监听 `8080`，compose 改为 `${GATEWAY_HTTP_PORT:-80}:8080` 并指定 `NGINX_UID/GID`；`iiot-web` 静态 nginx 改监听 `8080`、设置 `/tmp` pid/temp 目录并切换 `USER 101:101`；gateway nginx 的 web 代理改为 `http://iiot-web:8080`。readiness 探针改为 GNU/BSD `stat` 兼容，并修正 `8080` 被误判为 `80` 的前缀匹配问题。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端容器非Root权限契约.md`：nginx 容器必须使用非特权内部端口承载 HTTP，`iiot-web` / `nginx-gateway` 不得靠容器内 `80` 或 root 绑定低端口运行；Cloud 生产发布前必须运行容器非 root readiness 探针。C-06 的代码和发布前门禁已落地，但生产状态仍需真实容器启动、OIDC 和 Edge 上传/下载验收后关闭。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh && sh -n deploy/scripts/pre-deploy-check.sh && sh -n deploy/scripts/local-release.sh`
  - `COMPOSE_ENV_FILE=<temp-env> CLOUD_CONTAINER_UID=$(id -u) CLOUD_CONTAINER_GID=$(id -g) sh deploy/scripts/check-container-nonroot-readiness.sh`
  - `git diff --check`
- 验证结果：ConfigurationGuardTests 75/75 通过；compose config 通过；三个 shell 脚本语法检查通过；临时 PFX/Edge 更新目录权限探针输出 `nonroot_readiness_nginx_internal_port=8080` 和 `nonroot_readiness=passed`；`git diff --check` 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端后端非 root 容器第一步落地

- 改动范围：`IIoT.Gateway`、`IIoT.DataWorker`、`IIoT.MigrationWorkApp` Dockerfile、ConfigurationGuardTests、`docs/云端容器非Root权限契约.md`、`docs/云端架构治理清单.md` 和本复盘。未切换 `IIoT.HttpApi`、`iiot-web`、`nginx-gateway` 运行用户，未修改 EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续执行 C-06。Gateway/DataWorker/MigrationWorkApp 不依赖 `/app/certs` 和 `/app/edge-updates` 写权限，可以先创建固定非 root 用户并设置 `USER ${APP_UID}:${APP_GID}`；HttpApi 和 nginx/web 仍需要生产权限事实和非特权端口方案。
- 影响面：三个后端容器的发布文件复制为 `10001:10001`，默认以非 root 运行；Gateway 仍监听 8080，DataWorker healthcheck 仍走 `dotnet IIoT.DataWorker.dll --healthcheck`，MigrationWorkApp 保持 one-shot。文件日志目录不可写时已由上一条复盘改为非阻断。
- 规则提炼：无新增长期规则，原因是容器非 root 权限模型已经写入 `docs/云端容器非Root权限契约.md`；本次是按契约先落地不依赖宿主写目录的三个 .NET 容器。C-06 仍不得标为完成，直到 HttpApi、web/nginx 和真实容器启动验收通过。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "USER \\$\\{APP_UID\\}:\\$\\{APP_GID\\}|ARG APP_UID=10001|groupadd --gid" src/hosts/IIoT.Gateway/Dockerfile src/hosts/IIoT.DataWorker/Dockerfile src/hosts/IIoT.MigrationWorkApp/Dockerfile`
  - `git diff --check`
- 验证结果：ConfigurationGuardTests 75/75 通过；Gateway/DataWorker/MigrationWorkApp Dockerfile 的 `APP_UID`、`APP_GID`、`groupadd` 和 `USER ${APP_UID}:${APP_GID}` 均可检索；`git diff --check` 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端非 root 日志目录启动保护

- 改动范围：`IIoT.Infrastructure` Serilog 配置、InfrastructureBehaviorTests、`docs/云端容器非Root权限契约.md` 和本复盘。未修改 Dockerfile `USER`、compose 运行用户、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续推进 C-06 时确认 `/app/logs` 命名卷可能由 Docker 以 root owner 初始化，直接切非 root 可能让 Serilog 文件 sink 在启动阶段失败。文件日志不是业务启动硬依赖，不能阻断 Cloud 服务启动。
- 影响面：Cloud 服务仍保留 console 和 Seq 日志；`/app/logs` 可写时继续启用滚动文件日志；不可写时向 stderr 输出文件日志禁用原因并继续启动。C-06 仍保持阻塞待实施，原因是 PFX、Edge 更新目录和 nginx 非特权端口仍需生产权限事实。
- 规则提炼：形成长期规则，已写入 `docs/云端容器非Root权限契约.md`：文件日志是非阻断能力，`/app/logs` 不可写时不得导致 Cloud 服务 fatal。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `rg -n "TryEnsureWritableDirectory|IIoT file logging disabled|文件日志是非阻断能力|/app/logs 不可写" src docs -g '*.cs' -g '*.md'`
  - `git diff --check`
- 验证结果：InfrastructureBehaviorTests 39/39 通过；日志目录可写探针、文件日志禁用提示和非阻断日志规则均可检索；`git diff --check` 通过。

## 2026-07-06 云端容器非 root 权限契约和探针补强

- 改动范围：新增 `docs/云端容器非Root权限契约.md` 和 `deploy/scripts/check-container-nonroot-readiness.sh`，补 ConfigurationGuardTests，更新云端治理清单和本复盘。未修改 Dockerfile `USER`、compose 运行用户、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：继续推进 C-06 时确认非 root 切换不能只靠 Dockerfile 加 `USER`。当前 HttpApi 需要读 `/app/certs` PFX 并写 `/app/edge-updates`，web/nginx 当前容器内监听 `80`，直接非 root 会破坏 OIDC、Edge 更新上传或 nginx 启动。
- 影响面：C-06 仍保持“阻塞待实施”，但阻塞条件、目标权限模型和生产权限探针已专题化：必须明确生产 UID/GID、PFX 权限、Edge 更新目录权限、日志卷权限、nginx 非特权端口或 unprivileged 镜像方案，并通过真实容器启动和 Edge 上传/OIDC/migration 验证后才能把 C-06 改为已实施。
- 规则提炼：形成长期规则，已写入 `docs/云端容器非Root权限契约.md`：C-06 不得直接加 `USER` 后宣布完成；必须先解决 OIDC PFX、Edge 更新目录、logs、nginx 低端口和 migration 权限，并先运行 `check-container-nonroot-readiness.sh` 获取生产权限事实。同步更新 `docs/云端架构治理清单.md`。
- 验证命令：
  - `sh -n deploy/scripts/check-container-nonroot-readiness.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "云端容器非Root权限契约|check-container-nonroot-readiness|状态：阻塞待实施|PFX 文件|EDGE_UPDATES_DIR|非特权端口" docs deploy src/tests -g '*.md' -g '*.sh' -g '*.cs'`
  - `git diff --check`
- 验证结果：`check-container-nonroot-readiness.sh` 语法检查通过；ConfigurationGuardTests 75/75 通过；云端容器非Root权限契约、权限探针、C-06 阻塞状态、PFX、`EDGE_UPDATES_DIR` 和非特权端口要求均可检索；`git diff --check` 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端治理清单未决项收口

- 改动范围：更新 `docs/云端架构治理清单.md` 和本复盘。未修改运行时代码、部署脚本、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：执行完整 Cloud 治理计划收口时，除 C-06 外各项已落地或已有实现补强。C-06 容器非 root 不能继续以“待实施”空挂，否则后续容易被误判为遗漏；必须明确阻塞原因和需要的生产权限输入。
- 影响面：C-06 明确为“阻塞待实施”，原因是需要确认生产宿主 UID/GID、`/app/edge-updates` 可写目录、`/app/certs` 只读证书目录、日志目录和 one-shot migration 文件权限方案。C-07 保持“部分实施”，因为 Dockerfile root / 容器非 root 守卫必须等 C-06 方案明确后再落地。文档沉淀区改为反映 Edge/AI/DeviceClientState 三个专题契约已新增。
- 规则提炼：无新增长期规则，原因是容器非 root 仍未形成可执行权限契约；本次只把阻塞条件写入 `docs/云端架构治理清单.md`，避免把未验证的 `USER` 修改当成完成。
- 验证命令：
  - `rg -n "C-06 Cloud 容器非 root|状态：阻塞待实施|C-07 编译期和架构门禁|容器非 root 权限契约" docs/云端架构治理清单.md`
  - `git diff --check`
- 验证结果：C-06 阻塞状态、C-07 部分实施状态、后续容器非 root 权限契约要求均可检索；`git diff --check` 通过。

## 2026-07-06 云端发布 preflight 和 ops-check 补强

- 改动范围：`deploy/scripts/release-common.sh`、`deploy/scripts/pre-deploy-check.sh`、`deploy/OPERATIONS.md`、ConfigurationGuardTests、云端治理清单和本复盘。未修改 `ops-check.sh` 的 HTTP 本机健康检查，未修改 EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-17。当前 HTTP-only 生产基线需要把补偿控制前置到发布 preflight，避免靠人工记忆检查 secret、OIDC HTTP 边界、root 执行、Docker Hub、磁盘和旧 bootstrap 关闭变量。
- 影响面：`pre-deploy-check.sh` 现在默认拒绝 root 执行，除非设置 `ALLOW_ROOT_DEPLOY_PREFLIGHT=emergency`；拒绝 `BOOTSTRAP_AUTH_REQUIRE_SECRET=false` / `BootstrapAuth__RequireSecret=false`；验证 HTTP OIDC 只能使用 loopback/RFC1918 IPv4；检查磁盘使用率并在例行发布阻断阈值失败；继续检查 secret 模板、公共模板值、Docker Hub 依赖、compose config、OIDC signing cert 和已有 runtime ops-check；preflight 输出 HTTP-only 基线和补偿控制摘要。
- 规则提炼：形成长期规则，已写入 `deploy/OPERATIONS.md` 和 `docs/云端架构治理清单.md`：Cloud preflight 是 HTTP-only 补偿控制门禁，必须输出 `preflight_transport_baseline=http-only` 和补偿控制摘要；不得把 `/internal/healthz` 改成 HTTPS。同步更新 C-17 状态。
- 验证命令：
  - `sh -n deploy/scripts/release-common.sh`
  - `sh -n deploy/scripts/pre-deploy-check.sh`
  - `sh -n deploy/scripts/ops-check.sh`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "preflight_transport_baseline=http-only|ensure_oidc_http_boundary|ensure_bootstrap_secret_not_disabled|ensure_deploy_disk_headroom|ALLOW_ROOT_DEPLOY_PREFLIGHT" deploy src docs -g '*.sh' -g '*.cs' -g '*.md'`
- 验证结果：`release-common.sh`、`pre-deploy-check.sh`、`ops-check.sh` 语法检查通过；ConfigurationGuardTests 74/74 通过；loopback/RFC1918 helper 正负向检查通过；preflight HTTP-only 摘要、OIDC HTTP 边界、bootstrap secret 关闭变量拦截、磁盘检查和 root preflight 例外口径均可检索；`git diff --check` 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端 HTTP OIDC 内网边界收口

- 改动范围：Cloud OIDC provider 行为测试、`docs/云端安全部署契约.md`、云端治理清单和本复盘。运行时代码已具备 loopback/RFC1918 HTTP OIDC 判断，本次未修改 OIDC 运行时代码、AICopilot、EdgeClient 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-16。当前计划必须保留内网 HTTP，但 HTTP OIDC 不能泛化到公网 IP、普通域名或未知 host；原安全部署契约文字把“内网域名”也写进 OIDC HTTP 允许范围，和运行时代码不一致。
- 影响面：`ALLOW_INTRANET_HTTP_OIDC=true` 只允许 loopback 或 RFC1918 私网 IPv4；公网 IP、普通域名、模板域名和未知 host 的 HTTP OIDC 都会被拒绝。`PUBLIC_BASE_URL` 和 Edge 下载地址仍可使用受控内网域名，但 OIDC issuer/redirect 在 HTTP 模式下必须替换成私网 IP。
- 规则提炼：形成长期规则，已写入 `docs/云端安全部署契约.md`：HTTP OIDC 例外只允许 loopback/RFC1918 IPv4，不允许公网 IP、普通域名、模板域名或未知 host。同步更新 `docs/云端架构治理清单.md` C-16 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~CloudOidcProviderBehaviorTests --no-restore --disable-build-servers`
  - `rg -n "ALLOW_INTRANET_HTTP_OIDC|RFC1918|普通域名|公网 IP" src docs -g '*.cs' -g '*.md'`
- 验证结果：CloudOidcProviderBehaviorTests 23/23 通过；`ALLOW_INTRANET_HTTP_OIDC`、RFC1918、普通域名和公网 IP 的规则口径均可检索；`git diff --check` 通过。

## 2026-07-06 云端 AI 生产数据只读入口契约收口

- 改动范围：新增 `docs/AI只读接口契约.md`，补 ConfigurationGuardTests，更新云端治理清单和本复盘。运行时代码只做审计确认，未修改 `AiReadController`、`AiReadQueries`、AICopilot、EdgeClient 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-14。现有代码已经有 `GET /api/v1/ai/read/production-records`、`AiReadPermissions.ProductionRecord`、行数限制、时间窗、delegated device scope 和 schema 字段过滤，但缺少当前治理清单要求的专题契约与端到端配置守卫。
- 影响面：`GET /api/v1/ai/read/production-records` 继续是 AI 生产数据唯一只读入口；`AiReadController` 保持 GET-only 表面；所有 `GetAiRead*Query` 必须带 `AuthorizeAiRead` 并实现 `IAiReadQuery<>`；生产数据不返回 raw `payload_jsonb`，也不恢复 AI 专用 `pass-stations/{typeKey}` 旧语义入口。
- 规则提炼：无新增长期规则，原因是 AI Read 只读边界、`production-records` 唯一入口、禁止 raw `payload_jsonb` 和禁止 AI 专用 `pass-stations/{typeKey}` 已写入 `docs/云端规则.md`；本次是新增专题契约并用 ConfigurationGuardTests 固化。同步更新 `docs/云端架构治理清单.md` C-14 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "production-records|AI只读接口契约|AiReadPermissions\\.ProductionRecord|pass-stations/\\{typeKey\\}" src docs -g '*.cs' -g '*.md'`
- 验证结果：AiReadBehaviorTests 25/25 通过；InfrastructureBehaviorTests 39/39 通过；ConfigurationGuardTests 74/74 通过；production-records、AI只读接口契约、`AiReadPermissions.ProductionRecord`、旧 pass-station 禁用语义、专题契约和治理清单状态均可检索；`git diff --check` 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端设备客户端状态投影契约收口

- 改动范围：新增 `docs/设备客户端状态投影契约.md`，补 ConfigurationGuardTests，更新云端治理清单和本复盘。运行时代码只做审计确认，未修改版本上报、运行心跳、Human inventory、AI Read、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-13。现有代码已经有 `DeviceClientState` 官方投影、`IDeviceClientStateStore`、版本上报同步投影、运行心跳同步投影、Human/AI 读取投影，但缺少独立契约和可执行守卫。
- 影响面：`DeviceClientState` 继续作为客户端状态官方投影；版本上报必须同步 `ApplyVersionReport`，运行心跳必须同步 `ApplyRuntimeHeartbeat`；Human 设备客户端状态清单和 AI Read 设备客户端状态继续通过 `IDeviceClientStateStore.GetStatesByDevicesAsync` 读取，不临时拼接 snapshot/heartbeat 重新解释状态。
- 规则提炼：无新增长期规则，原因是 `DeviceClientState` 官方投影和禁止临时拼接状态已经写入 `docs/云端规则.md`；本次是新增专题契约并用 ConfigurationGuardTests 固化。同步更新 `docs/云端架构治理清单.md` C-13 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~ClientReleaseBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "DeviceClientState|IDeviceClientStateStore|device-client-states|设备客户端状态投影契约" src docs`
- 验证结果：ClientReleaseBehaviorTests 24/24 通过；AiReadBehaviorTests 25/25 通过；ConfigurationGuardTests 73/73 通过；`DeviceClientState`、`IDeviceClientStateStore`、AI Read 设备客户端状态、专题契约和治理清单状态均可检索。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端 Edge 上传与 PLC 状态接口契约收口

- 改动范围：新增 `docs/Edge上传与PLC状态接口契约.md`，补 ConfigurationGuardTests，更新云端治理清单和本复盘。运行时代码只做审计确认，未修改 Edge PLC runtime state Controller、命令、查询、store、EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-12。现有代码已经有 Edge 设备身份写入口、Human 只读查询和 ProductionService 行为测试，但缺少专题契约和防止写入口漂移到 Human/AI/Public 的配置守卫。
- 影响面：`POST /api/v1/edge/edge-hosts/plc-runtime-states` 继续是唯一 PLC runtime state 写入口，必须走 `RequireEdgeDeviceToken`、`EdgeHostPlcStateUpload` 限流和 `IDeviceCommand`；Human 端只能通过 `GET /api/v1/human/edge-hosts/{id}/plc-runtime-states` 读取，PLC 产能汇总继续要求 `EdgeHost.Read + Device.Read` 和设备授权过滤；AI Read 不得写 PLC runtime state。
- 规则提炼：无新增长期规则，原因是 PLC runtime state 写入边界、Human 只读、AI/Public 不得写已经在 `docs/云端规则.md`；本次是新增专题契约并用 ConfigurationGuardTests 固化。同步更新 `docs/云端架构治理清单.md` C-12 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~EdgeHostBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "plc-runtime-states|EdgeHostPlcRuntimeState|IEdgeHostPlcRuntimeStateStore" src docs`
- 验证结果：EdgeHostBehaviorTests 12/12 通过；ConfigurationGuardTests 72/72 通过；PLC runtime state 写入口、Human 只读查询、专用 store、专题契约和治理清单状态均可检索。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端 nginx 响应压缩边界收口

- 改动范围：Cloud nginx 部署模板、ConfigurationGuardTests、云端规则和云端治理清单。仅在 `deploy/nginx/nginx.conf` 开启 nginx gzip；未在 HttpApi 增加 `AddResponseCompression` / `UseResponseCompression`；未修改 EdgeClient、AICopilot 或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-11。原 nginx 和 HttpApi 都没有普通 API/静态资源压缩；直接在 HttpApi 和 nginx 两侧同时加压缩会造成双重压缩和下载链路风险。
- 影响面：nginx 现在压缩文本、JSON、JS/CSS、XML、SVG 等响应，最小长度 1024 字节；未把 zip、nupkg、exe、octet-stream 等安装包或已压缩包类型加入 `gzip_types`；edge bundle 下载和 `/edge-updates` 大文件链路不被纳入 gzip 类型清单。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：生产 HTTP 入口的响应压缩由 nginx 统一负责；HttpApi 不得同时启用 ResponseCompression；安装包和已压缩包类型不得加入 nginx `gzip_types`。同步更新 `docs/云端架构治理清单.md` C-11 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "gzip on|gzip_types|AddResponseCompression|UseResponseCompression" deploy/nginx/nginx.conf src/hosts/IIoT.HttpApi -g '*.cs' -g '*.conf'`
- 验证结果：ConfigurationGuardTests 71/71 通过；nginx gzip 配置可检索，HttpApi 未出现 `AddResponseCompression` / `UseResponseCompression`。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端设备级联删除影响统计和审计收口

- 改动范围：Cloud 设备级联删除基础设施、删除影响 DTO、删除审计摘要、ServiceLayer 源码守卫/编排测试、云端规则和云端治理清单。`EfDeviceDeletionDependencyService.GetImpactAsync` 从多次 count 改为单次 SQL 投影；关联表批量删除收口到同一事务内的参数化 SQL；`Device` 本体仍通过聚合 `MarkDeleted()` 后由 EF 删除。未修改 EdgeClient、AICopilot、部署脚本或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-10。原实现虽然已有事务、缓存失效和审计，但影响统计是多次 DB 往返，并且 `edge_hosts`、`edge_host_plc_bindings`、`edge_host_plc_runtime_states` 会被数据库 FK cascade 影响却没有进入影响查询和审计摘要。
- 影响面：设备删除影响查询和审计摘要现在包含配方、产能、日志、过站、客户端状态、版本快照、插件版本、运行心跳、上传幂等、人员授权、设备 refresh token、EdgeHost、PLC 绑定和 PLC runtime state。删除成功后仍清理受影响员工的 `DeviceAccessesByUser` 缓存；失败仍由同一 EF transaction 回滚。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：设备删除影响查询和审计必须覆盖数据库 FK 会实际删除的 EdgeHost/PLC 配置和状态，不得让 FK cascade 静默删除。同步更新 `docs/云端架构治理清单.md` C-10 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter Device --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~ApplicationFlowGuardTests --no-restore --disable-build-servers`
  - `rg -n "SqlQuery<DeviceDeletionImpactRow>|edge_host_plc_runtime_states|edge_host_plc_bindings|edge_hosts" src/infrastructure/IIoT.EntityFrameworkCore/QueryServices/EfDeviceDeletionDependencyService.cs`
  - `rg -n "设备删除影响查询和审计必须覆盖数据库 FK|C-10 设备级联删除批量化|状态：已实施" docs/云端规则.md docs/云端架构治理清单.md`
- 验证结果：ProductionService Device 定向 10/10 通过；InfrastructureBehaviorTests 39/39 通过；ApplicationFlowGuardTests 75/75 通过；批量统计/删除 SQL、EdgeHost/PLC 影响字段、规则沉淀和治理清单状态均可检索。

## 2026-07-06 云端 Identity lockout 和登录失败语义收口

- 改动范围：Cloud Identity 基础设施、Human 登录、Edge operator 登录、ServiceLayer 身份行为测试、EndToEnd 配置守卫、云端规则和云端治理清单。`AddIdentityCore` 配置 lockout；`IdentityPasswordService.CheckPasswordAsync` 增加历史账号 lockout 启用、锁定判断、失败计数和成功清零；Human/Edge operator 凭据校验阶段统一失败语义；未修改 EdgeClient、AICopilot、部署脚本或 HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` C-09。原实现只调用 `CheckPasswordAsync`，没有失败计数、锁定、成功清零，也会在账号停用等凭据阶段暴露不同错误语义。
- 影响面：连续 5 次密码错误会触发 Identity lockout，默认锁定 15 分钟；已部署历史账号即使 `LockoutEnabled=false`，进入密码校验时也会先启用 lockout；成功登录会清零失败计数；锁定账号即使用正确密码也不能登录。管理员重置密码仍只重设密码，不隐式清除 lockout。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：Human 登录和 Edge operator 登录必须执行 Identity lockout；凭据校验阶段对账号不存在、停用、锁定和密码错误使用统一失败语义；管理员重置密码不得隐式清锁，除非另建显式清锁命令并写审计。同步更新 `docs/云端架构治理清单.md` C-09 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AuthorizationAndIdentityBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "AccessFailedAsync|ResetAccessFailedCountAsync|IsLockedOutAsync|SetLockoutEnabledAsync|Lockout\\.AllowedForNewUsers|MaxFailedAccessAttempts" src -g '*.cs'`
  - `rg -n "Human 登录和 Edge operator 登录必须执行 Identity lockout|管理员重置密码不得隐式清除 lockout" docs/云端规则.md`
- 验证结果：AuthorizationAndIdentityBehaviorTests 32/32 通过；ConfigurationGuardTests 70/70 通过；lockout API、规则沉淀关键词和治理清单状态均可检索。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端多角色授权和 Admin 判断收口

- 改动范围：Cloud 身份上下文、权限管道、设备授权服务、员工 onboarding/profile 访问权维护、ServiceLayer/ProductionService 测试替身、ConfigurationGuardTests 和云端治理清单。`ICurrentUser` 删除单 `Role` 属性并改为 `IReadOnlyCollection<string> Roles`；HttpApi `CurrentUser` 从 JWT 中读取全部 role claims；Admin bypass 统一改为 `Roles.Contains(SystemRoles.Admin)`。
- 改动原因：执行 `docs/云端架构治理清单.md` C-08。原实现只读取第一个 role claim，导致 Admin 不在第一个 claim 时可能失去 Admin bypass，也容易让后续代码继续基于单 role 做授权判断。
- 影响面：Human 用户可以携带多个角色；Admin 判断不再依赖 claim 顺序；普通多角色用户不会因为具备多个非 Admin 角色而获得 Admin bypass。Edge Release publisher 仍按 ActorType 和权限点白名单执行，不受 Human Admin bypass 影响。
- 规则提炼：无新增长期规则，原因是多角色授权和访问权维护要求已经写入 `docs/云端规则.md`；本次是将既有规则落到接口、运行时代码和测试守卫。同步更新 `docs/云端架构治理清单.md` C-08 状态。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AuthorizationAndIdentityBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `! rg -n "currentUser\\.Role\\b|user\\.Role\\b|FindFirstValue\\(ClaimTypes\\.Role\\)|string\\?\\s+Role\\s*\\{" src/services src/hosts -g '*.cs'`
  - `rg -n "IReadOnlyCollection<string> Roles|FindAll\\(ClaimTypes\\.Role\\)|Roles.Contains\\(SystemRoles.Admin|AuthorizationBehavior_ShouldAllowAdminWhenAdminRoleIsNotFirst|AuthorizationBehavior_ShouldNotTreatNonAdminMultiRoleAsAdmin" src/services src/hosts src/tests -g '*.cs'`
- 验证结果：上述验证均通过；AuthorizationAndIdentityBehaviorTests 30/30 通过，ConfigurationGuardTests 69/69 通过，ProductionService.Tests 41/41 通过。ConfigurationGuardTests 仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端第一批安全红线执行：HTTP、Bootstrap、Runner/SSH 和部署模板

- 改动范围：Cloud 运行时代码、后端 Dockerfile、部署模板、部署脚本、部署 workflow、部署文档、ConfigurationGuardTests、ServiceLayer bootstrap 行为测试和云端治理清单。删除 `BootstrapAuthOptions.RequireSecret` 可配置旁路，移除 compose/appsettings/.env 中的 `BOOTSTRAP_AUTH_REQUIRE_SECRET`，将 `.env.example` 的真实现场 IP、`change-me-*` 和默认 `ALLOW_INTRANET_HTTP_OIDC=true` 改为内网示例域名、不可上线占位 secret 和默认 false；`pre-deploy-check.sh` 通过 `release-common.sh` 拦截模板 secret 和模板公开地址；`local-release.sh` 默认拒绝 root SSH；后端 Dockerfile、本机构建、镜像 mirror、installer catalog 校验和 PowerShell 下载脚本不再内置 `10.98.90.154` 默认值；`cloud-image` 灾备 workflow 挂 production environment 并要求显式 AICopilot challenge URL。未修改 EdgeClient、AICopilot、工作区顶层文档或 nginx HTTPS/TLS 配置。
- 改动原因：执行 `docs/云端架构治理清单.md` 第一批安全红线和部署治理项，先堵 HTTP 基线下更紧急的补偿控制：弱 secret/真实 IP 模板、bootstrap 可关闭绕过、HTTP-only 回归守卫、runner/SSH 权限和可复制脚本默认值。用户已明确当前云端必须 HTTP，因此本批没有引入 443、HSTS、TLS 证书或 `UseHttpsRedirection`。
- 影响面：设备 bootstrap 现在始终要求 `ClientCode + X-IIoT-Bootstrap-Secret`，不再存在只凭 `ClientCode` 的 cache-only 分支；部署 `.env.example` 不能被直接复制上线，生产 `.env` 必须替换数据库、RabbitMQ、JWT、Seq、种子管理员密码和公开地址；本机发布必须显式传入 Harbor registry，默认拒绝 `root@` SSH；镜像 mirror、catalog 校验和安装包下载脚本必须显式传入真实内网地址；ConfigurationGuardTests 会防止恢复 bootstrap 关闭变量、真实 IP 模板、弱占位 secret、HTTPS/TLS 配置、root SSH 示例和 runner 生产环境保护缺口。
- 规则提炼：无新增长期规则，原因是“内网受控 HTTP + 补偿控制”“Bootstrap secret 恒必需”“runner/SSH 权限收敛”和“部署模板不得含真实/弱默认值”已经写入 `docs/云端规则.md` 与 `docs/云端安全部署契约.md`；本次是按既有红线落地代码、部署模板、preflight、workflow 和测试守卫。同步更新 `docs/云端架构治理清单.md` 中 C-01/C-02/C-03/C-04/C-05/C-07/C-15 状态，避免计划和源码前后不一致。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~GetDeviceByInstanceHandler --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~EdgeBootstrap_ShouldRequireSecret_AndResolveByCode --no-restore --disable-build-servers`
  - `docker compose --env-file deploy/.env.example -f deploy/docker-compose.prod.yml config -q`
  - `! rg -n "10\\.98\\.90\\.154|change-me-|ALLOW_INTRANET_HTTP_OIDC=true" deploy/.env.example`
  - `! rg -n "BootstrapAuthOptions|BootstrapAuth__RequireSecret|BOOTSTRAP_AUTH_REQUIRE_SECRET" src/hosts src/services deploy`
  - `! rg -n "listen 443|Strict-Transport-Security|UseHttpsRedirection|GATEWAY_HTTPS_PORT" deploy src/hosts -g '*.conf' -g '*.yml' -g '*.cs'`
  - `sh -n deploy/scripts/release-common.sh`
  - `sh -n deploy/scripts/pre-deploy-check.sh`
  - `bash -n deploy/scripts/local-release.sh`
  - `bash -n deploy/scripts/build-and-push.sh`
  - `sh -n deploy/scripts/mirror-third-party-images.sh`
  - `sh -n deploy/scripts/verify-edge-installer-catalog.sh`
  - `pwsh -NoProfile -Command '$errors = @(); $null = [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw "deploy/scripts/InvokeEdgeInstallerPackageDownload.ps1"), [ref]$errors); if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Error $_ }; exit 1 }'`
  - `if REGISTRY=harbor.local:5000 VITE_AICOPILOT_CHALLENGE_URL=http://aicopilot.local:82/api/identity/cloud-oidc/challenge deploy/scripts/local-release.sh --services web --ssh-target root@cloud.lan --dry-run >/tmp/iiot-local-release-root.out 2>&1; then cat /tmp/iiot-local-release-root.out; exit 1; fi; rg -n "refuses root SSH" /tmp/iiot-local-release-root.out`
  - `if deploy/scripts/build-and-push.sh --services web --dry-run >/tmp/iiot-build-no-registry.out 2>&1; then cat /tmp/iiot-build-no-registry.out; exit 1; fi; rg -n "REGISTRY is required" /tmp/iiot-build-no-registry.out`
  - `if deploy/scripts/mirror-third-party-images.sh >/tmp/iiot-mirror-no-registry.out 2>&1; then cat /tmp/iiot-mirror-no-registry.out; exit 1; fi; rg -n "MIRROR_REGISTRY is required" /tmp/iiot-mirror-no-registry.out`
  - `if deploy/scripts/verify-edge-installer-catalog.sh >/tmp/iiot-verify-no-base.out 2>&1; then cat /tmp/iiot-verify-no-base.out; exit 1; fi; rg -n "BASE_URL is required" /tmp/iiot-verify-no-base.out`
  - `rg -n 'C-01 HTTP-only 生产基线|状态：已实施|C-02 .*弱 secret|C-03 Bootstrap secret 恒必需|状态：部分实施' docs/云端架构治理清单.md`
  - `git diff --check`
- 验证结果：上述验证均通过；ServiceLayer bootstrap 定向 2/2 通过；ConfigurationGuardTests 68/68 通过；Edge bootstrap 端到端 1/1 通过；compose config、禁用项负向检索、shell/PowerShell 语法检查、脚本 fail-fast 干跑、治理清单状态检索和 diff whitespace 检查均通过。ConfigurationGuardTests 和端到端测试仍保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning，本批未处理该既有 warning。

## 2026-07-06 云端 HTTP 部署红线与完整治理计划重排

- 改动范围：新增 `docs/云端安全部署契约.md`，重写 `docs/云端架构治理清单.md` 为完整 Cloud 修复计划，并更新 `docs/云端规则.md` 的云端部署传输红线；同步记录本复盘。未修改 Cloud 运行时代码、部署脚本、nginx/compose、测试、EdgeClient 或 AICopilot。
- 改动原因：前一版计划把 TLS/HTTPS 列为第一轮安全红线，但用户明确当前生产因证书成本和现场约束必须使用 HTTP，因此需要把 Cloud 当前生产传输基线修正为“内网受控 HTTP + 补偿控制”，避免后续 agent 误把 HTTPS/TLS 当成阻断目标。
- 影响面：本次只改变文档和后续执行计划口径，不改变现有部署行为。后续 Cloud 治理优先级调整为：HTTP-only 基线不作为失败项，优先处理 `.env.example` 弱 secret/真实 IP、bootstrap secret 旁路、runner/SSH 权限、默认 root 容器、身份多角色、lockout、设备级联删除、ResponseCompression、Edge PLC runtime state、AI 只读边界和部署 preflight。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/云端安全部署契约.md`：当前生产必须保持内网受控 HTTP，不启用 HTTPS/TLS/443/HSTS/HTTPS redirection；未经用户明确批准证书预算、证书来源和允许修改目录，不得新增 TLS 相关配置。因 HTTP 不提供传输加密，必须保留强 secret、短期 JWT、bootstrap secret 恒必需、限流、最小权限、runner/SSH 收敛、日志脱敏和网络边界控制。
- 验证命令：
  - `rg -n "云端部署传输红线|内网受控 HTTP|不启用 HTTPS/TLS|不得新增 nginx 443" IIoT.CloudPlatform/docs`
  - `rg -n "云端安全部署契约|云端架构治理清单|HTTP 生产基线误判|Bootstrap .*RequireSecret=false" IIoT.CloudPlatform/docs`
  - `rg -n "批次 A：文档和守卫先行|C-08 多角色授权|C-14 AI 生产数据唯一只读入口|停止条件" IIoT.CloudPlatform/docs/云端架构治理清单.md`
  - `git -C IIoT.CloudPlatform diff --check -- docs/云端规则.md docs/云端安全部署契约.md docs/云端架构治理清单.md docs/改动复盘与规则沉淀.md`
- 验证结果：上述 4 条验证均通过；HTTP-only 红线、云端安全部署契约、完整治理清单、bootstrap 绕过治理项、批次计划、多角色授权、AI 只读入口和停止条件均可检索；`git diff --check` 无 whitespace 问题。

## 2026-07-03 EdgeHost PLC 动态绑定云端改造最终验证收口

- 改动范围：对本轮 EdgeHost/PLC 动态绑定云端改造做最终验证和收口记录，覆盖后端 build、ProductionService/ServiceLayer 测试、EndToEnd 配置与限流守卫、Vue 单测/构建、diff 检查、核心关键词检索和跨项目状态核对。未修改 EdgeClient 或 AICopilot。
- 改动原因：前序批次已经完成 EdgeHost 聚合根审计、前端 runtime state 展示、安全 seed、产能汇总和权限/路由/限流/审计审计，需要统一记录最终验证结果和剩余风险，避免把环境型 EndToEnd 失败误报成全绿。
- 影响面：本次只更新复盘文档，不改变运行时行为。
- 最终验证结果：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers` 通过，0 warning / 0 error。
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers` 通过，41/41。
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers` 通过，219/219。
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers` 通过，68/68，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning。
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~RateLimitingConfigurationGuardTests --no-restore --disable-build-servers` 通过，3/3，保留同一既有 `Microsoft.OpenApi` warning。
  - `npm run test:unit` 通过，15 个测试文件、55/55。
  - `npm run build` 通过，保留既有 `@vueuse/core` Rollup pure annotation warning。
  - `git diff --check` 通过。
  - EdgeHost 前端和后端状态链路的假在线、mock、fake、dummy 关键词扫描无结果。
  - EdgeHostSeeds、Human/Edge 路由、权限点、产能汇总和规则沉淀关键词均可检索。
- 未通过项：`dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --no-restore --disable-build-servers` 全量运行时，`CloudProductionFlowTests.DeviceLogConsumer_RepeatedFailures_ShouldMoveMessageToErrorQueue` 在测试 fixture 启动阶段失败，错误为 Aspire 等待资源 `eventbus` healthy 时资源启动失败；随后测试进程长时间无新输出，已手动中断。`docker info` 显示 Docker 可用，`docker ps` 无正在运行测试容器，旧的 `eventbus-a49693a4` 容器为 7 天前退出状态。本轮未擅自清理或重建本机旧 Docker 容器。
- 规则提炼：无新增长期规则，原因是本次最终收口没有新增业务或技术边界；只是把已完成批次的验证结果和环境型 EndToEnd 风险记录清楚。

## 2026-07-03 EdgeHost 权限、审计、路由、限流与前端权限总审计

- 改动范围：仅审计 Cloud EdgeHost/PLC 绑定相关的 Human/Edge API 路由、权限点、写操作审计、限流配置、nginx 上传限流和 Vue 前端权限感知，并更新本滚动复盘。未修改运行时代码、EF migration、生产配置、部署脚本、EdgeClient 或 AICopilot。
- 改动原因：EdgeHost/PLC 动态绑定、runtime state 展示和产能汇总已串联后，需要确认新增能力没有绕开既有 Human/Edge surface、权限、限流和审计边界，避免后续批次在错误入口继续堆功能。
- 影响面：本次没有改变运行时行为。审计确认 Human 管理和查询只暴露在 `GET/POST/PUT/DELETE /api/v1/human/edge-hosts*`，Controller 统一走 `[Authorize]` 与 `HttpApiRateLimitPolicies.GeneralApi`；Edge PLC 状态上报只暴露在 `POST /api/v1/edge/edge-hosts/plc-runtime-states`，走 `RequireEdgeDeviceToken` 与 `EdgeHostPlcStateUpload`；未发现 AI Read 或 public 入口暴露 EdgeHost 写入/状态上报能力。
- 关键审计结论：
  - Human 查询使用 `EdgeHost.Read`，PLC 产能汇总额外要求 `Device.Read` 并做人员设备授权过滤；Human 写命令均使用 `EdgeHost.Manage`。
  - 上位机创建、修改、删除、启停以及 PLC 绑定新增、修改、删除、启停的成功和失败路径均通过 `EdgeHostAudit` 尝试写审计记录。
  - `RateLimitingConfigurationGuardTests` 已守卫 EdgeHost PLC 状态上报专用限流，nginx `edge_upload_limit` 已覆盖 `edge-hosts/plc-runtime-states`。
  - Vue 前端路由和导航使用 `EdgeHost.Read`；管理按钮由 `Permissions.EdgeHost.Manage` 控制；runtime state 与产能汇总表没有写操作列。
- 规则提炼：无新增长期规则，原因是本次审计未发现新增边界或缺口；Human/Edge/API surface、权限、限流、审计和前端权限感知要求已分别沉淀在 `docs/云端规则.md` 2、5.2、6.2、6.3 和 6.4。
- 验证命令：
  - `sed -n '1,150p' src/tests/IIoT.EndToEndTests/RateLimitingConfigurationGuardTests.cs`
  - `sed -n '380,460p' src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs`
  - `rg -n "AuthorizeRequirement|AdminOnly|IHuman(Query|Command)|IDeviceCommand" src/services/IIoT.ProductionService/Queries/Human/EdgeHosts src/services/IIoT.ProductionService/Commands/Human/EdgeHosts src/services/IIoT.ProductionService/Commands/Edge/EdgeHosts -g '*.cs'`
  - `rg -n "TryWriteAsync|EdgeHostAudit\\.|OperationType|PlcBinding" src/services/IIoT.ProductionService/Commands/Human/EdgeHosts/ManageEdgeHosts.cs src/tests/IIoT.ProductionService.Tests/EdgeHostBehaviorTests.cs`
  - `rg -n "Route\\(\"api/v1/(human|edge|ai|public).*edge-host|plc-runtime-states|plc-capacity-summary|HttpApiRateLimitPolicies" src/hosts/IIoT.HttpApi/Controllers src/tests/IIoT.EndToEndTests -g '*.cs'`
  - `rg -n "Permissions\\.EdgeHost\\.Manage|Permissions\\.EdgeHost\\.Read|hasPermission\\(Permissions\\.EdgeHost|permission: Permissions\\.EdgeHost|requiredPermission: Permissions\\.EdgeHost" src/ui/iiot-web/src/features/edge-hosts src/ui/iiot-web/src/layout/MainLayout.vue src/ui/iiot-web/src/types/permissions.ts -g '*.ts' -g '*.vue'`
  - `sed -n '185,214p' deploy/nginx/nginx.conf`
  - `rg -n "api/v1/(ai|public).*edge-host|api/v1/edge/edge-hosts|api/v1/human/edge-hosts" src/hosts/IIoT.HttpApi docs deploy -g '*.cs' -g '*.md' -g '*.conf'`
- 验证结果：审计确认权限、审计、路由、限流和前端权限感知与现有规则一致；未发现需要补代码的缺口；未触碰 EdgeClient 或 AICopilot。

## 2026-07-03 EdgeHost PLC 产能汇总只读串联

- 改动范围：Cloud 后端新增 EdgeHost 视角 PLC 产能汇总 DTO、Human 查询和 Controller 路由；Cloud 前端 EdgeHost 抽屉新增当天 PLC 产能汇总读取和只读表格；补充 ProductionService/EndToEnd/前端单测，更新 Cloud 规则和本滚动复盘。未修改 EdgeClient、AICopilot、产能上传命令、DataWorker、Dapper 写入仓储、EF migration、生产配置或部署脚本。
- 改动原因：EdgeHost/PLC 绑定和 runtime state 已经能展示，但现场需要在云端把“上位机-PLC-业务设备-产能”串起来查看。由于产能归档当前以 `DeviceId + PlcName` 为查询维度，不能用 PLC code、runtime state 或地址猜测产能归属，需要以绑定配置中的 `BusinessDeviceId + PlcName` 做只读查询。
- 影响面：新增 `GET /api/v1/human/edge-hosts/{id}/plc-capacity-summary?date=yyyy-MM-dd`。查询要求 `EdgeHost.Read` 和 `Device.Read`，并继续执行人员设备授权范围过滤。未配置业务设备、绑定禁用或无设备授权的 PLC 不读取产能，只返回状态；有权限时按绑定 `BusinessDeviceId + PlcName` 读取当天 `DailySummaryDto`。前端打开 EdgeHost PLC 抽屉时同时加载详情、runtime state 和当天产能汇总，配置变更后刷新状态和产能汇总。
- 关键实现：
  - `EdgeHostPlcCapacitySummaryDto` 返回 PLC 绑定、业务设备、日期、可读状态、产能状态和当天汇总。
  - `GetEdgeHostPlcCapacitySummaryHandler` 只组合 `EdgeHost` 聚合和 `ICapacityQueryService`，不写产能、不改 runtime state、不改原始归档表。
  - `HumanEdgeHostController` 新增 `plc-capacity-summary` Human 只读路由。
  - Vue EdgeHost 抽屉新增“PLC 产能汇总”表，状态文案只映射后端 `capacityStatus`，不在前端推断在线或实时产能。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：EdgeHost 视角 PLC 产能只能用绑定配置中的 `BusinessDeviceId + PlcName` 读取；不得使用 runtime state、PLC code、地址或展示名猜测归属；Human 查询必须同时具备 `EdgeHost.Read` / `Device.Read` 并执行设备授权过滤。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~EdgeHostBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `npm run test:unit -- src/features/edge-hosts/edge-hosts.test.ts`
  - `npm run test:unit`
  - `npm run build`
  - `wc -l src/ui/iiot-web/src/features/edge-hosts/*.vue`
  - `rg -n "plc-capacity-summary|EdgeHostPlcCapacitySummary|GetEdgeHostPlcCapacitySummary|createPlcCapacitySummaryColumns|capacitySummaries|PLC 产能汇总|NoBusinessDevice|BusinessDeviceId \\+ PlcName" src docs -g '*.cs' -g '*.ts' -g '*.vue' -g '*.md'`
  - `rg -n "在线|离线|实时|mock|模拟|fake|dummy" src/ui/iiot-web/src/features/edge-hosts src/services/IIoT.ProductionService/Queries/Human/EdgeHosts src/hosts/IIoT.HttpApi/Controllers/Human/HumanEdgeHostController.cs -g '*.vue' -g '*.ts' -g '*.cs'`
  - `git diff --check -- src/services/IIoT.ProductionService/EdgeHosts src/services/IIoT.ProductionService/Queries/Human/EdgeHosts src/hosts/IIoT.HttpApi/Controllers/Human/HumanEdgeHostController.cs src/tests/IIoT.ProductionService.Tests/EdgeHostBehaviorTests.cs src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs src/ui/iiot-web/src/features/edge-hosts docs/云端规则.md docs/改动复盘与规则沉淀.md`
- 验证结果：AppHost build 通过，0 warning / 0 error；EdgeHost 后端定向测试 12/12 通过；ProductionService 全量 41/41 通过；ConfigurationGuardTests 68/68 通过，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning；EdgeHost 前端专项 6/6 通过；Vue 全量单测 55/55 通过；Vue build 通过，保留既有 `@vueuse/core` Rollup pure annotation warning；EdgeHost SFC 最大 158 行；产能汇总 API、DTO、查询、前端表格和规则关键词可检索；假实时/模拟关键词扫描无结果；`git diff --check` 通过。

## 2026-07-03 EdgeHost PLC 初始播种与角色模板收口

- 改动范围：Cloud MigrationWorkApp 新增默认关闭的 `EdgeHostSeeds` 配置解析和 EdgeHost/PLC 初始化 seeder，接入数据库初始化链路；调整系统角色模板，让 `ProductionViewer` 获得 `EdgeHost.Read` 但不获得 `EdgeHost.Manage`；补充 ServiceLayer/EndToEnd 守卫测试、Cloud 规则和本滚动复盘。未修改 EdgeClient、AICopilot、Cloud 前端、EF migration、生产部署配置或 PLC/runtime state 上报链路。
- 改动原因：EdgeHost/PLC 绑定已经支持人工维护和 runtime state 展示后，需要给后续现场已知 PLC 清单预置留下安全入口，同时避免 migration worker 每次部署自动覆盖人员在前端维护的绑定结果；生产查看角色也需要能读取上位机 PLC 状态投影，但不能获得配置维护权限。
- 影响面：`MigrationWorkApp` 仍然默认不写 EdgeHost 业务数据；只有显式配置 `EdgeHostSeeds:Enabled=true` 时才会按 `Device.Code/ClientCode` 找已存在上位机设备，并按配置初始化 EdgeHost 和 PLC 绑定。新建 EdgeHost 时可以带入 PLC 绑定；已存在 EdgeHost 时默认不覆盖主数据、不补缺失 PLC 绑定、不更新已有 PLC 绑定，除非显式打开 `UpdateExisting`、`AddMissingBindingsToExistingHost` 或 `UpdateExistingBindings`。`ProductionViewer` 现在可读 EdgeHost/PLC 状态投影，但仍不能新增、修改、删除或启停绑定。
- 关键实现：
  - `EdgeHostSeedOptions` 解析 `EdgeHostSeeds`，默认 `Enabled=false`，启用后校验 Host 必填、PLC 必填、同一 Host 内 PLC code 不重复，并让更新/补缺开关默认关闭。
  - `EdgeHostSeedData` 通过 `IIoTDbContext.EdgeHosts` 聚合根入口创建或维护配置，按 `ClientCode` 解析已存在 `Device`，按可选 `ProcessCode` 和业务设备 `Device.Code` 解析绑定关系，不创建 Device、工序或 PLC 独立资产。
  - `DatabaseInitializationOrchestrator` 在系统账号/角色播种后检查 EdgeHost seed；默认关闭时只跳过，不写业务表。
  - `SystemRolePermissionTemplates` 为 `ProductionViewer` 增加 `EdgeHost.Read`，并用测试保证没有 `EdgeHost.Manage`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：`ProductionViewer` 只能读 EdgeHost/PLC 状态投影；EdgeHost/PLC 业务 seed 必须显式开启、默认关闭、不得出现在部署默认配置；seed 只能初始化已存在 Device/Process 的绑定关系，不得根据 runtime state 自动生成绑定；已存在配置默认保护人工维护结果。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AuthorizationAndIdentityBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~ConfigurationGuardTests --no-restore --disable-build-servers`
  - `rg -n "EdgeHostSeeds" src/hosts/IIoT.MigrationWorkApp/appsettings*.json src/hosts/IIoT.AppHost/appsettings*.json deploy -g '*.json' -g '*.env' -g '*.example' -g '*.sh'`
  - `rg -n "EdgeHostSeeds|SeedEdgeHostDataAsync|EdgeHostSeedData|ProductionViewer.*EdgeHost|AddMissingBindingsToExistingHost|UpdateExistingBindings" src/hosts/IIoT.MigrationWorkApp src/services/IIoT.Services.Contracts src/tests docs -g '*.cs' -g '*.md'`
  - `git diff --check -- src/hosts/IIoT.MigrationWorkApp src/services/IIoT.Services.Contracts/Contracts/Authorization/SystemRolePermissionTemplates.cs src/tests/IIoT.EndToEndTests/ConfigurationGuardTests.cs src/tests/IIoT.ServiceLayer.Tests/AuthorizationAndIdentityBehaviorTests.cs docs/云端规则.md docs/改动复盘与规则沉淀.md`
- 验证结果：AppHost build 通过，0 warning / 0 error；AuthorizationAndIdentityBehaviorTests 定向 28/28 通过；ConfigurationGuardTests 定向 68/68 通过，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning；默认 appsettings/deploy 配置未发现 `EdgeHostSeeds`；EdgeHost seed、默认安全开关、角色模板和规则关键词可检索；`git diff --check` 通过。

## 2026-07-03 EdgeHost PLC Runtime State 前端状态展示闭环

- 改动范围：Cloud Vue 前端 EdgeHost feature 的 API 类型、runtime state 查询、抽屉展示、表格列、样式和单测，以及本滚动复盘。未修改 Cloud 后端契约、EF migration、部署脚本、生产配置、EdgeClient 或 AICopilot。
- 改动原因：现有前端已能维护上位机和 PLC 绑定配置，但尚未读取 `GET /api/v1/human/edge-hosts/{id}/plc-runtime-states`，无法在云端管理界面展示 Edge 上报的真实 PLC runtime state。需要补齐 Human 只读状态展示闭环，避免用户误把绑定配置当成真实在线状态。
- 影响面：打开上位机 PLC 抽屉时，会同时读取上位机详情和 PLC runtime state 投影；抽屉内分为“PLC 绑定配置”和“PLC 状态投影”两张表。状态投影表展示已配置/未配置、运行状态、工序/业务设备、配置地址、上报地址、最后错误和最后上报时间。配置新增、修改、启停、删除后刷新状态投影，让“已配置/未配置”合并结果来自后端查询。
- 关键实现：
  - `edge-hosts/api.ts` 新增 `EdgeHostPlcRuntimeStateDto` 和 `getEdgeHostPlcRuntimeStatesApi`。
  - `columns.ts` 新增 `createPlcRuntimeStateColumns`，运行状态只按后端 `runtimeStatus` 映射展示，不自行推断在线。
  - `useEdgeHosts.ts` 新增 `runtimeStates` / `runtimeLoading` / `runtimeColumns`，打开抽屉时加载状态投影，配置变更后刷新。
  - `EdgeHostPlcDrawer.vue` 新增“PLC 状态投影”表，未收到状态时显示空态，不生成模拟状态。
- 规则提炼：无新增长期规则，原因是“不得伪造 UI/状态/设备在线”和“PLC runtime state 只能合并展示配置元数据和状态投影，不反向修配置”已分别写入工作区总规则和 `docs/云端规则.md` 5.2；本次只是按既有规则接入真实查询展示。
- 验证命令：
  - `npm run test:unit -- src/features/edge-hosts/edge-hosts.test.ts`
  - `npm run test:unit`
  - `npm run build`
  - `rg -n "在线|离线|心跳|mock|模拟|fake|dummy" src/ui/iiot-web/src/features/edge-hosts src/ui/iiot-web/src/router/index.ts src/ui/iiot-web/src/layout/MainLayout.vue src/ui/iiot-web/src/types/permissions.ts -g '*.vue' -g '*.ts'`
  - `wc -l src/ui/iiot-web/src/features/edge-hosts/*.vue`
  - `rg -n "plc-runtime-states|EdgeHostPlcRuntimeStateDto|createPlcRuntimeStateColumns|runtimeStates|PLC 状态投影|未收到 PLC 状态" src/ui/iiot-web/src/features/edge-hosts -g '*.ts' -g '*.vue'`
  - `git diff --check -- src/ui/iiot-web/src/features/edge-hosts docs/改动复盘与规则沉淀.md`
- 验证结果：EdgeHost 专项前端单测 5/5 通过；Vue 全量单测 54/54 通过；Vue build 通过，保留既有 `@vueuse/core` Rollup pure annotation warning；假在线/离线/心跳/mock/fake/dummy 词扫描无结果；EdgeHost SFC 最大 130 行，均低于 200 行；runtime state API、DTO、列、状态表和空态关键词可检索；`git diff --check` 通过。

## 2026-07-03 EdgeHost 聚合根与 PLC 绑定现有实现审计收口

- 改动范围：仅审计并验证 Cloud 现有 EdgeHost/PLC 绑定/runtime state 实现，更新本滚动复盘。未修改 Cloud 业务代码、EF migration、前端页面、部署脚本、生产配置、EdgeClient 或 AICopilot。
- 改动原因：执行完整云端改造计划前，需要先确认当前未提交的 EdgeHost 聚合根、PLC 动态绑定和 runtime state 投影实现是否已经满足既定边界，避免重复重建已有模型或误改其他窗口正在推进的客户端工作。
- 影响面：本次没有改变运行时行为。审计确认 `EdgeHost` 是聚合根，`EdgeHostPlcBinding` 作为聚合内子实体由聚合根方法维护，`EdgeHostPlcRuntimeState` 作为状态投影通过 `IEdgeHostPlcRuntimeStateStore` 专用端口维护；Human 管理入口、Edge 状态上报入口、权限点和限流守卫均已有测试覆盖。
- 关键实现审计结论：
  - `IIoTDbContext` 公开 `EdgeHosts` 聚合根入口和 `EdgeHostPlcRuntimeStates` 投影集合，未公开 `EdgeHostPlcBindings` 子实体 DbSet。
  - 运行时代码未发现 `IRepository<EdgeHostPlcBinding>`、`IReadRepository<EdgeHostPlcBinding>`、`DbSet<EdgeHostPlcBinding>`、`EdgeHostPlcBinding : IAggregateRoot`、`IRepository<EdgeHostPlcRuntimeState>` 或 `IReadRepository<EdgeHostPlcRuntimeState>` 入口。
  - `POST /api/v1/edge/edge-hosts/plc-runtime-states` 走 Edge device token 和 `EdgeHostPlcStateUpload` 限流；`GET/POST/PUT/DELETE /api/v1/human/edge-hosts*` 走 Human surface，并由 `EdgeHost.Read` / `EdgeHost.Manage` 控制。
- 规则提炼：无新增长期规则，原因是 EdgeHost 聚合边界、PLC 绑定子实体、runtime state 专用 store、Human/Edge 权限边界和未配置 PLC 不反向改绑定等规则已经沉淀在 `docs/云端规则.md` 5.2；本次只是审计和验证现有实现。
- 验证命令：
  - `git log --oneline -n 12 -- src/core/IIoT.Core.Production/Aggregates/EdgeHosts src/core/IIoT.Core.Production/Specifications/EdgeHosts src/core/IIoT.Core.Production/Contracts/EdgeHosts src/services/IIoT.ProductionService/Commands/Human/EdgeHosts src/services/IIoT.ProductionService/Commands/Edge/EdgeHosts src/services/IIoT.ProductionService/Queries/Human/EdgeHosts src/infrastructure/IIoT.EntityFrameworkCore/Configuration/Production/EdgeHostConfiguration.cs src/infrastructure/IIoT.EntityFrameworkCore/Configuration/Production/EdgeHostPlcRuntimeStateConfiguration.cs src/hosts/IIoT.HttpApi/Controllers/Human/HumanEdgeHostController.cs src/hosts/IIoT.HttpApi/Controllers/Edge/EdgeHostPlcRuntimeStateController.cs`
  - `rg -n "IRepository<EdgeHostPlcBinding|IReadRepository<EdgeHostPlcBinding|DbSet<EdgeHostPlcBinding>|class EdgeHostPlcBinding : .*IAggregateRoot|EdgeHostPlcBinding.*IAggregateRoot|IRepository<EdgeHostPlcRuntimeState|IReadRepository<EdgeHostPlcRuntimeState|class EdgeHostPlcRuntimeState : .*IAggregateRoot" src -g '*.cs' -g '!src/tests/**'`
  - `rg -n "api/v1/edge/edge-hosts/plc-runtime-states|api/v1/human/edge-hosts|EdgeHost\\.Read|EdgeHost\\.Manage|IEdgeHostPlcRuntimeStateStore|edge_host_plc_bindings|edge_host_plc_runtime_states|EdgeHostPlcStateUpload" src docs deploy -g '*.cs' -g '*.md' -g '*.json' -g '*.conf'`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~EdgeHostBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~AggregateBehaviorTests|FullyQualifiedName~InfrastructureBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers`
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
- 验证结果：EdgeHost 相关路径当前为未提交新增文件，`git log` 对这些路径无历史提交输出；违规仓储/DbSet/聚合根入口扫描无结果；EdgeHost 权限、路由、runtime state store、表名和限流关键词可检索；EdgeHost 行为定向测试 11/11 通过；ServiceLayer 聚合/基础设施定向测试 51/51 通过；EndToEnd 路由/限流守卫 64/64 通过，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning；AppHost build 通过，0 warning / 0 error；ProductionService 全量 40/40 通过；ServiceLayer 全量 218/218 通过。

## 2026-07-03 EdgeHost PLC Runtime State 云端状态投影闭环

- 改动范围：Cloud 后端 PLC runtime state 投影实体、专用 store、EF 配置和 migration、Edge 上报命令/API、Human 查询命令/API、独立限流策略、nginx 上传限流模板、ProductionService/ServiceLayer/EndToEnd 测试、Cloud 规则文档和本滚动复盘。未修改 EdgeClient、AICopilot、前端实时状态展示、PLC 采集逻辑、产能/过站上传链路、设备 bootstrap、发布脚本或生产配置。
- 改动原因：上位机与 PLC 绑定配置已经可维护，但现场实际连接的 PLC 需要独立状态投影承载“最后看到的 PLC、连接状态、错误和是否已配置”，避免把强实时事实塞回 `EdgeHost` 聚合根，也避免前端后续展示假状态。
- 影响面：新增 `edge_host_plc_runtime_states` 表，按 `DeviceId + ClientCode + PlcCode` 唯一；新增 `IEdgeHostPlcRuntimeStateStore` / `EfEdgeHostPlcRuntimeStateStore` 专用端口；新增 `POST /api/v1/edge/edge-hosts/plc-runtime-states` Edge 设备上报入口；新增 `GET /api/v1/human/edge-hosts/{id}/plc-runtime-states` Human 查询入口；新增 `EdgeHostPlcStateUpload` 应用限流并让 nginx `edge_upload_limit` 覆盖该路径。
- 关键实现：Edge 上报命令先校验 `DeviceId + ClientCode` 身份链，再按设备身份查找对应 `EdgeHost`；已配置 PLC 会关联 `PlcBindingId`，未配置但现场上报的 PLC 会保存为未配置状态投影，不会自动新增、启用或修改绑定；Human 查询只合并 `EdgeHostPlcBinding` 元数据和 runtime state 投影，不反向修配置；runtime state 不是聚合根，不通过通用仓储写入。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 5.2：`edge_host_plc_runtime_states` 是 PLC runtime state 官方投影表，必须走专用 store；写入只能走 Edge 设备身份入口并校验 `DeviceId + ClientCode`；未配置 PLC 可入状态投影但不得自动改绑定；Human 只能用 `EdgeHost.Read` 查询 runtime state。
- 验证命令：
  - `env 'ConnectionStrings__iiot-db=Host=localhost;Port=5432;Database=iiot_design_time;Username=postgres;Password=postgres' dotnet ef migrations add AddEdgeHostPlcRuntimeStates --project src/infrastructure/IIoT.EntityFrameworkCore/IIoT.EntityFrameworkCore.csproj --startup-project src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --context IIoTDbContext`
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~EdgeHostBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~ConfigurationGuardTests|FullyQualifiedName~RateLimitingConfigurationGuardTests" --no-restore --disable-build-servers`
  - `rg -n "IRepository<EdgeHostPlcRuntimeState|IReadRepository<EdgeHostPlcRuntimeState|class EdgeHostPlcRuntimeState : .*IAggregateRoot|EdgeHostPlcRuntimeState.*IAggregateRoot|IRepository<EdgeHostPlcBinding|IReadRepository<EdgeHostPlcBinding|DbSet<EdgeHostPlcBindings>|class EdgeHostPlcBinding : .*IAggregateRoot" src -g '*.cs' -g '!src/tests/**'`
  - `rg -n "api/v1/edge/edge-hosts/plc-runtime-states|plc-runtime-states|EdgeHostPlcRuntimeState|IEdgeHostPlcRuntimeStateStore|edge_host_plc_runtime_states|EdgeHostPlcStateUpload" src docs deploy -g '*.cs' -g '*.md' -g '*.json' -g '*.conf'`
  - `rg -n "xmin = table.Column" src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260703005532_AddEdgeHostPlcRuntimeStates.cs src/infrastructure/IIoT.EntityFrameworkCore/Migrations/20260703005532_AddEdgeHostPlcRuntimeStates.Designer.cs src/infrastructure/IIoT.EntityFrameworkCore/Migrations/IIoTDbContextModelSnapshot.cs`
  - `git diff --check`
- 验证结果：EF migration 生成成功，提示本机 EF tools `10.0.8` 低于 runtime `10.0.9`；AppHost build 通过，0 warning / 0 error；EdgeHost 行为定向测试 11/11 通过；ProductionService 全量 40/40 通过；InfrastructureBehaviorTests 39/39 通过；ServiceLayer 全量 218/218 通过；EndToEnd 路由/限流守卫 64/64 通过，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning；runtime state 和 PLC 绑定通用仓储/聚合根入口扫描无结果；runtime state API、store、表名、限流和规则关键词可检索；新 migration 未生成用户 `xmin` 列；`git diff --check` 通过。

## 2026-07-03 EdgeHost 前端管理闭环

- 改动范围：Cloud Vue 管理端 EdgeHost 页面、API client、类型、列表/表单/抽屉/确认弹窗、路由、导航、i18n、前端权限常量、EdgeHost 前端单测和 Playwright 视觉验收截图。未修改 EdgeClient、AICopilot、PLC runtime state 表、PLC 状态上报、产能/过站上传链路、设备 bootstrap、部署脚本或生产配置。
- 改动原因：后端已提供上位机和 PLC 绑定的 Human API 后，需要给现场维护人员一个可操作入口，支持“上位机确定、PLC 连接关系可动态维护”的配置闭环，避免现场 PLC 调整后继续依赖代码修改。
- 影响面：新增 `/edge-hosts` 管理路由和侧边栏“上位机”入口，读取要求 `EdgeHost.Read`，新增、编辑、启停、删除上位机或 PLC 绑定要求 `EdgeHost.Manage`；页面通过真实 Human API 读取/写入 EdgeHost 配置，并读取现有设备和工序下拉数据，不引入模拟 PLC 状态或前端写死关系。
- 关键实现：列表展示上位机、ClientCode、绑定设备、配置状态、PLC 绑定数量和更新时间；新增上位机时从设备选择带出只读 `ClientCode`，编辑时锁定身份链；PLC 绑定放在上位机详情抽屉中维护，支持新增、编辑、启停和删除，`ProcessId` / 业务设备可暂不关联，便于现场后续补齐；所有删除操作都有二次确认并限定为删除 Cloud 配置，不暗示删除设备主数据或 PLC 资产。
- 规则提炼：无新增长期规则，原因是 EdgeHost 管理权限、聚合边界、PLC 绑定不代表真实在线状态、状态投影/专用 store 边界已经写入 `docs/云端规则.md` 5.2；本次只是把既有规则落到前端管理闭环。
- 验证命令：
  - `npm run test:unit -- src/features/edge-hosts/edge-hosts.test.ts`
  - `npm run test:unit`
  - `npm run build`
  - `bash /Users/shushu/.codex/skills/playwright/scripts/playwright_cli.sh snapshot`
  - `bash /Users/shushu/.codex/skills/playwright/scripts/playwright_cli.sh screenshot --filename output/playwright/edge-host-list.png`
  - `bash /Users/shushu/.codex/skills/playwright/scripts/playwright_cli.sh screenshot --filename output/playwright/edge-host-plc-drawer.png`
  - `bash /Users/shushu/.codex/skills/playwright/scripts/playwright_cli.sh screenshot --filename output/playwright/edge-host-plc-modal.png`
  - `rg -n "在线|离线|心跳|实时|mock|模拟|fake" src/ui/iiot-web/src/features/edge-hosts src/ui/iiot-web/src/router/index.ts src/ui/iiot-web/src/layout/MainLayout.vue src/ui/iiot-web/src/types/permissions.ts -g '*.vue' -g '*.ts'`
  - `rg -n "EdgeHost\\.Read|EdgeHost\\.Manage|edge-hosts|上位机 PLC 绑定" src/ui/iiot-web/src docs -g '*.ts' -g '*.vue' -g '*.md'`
  - `wc -l src/ui/iiot-web/src/features/edge-hosts/*.vue src/ui/iiot-web/src/features/edge-hosts/*.ts`
  - `git diff --check`
- 验证结果：EdgeHost 专项单测 4/4 通过；Vue 全量单测 53/53 通过；Vue build 通过，保留既有 `@vueuse/core` Rollup pure annotation 告警；Playwright 已检查列表页、PLC 绑定抽屉和新增 PLC 弹窗，并输出 `output/playwright/edge-host-list.png`、`output/playwright/edge-host-plc-drawer.png`、`output/playwright/edge-host-plc-modal.png`；实时/在线/心跳/mock/fake 风险词扫描无结果；EdgeHost 权限、路由和标题关键词可检索；EdgeHost 新增 SFC 均不超过 200 行；`git diff --check` 通过。

## 2026-07-02 EdgeHost 后端管理闭环

- 改动范围：Cloud 后端 EdgeHost 权限、Human 命令/查询、DTO、Controller、审计、路由/限流守卫、ProductionService/ServiceLayer/EndToEnd 测试、Cloud 规则文档和本滚动复盘。未修改 EdgeClient、AICopilot、前端页面、PLC runtime state 表、状态上报、产能/过站上传链路、设备 bootstrap、部署脚本或生产配置。
- 改动原因：上位机固定但现场连接的 PLC 会动态调整，需要 Cloud 先提供稳定的后台管理闭环，让人员可以在后续前端页面维护“一个上位机连接多少台 PLC”的配置，而不是每次现场变动都改代码。
- 影响面：新增 `EdgeHost.Read` / `EdgeHost.Manage` 权限点并纳入权限清单和角色模板；新增 `GET/POST/PUT/DELETE /api/v1/human/edge-hosts*` 管理入口；新增上位机创建、修改、删除、启停和 PLC 绑定新增、修改、删除、启停命令；新增 EdgeHost 列表/详情查询。所有写操作通过 `IRepository<EdgeHost>` 维护聚合根，不引入 `EdgeHostPlcBinding` 仓储或 DbSet。
- 关键实现：创建 EdgeHost 必须引用已存在 Device，且 `ClientCode` 必须与 `Device.Code` 一致，创建后不允许通过 EdgeHost 接口改写 `DeviceId` / `ClientCode`；PLC 绑定允许 `ProcessId` / `BusinessDeviceId` 为空，但两者同时填写时校验设备属于工序；写操作全部写 `EdgeHost.*` / `EdgeHost.PlcBinding.*` 审计；删除 EdgeHost 只删除上位机配置和其 PLC 绑定配置，不级联删除 Device 主数据。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：EdgeHost 管理只能走 Human API，读取使用 `EdgeHost.Read`，维护使用 `EdgeHost.Manage`；创建必须校验既有 `DeviceId + ClientCode` 身份链；PLC 绑定关系校验、审计和删除边界必须明确；不得把管理写入口挂到 Edge、public 或 AI Read。
- 验证命令：
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter "FullyQualifiedName~RateLimitingConfigurationGuardTests|FullyQualifiedName~ConfigurationGuardTests.HumanEdgeHostController_ShouldExposeOnlyHumanManagementRoutes" --no-restore --disable-build-servers`
  - `rg -n "IRepository<EdgeHostPlcBinding|IReadRepository<EdgeHostPlcBinding|DbSet<EdgeHostPlcBinding>|EdgeHostPlcBindings|class EdgeHostPlcBinding : .*IAggregateRoot" src -g '*.cs' -g '!src/tests/**'`
  - `rg -n "api/v1/human/edge-hosts|EdgeHostPermissions|EdgeHost.PlcBinding|GetEdgeHost|CreateEdgeHost|UpdateEdgeHost" src docs -g '*.cs' -g '*.md'`
  - `rg -n "EdgeHost 后端管理闭环|EdgeHost.Read|EdgeHost.Manage|上位机和 PLC 绑定" docs -g '*.md'`
  - `git diff --check`
- 验证结果：AppHost build 通过，0 warning / 0 error；ProductionService 全量 35/35 通过；ServiceLayer 全量 217/217 通过；EndToEnd 路由/限流守卫 4/4 通过，保留既有 `Microsoft.OpenApi` 2.3.0/2.7.5 版本冲突 warning；PLC 绑定子实体仓储/DbSet/聚合根入口扫描无结果；EdgeHost API、权限、审计和规则关键词可检索；`git diff --check` 通过。

## 2026-07-02 EdgeHost 上位机 PLC 动态绑定聚合

- 改动范围：Cloud Production 领域模型、EF Core 配置、`IIoTDbContext` 聚合根 DbSet、EF migration、ServiceLayer 聚合/基础设施测试、Cloud 规则文档和本滚动复盘。未修改 EdgeClient、AICopilot、前端页面、PLC 状态上报接口、产能/过站上传链路、认证链路、部署脚本或生产数据库。
- 改动原因：现场上位机确定但连接 PLC 会动态变化，不能继续靠代码写死 PLC 归属；需要先在 Cloud 里沉淀“上位机聚合根 + PLC 绑定子集合”的可维护配置模型，后续前端可增删改查和播种，状态上报/产能查询再按该绑定串联。
- 影响面：新增 `EdgeHost` 聚合根，绑定现有 `DeviceId + ClientCode` 身份链；新增 `EdgeHostPlcBinding` 子实体承载 PLC 编码、名称、工序、业务设备、工位、协议、地址、启用状态、排序和备注；新增 `edge_hosts`、`edge_host_plc_bindings` 表及唯一索引/外键；`IIoTDbContext` 只公开 `EdgeHosts` 聚合根入口，不公开 PLC 绑定子实体 DbSet。
- 关键实现：`EdgeHost` 提供新增、修改、启用、禁用、删除 PLC 绑定的方法，PLC code 在聚合内规范化并按上位机唯一；PLC 绑定允许 `ProcessId` / `BusinessDeviceId` 为空，避免现场尚未映射完成时阻断配置维护；EF migration 只新增两张配置表，不新增 runtime state 表，不改现有上传、产能、过站、发布或身份表。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：`EdgeHost` 是上位机聚合根，`EdgeHostPlcBinding` 是聚合内期望绑定配置子实体；PLC 真实在线状态必须走状态投影/专用 store；PLC 绑定不得作为独立聚合根、公开 DbSet 或通用仓储入口。
- 验证命令：
  - `env 'ConnectionStrings__iiot-db=Host=localhost;Port=5432;Database=iiot_design_time;Username=postgres;Password=postgres' dotnet ef migrations add AddEdgeHosts --project src/infrastructure/IIoT.EntityFrameworkCore/IIoT.EntityFrameworkCore.csproj --startup-project src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --context IIoTDbContext`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter "FullyQualifiedName~AggregateBehaviorTests|FullyQualifiedName~InfrastructureBehaviorTests" --no-restore --disable-build-servers`
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `rg -n "EdgeHostPlcBindings|IRepository<EdgeHostPlcBinding|IReadRepository<EdgeHostPlcBinding|class EdgeHostPlcBinding : .*IAggregateRoot|DbSet<EdgeHostPlcBinding>|xmin = table.Column" src -g '*.cs' -g '!src/tests/**'`
  - `rg -n "EdgeHost|EdgeHostPlcBinding|edge_hosts|edge_host_plc_bindings|上位机和 PLC 绑定" src docs/云端规则.md docs/改动复盘与规则沉淀.md -g '*.cs' -g '*.md'`
  - `git diff --check`
- 验证结果：EF migration 生成成功，提示本机 EF tools `10.0.8` 低于 runtime `10.0.9`；定向 Aggregate/Infrastructure 测试 50/50 通过；AppHost build 通过，0 warning / 0 error；ServiceLayer 全量 217/217 通过；ProductionService 全量 29/29 通过；PLC 绑定子实体公开 DbSet/通用仓储/聚合根入口和新迁移 `xmin` 用户列扫描无结果；规则关键词可检索；`git diff --check` 通过。

## 2026-07-02 云端聚合根边界显式收口

- 改动范围：Cloud SharedKernel 领域基类、核心聚合根类型声明、客户端发布聚合子实体边界、客户端版本/运行状态投影专用存储端口、`IIoTDbContext` 公开 DbSet、ServiceLayer/ProductionService 聚合边界测试、Cloud 规则文档和本滚动复盘。未修改数据库 schema、migration、认证、上传、产能、过站、日志、前端、EdgeClient 或 AICopilot。
- 改动原因：为后续上位机-PLC 绑定聚合改造先清理 DDD 基础边界，避免 `BaseEntity<Guid>` 自动等同于聚合根，导致 `ClientReleaseVersion` / `ClientReleaseArtifact` 等子实体可被通用仓储绕过 `ClientReleaseComponent` 直接操作，也避免客户端版本快照、运行心跳和状态投影为了复用仓储伪装成业务聚合。
- 影响面：`BaseEntity<TId>` 仅保留普通实体和领域事件容器职责；真实业务聚合根显式实现 `IAggregateRoot<Guid>`；`ClientReleaseVersion`、`ClientReleaseArtifact` 保持发布聚合内部子实体身份；`DeviceClientVersionSnapshot`、`DeviceClientState`、`EdgeDeviceRuntimeHeartbeat` 改为投影/状态模型，通过 `IDeviceClientStateStore` 专用端口读写；状态投影查询规约收进专用 store 实现，不再保留面向通用仓储的 Core Specification；`IIoTDbContext` 不再公开 `ClientReleaseVersions` / `ClientReleaseArtifacts` 一等 DbSet 属性，但 EF 映射和现有表结构不变。
- 关键实现：显式标记 `IdentityAccount`、`Employee`、`MfgProcess`、`Device`、`Recipe`、`ClientReleaseComponent`、`ClientReleaseRetentionPolicy` 为聚合根；新增 `IDeviceClientStateStore` 和 EF 实现承载客户端版本快照、运行心跳、状态投影的读写；版本上报、运行心跳、设备客户端状态清单、AI Read、发布保留策略和发布包删除均改为通过专用 store 访问状态/投影表；删除状态投影面向通用仓储的 Specification；新增测试断言 `BaseEntity` 不再自动实现 `IAggregateRoot`，发布版本/文件清单、插件版本明细、版本快照、客户端状态投影和运行心跳都不是聚合根。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：`BaseEntity<TId>` 不代表聚合根，真实聚合根必须显式实现 `IAggregateRoot<TId>`；通用仓储只能用于显式聚合根；子实体、投影表、状态表和基础设施持久化模型不得为了复用仓储伪装成业务聚合根；状态/投影查询条件应收在专用 store、读服务或基础设施实现内部。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet build src/hosts/IIoT.AppHost/IIoT.AppHost.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `rg -n "BaseEntity<TId> : IEntity<TId>, IAggregateRoot|DeviceClientVersionSnapshot : .*IAggregateRoot|DeviceClientState : .*IAggregateRoot|EdgeDeviceRuntimeHeartbeat : .*IAggregateRoot|class ClientReleaseVersion : .*IAggregateRoot|class ClientReleaseArtifact : .*IAggregateRoot|IRepository<DeviceClientVersionSnapshot|IReadRepository<DeviceClientVersionSnapshot|IRepository<DeviceClientState|IReadRepository<DeviceClientState|IRepository<EdgeDeviceRuntimeHeartbeat|IReadRepository<EdgeDeviceRuntimeHeartbeat|DbSet<ClientReleaseVersion>|DbSet<ClientReleaseArtifact>|DeviceClientVersionSnapshotByDeviceSpec|DeviceClientVersionSnapshotsByDevicesSpec|DeviceClientStateByIdentitySpec|DeviceClientStatesByDevicesSpec|EdgeDeviceRuntimeHeartbeatByIdentitySpec|EdgeDeviceRuntimeHeartbeatsByDevicesSpec" src -g '*.cs'`
  - `rg -n "IDeviceClientStateStore|EfDeviceClientStateStore|BaseEntity<TId>|IAggregateRoot|ClientReleaseVersion|ClientReleaseArtifact|子实体|投影表" src docs/云端规则.md docs/改动复盘与规则沉淀.md`
  - `git diff --check`
- 验证结果：InfrastructureBehaviorTests 定向 37/37 通过；AppHost build 通过，0 warning / 0 error；ServiceLayer 全量 215/215 通过；ProductionService 全量 29/29 通过；旧 `BaseEntity` 自动聚合根、发布子实体聚合根、客户端状态/快照/心跳聚合根和通用仓储入口、发布子实体 DbSet 入口、状态投影 Specification 入口扫描无结果；专用 store 和规则关键词可检索；`git diff --check` 通过。

## 2026-07-02 Microsoft.OpenApi 安全依赖修复

- 改动范围：Cloud `IIoT.HttpApi` 顶级 NuGet 依赖和本滚动复盘。未修改 Cloud 业务代码、数据库 schema、API 契约、前端、EdgeClient 或 AICopilot。
- 改动原因：执行 Cloud 标准本机部署时，Docker restore 暴露 `Microsoft.OpenApi 2.3.0` 的 `NU1903` 高危漏洞告警；总规则要求 `NU190x` 默认阻断，不能继续构建和推送带已知高危漏洞的生产镜像。
- 影响面：`Microsoft.OpenApi` 从 `2.3.0` 升级到同一 2.x 主版本线的修复版本 `2.7.5`。曾尝试 `3.7.0`，但与 `Microsoft.AspNetCore.OpenApi 10.0.9` source generator 不兼容并导致 `IOpenApiMediaType.Example` 只读属性编译错误，因此采用官方修复线 `2.7.5`。
- 规则提炼：无新增长期规则，原因是“`NU190x` 默认阻断、不得保留已知漏洞依赖”已经写入工作区 `docs/总规则.md`；本次只是执行该既有安全门禁。
- 验证命令：
  - `dotnet list src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj package --vulnerable --include-transitive`
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --disable-build-servers`
  - `git diff --check`
- 验证结果：`dotnet list package --vulnerable --include-transitive` 显示当前源下 `IIoT.HttpApi` 没有易受攻击的包；HttpApi build 通过，0 warning / 0 error；`git diff --check` 见本轮部署前收尾验证。

## 2026-07-02 AI 高频查询 API 收口

- 改动范围：Cloud AI Read 权限、Human 权限定义、AI Read Controller、ProductionService 查询用例、Dapper 读服务、服务契约、ServiceLayer/ProductionService/E2E 测试和 Cloud 规则文档。未修改 `IIoT.EdgeClient`、`AICopilot`，未执行部署。
- 改动原因：AI 项目后续只把 Text-to-SQL 作为低频探索兜底；设备日志、设备产能和生产数据这类高频查询必须由 Cloud 提供正式只读 API，避免 AI 端直接查生产库、绕过权限、审计、限流和设备授权范围。
- 影响面：新增 `AiRead.ProductionRecord` 权限点；新增 `GET /api/v1/ai/read/production-records` 通用生产数据查询；新增 `GET /api/v1/ai/read/capacity/hourly` 小时产能查询；设备日志 AI 查询支持 `preset` 和 `minLevel`；移除 AI 专用 `/api/v1/ai/read/pass-stations/{typeKey}` 语义入口。Human 过站接口和 Edge 上传接口不变。
- 关键实现：通用生产数据查询读取 `pass_station_records` 公共列并按 `pass-station-types.json` 返回 schema 化字段，支持 `typeKey`、`processId`、`deviceId`、`barcode`、`result`、显式时间范围或 `preset`；`fieldMode=list/full` 控制字段集合；不向 AI 返回 raw `payload_jsonb` 字符串。所有新增 AI Query 继续走现有 `IAiReadQuery<T>` + MediatR pipeline，复用鉴权、审计、限流和设备授权 scope。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和 `docs/过站工序扩展规则.md`：AI 高频业务读取必须通过 Cloud `/api/v1/ai/read/*` 只读 API；通用生产数据只使用 `/api/v1/ai/read/production-records`；Text-to-SQL 只作为低频探索兜底；新工序进入 `pass_station_records + pass-station-types.json` 后默认具备 AI 通用读取能力。
- 验证命令：
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --no-restore --disable-build-servers --filter FullyQualifiedName~ConfigurationGuardTests`
  - `cd src/ui/iiot-web && npm run build`
  - `git diff --check`
  - `rg -n "AiReadPermissions\\.PassStation|GetAiReadPassStations|AiReadPassStation|/api/v1/ai/read/pass-stations" src -S -g '!src/tests/IIoT.ServiceLayer.Tests/InfrastructureBehaviorTests.cs'`
  - `rg -n "production-records|capacity/hourly|minLevel|AiRead.ProductionRecord" src docs -S`
  - `rg -n "payload_jsonb" src/services/IIoT.ProductionService/Queries/AiRead src/hosts/IIoT.HttpApi/Controllers/AiRead -S`
- 验证结果：HttpApi build 通过，0 warning / 0 error；AiReadBehaviorTests 25/25 通过；InfrastructureBehaviorTests 35/35 通过；ProductionService 全量 29/29 通过；ServiceLayer 全量 213/213 通过；ConfigurationGuardTests 62/62 通过；Vue build 通过；`git diff --check` 通过；旧 AI 过站入口在运行时代码无残留；AI Read Controller/Query 未向 AI 暴露 raw `payload_jsonb`；新 `production-records`、`capacity/hourly`、`minLevel` 和 `AiRead.ProductionRecord` 可检索。

## 2026-07-02 设备状态投影、设备删除 EF 收口和 AI Read 扩展

- 改动范围：Cloud 客户端状态投影聚合/EF 配置/migration、版本上报和运行心跳写入投影、设备客户端状态查询、设备删除影响统计和级联删除基础设施、Human 删除确认前端类型、AI Read 权限/查询/API、工序读服务契约、ServiceLayer/ProductionService 测试和规则文档。未修改 `IIoT.EdgeClient`、`AICopilot`，未执行部署。
- 改动原因：开发阶段要求一次规范化处理，不保留兼容层和双轨。客户端状态不能继续由页面临时拼版本快照和心跳解释；设备删除不能由 Dapper 绕开 EF/DDD/Outbox/cache 的写链路；AI 项目后续需要走 Cloud 正式只读 API，而不是直接查生产库。
- 影响面：新增 `DeviceClientState` 投影和 `edge_device_client_states` 表，按 `device_id + client_code` 唯一并迁移历史版本快照/运行心跳；版本上报、运行心跳、设备客户端状态列表统一读取投影；设备删除影响统计新增客户端状态投影数量；级联删除实现迁移到 `EfDeviceDeletionDependencyService`，Dapper 删除实现移除；AI Read 新增工序、客户端发布版本、设备客户端状态三类只读端点和权限点。
- 关键实现：`ReportDeviceClientVersionHandler` 和 `ReportDeviceRuntimeHeartbeatHandler` 在写入原事实表后同步 upsert `DeviceClientState`；`GetDeviceClientVersionInventoryHandler` 以投影作为软件状态和版本状态的权威来源；设备级联删除在 EF Core 基础设施服务中开启事务，先统计影响和受影响员工，再删除客户端状态、版本快照/插件版本、运行心跳、上传登记、人员授权、refresh token、历史产能/日志/过站、配方和设备主数据；AI Read 工序查询通过 `IProcessReadQueryService` 读模型接口，不跨层引用 MasterData 聚合。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：`DeviceClientState` 是客户端状态官方投影；设备级联删除写路径必须走 EF Core 服务，Dapper 不得实现设备级联删除命令；AI-facing 数据读取必须通过 Cloud `/api/v1/ai/read/*` 只读 API，不让 AI 直接连生产库。
- 验证命令：
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~AiReadBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~InfrastructureBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --no-restore --disable-build-servers`
  - `cd src/ui/iiot-web && npm run test:unit`
  - `cd src/ui/iiot-web && npm run build`
  - `rg -n "new ClientHostRelease|new ClientPluginRelease|IRepository<ClientHostRelease|IReadRepository<ClientHostRelease|IRepository<ClientPluginRelease|IReadRepository<ClientPluginRelease|ClientHostReleaseBy|ClientPluginReleaseBy|EdgeInstallerArtifactCatalogReader|IEdgeInstallerArtifactCatalogReader" src -S`
  - `rg -n "DeviceDeletionDependencyQueryService|DeleteCascadeAsync" src/infrastructure/IIoT.Dapper src/infrastructure/IIoT.EntityFrameworkCore src/services -S`
  - `rg -n "AiRead.Process|AiRead.ClientRelease|AiRead.DeviceClientState|device-client-states|client-releases|processes" src -S`
- 验证结果：AI Read 定向 17/17 通过；InfrastructureBehaviorTests 34/34 通过；ProductionService 全量 29/29 通过；ServiceLayer 全量 204/204 通过；HttpApi build 通过，0 warning / 0 error；Vue 单测 49/49 通过；Vue build 通过。旧 release 类型、旧 release 仓储和旧目录扫描器运行时代码无残留；Dapper 项目无设备级联删除实现；AI Read 新权限和新路由可检索。未执行完整 `IIoT.EndToEndTests` 和生产部署，原因是本次目标是 Cloud 代码规范化收口，部署需另按 `docs/上传部署总览.md` 和 Cloud 部署文档确认发布窗口。

## 2026-07-02 客户端发布聚合一次收口

- 改动范围：Cloud 客户端发布领域模型、EF 配置和 migration、Human/Edge/Public catalog 查询、首装生成、HTTP 宿主/插件发布、管理员手动登记、发布状态变更、retention、发布包物理删除、Human API、前端发布 API/types、ProductionService 与 ServiceLayer 测试。未修改 `IIoT.EdgeClient` 或 `AICopilot`。
- 改动原因：开发阶段要求一次规范化处理，不做兼容层、不做新旧双轨。旧实现把宿主发布、插件发布和目录扫描拆成多套来源，删除错误包时容易出现“数据库状态隐藏但残留文件又被扫出来”的误导；同时发布生命周期、首装生成、包删除和更新说明保留没有被同一个聚合约束。
- 影响面：新增 `ClientReleaseComponent` 聚合根，使用 `ClientReleaseVersion` 承载版本生命周期，使用 `ClientReleaseArtifact` 承载受控文件清单；新增 `edge_client_release_components`、`edge_client_release_versions`、`edge_client_release_artifacts`；一次性迁移旧 `edge_client_host_releases` / `edge_client_plugin_releases` 数据后 drop 旧表；运行时不再注入旧 release 仓储，不再使用 `EdgeInstallerArtifactCatalogReader` 或 `ClientReleaseCatalogMerge` 扫目录补 catalog。
- 关键实现：发布写入、状态修改、保留策略、首装生成、Edge 更新 catalog、公开下载 catalog 和管理员删除包全部按 `ClientReleaseComponent` 聚合读写；首装生成只从聚合里选择 `Published` 版本，物理 artifact 仅用于校验已登记版本的素材；删除发布包先用当前版本快照判定是否在用，再按 `ClientReleaseArtifact` 和 `EdgeInstallerArtifactOptions.RootPath` 生成受控删除计划，删除成功标记 `Deleted`，删除失败标记 `DeleteFailed`，`releaseNotes` 等更新内容继续保留。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md`：客户端发布运行时聚合根是 `ClientReleaseComponent`；旧 host/plugin release 表只允许出现在历史 migration 和一次性迁移脚本中；发布、首装、catalog、状态变更、删除和 retention 不得再依赖旧表或目录扫描补版本链路。
- 后续批次：`DeviceClientState` 物化投影、设备删除 EF 收口和 AI Read API 已在上方 `2026-07-02 设备状态投影、设备删除 EF 收口和 AI Read 扩展` 继续完成；部署验收未执行，需另按部署规则确认发布窗口。
- 验证命令：
  - `dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --filter FullyQualifiedName~ClientReleaseBehaviorTests --no-restore --disable-build-servers`
  - `dotnet test IIoT.CloudPlatform/src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --filter FullyQualifiedName~GenerateEdgeInstallerPackageHandler --no-restore --disable-build-servers`
  - `dotnet test IIoT.CloudPlatform/src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers`
  - `dotnet build IIoT.CloudPlatform/src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --no-restore --disable-build-servers`
  - `cd IIoT.CloudPlatform/src/ui/iiot-web && npm run build`
  - `rg -n "new ClientHostRelease|new ClientPluginRelease|IRepository<ClientHostRelease|IReadRepository<ClientHostRelease|IRepository<ClientPluginRelease|IReadRepository<ClientPluginRelease|ClientHostReleaseBy|ClientPluginReleaseBy|EdgeInstallerArtifactCatalogReader|IEdgeInstallerArtifactCatalogReader|edge_client_host_releases|edge_client_plugin_releases" IIoT.CloudPlatform/src/core IIoT.CloudPlatform/src/services IIoT.CloudPlatform/src/hosts IIoT.CloudPlatform/src/infrastructure -S`
- 验证结果：ProductionService 客户端发布行为测试 24/24 通过；ProductionService 全量 29/29 通过；ServiceLayer 首装生成定向 5/5 通过；ServiceLayer 全量 200/200 通过；HttpApi build 通过，0 warning / 0 error；Vue build 通过。旧 release 类型、旧仓储和旧目录扫描器在运行时代码无残留；旧表名仅存在于历史 migration 和本次 `NormalizeClientReleaseComponents` 迁移的数据搬迁/drop 旧表逻辑中。

## 2026-07-01 Edge 发布 API key 稳定通道

- 改动范围：Cloud 后端新增 Edge Release API key 持久化、机器发布 JWT 签发接口、Human 管理 API、JWT claim、权限管道、Gateway machine surface、OpenAPI route surface 和 EF migration；同步更新 Cloud 规则和复盘。未修改 Cloud 前端、Edge 安装包产物、Cloud/AICopilot 部署脚本或现有发布包校验/保留策略。
- 改动原因：Edge 本机发布凭据使用 Human refresh token 时会受 Human 活跃会话上限影响，同账号多端登录会导致 Keychain 中 refresh token 被服务端撤销，部署 preflight 反复 401。用户要求形成稳定发布渠道，不能每次部署都要求重新提供账号密码。
- 影响面：新增 `edge_release_api_keys` 表；新增 `POST /api/v1/machine/edge-release/token` 用 API key 换短期发布 JWT；Gateway 正式放行 `/api/v1/machine/{**catch-all}` 并标记 `X-IIoT-Route-Surface: machine`；新增 `/api/v1/human/client-release-api-keys` 创建/列表/吊销接口。现有 `/api/v1/human/client-releases/*` 上传、catalog、审计和保留策略继续复用，不新增绕过发布审计的旁路。
- 关键实现：API key 明文只创建时返回一次，数据库只保存 SHA256 hash；机器 JWT 使用 `actor_type=edge-release-publisher`，只携带 `ClientRelease.Read` / `ClientRelease.Publish`；`AuthorizationBehavior` 路线 A 扩展为机器 actor 直接校验 permission claims，不改现有发布命令 `IHumanCommand` 类型，也不改 `RequestKindGuardBehavior` 分类规则；机器 actor 不能执行 `ClientRelease.Manage` 或未声明权限点的 human 请求；API key 创建/吊销写 `ClientRelease.ApiKey.Create/Revoke` 审计，发布上传继续复用 `ClientRelease.Publish` 审计，机器发布审计中 `ActorUserId` 可为空，`ActorEmployeeNo` 显示 `edge-publisher:<keyName>`。
- 规则提炼：形成长期规则，已写入 `docs/云端规则.md` 和工作区 `docs/上传部署总览.md` / `docs/总规则.md`：Edge 日常发布使用 Edge Release API key，不依赖 Human refresh token；机器发布身份权限必须限定为发布读取和上传，不得获得管理/删除能力。
- 验证命令：
  - `env 'ConnectionStrings__iiot-db=Host=localhost;Port=5432;Database=iiot_design_time;Username=postgres;Password=postgres' dotnet ef migrations add AddEdgeReleaseApiKeys --project src/infrastructure/IIoT.EntityFrameworkCore/IIoT.EntityFrameworkCore.csproj --startup-project src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --context IIoTDbContext`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj --no-restore --disable-build-servers --filter "FullyQualifiedName~AuthorizationAndIdentityBehaviorTests|FullyQualifiedName~InfrastructureBehaviorTests"`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj --no-restore --disable-build-servers --filter "FullyQualifiedName~ClientReleaseBehaviorTests"`
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj --no-restore --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --no-restore --disable-build-servers --filter "FullyQualifiedName~ConfigurationGuardTests"`
  - `dotnet build src/hosts/IIoT.Gateway/IIoT.Gateway.csproj --no-restore --disable-build-servers`
  - `DEPLOY_SSH_TARGET=<legacy-root-ssh-target> ./deploy/scripts/local-release.sh --services httpapi,migration`
  - `DEPLOY_SSH_TARGET=<legacy-root-ssh-target> ./deploy/scripts/local-release.sh --services gateway`
  - `curl -i -X POST http://<cloud-private-ip>:81/api/v1/machine/edge-release/token -H 'X-IIoT-Edge-Release-Key: invalid'`
  - `ssh <legacy-root-ssh-target> 'cd /data/iiot-platform/cloud/deploy && docker compose --env-file .env -f docker-compose.prod.yml exec -T postgres psql -U postgres -d iiot-db -tAc "select to_regclass('\'public.edge_release_api_keys\''), count(*) from edge_release_api_keys;"'`
- 验证结果：EF migration 生成成功，提示本机 EF tools `10.0.8` 低于 runtime `10.0.9`；ServiceLayer 定向 60/60 通过；ProductionService 客户端发布行为 24/24 通过；ConfigurationGuardTests 62/62 通过；HttpApi 和 Gateway build 0 warning / 0 error；API key / machine token 规则关键词扫描通过；`git diff --check` 通过。Cloud 已部署：`httpapi,migration` 发布到 `sha-6c2a4dd89a7f8c64be2e226684c877788801a27e`，随后 `gateway` 发布到 `sha-4a1ecdd1b97fe21083f1d66245645665abd76aa4`；post-deploy healthz 200；`edge_release_api_keys` 表存在且当前记录数 0；无效 API key 访问 `/api/v1/machine/edge-release/token` 返回 401 且 `X-IIoT-Route-Surface: machine`。本机旧 Human refresh token 已被会话上限撤销，尚未创建生产 API key，尚未执行 Edge 发布。

## 2026-06-30 客户端状态拆分、发布包删除和设备删除级联收口

- 改动范围：Cloud 运行心跳实体/迁移/API/查询、客户端发布 catalog 删除文件命令和 Human API、设备级联删除影响统计/缓存失效、Vue 设备删除确认和客户端发布页面状态列/操作列、Cloud 后端/前端测试与规则文档。同步修改 `IIoT.EdgeClient` 仅限运行心跳调用方契约，已获用户跨项目授权。
- 改动原因：设备安装状态把“版本上报超过 24 小时”误读成未启动，会误导管理员；发布 catalog 缺少管理员物理删除包能力，废弃插件包长期留存误导人；设备删除后普通用户设备授权缓存未清理；删除弹窗要求重复输入设备名称体验差。
- 影响面：新增 `edge_device_runtime_heartbeats` 表和 `POST /api/v1/edge/runtime-heartbeats`；设备客户端状态页拆成软件状态、最后运行心跳、安装状态、当前版本、版本上报时间和问题；管理员可在发布管理中删除未在用发布包文件；设备删除会级联运行心跳并清理受影响员工授权缓存。未把运行心跳伪装成 Cloud/MES 上传状态，Cloud 问题列没有真实来源时保持空值。
- 关键实现：运行心跳按 `device_id + client_code` upsert 并校验身份链；安装状态只看版本/catalog 差异，软件状态只看运行心跳；发布包在用判定只看当前版本快照是否等于目标版本，不加时间窗口；物理删除路径由服务端 release 元数据推导，限制在 `/edge-updates` 根内并拒绝 `..`、符号链接/重解析点逃逸；删除前审计文件清单；设备删除前捕获受影响员工 ID，删除后清理 `DeviceAccessesByUser`。
- 规则提炼：形成长期规则，已写入工作区 `docs/总规则.md` 和 Cloud `docs/云端规则.md`：版本上报、运行心跳、上传同步是不同事实源；运行心跳不得替代安装状态或上传状态；发布包物理删除必须用 `ClientRelease.Manage`、当前快照在用保护、服务端路径推导和文件清单审计；设备删除必须清理受影响员工授权缓存。
- 验证命令：
  - `env 'ConnectionStrings__iiot-db=Host=localhost;Port=5432;Database=iiot_design_time;Username=postgres;Password=postgres' dotnet ef migrations add AddEdgeDeviceRuntimeHeartbeats --project src/infrastructure/IIoT.EntityFrameworkCore --startup-project src/hosts/IIoT.HttpApi --context IIoTDbContext`
  - `dotnet build src/hosts/IIoT.HttpApi/IIoT.HttpApi.csproj -v:minimal`
  - `dotnet test src/tests/IIoT.ProductionService.Tests/IIoT.ProductionService.Tests.csproj -v:minimal -p:BuildInParallel=false --disable-build-servers`
  - `dotnet test src/tests/IIoT.ServiceLayer.Tests/IIoT.ServiceLayer.Tests.csproj -v:minimal -p:BuildInParallel=false --disable-build-servers`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter FullyQualifiedName~RuntimeHeartbeatControllerTests -v:minimal -p:BuildInParallel=false --disable-build-servers`
  - `cd src/ui/iiot-web && npm run test:unit`
  - `cd src/ui/iiot-web && npm run build`
- 验证结果：EF migration 生成成功，提示本机 EF tools `10.0.8` 低于 runtime `10.0.9`；HttpApi build 通过；ProductionService 29/29 通过；ServiceLayer 193/193 通过；新增 RuntimeHeartbeatController 过滤测试 1/1 通过；Vue 单测 49/49 通过；Vue build 通过。全量 `IIoT.EndToEndTests` 曾启动但长时间无输出，已中止并改跑新增控制器测试过滤项，避免现有重型外部链路等待混入本次判断。

## 2026-06-30 Cloud 本机标准发布流程重写

- 改动范围：Cloud 部署文档、`deploy/scripts/build-and-push.sh`、`deploy/scripts/local-release.sh`、`cloud-image` / `cloud-deploy` workflow、部署门禁测试、`.gitignore`、工作区上传部署总览/总规则/历史核心记录。未修改 Cloud 业务代码、数据库 schema、真实 `.env`、服务器 `deploy-release.sh` 行为或 Edge/AICopilot 运行代码。
- 改动原因：旧日常路径依赖 self-hosted runner 构建镜像，多次出现等待、超时和误读旧文档问题；用户明确要求 Cloud/AICopilot 标准生产发布改为“先 push GitHub -> 本机构建镜像 -> 推 Harbor -> 本机 SSH 触发服务器 deploy-release.sh -> 服务器 pull/restart/health/cleanup”。
- 影响面：Cloud 日常发布入口改为本机 `deploy/scripts/local-release.sh --services <services>` 或显式 `--all`；`cloud-image` / `cloud-deploy` 只保留带确认词的灾备入口；服务器端 `deploy-release.sh` 继续作为唯一部署入口，负责 pre-check、backup、pull、restart、health、cleanup 和 history。
- 关键实现：新增 Cloud 本机 `build-and-push.sh`，支持显式 `--services httpapi,gateway,dataworker,migration,web`、`--all`、`--dry-run`、portable timeout、Harbor 登录检查、固定输出 `Release tag` / `Deploy services input` / `IIOT_*_IMAGE` 和 `artifacts/deploy/cloud-built-services.txt`；新增 `local-release.sh` 校验工作区干净、HEAD 已推送、SSH target 显式传入，并执行 `DEPLOY_GIT_SHA=<sha> DEPLOY_TRIGGERED_BY=local ./scripts/deploy-release.sh sha-<sha> --services <services>`；workflow push 日常触发被移除并要求 `EMERGENCY_CLOUD_IMAGE_BUILD` / `EMERGENCY_CLOUD_DEPLOY`。
- 规则提炼：形成长期部署规则，已写入工作区 `docs/总规则.md`、`docs/上传部署总览.md`、`docs/历史核心记录.md` 和 Cloud `docs/云端规则.md` / `deploy/README.md` / `deploy/OPERATIONS.md`：Cloud 生产发布不得等待 GitHub image/deploy workflow，本机构建、Harbor 检查和 SSH deploy 必须 fail-fast；超时后停止诊断 Docker buildx、Harbor tag、服务器 compose/logs 和 release 状态，不得继续 watch 或无限等待。
- 验证命令：
  - `bash -n deploy/scripts/build-and-push.sh deploy/scripts/local-release.sh deploy/scripts/deploy-release.sh`
  - `ruby -e 'require "yaml"; ARGV.each { |f| YAML.load_file(f); puts "YAML ok: #{f}" }' .github/workflows/cloud-image.yml .github/workflows/cloud-deploy.yml`
  - `./deploy/scripts/build-and-push.sh --dry-run --services httpapi,httpapi,web`
  - `if ./deploy/scripts/build-and-push.sh --dry-run --services nope; then exit 1; else echo 'Cloud invalid service rejected'; fi`
  - `./deploy/scripts/local-release.sh --dry-run --services web --ssh-target <legacy-root-ssh-target>`
  - `dotnet test src/tests/IIoT.EndToEndTests/IIoT.EndToEndTests.csproj --filter ConfigurationGuardTests --logger "console;verbosity=minimal"`
  - `rg` 检查旧标准入口描述和 fail-fast 规则。
- 验证结果：Shell 语法检查通过；Cloud workflow YAML 解析通过；dry-run 服务去重、非法服务拒绝、本机 SSH 命令打印通过；`ConfigurationGuardTests` 62 通过、0 失败；检索确认旧 `cloud-image` 日常标准入口描述已替换为灾备口径。

## 2026-06-29 改动收口规则同步

- 改动范围：`AGENTS.md`、`docs/云端规则.md`、`docs/改动复盘与规则沉淀.md` 和 Cloud 文档门禁测试。
- 改动原因：将“代码改完必须复盘、提炼规则、更新规则入口”的强制机制扩展到 CloudPlatform，避免后续只靠全局规则记忆。
- 影响面：仅规则、文档和仓库卫生测试；不修改 Cloud 业务代码、接口、数据库、前端页面或部署脚本行为。
- 改前阅读：已阅读工作区总规则、历史核心记录、Cloud 项目入口、云端规则和现有文档门禁测试。
- 规则提炼：
  - Cloud 代码改动前必须阅读规则、源码、测试和近期 git/GitHub 历史。
  - Cloud 代码改动完成前必须更新本滚动复盘文档，并判断是否沉淀长期规则。
  - Cloud 文档门禁必须保护滚动复盘、规则沉淀、无新增长期规则和最终回复三要素。
- 验证命令：
  - `rg "改动复盘|规则沉淀|无新增长期规则|最终回复必须列出|已验收功能默认冻结" AGENTS.md docs IIoT.CloudPlatform AICopilot`
  - `dotnet test IIoT.CloudPlatform/src/tests/IIoT.EndToEndTests --filter ConfigurationGuardTests`
- 验证结果：`rg` 检索通过；`IIoT.EndToEndTests --filter ConfigurationGuardTests` 通过 62 个测试。
