# IIoT.CloudPlatform 规则入口 (Claude Code)

本文件是 Claude Code 的接入点，不持有规则内容。规则事实源是本目录的 `AGENTS.md`（它再指向 `docs/云端规则.md` 等）；改规则只改 `AGENTS.md` 和 `docs/`，不要把规则写进本文件。

@AGENTS.md

修改前端 `src/ui/iiot-web` 时，该目录下的嵌套规则会自动加载。
